// Generated by Petrel. DO NOT EDIT.
import Foundation
import Petrel

// lexicon: 1, id: blue.catbird.chat.defs

public enum BlueCatbirdChatDefs {
    public static let typeIdentifier = "blue.catbird.chat.defs"

    public struct ConversationCoordinates: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#conversationCoordinates"
        public let conversationId: OperationId
        public let generation: Int
        public let stateVersion: Int
        public let groupId: Bytes
        public let epoch: Int
        public let groupContextHash: Bytes
        public let confirmationTag: Bytes
        public let lifecycle: DefsLifecycle

        public init(
            conversationId: OperationId, generation: Int, stateVersion: Int, groupId: Bytes, epoch: Int, groupContextHash: Bytes, confirmationTag: Bytes, lifecycle: DefsLifecycle
        ) {
            self.conversationId = conversationId
            self.generation = generation
            self.stateVersion = stateVersion
            self.groupId = groupId
            self.epoch = epoch
            self.groupContextHash = groupContextHash
            self.confirmationTag = confirmationTag
            self.lifecycle = lifecycle
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                generation = try container.decode(Int.self, forKey: .generation)
            } catch {
                LogManager.logError("Decoding error for required property 'generation': \(error)")
                throw error
            }
            do {
                stateVersion = try container.decode(Int.self, forKey: .stateVersion)
            } catch {
                LogManager.logError("Decoding error for required property 'stateVersion': \(error)")
                throw error
            }
            do {
                groupId = try container.decode(Bytes.self, forKey: .groupId)
            } catch {
                LogManager.logError("Decoding error for required property 'groupId': \(error)")
                throw error
            }
            do {
                epoch = try container.decode(Int.self, forKey: .epoch)
            } catch {
                LogManager.logError("Decoding error for required property 'epoch': \(error)")
                throw error
            }
            do {
                groupContextHash = try container.decode(Bytes.self, forKey: .groupContextHash)
            } catch {
                LogManager.logError("Decoding error for required property 'groupContextHash': \(error)")
                throw error
            }
            do {
                confirmationTag = try container.decode(Bytes.self, forKey: .confirmationTag)
            } catch {
                LogManager.logError("Decoding error for required property 'confirmationTag': \(error)")
                throw error
            }
            do {
                lifecycle = try container.decode(DefsLifecycle.self, forKey: .lifecycle)
            } catch {
                LogManager.logError("Decoding error for required property 'lifecycle': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(generation, forKey: .generation)
            try container.encode(stateVersion, forKey: .stateVersion)
            try container.encode(groupId, forKey: .groupId)
            try container.encode(epoch, forKey: .epoch)
            try container.encode(groupContextHash, forKey: .groupContextHash)
            try container.encode(confirmationTag, forKey: .confirmationTag)
            try container.encode(lifecycle, forKey: .lifecycle)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(conversationId)
            hasher.combine(generation)
            hasher.combine(stateVersion)
            hasher.combine(groupId)
            hasher.combine(epoch)
            hasher.combine(groupContextHash)
            hasher.combine(confirmationTag)
            hasher.combine(lifecycle)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if conversationId != other.conversationId {
                return false
            }
            if generation != other.generation {
                return false
            }
            if stateVersion != other.stateVersion {
                return false
            }
            if groupId != other.groupId {
                return false
            }
            if epoch != other.epoch {
                return false
            }
            if groupContextHash != other.groupContextHash {
                return false
            }
            if confirmationTag != other.confirmationTag {
                return false
            }
            if lifecycle != other.lifecycle {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let generationValue = try generation.toCBORValue()
            map = map.adding(key: "generation", value: generationValue)
            let stateVersionValue = try stateVersion.toCBORValue()
            map = map.adding(key: "stateVersion", value: stateVersionValue)
            let groupIdValue = try groupId.toCBORValue()
            map = map.adding(key: "groupId", value: groupIdValue)
            let epochValue = try epoch.toCBORValue()
            map = map.adding(key: "epoch", value: epochValue)
            let groupContextHashValue = try groupContextHash.toCBORValue()
            map = map.adding(key: "groupContextHash", value: groupContextHashValue)
            let confirmationTagValue = try confirmationTag.toCBORValue()
            map = map.adding(key: "confirmationTag", value: confirmationTagValue)
            let lifecycleValue = try lifecycle.toCBORValue()
            map = map.adding(key: "lifecycle", value: lifecycleValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case conversationId
            case generation
            case stateVersion
            case groupId
            case epoch
            case groupContextHash
            case confirmationTag
            case lifecycle
        }
    }

    public struct ApplicationFrameContext: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#applicationFrameContext"
        public let conversationId: Bytes
        public let generation: Int
        public let stateVersion: Int
        public let groupId: Bytes
        public let epoch: Int
        public let groupContextHash: Bytes
        public let confirmationTag: Bytes
        public let lifecycle: String

        public init(
            conversationId: Bytes, generation: Int, stateVersion: Int, groupId: Bytes, epoch: Int, groupContextHash: Bytes, confirmationTag: Bytes, lifecycle: String
        ) {
            self.conversationId = conversationId
            self.generation = generation
            self.stateVersion = stateVersion
            self.groupId = groupId
            self.epoch = epoch
            self.groupContextHash = groupContextHash
            self.confirmationTag = confirmationTag
            self.lifecycle = lifecycle
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                conversationId = try container.decode(Bytes.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                generation = try container.decode(Int.self, forKey: .generation)
            } catch {
                LogManager.logError("Decoding error for required property 'generation': \(error)")
                throw error
            }
            do {
                stateVersion = try container.decode(Int.self, forKey: .stateVersion)
            } catch {
                LogManager.logError("Decoding error for required property 'stateVersion': \(error)")
                throw error
            }
            do {
                groupId = try container.decode(Bytes.self, forKey: .groupId)
            } catch {
                LogManager.logError("Decoding error for required property 'groupId': \(error)")
                throw error
            }
            do {
                epoch = try container.decode(Int.self, forKey: .epoch)
            } catch {
                LogManager.logError("Decoding error for required property 'epoch': \(error)")
                throw error
            }
            do {
                groupContextHash = try container.decode(Bytes.self, forKey: .groupContextHash)
            } catch {
                LogManager.logError("Decoding error for required property 'groupContextHash': \(error)")
                throw error
            }
            do {
                confirmationTag = try container.decode(Bytes.self, forKey: .confirmationTag)
            } catch {
                LogManager.logError("Decoding error for required property 'confirmationTag': \(error)")
                throw error
            }
            do {
                lifecycle = try container.decode(String.self, forKey: .lifecycle)
            } catch {
                LogManager.logError("Decoding error for required property 'lifecycle': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(generation, forKey: .generation)
            try container.encode(stateVersion, forKey: .stateVersion)
            try container.encode(groupId, forKey: .groupId)
            try container.encode(epoch, forKey: .epoch)
            try container.encode(groupContextHash, forKey: .groupContextHash)
            try container.encode(confirmationTag, forKey: .confirmationTag)
            try container.encode(lifecycle, forKey: .lifecycle)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(conversationId)
            hasher.combine(generation)
            hasher.combine(stateVersion)
            hasher.combine(groupId)
            hasher.combine(epoch)
            hasher.combine(groupContextHash)
            hasher.combine(confirmationTag)
            hasher.combine(lifecycle)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if conversationId != other.conversationId {
                return false
            }
            if generation != other.generation {
                return false
            }
            if stateVersion != other.stateVersion {
                return false
            }
            if groupId != other.groupId {
                return false
            }
            if epoch != other.epoch {
                return false
            }
            if groupContextHash != other.groupContextHash {
                return false
            }
            if confirmationTag != other.confirmationTag {
                return false
            }
            if lifecycle != other.lifecycle {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let generationValue = try generation.toCBORValue()
            map = map.adding(key: "generation", value: generationValue)
            let stateVersionValue = try stateVersion.toCBORValue()
            map = map.adding(key: "stateVersion", value: stateVersionValue)
            let groupIdValue = try groupId.toCBORValue()
            map = map.adding(key: "groupId", value: groupIdValue)
            let epochValue = try epoch.toCBORValue()
            map = map.adding(key: "epoch", value: epochValue)
            let groupContextHashValue = try groupContextHash.toCBORValue()
            map = map.adding(key: "groupContextHash", value: groupContextHashValue)
            let confirmationTagValue = try confirmationTag.toCBORValue()
            map = map.adding(key: "confirmationTag", value: confirmationTagValue)
            let lifecycleValue = try lifecycle.toCBORValue()
            map = map.adding(key: "lifecycle", value: lifecycleValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case conversationId
            case generation
            case stateVersion
            case groupId
            case epoch
            case groupContextHash
            case confirmationTag
            case lifecycle
        }
    }

    public struct MetadataCryptoContext: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#metadataCryptoContext"
        public let conversationId: Bytes
        public let generation: Int
        public let groupId: Bytes
        public let epoch: Int
        public let groupContextHash: Bytes
        public let confirmationTag: Bytes

        public init(
            conversationId: Bytes, generation: Int, groupId: Bytes, epoch: Int, groupContextHash: Bytes, confirmationTag: Bytes
        ) {
            self.conversationId = conversationId
            self.generation = generation
            self.groupId = groupId
            self.epoch = epoch
            self.groupContextHash = groupContextHash
            self.confirmationTag = confirmationTag
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                conversationId = try container.decode(Bytes.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                generation = try container.decode(Int.self, forKey: .generation)
            } catch {
                LogManager.logError("Decoding error for required property 'generation': \(error)")
                throw error
            }
            do {
                groupId = try container.decode(Bytes.self, forKey: .groupId)
            } catch {
                LogManager.logError("Decoding error for required property 'groupId': \(error)")
                throw error
            }
            do {
                epoch = try container.decode(Int.self, forKey: .epoch)
            } catch {
                LogManager.logError("Decoding error for required property 'epoch': \(error)")
                throw error
            }
            do {
                groupContextHash = try container.decode(Bytes.self, forKey: .groupContextHash)
            } catch {
                LogManager.logError("Decoding error for required property 'groupContextHash': \(error)")
                throw error
            }
            do {
                confirmationTag = try container.decode(Bytes.self, forKey: .confirmationTag)
            } catch {
                LogManager.logError("Decoding error for required property 'confirmationTag': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(generation, forKey: .generation)
            try container.encode(groupId, forKey: .groupId)
            try container.encode(epoch, forKey: .epoch)
            try container.encode(groupContextHash, forKey: .groupContextHash)
            try container.encode(confirmationTag, forKey: .confirmationTag)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(conversationId)
            hasher.combine(generation)
            hasher.combine(groupId)
            hasher.combine(epoch)
            hasher.combine(groupContextHash)
            hasher.combine(confirmationTag)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if conversationId != other.conversationId {
                return false
            }
            if generation != other.generation {
                return false
            }
            if groupId != other.groupId {
                return false
            }
            if epoch != other.epoch {
                return false
            }
            if groupContextHash != other.groupContextHash {
                return false
            }
            if confirmationTag != other.confirmationTag {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let generationValue = try generation.toCBORValue()
            map = map.adding(key: "generation", value: generationValue)
            let groupIdValue = try groupId.toCBORValue()
            map = map.adding(key: "groupId", value: groupIdValue)
            let epochValue = try epoch.toCBORValue()
            map = map.adding(key: "epoch", value: epochValue)
            let groupContextHashValue = try groupContextHash.toCBORValue()
            map = map.adding(key: "groupContextHash", value: groupContextHashValue)
            let confirmationTagValue = try confirmationTag.toCBORValue()
            map = map.adding(key: "confirmationTag", value: confirmationTagValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case conversationId
            case generation
            case groupId
            case epoch
            case groupContextHash
            case confirmationTag
        }
    }

    public struct MlsAadPriorContext: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#mlsAadPriorContext"
        public let conversationId: Bytes
        public let generation: Int
        public let stateVersion: Int
        public let groupId: Bytes
        public let epoch: Int
        public let groupContextHash: Bytes
        public let confirmationTag: Bytes
        public let lifecycle: String

        public init(
            conversationId: Bytes, generation: Int, stateVersion: Int, groupId: Bytes, epoch: Int, groupContextHash: Bytes, confirmationTag: Bytes, lifecycle: String
        ) {
            self.conversationId = conversationId
            self.generation = generation
            self.stateVersion = stateVersion
            self.groupId = groupId
            self.epoch = epoch
            self.groupContextHash = groupContextHash
            self.confirmationTag = confirmationTag
            self.lifecycle = lifecycle
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                conversationId = try container.decode(Bytes.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                generation = try container.decode(Int.self, forKey: .generation)
            } catch {
                LogManager.logError("Decoding error for required property 'generation': \(error)")
                throw error
            }
            do {
                stateVersion = try container.decode(Int.self, forKey: .stateVersion)
            } catch {
                LogManager.logError("Decoding error for required property 'stateVersion': \(error)")
                throw error
            }
            do {
                groupId = try container.decode(Bytes.self, forKey: .groupId)
            } catch {
                LogManager.logError("Decoding error for required property 'groupId': \(error)")
                throw error
            }
            do {
                epoch = try container.decode(Int.self, forKey: .epoch)
            } catch {
                LogManager.logError("Decoding error for required property 'epoch': \(error)")
                throw error
            }
            do {
                groupContextHash = try container.decode(Bytes.self, forKey: .groupContextHash)
            } catch {
                LogManager.logError("Decoding error for required property 'groupContextHash': \(error)")
                throw error
            }
            do {
                confirmationTag = try container.decode(Bytes.self, forKey: .confirmationTag)
            } catch {
                LogManager.logError("Decoding error for required property 'confirmationTag': \(error)")
                throw error
            }
            do {
                lifecycle = try container.decode(String.self, forKey: .lifecycle)
            } catch {
                LogManager.logError("Decoding error for required property 'lifecycle': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(generation, forKey: .generation)
            try container.encode(stateVersion, forKey: .stateVersion)
            try container.encode(groupId, forKey: .groupId)
            try container.encode(epoch, forKey: .epoch)
            try container.encode(groupContextHash, forKey: .groupContextHash)
            try container.encode(confirmationTag, forKey: .confirmationTag)
            try container.encode(lifecycle, forKey: .lifecycle)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(conversationId)
            hasher.combine(generation)
            hasher.combine(stateVersion)
            hasher.combine(groupId)
            hasher.combine(epoch)
            hasher.combine(groupContextHash)
            hasher.combine(confirmationTag)
            hasher.combine(lifecycle)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if conversationId != other.conversationId {
                return false
            }
            if generation != other.generation {
                return false
            }
            if stateVersion != other.stateVersion {
                return false
            }
            if groupId != other.groupId {
                return false
            }
            if epoch != other.epoch {
                return false
            }
            if groupContextHash != other.groupContextHash {
                return false
            }
            if confirmationTag != other.confirmationTag {
                return false
            }
            if lifecycle != other.lifecycle {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let generationValue = try generation.toCBORValue()
            map = map.adding(key: "generation", value: generationValue)
            let stateVersionValue = try stateVersion.toCBORValue()
            map = map.adding(key: "stateVersion", value: stateVersionValue)
            let groupIdValue = try groupId.toCBORValue()
            map = map.adding(key: "groupId", value: groupIdValue)
            let epochValue = try epoch.toCBORValue()
            map = map.adding(key: "epoch", value: epochValue)
            let groupContextHashValue = try groupContextHash.toCBORValue()
            map = map.adding(key: "groupContextHash", value: groupContextHashValue)
            let confirmationTagValue = try confirmationTag.toCBORValue()
            map = map.adding(key: "confirmationTag", value: confirmationTagValue)
            let lifecycleValue = try lifecycle.toCBORValue()
            map = map.adding(key: "lifecycle", value: lifecycleValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case conversationId
            case generation
            case stateVersion
            case groupId
            case epoch
            case groupContextHash
            case confirmationTag
            case lifecycle
        }
    }

    public struct KeyPackageArtifact: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#keyPackageArtifact"
        public let framing: String
        public let contentType: String
        public let bytes: Bytes
        public let sha256: Bytes
        public let keyPackageRef: Bytes

        public init(
            framing: String, contentType: String, bytes: Bytes, sha256: Bytes, keyPackageRef: Bytes
        ) {
            self.framing = framing
            self.contentType = contentType
            self.bytes = bytes
            self.sha256 = sha256
            self.keyPackageRef = keyPackageRef
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                framing = try container.decode(String.self, forKey: .framing)
            } catch {
                LogManager.logError("Decoding error for required property 'framing': \(error)")
                throw error
            }
            do {
                contentType = try container.decode(String.self, forKey: .contentType)
            } catch {
                LogManager.logError("Decoding error for required property 'contentType': \(error)")
                throw error
            }
            do {
                bytes = try container.decode(Bytes.self, forKey: .bytes)
            } catch {
                LogManager.logError("Decoding error for required property 'bytes': \(error)")
                throw error
            }
            do {
                sha256 = try container.decode(Bytes.self, forKey: .sha256)
            } catch {
                LogManager.logError("Decoding error for required property 'sha256': \(error)")
                throw error
            }
            do {
                keyPackageRef = try container.decode(Bytes.self, forKey: .keyPackageRef)
            } catch {
                LogManager.logError("Decoding error for required property 'keyPackageRef': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(framing, forKey: .framing)
            try container.encode(contentType, forKey: .contentType)
            try container.encode(bytes, forKey: .bytes)
            try container.encode(sha256, forKey: .sha256)
            try container.encode(keyPackageRef, forKey: .keyPackageRef)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(framing)
            hasher.combine(contentType)
            hasher.combine(bytes)
            hasher.combine(sha256)
            hasher.combine(keyPackageRef)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if framing != other.framing {
                return false
            }
            if contentType != other.contentType {
                return false
            }
            if bytes != other.bytes {
                return false
            }
            if sha256 != other.sha256 {
                return false
            }
            if keyPackageRef != other.keyPackageRef {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let framingValue = try framing.toCBORValue()
            map = map.adding(key: "framing", value: framingValue)
            let contentTypeValue = try contentType.toCBORValue()
            map = map.adding(key: "contentType", value: contentTypeValue)
            let bytesValue = try bytes.toCBORValue()
            map = map.adding(key: "bytes", value: bytesValue)
            let sha256Value = try sha256.toCBORValue()
            map = map.adding(key: "sha256", value: sha256Value)
            let keyPackageRefValue = try keyPackageRef.toCBORValue()
            map = map.adding(key: "keyPackageRef", value: keyPackageRefValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case framing
            case contentType
            case bytes
            case sha256
            case keyPackageRef
        }
    }

    public struct PublicCommit: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#publicCommit"
        public let framing: String
        public let contentType: String
        public let bytes: Bytes
        public let sha256: Bytes

        public init(
            framing: String, contentType: String, bytes: Bytes, sha256: Bytes
        ) {
            self.framing = framing
            self.contentType = contentType
            self.bytes = bytes
            self.sha256 = sha256
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                framing = try container.decode(String.self, forKey: .framing)
            } catch {
                LogManager.logError("Decoding error for required property 'framing': \(error)")
                throw error
            }
            do {
                contentType = try container.decode(String.self, forKey: .contentType)
            } catch {
                LogManager.logError("Decoding error for required property 'contentType': \(error)")
                throw error
            }
            do {
                bytes = try container.decode(Bytes.self, forKey: .bytes)
            } catch {
                LogManager.logError("Decoding error for required property 'bytes': \(error)")
                throw error
            }
            do {
                sha256 = try container.decode(Bytes.self, forKey: .sha256)
            } catch {
                LogManager.logError("Decoding error for required property 'sha256': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(framing, forKey: .framing)
            try container.encode(contentType, forKey: .contentType)
            try container.encode(bytes, forKey: .bytes)
            try container.encode(sha256, forKey: .sha256)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(framing)
            hasher.combine(contentType)
            hasher.combine(bytes)
            hasher.combine(sha256)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if framing != other.framing {
                return false
            }
            if contentType != other.contentType {
                return false
            }
            if bytes != other.bytes {
                return false
            }
            if sha256 != other.sha256 {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let framingValue = try framing.toCBORValue()
            map = map.adding(key: "framing", value: framingValue)
            let contentTypeValue = try contentType.toCBORValue()
            map = map.adding(key: "contentType", value: contentTypeValue)
            let bytesValue = try bytes.toCBORValue()
            map = map.adding(key: "bytes", value: bytesValue)
            let sha256Value = try sha256.toCBORValue()
            map = map.adding(key: "sha256", value: sha256Value)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case framing
            case contentType
            case bytes
            case sha256
        }
    }

    public struct GroupInfoArtifact: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#groupInfoArtifact"
        public let framing: String
        public let contentType: String
        public let bytes: Bytes
        public let sha256: Bytes

        public init(
            framing: String, contentType: String, bytes: Bytes, sha256: Bytes
        ) {
            self.framing = framing
            self.contentType = contentType
            self.bytes = bytes
            self.sha256 = sha256
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                framing = try container.decode(String.self, forKey: .framing)
            } catch {
                LogManager.logError("Decoding error for required property 'framing': \(error)")
                throw error
            }
            do {
                contentType = try container.decode(String.self, forKey: .contentType)
            } catch {
                LogManager.logError("Decoding error for required property 'contentType': \(error)")
                throw error
            }
            do {
                bytes = try container.decode(Bytes.self, forKey: .bytes)
            } catch {
                LogManager.logError("Decoding error for required property 'bytes': \(error)")
                throw error
            }
            do {
                sha256 = try container.decode(Bytes.self, forKey: .sha256)
            } catch {
                LogManager.logError("Decoding error for required property 'sha256': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(framing, forKey: .framing)
            try container.encode(contentType, forKey: .contentType)
            try container.encode(bytes, forKey: .bytes)
            try container.encode(sha256, forKey: .sha256)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(framing)
            hasher.combine(contentType)
            hasher.combine(bytes)
            hasher.combine(sha256)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if framing != other.framing {
                return false
            }
            if contentType != other.contentType {
                return false
            }
            if bytes != other.bytes {
                return false
            }
            if sha256 != other.sha256 {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let framingValue = try framing.toCBORValue()
            map = map.adding(key: "framing", value: framingValue)
            let contentTypeValue = try contentType.toCBORValue()
            map = map.adding(key: "contentType", value: contentTypeValue)
            let bytesValue = try bytes.toCBORValue()
            map = map.adding(key: "bytes", value: bytesValue)
            let sha256Value = try sha256.toCBORValue()
            map = map.adding(key: "sha256", value: sha256Value)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case framing
            case contentType
            case bytes
            case sha256
        }
    }

    public struct PrivateApplicationMessage: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#privateApplicationMessage"
        public let framing: String
        public let contentType: String
        public let bytes: Bytes
        public let sha256: Bytes

        public init(
            framing: String, contentType: String, bytes: Bytes, sha256: Bytes
        ) {
            self.framing = framing
            self.contentType = contentType
            self.bytes = bytes
            self.sha256 = sha256
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                framing = try container.decode(String.self, forKey: .framing)
            } catch {
                LogManager.logError("Decoding error for required property 'framing': \(error)")
                throw error
            }
            do {
                contentType = try container.decode(String.self, forKey: .contentType)
            } catch {
                LogManager.logError("Decoding error for required property 'contentType': \(error)")
                throw error
            }
            do {
                bytes = try container.decode(Bytes.self, forKey: .bytes)
            } catch {
                LogManager.logError("Decoding error for required property 'bytes': \(error)")
                throw error
            }
            do {
                sha256 = try container.decode(Bytes.self, forKey: .sha256)
            } catch {
                LogManager.logError("Decoding error for required property 'sha256': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(framing, forKey: .framing)
            try container.encode(contentType, forKey: .contentType)
            try container.encode(bytes, forKey: .bytes)
            try container.encode(sha256, forKey: .sha256)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(framing)
            hasher.combine(contentType)
            hasher.combine(bytes)
            hasher.combine(sha256)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if framing != other.framing {
                return false
            }
            if contentType != other.contentType {
                return false
            }
            if bytes != other.bytes {
                return false
            }
            if sha256 != other.sha256 {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let framingValue = try framing.toCBORValue()
            map = map.adding(key: "framing", value: framingValue)
            let contentTypeValue = try contentType.toCBORValue()
            map = map.adding(key: "contentType", value: contentTypeValue)
            let bytesValue = try bytes.toCBORValue()
            map = map.adding(key: "bytes", value: bytesValue)
            let sha256Value = try sha256.toCBORValue()
            map = map.adding(key: "sha256", value: sha256Value)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case framing
            case contentType
            case bytes
            case sha256
        }
    }

    public struct DeviceCapability: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#deviceCapability"
        public let protocolVersion: DefsProtocolVersion
        public let mlsVersion: String
        public let cipherSuite: DefsCipherSuite
        public let credentialType: String
        public let addByValue: String
        public let updatePath: String
        public let removeByValue: String
        public let ratchetTreeGroupInfo: String
        public let externalPubGroupInfo: String
        public let applicationFrameProfile: String
        public let controlProfile: String
        public let attachmentProfile: String
        public let metadataProfile: String
        public let typingProfile: String

        public init(
            protocolVersion: DefsProtocolVersion, mlsVersion: String, cipherSuite: DefsCipherSuite, credentialType: String, addByValue: String, updatePath: String, removeByValue: String, ratchetTreeGroupInfo: String, externalPubGroupInfo: String, applicationFrameProfile: String, controlProfile: String, attachmentProfile: String, metadataProfile: String, typingProfile: String
        ) {
            self.protocolVersion = protocolVersion
            self.mlsVersion = mlsVersion
            self.cipherSuite = cipherSuite
            self.credentialType = credentialType
            self.addByValue = addByValue
            self.updatePath = updatePath
            self.removeByValue = removeByValue
            self.ratchetTreeGroupInfo = ratchetTreeGroupInfo
            self.externalPubGroupInfo = externalPubGroupInfo
            self.applicationFrameProfile = applicationFrameProfile
            self.controlProfile = controlProfile
            self.attachmentProfile = attachmentProfile
            self.metadataProfile = metadataProfile
            self.typingProfile = typingProfile
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                protocolVersion = try container.decode(DefsProtocolVersion.self, forKey: .protocolVersion)
            } catch {
                LogManager.logError("Decoding error for required property 'protocolVersion': \(error)")
                throw error
            }
            do {
                mlsVersion = try container.decode(String.self, forKey: .mlsVersion)
            } catch {
                LogManager.logError("Decoding error for required property 'mlsVersion': \(error)")
                throw error
            }
            do {
                cipherSuite = try container.decode(DefsCipherSuite.self, forKey: .cipherSuite)
            } catch {
                LogManager.logError("Decoding error for required property 'cipherSuite': \(error)")
                throw error
            }
            do {
                credentialType = try container.decode(String.self, forKey: .credentialType)
            } catch {
                LogManager.logError("Decoding error for required property 'credentialType': \(error)")
                throw error
            }
            do {
                addByValue = try container.decode(String.self, forKey: .addByValue)
            } catch {
                LogManager.logError("Decoding error for required property 'addByValue': \(error)")
                throw error
            }
            do {
                updatePath = try container.decode(String.self, forKey: .updatePath)
            } catch {
                LogManager.logError("Decoding error for required property 'updatePath': \(error)")
                throw error
            }
            do {
                removeByValue = try container.decode(String.self, forKey: .removeByValue)
            } catch {
                LogManager.logError("Decoding error for required property 'removeByValue': \(error)")
                throw error
            }
            do {
                ratchetTreeGroupInfo = try container.decode(String.self, forKey: .ratchetTreeGroupInfo)
            } catch {
                LogManager.logError("Decoding error for required property 'ratchetTreeGroupInfo': \(error)")
                throw error
            }
            do {
                externalPubGroupInfo = try container.decode(String.self, forKey: .externalPubGroupInfo)
            } catch {
                LogManager.logError("Decoding error for required property 'externalPubGroupInfo': \(error)")
                throw error
            }
            do {
                applicationFrameProfile = try container.decode(String.self, forKey: .applicationFrameProfile)
            } catch {
                LogManager.logError("Decoding error for required property 'applicationFrameProfile': \(error)")
                throw error
            }
            do {
                controlProfile = try container.decode(String.self, forKey: .controlProfile)
            } catch {
                LogManager.logError("Decoding error for required property 'controlProfile': \(error)")
                throw error
            }
            do {
                attachmentProfile = try container.decode(String.self, forKey: .attachmentProfile)
            } catch {
                LogManager.logError("Decoding error for required property 'attachmentProfile': \(error)")
                throw error
            }
            do {
                metadataProfile = try container.decode(String.self, forKey: .metadataProfile)
            } catch {
                LogManager.logError("Decoding error for required property 'metadataProfile': \(error)")
                throw error
            }
            do {
                typingProfile = try container.decode(String.self, forKey: .typingProfile)
            } catch {
                LogManager.logError("Decoding error for required property 'typingProfile': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(protocolVersion, forKey: .protocolVersion)
            try container.encode(mlsVersion, forKey: .mlsVersion)
            try container.encode(cipherSuite, forKey: .cipherSuite)
            try container.encode(credentialType, forKey: .credentialType)
            try container.encode(addByValue, forKey: .addByValue)
            try container.encode(updatePath, forKey: .updatePath)
            try container.encode(removeByValue, forKey: .removeByValue)
            try container.encode(ratchetTreeGroupInfo, forKey: .ratchetTreeGroupInfo)
            try container.encode(externalPubGroupInfo, forKey: .externalPubGroupInfo)
            try container.encode(applicationFrameProfile, forKey: .applicationFrameProfile)
            try container.encode(controlProfile, forKey: .controlProfile)
            try container.encode(attachmentProfile, forKey: .attachmentProfile)
            try container.encode(metadataProfile, forKey: .metadataProfile)
            try container.encode(typingProfile, forKey: .typingProfile)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(protocolVersion)
            hasher.combine(mlsVersion)
            hasher.combine(cipherSuite)
            hasher.combine(credentialType)
            hasher.combine(addByValue)
            hasher.combine(updatePath)
            hasher.combine(removeByValue)
            hasher.combine(ratchetTreeGroupInfo)
            hasher.combine(externalPubGroupInfo)
            hasher.combine(applicationFrameProfile)
            hasher.combine(controlProfile)
            hasher.combine(attachmentProfile)
            hasher.combine(metadataProfile)
            hasher.combine(typingProfile)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if protocolVersion != other.protocolVersion {
                return false
            }
            if mlsVersion != other.mlsVersion {
                return false
            }
            if cipherSuite != other.cipherSuite {
                return false
            }
            if credentialType != other.credentialType {
                return false
            }
            if addByValue != other.addByValue {
                return false
            }
            if updatePath != other.updatePath {
                return false
            }
            if removeByValue != other.removeByValue {
                return false
            }
            if ratchetTreeGroupInfo != other.ratchetTreeGroupInfo {
                return false
            }
            if externalPubGroupInfo != other.externalPubGroupInfo {
                return false
            }
            if applicationFrameProfile != other.applicationFrameProfile {
                return false
            }
            if controlProfile != other.controlProfile {
                return false
            }
            if attachmentProfile != other.attachmentProfile {
                return false
            }
            if metadataProfile != other.metadataProfile {
                return false
            }
            if typingProfile != other.typingProfile {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let protocolVersionValue = try protocolVersion.toCBORValue()
            map = map.adding(key: "protocolVersion", value: protocolVersionValue)
            let mlsVersionValue = try mlsVersion.toCBORValue()
            map = map.adding(key: "mlsVersion", value: mlsVersionValue)
            let cipherSuiteValue = try cipherSuite.toCBORValue()
            map = map.adding(key: "cipherSuite", value: cipherSuiteValue)
            let credentialTypeValue = try credentialType.toCBORValue()
            map = map.adding(key: "credentialType", value: credentialTypeValue)
            let addByValueValue = try addByValue.toCBORValue()
            map = map.adding(key: "addByValue", value: addByValueValue)
            let updatePathValue = try updatePath.toCBORValue()
            map = map.adding(key: "updatePath", value: updatePathValue)
            let removeByValueValue = try removeByValue.toCBORValue()
            map = map.adding(key: "removeByValue", value: removeByValueValue)
            let ratchetTreeGroupInfoValue = try ratchetTreeGroupInfo.toCBORValue()
            map = map.adding(key: "ratchetTreeGroupInfo", value: ratchetTreeGroupInfoValue)
            let externalPubGroupInfoValue = try externalPubGroupInfo.toCBORValue()
            map = map.adding(key: "externalPubGroupInfo", value: externalPubGroupInfoValue)
            let applicationFrameProfileValue = try applicationFrameProfile.toCBORValue()
            map = map.adding(key: "applicationFrameProfile", value: applicationFrameProfileValue)
            let controlProfileValue = try controlProfile.toCBORValue()
            map = map.adding(key: "controlProfile", value: controlProfileValue)
            let attachmentProfileValue = try attachmentProfile.toCBORValue()
            map = map.adding(key: "attachmentProfile", value: attachmentProfileValue)
            let metadataProfileValue = try metadataProfile.toCBORValue()
            map = map.adding(key: "metadataProfile", value: metadataProfileValue)
            let typingProfileValue = try typingProfile.toCBORValue()
            map = map.adding(key: "typingProfile", value: typingProfileValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case protocolVersion
            case mlsVersion
            case cipherSuite
            case credentialType
            case addByValue
            case updatePath
            case removeByValue
            case ratchetTreeGroupInfo
            case externalPubGroupInfo
            case applicationFrameProfile
            case controlProfile
            case attachmentProfile
            case metadataProfile
            case typingProfile
        }
    }

    public struct DeviceView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#deviceView"
        public let deviceId: DeviceId
        public let keyId: KeyId
        public let signaturePublicKey: Bytes
        public let authGeneration: Int
        public let dpopJkt: String
        public let status: DefsDeviceStatus
        public let createdAt: CanonicalDatetime
        public let updatedAt: CanonicalDatetime
        public let availablePackageCount: Int
        public let reservedPackageCount: Int

        public init(
            deviceId: DeviceId, keyId: KeyId, signaturePublicKey: Bytes, authGeneration: Int, dpopJkt: String, status: DefsDeviceStatus, createdAt: CanonicalDatetime, updatedAt: CanonicalDatetime, availablePackageCount: Int, reservedPackageCount: Int
        ) {
            self.deviceId = deviceId
            self.keyId = keyId
            self.signaturePublicKey = signaturePublicKey
            self.authGeneration = authGeneration
            self.dpopJkt = dpopJkt
            self.status = status
            self.createdAt = createdAt
            self.updatedAt = updatedAt
            self.availablePackageCount = availablePackageCount
            self.reservedPackageCount = reservedPackageCount
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                deviceId = try container.decode(DeviceId.self, forKey: .deviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                signaturePublicKey = try container.decode(Bytes.self, forKey: .signaturePublicKey)
            } catch {
                LogManager.logError("Decoding error for required property 'signaturePublicKey': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                dpopJkt = try container.decode(String.self, forKey: .dpopJkt)
            } catch {
                LogManager.logError("Decoding error for required property 'dpopJkt': \(error)")
                throw error
            }
            do {
                status = try container.decode(DefsDeviceStatus.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
            do {
                createdAt = try container.decode(CanonicalDatetime.self, forKey: .createdAt)
            } catch {
                LogManager.logError("Decoding error for required property 'createdAt': \(error)")
                throw error
            }
            do {
                updatedAt = try container.decode(CanonicalDatetime.self, forKey: .updatedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'updatedAt': \(error)")
                throw error
            }
            do {
                availablePackageCount = try container.decode(Int.self, forKey: .availablePackageCount)
            } catch {
                LogManager.logError("Decoding error for required property 'availablePackageCount': \(error)")
                throw error
            }
            do {
                reservedPackageCount = try container.decode(Int.self, forKey: .reservedPackageCount)
            } catch {
                LogManager.logError("Decoding error for required property 'reservedPackageCount': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(deviceId, forKey: .deviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(signaturePublicKey, forKey: .signaturePublicKey)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(dpopJkt, forKey: .dpopJkt)
            try container.encode(status, forKey: .status)
            try container.encode(createdAt, forKey: .createdAt)
            try container.encode(updatedAt, forKey: .updatedAt)
            try container.encode(availablePackageCount, forKey: .availablePackageCount)
            try container.encode(reservedPackageCount, forKey: .reservedPackageCount)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(deviceId)
            hasher.combine(keyId)
            hasher.combine(signaturePublicKey)
            hasher.combine(authGeneration)
            hasher.combine(dpopJkt)
            hasher.combine(status)
            hasher.combine(createdAt)
            hasher.combine(updatedAt)
            hasher.combine(availablePackageCount)
            hasher.combine(reservedPackageCount)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if deviceId != other.deviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if signaturePublicKey != other.signaturePublicKey {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if dpopJkt != other.dpopJkt {
                return false
            }
            if status != other.status {
                return false
            }
            if createdAt != other.createdAt {
                return false
            }
            if updatedAt != other.updatedAt {
                return false
            }
            if availablePackageCount != other.availablePackageCount {
                return false
            }
            if reservedPackageCount != other.reservedPackageCount {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let deviceIdValue = try deviceId.toCBORValue()
            map = map.adding(key: "deviceId", value: deviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let signaturePublicKeyValue = try signaturePublicKey.toCBORValue()
            map = map.adding(key: "signaturePublicKey", value: signaturePublicKeyValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let dpopJktValue = try dpopJkt.toCBORValue()
            map = map.adding(key: "dpopJkt", value: dpopJktValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            let createdAtValue = try createdAt.toCBORValue()
            map = map.adding(key: "createdAt", value: createdAtValue)
            let updatedAtValue = try updatedAt.toCBORValue()
            map = map.adding(key: "updatedAt", value: updatedAtValue)
            let availablePackageCountValue = try availablePackageCount.toCBORValue()
            map = map.adding(key: "availablePackageCount", value: availablePackageCountValue)
            let reservedPackageCountValue = try reservedPackageCount.toCBORValue()
            map = map.adding(key: "reservedPackageCount", value: reservedPackageCountValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case deviceId
            case keyId
            case signaturePublicKey
            case authGeneration
            case dpopJkt
            case status
            case createdAt
            case updatedAt
            case availablePackageCount
            case reservedPackageCount
        }
    }

    public struct OwnDeviceView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#ownDeviceView"
        public let device: DeviceView

        public init(
            device: DeviceView
        ) {
            self.device = device
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                device = try container.decode(DeviceView.self, forKey: .device)
            } catch {
                LogManager.logError("Decoding error for required property 'device': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(device, forKey: .device)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(device)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if device != other.device {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let deviceValue = try device.toCBORValue()
            map = map.adding(key: "device", value: deviceValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case device
        }
    }

    public struct AddressableDevice: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#addressableDevice"
        public let userDid: BareDid
        public let deviceId: DeviceId
        public let keyId: KeyId
        public let capability: DeviceCapability
        public let availablePackageCount: Int

        public init(
            userDid: BareDid, deviceId: DeviceId, keyId: KeyId, capability: DeviceCapability, availablePackageCount: Int
        ) {
            self.userDid = userDid
            self.deviceId = deviceId
            self.keyId = keyId
            self.capability = capability
            self.availablePackageCount = availablePackageCount
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                userDid = try container.decode(BareDid.self, forKey: .userDid)
            } catch {
                LogManager.logError("Decoding error for required property 'userDid': \(error)")
                throw error
            }
            do {
                deviceId = try container.decode(DeviceId.self, forKey: .deviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                capability = try container.decode(DeviceCapability.self, forKey: .capability)
            } catch {
                LogManager.logError("Decoding error for required property 'capability': \(error)")
                throw error
            }
            do {
                availablePackageCount = try container.decode(Int.self, forKey: .availablePackageCount)
            } catch {
                LogManager.logError("Decoding error for required property 'availablePackageCount': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(userDid, forKey: .userDid)
            try container.encode(deviceId, forKey: .deviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(capability, forKey: .capability)
            try container.encode(availablePackageCount, forKey: .availablePackageCount)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(userDid)
            hasher.combine(deviceId)
            hasher.combine(keyId)
            hasher.combine(capability)
            hasher.combine(availablePackageCount)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if userDid != other.userDid {
                return false
            }
            if deviceId != other.deviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if capability != other.capability {
                return false
            }
            if availablePackageCount != other.availablePackageCount {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let userDidValue = try userDid.toCBORValue()
            map = map.adding(key: "userDid", value: userDidValue)
            let deviceIdValue = try deviceId.toCBORValue()
            map = map.adding(key: "deviceId", value: deviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let capabilityValue = try capability.toCBORValue()
            map = map.adding(key: "capability", value: capabilityValue)
            let availablePackageCountValue = try availablePackageCount.toCBORValue()
            map = map.adding(key: "availablePackageCount", value: availablePackageCountValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case userDid
            case deviceId
            case keyId
            case capability
            case availablePackageCount
        }
    }

    public struct InvitationProvenance: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#invitationProvenance"
        public let invitationTransitionId: OperationId
        public let invitedByDid: BareDid
        public let invitedByDeviceId: DeviceId

        public init(
            invitationTransitionId: OperationId, invitedByDid: BareDid, invitedByDeviceId: DeviceId
        ) {
            self.invitationTransitionId = invitationTransitionId
            self.invitedByDid = invitedByDid
            self.invitedByDeviceId = invitedByDeviceId
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                invitationTransitionId = try container.decode(OperationId.self, forKey: .invitationTransitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'invitationTransitionId': \(error)")
                throw error
            }
            do {
                invitedByDid = try container.decode(BareDid.self, forKey: .invitedByDid)
            } catch {
                LogManager.logError("Decoding error for required property 'invitedByDid': \(error)")
                throw error
            }
            do {
                invitedByDeviceId = try container.decode(DeviceId.self, forKey: .invitedByDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'invitedByDeviceId': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(invitationTransitionId, forKey: .invitationTransitionId)
            try container.encode(invitedByDid, forKey: .invitedByDid)
            try container.encode(invitedByDeviceId, forKey: .invitedByDeviceId)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(invitationTransitionId)
            hasher.combine(invitedByDid)
            hasher.combine(invitedByDeviceId)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if invitationTransitionId != other.invitationTransitionId {
                return false
            }
            if invitedByDid != other.invitedByDid {
                return false
            }
            if invitedByDeviceId != other.invitedByDeviceId {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let invitationTransitionIdValue = try invitationTransitionId.toCBORValue()
            map = map.adding(key: "invitationTransitionId", value: invitationTransitionIdValue)
            let invitedByDidValue = try invitedByDid.toCBORValue()
            map = map.adding(key: "invitedByDid", value: invitedByDidValue)
            let invitedByDeviceIdValue = try invitedByDeviceId.toCBORValue()
            map = map.adding(key: "invitedByDeviceId", value: invitedByDeviceIdValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case invitationTransitionId
            case invitedByDid
            case invitedByDeviceId
        }
    }

    public struct Participant: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#participant"
        public let userDid: BareDid
        public let role: DefsMemberRole
        public let status: DefsParticipantStatus
        public let invitationProvenance: InvitationProvenance?

        public init(
            userDid: BareDid, role: DefsMemberRole, status: DefsParticipantStatus, invitationProvenance: InvitationProvenance?
        ) {
            self.userDid = userDid
            self.role = role
            self.status = status
            self.invitationProvenance = invitationProvenance
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                userDid = try container.decode(BareDid.self, forKey: .userDid)
            } catch {
                LogManager.logError("Decoding error for required property 'userDid': \(error)")
                throw error
            }
            do {
                role = try container.decode(DefsMemberRole.self, forKey: .role)
            } catch {
                LogManager.logError("Decoding error for required property 'role': \(error)")
                throw error
            }
            do {
                status = try container.decode(DefsParticipantStatus.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
            do {
                invitationProvenance = try container.decodeIfPresent(InvitationProvenance.self, forKey: .invitationProvenance)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'invitationProvenance' — degrading to nil: \(error)")
                invitationProvenance = nil
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(userDid, forKey: .userDid)
            try container.encode(role, forKey: .role)
            try container.encode(status, forKey: .status)
            try container.encodeIfPresent(invitationProvenance, forKey: .invitationProvenance)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(userDid)
            hasher.combine(role)
            hasher.combine(status)
            if let value = invitationProvenance {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if userDid != other.userDid {
                return false
            }
            if role != other.role {
                return false
            }
            if status != other.status {
                return false
            }
            if invitationProvenance != other.invitationProvenance {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let userDidValue = try userDid.toCBORValue()
            map = map.adding(key: "userDid", value: userDidValue)
            let roleValue = try role.toCBORValue()
            map = map.adding(key: "role", value: roleValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            if let value = invitationProvenance {
                let invitationProvenanceValue = try value.toCBORValue()
                map = map.adding(key: "invitationProvenance", value: invitationProvenanceValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case userDid
            case role
            case status
            case invitationProvenance
        }
    }

    public struct ParticipantView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#participantView"
        public let userDid: BareDid
        public let role: DefsMemberRole
        public let status: DefsParticipantStatus
        public let invitationProvenance: InvitationProvenance?
        public let leafCount: Int

        public init(
            userDid: BareDid, role: DefsMemberRole, status: DefsParticipantStatus, invitationProvenance: InvitationProvenance?, leafCount: Int
        ) {
            self.userDid = userDid
            self.role = role
            self.status = status
            self.invitationProvenance = invitationProvenance
            self.leafCount = leafCount
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                userDid = try container.decode(BareDid.self, forKey: .userDid)
            } catch {
                LogManager.logError("Decoding error for required property 'userDid': \(error)")
                throw error
            }
            do {
                role = try container.decode(DefsMemberRole.self, forKey: .role)
            } catch {
                LogManager.logError("Decoding error for required property 'role': \(error)")
                throw error
            }
            do {
                status = try container.decode(DefsParticipantStatus.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
            do {
                invitationProvenance = try container.decodeIfPresent(InvitationProvenance.self, forKey: .invitationProvenance)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'invitationProvenance' — degrading to nil: \(error)")
                invitationProvenance = nil
            }
            do {
                leafCount = try container.decode(Int.self, forKey: .leafCount)
            } catch {
                LogManager.logError("Decoding error for required property 'leafCount': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(userDid, forKey: .userDid)
            try container.encode(role, forKey: .role)
            try container.encode(status, forKey: .status)
            try container.encodeIfPresent(invitationProvenance, forKey: .invitationProvenance)
            try container.encode(leafCount, forKey: .leafCount)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(userDid)
            hasher.combine(role)
            hasher.combine(status)
            if let value = invitationProvenance {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
            hasher.combine(leafCount)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if userDid != other.userDid {
                return false
            }
            if role != other.role {
                return false
            }
            if status != other.status {
                return false
            }
            if invitationProvenance != other.invitationProvenance {
                return false
            }
            if leafCount != other.leafCount {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let userDidValue = try userDid.toCBORValue()
            map = map.adding(key: "userDid", value: userDidValue)
            let roleValue = try role.toCBORValue()
            map = map.adding(key: "role", value: roleValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            if let value = invitationProvenance {
                let invitationProvenanceValue = try value.toCBORValue()
                map = map.adding(key: "invitationProvenance", value: invitationProvenanceValue)
            }
            let leafCountValue = try leafCount.toCBORValue()
            map = map.adding(key: "leafCount", value: leafCountValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case userDid
            case role
            case status
            case invitationProvenance
            case leafCount
        }
    }

    public struct DeviceLeaf: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#deviceLeaf"
        public let userDid: BareDid
        public let deviceId: DeviceId
        public let leafOrigin: DefsLeafOrigin
        public let joinKeyPackageRef: Bytes?

        public init(
            userDid: BareDid, deviceId: DeviceId, leafOrigin: DefsLeafOrigin, joinKeyPackageRef: Bytes?
        ) {
            self.userDid = userDid
            self.deviceId = deviceId
            self.leafOrigin = leafOrigin
            self.joinKeyPackageRef = joinKeyPackageRef
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                userDid = try container.decode(BareDid.self, forKey: .userDid)
            } catch {
                LogManager.logError("Decoding error for required property 'userDid': \(error)")
                throw error
            }
            do {
                deviceId = try container.decode(DeviceId.self, forKey: .deviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceId': \(error)")
                throw error
            }
            do {
                leafOrigin = try container.decode(DefsLeafOrigin.self, forKey: .leafOrigin)
            } catch {
                LogManager.logError("Decoding error for required property 'leafOrigin': \(error)")
                throw error
            }
            do {
                joinKeyPackageRef = try container.decodeIfPresent(Bytes.self, forKey: .joinKeyPackageRef)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'joinKeyPackageRef' — degrading to nil: \(error)")
                joinKeyPackageRef = nil
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(userDid, forKey: .userDid)
            try container.encode(deviceId, forKey: .deviceId)
            try container.encode(leafOrigin, forKey: .leafOrigin)
            try container.encodeIfPresent(joinKeyPackageRef, forKey: .joinKeyPackageRef)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(userDid)
            hasher.combine(deviceId)
            hasher.combine(leafOrigin)
            if let value = joinKeyPackageRef {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if userDid != other.userDid {
                return false
            }
            if deviceId != other.deviceId {
                return false
            }
            if leafOrigin != other.leafOrigin {
                return false
            }
            if joinKeyPackageRef != other.joinKeyPackageRef {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let userDidValue = try userDid.toCBORValue()
            map = map.adding(key: "userDid", value: userDidValue)
            let deviceIdValue = try deviceId.toCBORValue()
            map = map.adding(key: "deviceId", value: deviceIdValue)
            let leafOriginValue = try leafOrigin.toCBORValue()
            map = map.adding(key: "leafOrigin", value: leafOriginValue)
            if let value = joinKeyPackageRef {
                let joinKeyPackageRefValue = try value.toCBORValue()
                map = map.adding(key: "joinKeyPackageRef", value: joinKeyPackageRefValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case userDid
            case deviceId
            case leafOrigin
            case joinKeyPackageRef
        }
    }

    public struct DeviceLeafView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#deviceLeafView"
        public let userDid: BareDid
        public let deviceId: DeviceId
        public let leafOrigin: DefsLeafOrigin
        public let joinKeyPackageRef: Bytes?
        public let keyId: KeyId
        public let deviceStatus: DefsDeviceStatus

        public init(
            userDid: BareDid, deviceId: DeviceId, leafOrigin: DefsLeafOrigin, joinKeyPackageRef: Bytes?, keyId: KeyId, deviceStatus: DefsDeviceStatus
        ) {
            self.userDid = userDid
            self.deviceId = deviceId
            self.leafOrigin = leafOrigin
            self.joinKeyPackageRef = joinKeyPackageRef
            self.keyId = keyId
            self.deviceStatus = deviceStatus
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                userDid = try container.decode(BareDid.self, forKey: .userDid)
            } catch {
                LogManager.logError("Decoding error for required property 'userDid': \(error)")
                throw error
            }
            do {
                deviceId = try container.decode(DeviceId.self, forKey: .deviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceId': \(error)")
                throw error
            }
            do {
                leafOrigin = try container.decode(DefsLeafOrigin.self, forKey: .leafOrigin)
            } catch {
                LogManager.logError("Decoding error for required property 'leafOrigin': \(error)")
                throw error
            }
            do {
                joinKeyPackageRef = try container.decodeIfPresent(Bytes.self, forKey: .joinKeyPackageRef)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'joinKeyPackageRef' — degrading to nil: \(error)")
                joinKeyPackageRef = nil
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                deviceStatus = try container.decode(DefsDeviceStatus.self, forKey: .deviceStatus)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceStatus': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(userDid, forKey: .userDid)
            try container.encode(deviceId, forKey: .deviceId)
            try container.encode(leafOrigin, forKey: .leafOrigin)
            try container.encodeIfPresent(joinKeyPackageRef, forKey: .joinKeyPackageRef)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(deviceStatus, forKey: .deviceStatus)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(userDid)
            hasher.combine(deviceId)
            hasher.combine(leafOrigin)
            if let value = joinKeyPackageRef {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
            hasher.combine(keyId)
            hasher.combine(deviceStatus)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if userDid != other.userDid {
                return false
            }
            if deviceId != other.deviceId {
                return false
            }
            if leafOrigin != other.leafOrigin {
                return false
            }
            if joinKeyPackageRef != other.joinKeyPackageRef {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if deviceStatus != other.deviceStatus {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let userDidValue = try userDid.toCBORValue()
            map = map.adding(key: "userDid", value: userDidValue)
            let deviceIdValue = try deviceId.toCBORValue()
            map = map.adding(key: "deviceId", value: deviceIdValue)
            let leafOriginValue = try leafOrigin.toCBORValue()
            map = map.adding(key: "leafOrigin", value: leafOriginValue)
            if let value = joinKeyPackageRef {
                let joinKeyPackageRefValue = try value.toCBORValue()
                map = map.adding(key: "joinKeyPackageRef", value: joinKeyPackageRefValue)
            }
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let deviceStatusValue = try deviceStatus.toCBORValue()
            map = map.adding(key: "deviceStatus", value: deviceStatusValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case userDid
            case deviceId
            case leafOrigin
            case joinKeyPackageRef
            case keyId
            case deviceStatus
        }
    }

    public struct AddParticipant: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#addParticipant"
        public let userDid: BareDid
        public let role: String
        public let status: String
        public let invitationProvenance: InvitationProvenance

        public init(
            userDid: BareDid, role: String, status: String, invitationProvenance: InvitationProvenance
        ) {
            self.userDid = userDid
            self.role = role
            self.status = status
            self.invitationProvenance = invitationProvenance
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                userDid = try container.decode(BareDid.self, forKey: .userDid)
            } catch {
                LogManager.logError("Decoding error for required property 'userDid': \(error)")
                throw error
            }
            do {
                role = try container.decode(String.self, forKey: .role)
            } catch {
                LogManager.logError("Decoding error for required property 'role': \(error)")
                throw error
            }
            do {
                status = try container.decode(String.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
            do {
                invitationProvenance = try container.decode(InvitationProvenance.self, forKey: .invitationProvenance)
            } catch {
                LogManager.logError("Decoding error for required property 'invitationProvenance': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(userDid, forKey: .userDid)
            try container.encode(role, forKey: .role)
            try container.encode(status, forKey: .status)
            try container.encode(invitationProvenance, forKey: .invitationProvenance)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(userDid)
            hasher.combine(role)
            hasher.combine(status)
            hasher.combine(invitationProvenance)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if userDid != other.userDid {
                return false
            }
            if role != other.role {
                return false
            }
            if status != other.status {
                return false
            }
            if invitationProvenance != other.invitationProvenance {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let userDidValue = try userDid.toCBORValue()
            map = map.adding(key: "userDid", value: userDidValue)
            let roleValue = try role.toCBORValue()
            map = map.adding(key: "role", value: roleValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            let invitationProvenanceValue = try invitationProvenance.toCBORValue()
            map = map.adding(key: "invitationProvenance", value: invitationProvenanceValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case userDid
            case role
            case status
            case invitationProvenance
        }
    }

    public struct RemoveParticipant: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#removeParticipant"
        public let userDid: BareDid

        public init(
            userDid: BareDid
        ) {
            self.userDid = userDid
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                userDid = try container.decode(BareDid.self, forKey: .userDid)
            } catch {
                LogManager.logError("Decoding error for required property 'userDid': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(userDid, forKey: .userDid)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(userDid)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if userDid != other.userDid {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let userDidValue = try userDid.toCBORValue()
            map = map.adding(key: "userDid", value: userDidValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case userDid
        }
    }

    public struct ChangeParticipantRole: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#changeParticipantRole"
        public let userDid: BareDid
        public let role: DefsMemberRole

        public init(
            userDid: BareDid, role: DefsMemberRole
        ) {
            self.userDid = userDid
            self.role = role
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                userDid = try container.decode(BareDid.self, forKey: .userDid)
            } catch {
                LogManager.logError("Decoding error for required property 'userDid': \(error)")
                throw error
            }
            do {
                role = try container.decode(DefsMemberRole.self, forKey: .role)
            } catch {
                LogManager.logError("Decoding error for required property 'role': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(userDid, forKey: .userDid)
            try container.encode(role, forKey: .role)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(userDid)
            hasher.combine(role)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if userDid != other.userDid {
                return false
            }
            if role != other.role {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let userDidValue = try userDid.toCBORValue()
            map = map.adding(key: "userDid", value: userDidValue)
            let roleValue = try role.toCBORValue()
            map = map.adding(key: "role", value: roleValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case userDid
            case role
        }
    }

    public struct AddLeafByRecovery: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#addLeafByRecovery"
        public let userDid: BareDid
        public let deviceId: DeviceId
        public let recoveryRequestId: OperationId
        public let keyPackageRef: Bytes

        public init(
            userDid: BareDid, deviceId: DeviceId, recoveryRequestId: OperationId, keyPackageRef: Bytes
        ) {
            self.userDid = userDid
            self.deviceId = deviceId
            self.recoveryRequestId = recoveryRequestId
            self.keyPackageRef = keyPackageRef
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                userDid = try container.decode(BareDid.self, forKey: .userDid)
            } catch {
                LogManager.logError("Decoding error for required property 'userDid': \(error)")
                throw error
            }
            do {
                deviceId = try container.decode(DeviceId.self, forKey: .deviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceId': \(error)")
                throw error
            }
            do {
                recoveryRequestId = try container.decode(OperationId.self, forKey: .recoveryRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryRequestId': \(error)")
                throw error
            }
            do {
                keyPackageRef = try container.decode(Bytes.self, forKey: .keyPackageRef)
            } catch {
                LogManager.logError("Decoding error for required property 'keyPackageRef': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(userDid, forKey: .userDid)
            try container.encode(deviceId, forKey: .deviceId)
            try container.encode(recoveryRequestId, forKey: .recoveryRequestId)
            try container.encode(keyPackageRef, forKey: .keyPackageRef)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(userDid)
            hasher.combine(deviceId)
            hasher.combine(recoveryRequestId)
            hasher.combine(keyPackageRef)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if userDid != other.userDid {
                return false
            }
            if deviceId != other.deviceId {
                return false
            }
            if recoveryRequestId != other.recoveryRequestId {
                return false
            }
            if keyPackageRef != other.keyPackageRef {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let userDidValue = try userDid.toCBORValue()
            map = map.adding(key: "userDid", value: userDidValue)
            let deviceIdValue = try deviceId.toCBORValue()
            map = map.adding(key: "deviceId", value: deviceIdValue)
            let recoveryRequestIdValue = try recoveryRequestId.toCBORValue()
            map = map.adding(key: "recoveryRequestId", value: recoveryRequestIdValue)
            let keyPackageRefValue = try keyPackageRef.toCBORValue()
            map = map.adding(key: "keyPackageRef", value: keyPackageRefValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case userDid
            case deviceId
            case recoveryRequestId
            case keyPackageRef
        }
    }

    public struct RemoveLeaf: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#removeLeaf"
        public let userDid: BareDid
        public let deviceId: DeviceId

        public init(
            userDid: BareDid, deviceId: DeviceId
        ) {
            self.userDid = userDid
            self.deviceId = deviceId
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                userDid = try container.decode(BareDid.self, forKey: .userDid)
            } catch {
                LogManager.logError("Decoding error for required property 'userDid': \(error)")
                throw error
            }
            do {
                deviceId = try container.decode(DeviceId.self, forKey: .deviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceId': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(userDid, forKey: .userDid)
            try container.encode(deviceId, forKey: .deviceId)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(userDid)
            hasher.combine(deviceId)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if userDid != other.userDid {
                return false
            }
            if deviceId != other.deviceId {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let userDidValue = try userDid.toCBORValue()
            map = map.adding(key: "userDid", value: userDidValue)
            let deviceIdValue = try deviceId.toCBORValue()
            map = map.adding(key: "deviceId", value: deviceIdValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case userDid
            case deviceId
        }
    }

    public struct RecoveryWelcomeProvenance: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#recoveryWelcomeProvenance"
        public let recoveryRequestId: OperationId
        public let keyPackageRef: Bytes

        public init(
            recoveryRequestId: OperationId, keyPackageRef: Bytes
        ) {
            self.recoveryRequestId = recoveryRequestId
            self.keyPackageRef = keyPackageRef
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                recoveryRequestId = try container.decode(OperationId.self, forKey: .recoveryRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryRequestId': \(error)")
                throw error
            }
            do {
                keyPackageRef = try container.decode(Bytes.self, forKey: .keyPackageRef)
            } catch {
                LogManager.logError("Decoding error for required property 'keyPackageRef': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(recoveryRequestId, forKey: .recoveryRequestId)
            try container.encode(keyPackageRef, forKey: .keyPackageRef)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(recoveryRequestId)
            hasher.combine(keyPackageRef)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if recoveryRequestId != other.recoveryRequestId {
                return false
            }
            if keyPackageRef != other.keyPackageRef {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let recoveryRequestIdValue = try recoveryRequestId.toCBORValue()
            map = map.adding(key: "recoveryRequestId", value: recoveryRequestIdValue)
            let keyPackageRefValue = try keyPackageRef.toCBORValue()
            map = map.adding(key: "keyPackageRef", value: keyPackageRefValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case recoveryRequestId
            case keyPackageRef
        }
    }

    public struct WelcomeDelivery: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#welcomeDelivery"
        public let recipientDid: BareDid
        public let recipientDeviceId: DeviceId
        public let provenance: RecoveryWelcomeProvenance

        public init(
            recipientDid: BareDid, recipientDeviceId: DeviceId, provenance: RecoveryWelcomeProvenance
        ) {
            self.recipientDid = recipientDid
            self.recipientDeviceId = recipientDeviceId
            self.provenance = provenance
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                recipientDid = try container.decode(BareDid.self, forKey: .recipientDid)
            } catch {
                LogManager.logError("Decoding error for required property 'recipientDid': \(error)")
                throw error
            }
            do {
                recipientDeviceId = try container.decode(DeviceId.self, forKey: .recipientDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'recipientDeviceId': \(error)")
                throw error
            }
            do {
                provenance = try container.decode(RecoveryWelcomeProvenance.self, forKey: .provenance)
            } catch {
                LogManager.logError("Decoding error for required property 'provenance': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(recipientDid, forKey: .recipientDid)
            try container.encode(recipientDeviceId, forKey: .recipientDeviceId)
            try container.encode(provenance, forKey: .provenance)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(recipientDid)
            hasher.combine(recipientDeviceId)
            hasher.combine(provenance)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if recipientDid != other.recipientDid {
                return false
            }
            if recipientDeviceId != other.recipientDeviceId {
                return false
            }
            if provenance != other.provenance {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let recipientDidValue = try recipientDid.toCBORValue()
            map = map.adding(key: "recipientDid", value: recipientDidValue)
            let recipientDeviceIdValue = try recipientDeviceId.toCBORValue()
            map = map.adding(key: "recipientDeviceId", value: recipientDeviceIdValue)
            let provenanceValue = try provenance.toCBORValue()
            map = map.adding(key: "provenance", value: provenanceValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case recipientDid
            case recipientDeviceId
            case provenance
        }
    }

    public struct WelcomeBundle: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#welcomeBundle"
        public let welcomeId: OperationId
        public let framing: String
        public let contentType: String
        public let opaqueWelcome: Bytes
        public let sha256: Bytes
        public let deliveries: [WelcomeDelivery]

        public init(
            welcomeId: OperationId, framing: String, contentType: String, opaqueWelcome: Bytes, sha256: Bytes, deliveries: [WelcomeDelivery]
        ) {
            self.welcomeId = welcomeId
            self.framing = framing
            self.contentType = contentType
            self.opaqueWelcome = opaqueWelcome
            self.sha256 = sha256
            self.deliveries = deliveries
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                welcomeId = try container.decode(OperationId.self, forKey: .welcomeId)
            } catch {
                LogManager.logError("Decoding error for required property 'welcomeId': \(error)")
                throw error
            }
            do {
                framing = try container.decode(String.self, forKey: .framing)
            } catch {
                LogManager.logError("Decoding error for required property 'framing': \(error)")
                throw error
            }
            do {
                contentType = try container.decode(String.self, forKey: .contentType)
            } catch {
                LogManager.logError("Decoding error for required property 'contentType': \(error)")
                throw error
            }
            do {
                opaqueWelcome = try container.decode(Bytes.self, forKey: .opaqueWelcome)
            } catch {
                LogManager.logError("Decoding error for required property 'opaqueWelcome': \(error)")
                throw error
            }
            do {
                sha256 = try container.decode(Bytes.self, forKey: .sha256)
            } catch {
                LogManager.logError("Decoding error for required property 'sha256': \(error)")
                throw error
            }
            do {
                deliveries = try container.decode([WelcomeDelivery].self, forKey: .deliveries)
            } catch {
                LogManager.logError("Decoding error for required property 'deliveries': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(welcomeId, forKey: .welcomeId)
            try container.encode(framing, forKey: .framing)
            try container.encode(contentType, forKey: .contentType)
            try container.encode(opaqueWelcome, forKey: .opaqueWelcome)
            try container.encode(sha256, forKey: .sha256)
            try container.encode(deliveries, forKey: .deliveries)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(welcomeId)
            hasher.combine(framing)
            hasher.combine(contentType)
            hasher.combine(opaqueWelcome)
            hasher.combine(sha256)
            hasher.combine(deliveries)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if welcomeId != other.welcomeId {
                return false
            }
            if framing != other.framing {
                return false
            }
            if contentType != other.contentType {
                return false
            }
            if opaqueWelcome != other.opaqueWelcome {
                return false
            }
            if sha256 != other.sha256 {
                return false
            }
            if deliveries != other.deliveries {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let welcomeIdValue = try welcomeId.toCBORValue()
            map = map.adding(key: "welcomeId", value: welcomeIdValue)
            let framingValue = try framing.toCBORValue()
            map = map.adding(key: "framing", value: framingValue)
            let contentTypeValue = try contentType.toCBORValue()
            map = map.adding(key: "contentType", value: contentTypeValue)
            let opaqueWelcomeValue = try opaqueWelcome.toCBORValue()
            map = map.adding(key: "opaqueWelcome", value: opaqueWelcomeValue)
            let sha256Value = try sha256.toCBORValue()
            map = map.adding(key: "sha256", value: sha256Value)
            let deliveriesValue = try deliveries.toCBORValue()
            map = map.adding(key: "deliveries", value: deliveriesValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case welcomeId
            case framing
            case contentType
            case opaqueWelcome
            case sha256
            case deliveries
        }
    }

    public struct WelcomeView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#welcomeView"
        public let welcomeId: OperationId
        public let conversationId: OperationId
        public let transitionSeq: Int
        public let coordinates: ConversationCoordinates
        public let status: DefsWelcomeStatus
        public let opaqueWelcome: Bytes
        public let sha256: Bytes
        public let recipientDid: BareDid
        public let recipientDeviceId: DeviceId
        public let provenance: RecoveryWelcomeProvenance
        public let expiresAt: CanonicalDatetime

        public init(
            welcomeId: OperationId, conversationId: OperationId, transitionSeq: Int, coordinates: ConversationCoordinates, status: DefsWelcomeStatus, opaqueWelcome: Bytes, sha256: Bytes, recipientDid: BareDid, recipientDeviceId: DeviceId, provenance: RecoveryWelcomeProvenance, expiresAt: CanonicalDatetime
        ) {
            self.welcomeId = welcomeId
            self.conversationId = conversationId
            self.transitionSeq = transitionSeq
            self.coordinates = coordinates
            self.status = status
            self.opaqueWelcome = opaqueWelcome
            self.sha256 = sha256
            self.recipientDid = recipientDid
            self.recipientDeviceId = recipientDeviceId
            self.provenance = provenance
            self.expiresAt = expiresAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                welcomeId = try container.decode(OperationId.self, forKey: .welcomeId)
            } catch {
                LogManager.logError("Decoding error for required property 'welcomeId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                transitionSeq = try container.decode(Int.self, forKey: .transitionSeq)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionSeq': \(error)")
                throw error
            }
            do {
                coordinates = try container.decode(ConversationCoordinates.self, forKey: .coordinates)
            } catch {
                LogManager.logError("Decoding error for required property 'coordinates': \(error)")
                throw error
            }
            do {
                status = try container.decode(DefsWelcomeStatus.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
            do {
                opaqueWelcome = try container.decode(Bytes.self, forKey: .opaqueWelcome)
            } catch {
                LogManager.logError("Decoding error for required property 'opaqueWelcome': \(error)")
                throw error
            }
            do {
                sha256 = try container.decode(Bytes.self, forKey: .sha256)
            } catch {
                LogManager.logError("Decoding error for required property 'sha256': \(error)")
                throw error
            }
            do {
                recipientDid = try container.decode(BareDid.self, forKey: .recipientDid)
            } catch {
                LogManager.logError("Decoding error for required property 'recipientDid': \(error)")
                throw error
            }
            do {
                recipientDeviceId = try container.decode(DeviceId.self, forKey: .recipientDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'recipientDeviceId': \(error)")
                throw error
            }
            do {
                provenance = try container.decode(RecoveryWelcomeProvenance.self, forKey: .provenance)
            } catch {
                LogManager.logError("Decoding error for required property 'provenance': \(error)")
                throw error
            }
            do {
                expiresAt = try container.decode(CanonicalDatetime.self, forKey: .expiresAt)
            } catch {
                LogManager.logError("Decoding error for required property 'expiresAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(welcomeId, forKey: .welcomeId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(transitionSeq, forKey: .transitionSeq)
            try container.encode(coordinates, forKey: .coordinates)
            try container.encode(status, forKey: .status)
            try container.encode(opaqueWelcome, forKey: .opaqueWelcome)
            try container.encode(sha256, forKey: .sha256)
            try container.encode(recipientDid, forKey: .recipientDid)
            try container.encode(recipientDeviceId, forKey: .recipientDeviceId)
            try container.encode(provenance, forKey: .provenance)
            try container.encode(expiresAt, forKey: .expiresAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(welcomeId)
            hasher.combine(conversationId)
            hasher.combine(transitionSeq)
            hasher.combine(coordinates)
            hasher.combine(status)
            hasher.combine(opaqueWelcome)
            hasher.combine(sha256)
            hasher.combine(recipientDid)
            hasher.combine(recipientDeviceId)
            hasher.combine(provenance)
            hasher.combine(expiresAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if welcomeId != other.welcomeId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if transitionSeq != other.transitionSeq {
                return false
            }
            if coordinates != other.coordinates {
                return false
            }
            if status != other.status {
                return false
            }
            if opaqueWelcome != other.opaqueWelcome {
                return false
            }
            if sha256 != other.sha256 {
                return false
            }
            if recipientDid != other.recipientDid {
                return false
            }
            if recipientDeviceId != other.recipientDeviceId {
                return false
            }
            if provenance != other.provenance {
                return false
            }
            if expiresAt != other.expiresAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let welcomeIdValue = try welcomeId.toCBORValue()
            map = map.adding(key: "welcomeId", value: welcomeIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let transitionSeqValue = try transitionSeq.toCBORValue()
            map = map.adding(key: "transitionSeq", value: transitionSeqValue)
            let coordinatesValue = try coordinates.toCBORValue()
            map = map.adding(key: "coordinates", value: coordinatesValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            let opaqueWelcomeValue = try opaqueWelcome.toCBORValue()
            map = map.adding(key: "opaqueWelcome", value: opaqueWelcomeValue)
            let sha256Value = try sha256.toCBORValue()
            map = map.adding(key: "sha256", value: sha256Value)
            let recipientDidValue = try recipientDid.toCBORValue()
            map = map.adding(key: "recipientDid", value: recipientDidValue)
            let recipientDeviceIdValue = try recipientDeviceId.toCBORValue()
            map = map.adding(key: "recipientDeviceId", value: recipientDeviceIdValue)
            let provenanceValue = try provenance.toCBORValue()
            map = map.adding(key: "provenance", value: provenanceValue)
            let expiresAtValue = try expiresAt.toCBORValue()
            map = map.adding(key: "expiresAt", value: expiresAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case welcomeId
            case conversationId
            case transitionSeq
            case coordinates
            case status
            case opaqueWelcome
            case sha256
            case recipientDid
            case recipientDeviceId
            case provenance
            case expiresAt
        }
    }

    public struct CommitAad: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#commitAad"
        public let protocolVersion: DefsProtocolVersion
        public let conversationId: Bytes
        public let generation: Int
        public let transitionId: Bytes
        public let prior: MlsAadPriorContext

        public init(
            protocolVersion: DefsProtocolVersion, conversationId: Bytes, generation: Int, transitionId: Bytes, prior: MlsAadPriorContext
        ) {
            self.protocolVersion = protocolVersion
            self.conversationId = conversationId
            self.generation = generation
            self.transitionId = transitionId
            self.prior = prior
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                protocolVersion = try container.decode(DefsProtocolVersion.self, forKey: .protocolVersion)
            } catch {
                LogManager.logError("Decoding error for required property 'protocolVersion': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(Bytes.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                generation = try container.decode(Int.self, forKey: .generation)
            } catch {
                LogManager.logError("Decoding error for required property 'generation': \(error)")
                throw error
            }
            do {
                transitionId = try container.decode(Bytes.self, forKey: .transitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionId': \(error)")
                throw error
            }
            do {
                prior = try container.decode(MlsAadPriorContext.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(protocolVersion, forKey: .protocolVersion)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(generation, forKey: .generation)
            try container.encode(transitionId, forKey: .transitionId)
            try container.encode(prior, forKey: .prior)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(protocolVersion)
            hasher.combine(conversationId)
            hasher.combine(generation)
            hasher.combine(transitionId)
            hasher.combine(prior)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if protocolVersion != other.protocolVersion {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if generation != other.generation {
                return false
            }
            if transitionId != other.transitionId {
                return false
            }
            if prior != other.prior {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let protocolVersionValue = try protocolVersion.toCBORValue()
            map = map.adding(key: "protocolVersion", value: protocolVersionValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let generationValue = try generation.toCBORValue()
            map = map.adding(key: "generation", value: generationValue)
            let transitionIdValue = try transitionId.toCBORValue()
            map = map.adding(key: "transitionId", value: transitionIdValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case protocolVersion
            case conversationId
            case generation
            case transitionId
            case prior
        }
    }

    public struct ApplicationAad: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#applicationAad"
        public let protocolVersion: DefsProtocolVersion
        public let conversationId: Bytes
        public let generation: Int
        public let messageId: Bytes
        public let prior: MlsAadPriorContext

        public init(
            protocolVersion: DefsProtocolVersion, conversationId: Bytes, generation: Int, messageId: Bytes, prior: MlsAadPriorContext
        ) {
            self.protocolVersion = protocolVersion
            self.conversationId = conversationId
            self.generation = generation
            self.messageId = messageId
            self.prior = prior
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                protocolVersion = try container.decode(DefsProtocolVersion.self, forKey: .protocolVersion)
            } catch {
                LogManager.logError("Decoding error for required property 'protocolVersion': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(Bytes.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                generation = try container.decode(Int.self, forKey: .generation)
            } catch {
                LogManager.logError("Decoding error for required property 'generation': \(error)")
                throw error
            }
            do {
                messageId = try container.decode(Bytes.self, forKey: .messageId)
            } catch {
                LogManager.logError("Decoding error for required property 'messageId': \(error)")
                throw error
            }
            do {
                prior = try container.decode(MlsAadPriorContext.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(protocolVersion, forKey: .protocolVersion)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(generation, forKey: .generation)
            try container.encode(messageId, forKey: .messageId)
            try container.encode(prior, forKey: .prior)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(protocolVersion)
            hasher.combine(conversationId)
            hasher.combine(generation)
            hasher.combine(messageId)
            hasher.combine(prior)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if protocolVersion != other.protocolVersion {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if generation != other.generation {
                return false
            }
            if messageId != other.messageId {
                return false
            }
            if prior != other.prior {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let protocolVersionValue = try protocolVersion.toCBORValue()
            map = map.adding(key: "protocolVersion", value: protocolVersionValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let generationValue = try generation.toCBORValue()
            map = map.adding(key: "generation", value: generationValue)
            let messageIdValue = try messageId.toCBORValue()
            map = map.adding(key: "messageId", value: messageIdValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case protocolVersion
            case conversationId
            case generation
            case messageId
            case prior
        }
    }

    public struct TransitionManifest: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#transitionManifest"
        public let participantChanges: [ParticipantChange]
        public let leafChanges: [LeafChange]
        public let welcomeBundle: WelcomeBundle?
        public let leafRecoveryRequestId: OperationId?

        public init(
            participantChanges: [ParticipantChange], leafChanges: [LeafChange], welcomeBundle: WelcomeBundle?, leafRecoveryRequestId: OperationId?
        ) {
            self.participantChanges = participantChanges
            self.leafChanges = leafChanges
            self.welcomeBundle = welcomeBundle
            self.leafRecoveryRequestId = leafRecoveryRequestId
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                participantChanges = try container.decode([ParticipantChange].self, forKey: .participantChanges)
            } catch {
                LogManager.logError("Decoding error for required property 'participantChanges': \(error)")
                throw error
            }
            do {
                leafChanges = try container.decode([LeafChange].self, forKey: .leafChanges)
            } catch {
                LogManager.logError("Decoding error for required property 'leafChanges': \(error)")
                throw error
            }
            do {
                welcomeBundle = try container.decodeIfPresent(WelcomeBundle.self, forKey: .welcomeBundle)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'welcomeBundle' — degrading to nil: \(error)")
                welcomeBundle = nil
            }
            do {
                leafRecoveryRequestId = try container.decodeIfPresent(OperationId.self, forKey: .leafRecoveryRequestId)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'leafRecoveryRequestId' — degrading to nil: \(error)")
                leafRecoveryRequestId = nil
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(participantChanges, forKey: .participantChanges)
            try container.encode(leafChanges, forKey: .leafChanges)
            try container.encodeIfPresent(welcomeBundle, forKey: .welcomeBundle)
            try container.encodeIfPresent(leafRecoveryRequestId, forKey: .leafRecoveryRequestId)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(participantChanges)
            hasher.combine(leafChanges)
            if let value = welcomeBundle {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
            if let value = leafRecoveryRequestId {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if participantChanges != other.participantChanges {
                return false
            }
            if leafChanges != other.leafChanges {
                return false
            }
            if welcomeBundle != other.welcomeBundle {
                return false
            }
            if leafRecoveryRequestId != other.leafRecoveryRequestId {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let participantChangesValue = try participantChanges.toCBORValue()
            map = map.adding(key: "participantChanges", value: participantChangesValue)
            let leafChangesValue = try leafChanges.toCBORValue()
            map = map.adding(key: "leafChanges", value: leafChangesValue)
            if let value = welcomeBundle {
                let welcomeBundleValue = try value.toCBORValue()
                map = map.adding(key: "welcomeBundle", value: welcomeBundleValue)
            }
            if let value = leafRecoveryRequestId {
                let leafRecoveryRequestIdValue = try value.toCBORValue()
                map = map.adding(key: "leafRecoveryRequestId", value: leafRecoveryRequestIdValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case participantChanges
            case leafChanges
            case welcomeBundle
            case leafRecoveryRequestId
        }
    }

    public struct CreationManifest: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#creationManifest"
        public let participants: [Participant]
        public let actorLeaf: DeviceLeaf

        public init(
            participants: [Participant], actorLeaf: DeviceLeaf
        ) {
            self.participants = participants
            self.actorLeaf = actorLeaf
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                participants = try container.decode([Participant].self, forKey: .participants)
            } catch {
                LogManager.logError("Decoding error for required property 'participants': \(error)")
                throw error
            }
            do {
                actorLeaf = try container.decode(DeviceLeaf.self, forKey: .actorLeaf)
            } catch {
                LogManager.logError("Decoding error for required property 'actorLeaf': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(participants, forKey: .participants)
            try container.encode(actorLeaf, forKey: .actorLeaf)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(participants)
            hasher.combine(actorLeaf)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if participants != other.participants {
                return false
            }
            if actorLeaf != other.actorLeaf {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let participantsValue = try participants.toCBORValue()
            map = map.adding(key: "participants", value: participantsValue)
            let actorLeafValue = try actorLeaf.toCBORValue()
            map = map.adding(key: "actorLeaf", value: actorLeafValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case participants
            case actorLeaf
        }
    }

    public struct ResetActivationManifest: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#resetActivationManifest"
        public let participants: [Participant]
        public let actorLeaf: DeviceLeaf

        public init(
            participants: [Participant], actorLeaf: DeviceLeaf
        ) {
            self.participants = participants
            self.actorLeaf = actorLeaf
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                participants = try container.decode([Participant].self, forKey: .participants)
            } catch {
                LogManager.logError("Decoding error for required property 'participants': \(error)")
                throw error
            }
            do {
                actorLeaf = try container.decode(DeviceLeaf.self, forKey: .actorLeaf)
            } catch {
                LogManager.logError("Decoding error for required property 'actorLeaf': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(participants, forKey: .participants)
            try container.encode(actorLeaf, forKey: .actorLeaf)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(participants)
            hasher.combine(actorLeaf)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if participants != other.participants {
                return false
            }
            if actorLeaf != other.actorLeaf {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let participantsValue = try participants.toCBORValue()
            map = map.adding(key: "participants", value: participantsValue)
            let actorLeafValue = try actorLeaf.toCBORValue()
            map = map.adding(key: "actorLeaf", value: actorLeafValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case participants
            case actorLeaf
        }
    }

    public struct LeafRecoveryReservation: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leafRecoveryReservation"
        public let recoveryRequestId: OperationId
        public let conversationId: OperationId
        public let boundCoordinate: ConversationCoordinates
        public let requesterDid: BareDid
        public let requesterDeviceId: DeviceId
        public let requesterKeyId: KeyId
        public let requesterAuthGeneration: Int
        public let keyPackageRef: Bytes
        public let cipherSuite: DefsCipherSuite
        public let purpose: String
        public let status: DefsReservationStatus
        public let expiresAt: CanonicalDatetime
        public let keyPackage: KeyPackageArtifact

        public init(
            recoveryRequestId: OperationId, conversationId: OperationId, boundCoordinate: ConversationCoordinates, requesterDid: BareDid, requesterDeviceId: DeviceId, requesterKeyId: KeyId, requesterAuthGeneration: Int, keyPackageRef: Bytes, cipherSuite: DefsCipherSuite, purpose: String, status: DefsReservationStatus, expiresAt: CanonicalDatetime, keyPackage: KeyPackageArtifact
        ) {
            self.recoveryRequestId = recoveryRequestId
            self.conversationId = conversationId
            self.boundCoordinate = boundCoordinate
            self.requesterDid = requesterDid
            self.requesterDeviceId = requesterDeviceId
            self.requesterKeyId = requesterKeyId
            self.requesterAuthGeneration = requesterAuthGeneration
            self.keyPackageRef = keyPackageRef
            self.cipherSuite = cipherSuite
            self.purpose = purpose
            self.status = status
            self.expiresAt = expiresAt
            self.keyPackage = keyPackage
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                recoveryRequestId = try container.decode(OperationId.self, forKey: .recoveryRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryRequestId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                boundCoordinate = try container.decode(ConversationCoordinates.self, forKey: .boundCoordinate)
            } catch {
                LogManager.logError("Decoding error for required property 'boundCoordinate': \(error)")
                throw error
            }
            do {
                requesterDid = try container.decode(BareDid.self, forKey: .requesterDid)
            } catch {
                LogManager.logError("Decoding error for required property 'requesterDid': \(error)")
                throw error
            }
            do {
                requesterDeviceId = try container.decode(DeviceId.self, forKey: .requesterDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'requesterDeviceId': \(error)")
                throw error
            }
            do {
                requesterKeyId = try container.decode(KeyId.self, forKey: .requesterKeyId)
            } catch {
                LogManager.logError("Decoding error for required property 'requesterKeyId': \(error)")
                throw error
            }
            do {
                requesterAuthGeneration = try container.decode(Int.self, forKey: .requesterAuthGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'requesterAuthGeneration': \(error)")
                throw error
            }
            do {
                keyPackageRef = try container.decode(Bytes.self, forKey: .keyPackageRef)
            } catch {
                LogManager.logError("Decoding error for required property 'keyPackageRef': \(error)")
                throw error
            }
            do {
                cipherSuite = try container.decode(DefsCipherSuite.self, forKey: .cipherSuite)
            } catch {
                LogManager.logError("Decoding error for required property 'cipherSuite': \(error)")
                throw error
            }
            do {
                purpose = try container.decode(String.self, forKey: .purpose)
            } catch {
                LogManager.logError("Decoding error for required property 'purpose': \(error)")
                throw error
            }
            do {
                status = try container.decode(DefsReservationStatus.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
            do {
                expiresAt = try container.decode(CanonicalDatetime.self, forKey: .expiresAt)
            } catch {
                LogManager.logError("Decoding error for required property 'expiresAt': \(error)")
                throw error
            }
            do {
                keyPackage = try container.decode(KeyPackageArtifact.self, forKey: .keyPackage)
            } catch {
                LogManager.logError("Decoding error for required property 'keyPackage': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(recoveryRequestId, forKey: .recoveryRequestId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(boundCoordinate, forKey: .boundCoordinate)
            try container.encode(requesterDid, forKey: .requesterDid)
            try container.encode(requesterDeviceId, forKey: .requesterDeviceId)
            try container.encode(requesterKeyId, forKey: .requesterKeyId)
            try container.encode(requesterAuthGeneration, forKey: .requesterAuthGeneration)
            try container.encode(keyPackageRef, forKey: .keyPackageRef)
            try container.encode(cipherSuite, forKey: .cipherSuite)
            try container.encode(purpose, forKey: .purpose)
            try container.encode(status, forKey: .status)
            try container.encode(expiresAt, forKey: .expiresAt)
            try container.encode(keyPackage, forKey: .keyPackage)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(recoveryRequestId)
            hasher.combine(conversationId)
            hasher.combine(boundCoordinate)
            hasher.combine(requesterDid)
            hasher.combine(requesterDeviceId)
            hasher.combine(requesterKeyId)
            hasher.combine(requesterAuthGeneration)
            hasher.combine(keyPackageRef)
            hasher.combine(cipherSuite)
            hasher.combine(purpose)
            hasher.combine(status)
            hasher.combine(expiresAt)
            hasher.combine(keyPackage)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if recoveryRequestId != other.recoveryRequestId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if boundCoordinate != other.boundCoordinate {
                return false
            }
            if requesterDid != other.requesterDid {
                return false
            }
            if requesterDeviceId != other.requesterDeviceId {
                return false
            }
            if requesterKeyId != other.requesterKeyId {
                return false
            }
            if requesterAuthGeneration != other.requesterAuthGeneration {
                return false
            }
            if keyPackageRef != other.keyPackageRef {
                return false
            }
            if cipherSuite != other.cipherSuite {
                return false
            }
            if purpose != other.purpose {
                return false
            }
            if status != other.status {
                return false
            }
            if expiresAt != other.expiresAt {
                return false
            }
            if keyPackage != other.keyPackage {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let recoveryRequestIdValue = try recoveryRequestId.toCBORValue()
            map = map.adding(key: "recoveryRequestId", value: recoveryRequestIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let boundCoordinateValue = try boundCoordinate.toCBORValue()
            map = map.adding(key: "boundCoordinate", value: boundCoordinateValue)
            let requesterDidValue = try requesterDid.toCBORValue()
            map = map.adding(key: "requesterDid", value: requesterDidValue)
            let requesterDeviceIdValue = try requesterDeviceId.toCBORValue()
            map = map.adding(key: "requesterDeviceId", value: requesterDeviceIdValue)
            let requesterKeyIdValue = try requesterKeyId.toCBORValue()
            map = map.adding(key: "requesterKeyId", value: requesterKeyIdValue)
            let requesterAuthGenerationValue = try requesterAuthGeneration.toCBORValue()
            map = map.adding(key: "requesterAuthGeneration", value: requesterAuthGenerationValue)
            let keyPackageRefValue = try keyPackageRef.toCBORValue()
            map = map.adding(key: "keyPackageRef", value: keyPackageRefValue)
            let cipherSuiteValue = try cipherSuite.toCBORValue()
            map = map.adding(key: "cipherSuite", value: cipherSuiteValue)
            let purposeValue = try purpose.toCBORValue()
            map = map.adding(key: "purpose", value: purposeValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            let expiresAtValue = try expiresAt.toCBORValue()
            map = map.adding(key: "expiresAt", value: expiresAtValue)
            let keyPackageValue = try keyPackage.toCBORValue()
            map = map.adding(key: "keyPackage", value: keyPackageValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case recoveryRequestId
            case conversationId
            case boundCoordinate
            case requesterDid
            case requesterDeviceId
            case requesterKeyId
            case requesterAuthGeneration
            case keyPackageRef
            case cipherSuite
            case purpose
            case status
            case expiresAt
            case keyPackage
        }
    }

    public struct UploadedBlobBinding: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#uploadedBlobBinding"
        public let blobId: OperationId
        public let ciphertextSha256: Bytes
        public let ciphertextSize: Int
        public let purpose: DefsBlobPurpose

        public init(
            blobId: OperationId, ciphertextSha256: Bytes, ciphertextSize: Int, purpose: DefsBlobPurpose
        ) {
            self.blobId = blobId
            self.ciphertextSha256 = ciphertextSha256
            self.ciphertextSize = ciphertextSize
            self.purpose = purpose
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                blobId = try container.decode(OperationId.self, forKey: .blobId)
            } catch {
                LogManager.logError("Decoding error for required property 'blobId': \(error)")
                throw error
            }
            do {
                ciphertextSha256 = try container.decode(Bytes.self, forKey: .ciphertextSha256)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSha256': \(error)")
                throw error
            }
            do {
                ciphertextSize = try container.decode(Int.self, forKey: .ciphertextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSize': \(error)")
                throw error
            }
            do {
                purpose = try container.decode(DefsBlobPurpose.self, forKey: .purpose)
            } catch {
                LogManager.logError("Decoding error for required property 'purpose': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(blobId, forKey: .blobId)
            try container.encode(ciphertextSha256, forKey: .ciphertextSha256)
            try container.encode(ciphertextSize, forKey: .ciphertextSize)
            try container.encode(purpose, forKey: .purpose)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(blobId)
            hasher.combine(ciphertextSha256)
            hasher.combine(ciphertextSize)
            hasher.combine(purpose)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if blobId != other.blobId {
                return false
            }
            if ciphertextSha256 != other.ciphertextSha256 {
                return false
            }
            if ciphertextSize != other.ciphertextSize {
                return false
            }
            if purpose != other.purpose {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let blobIdValue = try blobId.toCBORValue()
            map = map.adding(key: "blobId", value: blobIdValue)
            let ciphertextSha256Value = try ciphertextSha256.toCBORValue()
            map = map.adding(key: "ciphertextSha256", value: ciphertextSha256Value)
            let ciphertextSizeValue = try ciphertextSize.toCBORValue()
            map = map.adding(key: "ciphertextSize", value: ciphertextSizeValue)
            let purposeValue = try purpose.toCBORValue()
            map = map.adding(key: "purpose", value: purposeValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case blobId
            case ciphertextSha256
            case ciphertextSize
            case purpose
        }
    }

    public struct ApplicationAttachmentBinding: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#applicationAttachmentBinding"
        public let blobId: OperationId
        public let ciphertextSha256: Bytes
        public let ciphertextSize: Int
        public let purpose: String

        public init(
            blobId: OperationId, ciphertextSha256: Bytes, ciphertextSize: Int, purpose: String
        ) {
            self.blobId = blobId
            self.ciphertextSha256 = ciphertextSha256
            self.ciphertextSize = ciphertextSize
            self.purpose = purpose
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                blobId = try container.decode(OperationId.self, forKey: .blobId)
            } catch {
                LogManager.logError("Decoding error for required property 'blobId': \(error)")
                throw error
            }
            do {
                ciphertextSha256 = try container.decode(Bytes.self, forKey: .ciphertextSha256)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSha256': \(error)")
                throw error
            }
            do {
                ciphertextSize = try container.decode(Int.self, forKey: .ciphertextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSize': \(error)")
                throw error
            }
            do {
                purpose = try container.decode(String.self, forKey: .purpose)
            } catch {
                LogManager.logError("Decoding error for required property 'purpose': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(blobId, forKey: .blobId)
            try container.encode(ciphertextSha256, forKey: .ciphertextSha256)
            try container.encode(ciphertextSize, forKey: .ciphertextSize)
            try container.encode(purpose, forKey: .purpose)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(blobId)
            hasher.combine(ciphertextSha256)
            hasher.combine(ciphertextSize)
            hasher.combine(purpose)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if blobId != other.blobId {
                return false
            }
            if ciphertextSha256 != other.ciphertextSha256 {
                return false
            }
            if ciphertextSize != other.ciphertextSize {
                return false
            }
            if purpose != other.purpose {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let blobIdValue = try blobId.toCBORValue()
            map = map.adding(key: "blobId", value: blobIdValue)
            let ciphertextSha256Value = try ciphertextSha256.toCBORValue()
            map = map.adding(key: "ciphertextSha256", value: ciphertextSha256Value)
            let ciphertextSizeValue = try ciphertextSize.toCBORValue()
            map = map.adding(key: "ciphertextSize", value: ciphertextSizeValue)
            let purposeValue = try purpose.toCBORValue()
            map = map.adding(key: "purpose", value: purposeValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case blobId
            case ciphertextSha256
            case ciphertextSize
            case purpose
        }
    }

    public struct MetadataAvatarBinding: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#metadataAvatarBinding"
        public let blobId: OperationId
        public let ciphertextSha256: Bytes
        public let ciphertextSize: Int
        public let purpose: String

        public init(
            blobId: OperationId, ciphertextSha256: Bytes, ciphertextSize: Int, purpose: String
        ) {
            self.blobId = blobId
            self.ciphertextSha256 = ciphertextSha256
            self.ciphertextSize = ciphertextSize
            self.purpose = purpose
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                blobId = try container.decode(OperationId.self, forKey: .blobId)
            } catch {
                LogManager.logError("Decoding error for required property 'blobId': \(error)")
                throw error
            }
            do {
                ciphertextSha256 = try container.decode(Bytes.self, forKey: .ciphertextSha256)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSha256': \(error)")
                throw error
            }
            do {
                ciphertextSize = try container.decode(Int.self, forKey: .ciphertextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSize': \(error)")
                throw error
            }
            do {
                purpose = try container.decode(String.self, forKey: .purpose)
            } catch {
                LogManager.logError("Decoding error for required property 'purpose': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(blobId, forKey: .blobId)
            try container.encode(ciphertextSha256, forKey: .ciphertextSha256)
            try container.encode(ciphertextSize, forKey: .ciphertextSize)
            try container.encode(purpose, forKey: .purpose)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(blobId)
            hasher.combine(ciphertextSha256)
            hasher.combine(ciphertextSize)
            hasher.combine(purpose)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if blobId != other.blobId {
                return false
            }
            if ciphertextSha256 != other.ciphertextSha256 {
                return false
            }
            if ciphertextSize != other.ciphertextSize {
                return false
            }
            if purpose != other.purpose {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let blobIdValue = try blobId.toCBORValue()
            map = map.adding(key: "blobId", value: blobIdValue)
            let ciphertextSha256Value = try ciphertextSha256.toCBORValue()
            map = map.adding(key: "ciphertextSha256", value: ciphertextSha256Value)
            let ciphertextSizeValue = try ciphertextSize.toCBORValue()
            map = map.adding(key: "ciphertextSize", value: ciphertextSizeValue)
            let purposeValue = try purpose.toCBORValue()
            map = map.adding(key: "purpose", value: purposeValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case blobId
            case ciphertextSha256
            case ciphertextSize
            case purpose
        }
    }

    public struct BlobUploadView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#blobUploadView"
        public let blobId: OperationId
        public let uploadTicket: String
        public let ciphertextSha256: Bytes
        public let ciphertextSize: Int
        public let purpose: DefsBlobPurpose
        public let expiresAt: CanonicalDatetime

        public init(
            blobId: OperationId, uploadTicket: String, ciphertextSha256: Bytes, ciphertextSize: Int, purpose: DefsBlobPurpose, expiresAt: CanonicalDatetime
        ) {
            self.blobId = blobId
            self.uploadTicket = uploadTicket
            self.ciphertextSha256 = ciphertextSha256
            self.ciphertextSize = ciphertextSize
            self.purpose = purpose
            self.expiresAt = expiresAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                blobId = try container.decode(OperationId.self, forKey: .blobId)
            } catch {
                LogManager.logError("Decoding error for required property 'blobId': \(error)")
                throw error
            }
            do {
                uploadTicket = try container.decode(String.self, forKey: .uploadTicket)
            } catch {
                LogManager.logError("Decoding error for required property 'uploadTicket': \(error)")
                throw error
            }
            do {
                ciphertextSha256 = try container.decode(Bytes.self, forKey: .ciphertextSha256)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSha256': \(error)")
                throw error
            }
            do {
                ciphertextSize = try container.decode(Int.self, forKey: .ciphertextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSize': \(error)")
                throw error
            }
            do {
                purpose = try container.decode(DefsBlobPurpose.self, forKey: .purpose)
            } catch {
                LogManager.logError("Decoding error for required property 'purpose': \(error)")
                throw error
            }
            do {
                expiresAt = try container.decode(CanonicalDatetime.self, forKey: .expiresAt)
            } catch {
                LogManager.logError("Decoding error for required property 'expiresAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(blobId, forKey: .blobId)
            try container.encode(uploadTicket, forKey: .uploadTicket)
            try container.encode(ciphertextSha256, forKey: .ciphertextSha256)
            try container.encode(ciphertextSize, forKey: .ciphertextSize)
            try container.encode(purpose, forKey: .purpose)
            try container.encode(expiresAt, forKey: .expiresAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(blobId)
            hasher.combine(uploadTicket)
            hasher.combine(ciphertextSha256)
            hasher.combine(ciphertextSize)
            hasher.combine(purpose)
            hasher.combine(expiresAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if blobId != other.blobId {
                return false
            }
            if uploadTicket != other.uploadTicket {
                return false
            }
            if ciphertextSha256 != other.ciphertextSha256 {
                return false
            }
            if ciphertextSize != other.ciphertextSize {
                return false
            }
            if purpose != other.purpose {
                return false
            }
            if expiresAt != other.expiresAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let blobIdValue = try blobId.toCBORValue()
            map = map.adding(key: "blobId", value: blobIdValue)
            let uploadTicketValue = try uploadTicket.toCBORValue()
            map = map.adding(key: "uploadTicket", value: uploadTicketValue)
            let ciphertextSha256Value = try ciphertextSha256.toCBORValue()
            map = map.adding(key: "ciphertextSha256", value: ciphertextSha256Value)
            let ciphertextSizeValue = try ciphertextSize.toCBORValue()
            map = map.adding(key: "ciphertextSize", value: ciphertextSizeValue)
            let purposeValue = try purpose.toCBORValue()
            map = map.adding(key: "purpose", value: purposeValue)
            let expiresAtValue = try expiresAt.toCBORValue()
            map = map.adding(key: "expiresAt", value: expiresAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case blobId
            case uploadTicket
            case ciphertextSha256
            case ciphertextSize
            case purpose
            case expiresAt
        }
    }

    public struct BlobUsageView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#blobUsageView"
        public let usedBytes: Int
        public let reservedBytes: Int
        public let quotaBytes: Int
        public let blobCount: Int
        public let liveUnboundCount: Int

        public init(
            usedBytes: Int, reservedBytes: Int, quotaBytes: Int, blobCount: Int, liveUnboundCount: Int
        ) {
            self.usedBytes = usedBytes
            self.reservedBytes = reservedBytes
            self.quotaBytes = quotaBytes
            self.blobCount = blobCount
            self.liveUnboundCount = liveUnboundCount
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                usedBytes = try container.decode(Int.self, forKey: .usedBytes)
            } catch {
                LogManager.logError("Decoding error for required property 'usedBytes': \(error)")
                throw error
            }
            do {
                reservedBytes = try container.decode(Int.self, forKey: .reservedBytes)
            } catch {
                LogManager.logError("Decoding error for required property 'reservedBytes': \(error)")
                throw error
            }
            do {
                quotaBytes = try container.decode(Int.self, forKey: .quotaBytes)
            } catch {
                LogManager.logError("Decoding error for required property 'quotaBytes': \(error)")
                throw error
            }
            do {
                blobCount = try container.decode(Int.self, forKey: .blobCount)
            } catch {
                LogManager.logError("Decoding error for required property 'blobCount': \(error)")
                throw error
            }
            do {
                liveUnboundCount = try container.decode(Int.self, forKey: .liveUnboundCount)
            } catch {
                LogManager.logError("Decoding error for required property 'liveUnboundCount': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(usedBytes, forKey: .usedBytes)
            try container.encode(reservedBytes, forKey: .reservedBytes)
            try container.encode(quotaBytes, forKey: .quotaBytes)
            try container.encode(blobCount, forKey: .blobCount)
            try container.encode(liveUnboundCount, forKey: .liveUnboundCount)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(usedBytes)
            hasher.combine(reservedBytes)
            hasher.combine(quotaBytes)
            hasher.combine(blobCount)
            hasher.combine(liveUnboundCount)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if usedBytes != other.usedBytes {
                return false
            }
            if reservedBytes != other.reservedBytes {
                return false
            }
            if quotaBytes != other.quotaBytes {
                return false
            }
            if blobCount != other.blobCount {
                return false
            }
            if liveUnboundCount != other.liveUnboundCount {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let usedBytesValue = try usedBytes.toCBORValue()
            map = map.adding(key: "usedBytes", value: usedBytesValue)
            let reservedBytesValue = try reservedBytes.toCBORValue()
            map = map.adding(key: "reservedBytes", value: reservedBytesValue)
            let quotaBytesValue = try quotaBytes.toCBORValue()
            map = map.adding(key: "quotaBytes", value: quotaBytesValue)
            let blobCountValue = try blobCount.toCBORValue()
            map = map.adding(key: "blobCount", value: blobCountValue)
            let liveUnboundCountValue = try liveUnboundCount.toCBORValue()
            map = map.adding(key: "liveUnboundCount", value: liveUnboundCountValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case usedBytes
            case reservedBytes
            case quotaBytes
            case blobCount
            case liveUnboundCount
        }
    }

    public struct DeviceEnrollmentBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#deviceEnrollmentBody"
        public let signatureDomain: String
        public let actorDid: BareDid
        public let deviceId: DeviceId
        public let deviceName: String
        public let keyId: KeyId
        public let signaturePublicKey: Bytes
        public let dpopJkt: String
        public let expectedAuthGeneration: Int
        public let capability: DeviceCapability
        public let keyPackages: [KeyPackageArtifact]
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, actorDid: BareDid, deviceId: DeviceId, deviceName: String, keyId: KeyId, signaturePublicKey: Bytes, dpopJkt: String, expectedAuthGeneration: Int, capability: DeviceCapability, keyPackages: [KeyPackageArtifact], idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.actorDid = actorDid
            self.deviceId = deviceId
            self.deviceName = deviceName
            self.keyId = keyId
            self.signaturePublicKey = signaturePublicKey
            self.dpopJkt = dpopJkt
            self.expectedAuthGeneration = expectedAuthGeneration
            self.capability = capability
            self.keyPackages = keyPackages
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                deviceId = try container.decode(DeviceId.self, forKey: .deviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceId': \(error)")
                throw error
            }
            do {
                deviceName = try container.decode(String.self, forKey: .deviceName)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceName': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                signaturePublicKey = try container.decode(Bytes.self, forKey: .signaturePublicKey)
            } catch {
                LogManager.logError("Decoding error for required property 'signaturePublicKey': \(error)")
                throw error
            }
            do {
                dpopJkt = try container.decode(String.self, forKey: .dpopJkt)
            } catch {
                LogManager.logError("Decoding error for required property 'dpopJkt': \(error)")
                throw error
            }
            do {
                expectedAuthGeneration = try container.decode(Int.self, forKey: .expectedAuthGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'expectedAuthGeneration': \(error)")
                throw error
            }
            do {
                capability = try container.decode(DeviceCapability.self, forKey: .capability)
            } catch {
                LogManager.logError("Decoding error for required property 'capability': \(error)")
                throw error
            }
            do {
                keyPackages = try container.decode([KeyPackageArtifact].self, forKey: .keyPackages)
            } catch {
                LogManager.logError("Decoding error for required property 'keyPackages': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(deviceId, forKey: .deviceId)
            try container.encode(deviceName, forKey: .deviceName)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(signaturePublicKey, forKey: .signaturePublicKey)
            try container.encode(dpopJkt, forKey: .dpopJkt)
            try container.encode(expectedAuthGeneration, forKey: .expectedAuthGeneration)
            try container.encode(capability, forKey: .capability)
            try container.encode(keyPackages, forKey: .keyPackages)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(actorDid)
            hasher.combine(deviceId)
            hasher.combine(deviceName)
            hasher.combine(keyId)
            hasher.combine(signaturePublicKey)
            hasher.combine(dpopJkt)
            hasher.combine(expectedAuthGeneration)
            hasher.combine(capability)
            hasher.combine(keyPackages)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if deviceId != other.deviceId {
                return false
            }
            if deviceName != other.deviceName {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if signaturePublicKey != other.signaturePublicKey {
                return false
            }
            if dpopJkt != other.dpopJkt {
                return false
            }
            if expectedAuthGeneration != other.expectedAuthGeneration {
                return false
            }
            if capability != other.capability {
                return false
            }
            if keyPackages != other.keyPackages {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let deviceIdValue = try deviceId.toCBORValue()
            map = map.adding(key: "deviceId", value: deviceIdValue)
            let deviceNameValue = try deviceName.toCBORValue()
            map = map.adding(key: "deviceName", value: deviceNameValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let signaturePublicKeyValue = try signaturePublicKey.toCBORValue()
            map = map.adding(key: "signaturePublicKey", value: signaturePublicKeyValue)
            let dpopJktValue = try dpopJkt.toCBORValue()
            map = map.adding(key: "dpopJkt", value: dpopJktValue)
            let expectedAuthGenerationValue = try expectedAuthGeneration.toCBORValue()
            map = map.adding(key: "expectedAuthGeneration", value: expectedAuthGenerationValue)
            let capabilityValue = try capability.toCBORValue()
            map = map.adding(key: "capability", value: capabilityValue)
            let keyPackagesValue = try keyPackages.toCBORValue()
            map = map.adding(key: "keyPackages", value: keyPackagesValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case actorDid
            case deviceId
            case deviceName
            case keyId
            case signaturePublicKey
            case dpopJkt
            case expectedAuthGeneration
            case capability
            case keyPackages
            case idempotencyKey
            case signedAt
        }
    }

    public struct KeyPackageReplenishmentBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#keyPackageReplenishmentBody"
        public let signatureDomain: String
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let dpopJkt: String
        public let signaturePublicKey: Bytes
        public let keyPackages: [KeyPackageArtifact]
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, dpopJkt: String, signaturePublicKey: Bytes, keyPackages: [KeyPackageArtifact], idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.dpopJkt = dpopJkt
            self.signaturePublicKey = signaturePublicKey
            self.keyPackages = keyPackages
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                dpopJkt = try container.decode(String.self, forKey: .dpopJkt)
            } catch {
                LogManager.logError("Decoding error for required property 'dpopJkt': \(error)")
                throw error
            }
            do {
                signaturePublicKey = try container.decode(Bytes.self, forKey: .signaturePublicKey)
            } catch {
                LogManager.logError("Decoding error for required property 'signaturePublicKey': \(error)")
                throw error
            }
            do {
                keyPackages = try container.decode([KeyPackageArtifact].self, forKey: .keyPackages)
            } catch {
                LogManager.logError("Decoding error for required property 'keyPackages': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(dpopJkt, forKey: .dpopJkt)
            try container.encode(signaturePublicKey, forKey: .signaturePublicKey)
            try container.encode(keyPackages, forKey: .keyPackages)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(dpopJkt)
            hasher.combine(signaturePublicKey)
            hasher.combine(keyPackages)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if dpopJkt != other.dpopJkt {
                return false
            }
            if signaturePublicKey != other.signaturePublicKey {
                return false
            }
            if keyPackages != other.keyPackages {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let dpopJktValue = try dpopJkt.toCBORValue()
            map = map.adding(key: "dpopJkt", value: dpopJktValue)
            let signaturePublicKeyValue = try signaturePublicKey.toCBORValue()
            map = map.adding(key: "signaturePublicKey", value: signaturePublicKeyValue)
            let keyPackagesValue = try keyPackages.toCBORValue()
            map = map.adding(key: "keyPackages", value: keyPackagesValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case dpopJkt
            case signaturePublicKey
            case keyPackages
            case idempotencyKey
            case signedAt
        }
    }

    public struct DeviceAuthenticationRebindBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#deviceAuthenticationRebindBody"
        public let signatureDomain: String
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let expectedAuthGeneration: Int
        public let currentDpopJkt: String
        public let newDpopJkt: String
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, expectedAuthGeneration: Int, currentDpopJkt: String, newDpopJkt: String, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.expectedAuthGeneration = expectedAuthGeneration
            self.currentDpopJkt = currentDpopJkt
            self.newDpopJkt = newDpopJkt
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                expectedAuthGeneration = try container.decode(Int.self, forKey: .expectedAuthGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'expectedAuthGeneration': \(error)")
                throw error
            }
            do {
                currentDpopJkt = try container.decode(String.self, forKey: .currentDpopJkt)
            } catch {
                LogManager.logError("Decoding error for required property 'currentDpopJkt': \(error)")
                throw error
            }
            do {
                newDpopJkt = try container.decode(String.self, forKey: .newDpopJkt)
            } catch {
                LogManager.logError("Decoding error for required property 'newDpopJkt': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(expectedAuthGeneration, forKey: .expectedAuthGeneration)
            try container.encode(currentDpopJkt, forKey: .currentDpopJkt)
            try container.encode(newDpopJkt, forKey: .newDpopJkt)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(expectedAuthGeneration)
            hasher.combine(currentDpopJkt)
            hasher.combine(newDpopJkt)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if expectedAuthGeneration != other.expectedAuthGeneration {
                return false
            }
            if currentDpopJkt != other.currentDpopJkt {
                return false
            }
            if newDpopJkt != other.newDpopJkt {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let expectedAuthGenerationValue = try expectedAuthGeneration.toCBORValue()
            map = map.adding(key: "expectedAuthGeneration", value: expectedAuthGenerationValue)
            let currentDpopJktValue = try currentDpopJkt.toCBORValue()
            map = map.adding(key: "currentDpopJkt", value: currentDpopJktValue)
            let newDpopJktValue = try newDpopJkt.toCBORValue()
            map = map.adding(key: "newDpopJkt", value: newDpopJktValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case actorDid
            case actorDeviceId
            case keyId
            case expectedAuthGeneration
            case currentDpopJkt
            case newDpopJkt
            case idempotencyKey
            case signedAt
        }
    }

    public struct DeviceRevocationBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#deviceRevocationBody"
        public let signatureDomain: String
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let targetDeviceId: DeviceId
        public let targetAuthGeneration: Int
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, targetDeviceId: DeviceId, targetAuthGeneration: Int, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.targetDeviceId = targetDeviceId
            self.targetAuthGeneration = targetAuthGeneration
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                targetDeviceId = try container.decode(DeviceId.self, forKey: .targetDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'targetDeviceId': \(error)")
                throw error
            }
            do {
                targetAuthGeneration = try container.decode(Int.self, forKey: .targetAuthGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'targetAuthGeneration': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(targetDeviceId, forKey: .targetDeviceId)
            try container.encode(targetAuthGeneration, forKey: .targetAuthGeneration)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(targetDeviceId)
            hasher.combine(targetAuthGeneration)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if targetDeviceId != other.targetDeviceId {
                return false
            }
            if targetAuthGeneration != other.targetAuthGeneration {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let targetDeviceIdValue = try targetDeviceId.toCBORValue()
            map = map.adding(key: "targetDeviceId", value: targetDeviceIdValue)
            let targetAuthGenerationValue = try targetAuthGeneration.toCBORValue()
            map = map.adding(key: "targetAuthGeneration", value: targetAuthGenerationValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case targetDeviceId
            case targetAuthGeneration
            case idempotencyKey
            case signedAt
        }
    }

    public struct CreationBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#creationBody"
        public let signatureDomain: String
        public let conversationId: OperationId
        public let conversationKind: DefsConversationKind
        public let transitionId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let absence: Bool
        public let next: ConversationCoordinates
        public let manifest: CreationManifest
        public let genesisGroupInfo: GroupInfoArtifact
        public let metadataSnapshot: MetadataSnapshot
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, conversationId: OperationId, conversationKind: DefsConversationKind, transitionId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, absence: Bool, next: ConversationCoordinates, manifest: CreationManifest, genesisGroupInfo: GroupInfoArtifact, metadataSnapshot: MetadataSnapshot, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.conversationId = conversationId
            self.conversationKind = conversationKind
            self.transitionId = transitionId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.absence = absence
            self.next = next
            self.manifest = manifest
            self.genesisGroupInfo = genesisGroupInfo
            self.metadataSnapshot = metadataSnapshot
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                conversationKind = try container.decode(DefsConversationKind.self, forKey: .conversationKind)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationKind': \(error)")
                throw error
            }
            do {
                transitionId = try container.decode(OperationId.self, forKey: .transitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                absence = try container.decode(Bool.self, forKey: .absence)
            } catch {
                LogManager.logError("Decoding error for required property 'absence': \(error)")
                throw error
            }
            do {
                next = try container.decode(ConversationCoordinates.self, forKey: .next)
            } catch {
                LogManager.logError("Decoding error for required property 'next': \(error)")
                throw error
            }
            do {
                manifest = try container.decode(CreationManifest.self, forKey: .manifest)
            } catch {
                LogManager.logError("Decoding error for required property 'manifest': \(error)")
                throw error
            }
            do {
                genesisGroupInfo = try container.decode(GroupInfoArtifact.self, forKey: .genesisGroupInfo)
            } catch {
                LogManager.logError("Decoding error for required property 'genesisGroupInfo': \(error)")
                throw error
            }
            do {
                metadataSnapshot = try container.decode(MetadataSnapshot.self, forKey: .metadataSnapshot)
            } catch {
                LogManager.logError("Decoding error for required property 'metadataSnapshot': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(conversationKind, forKey: .conversationKind)
            try container.encode(transitionId, forKey: .transitionId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(absence, forKey: .absence)
            try container.encode(next, forKey: .next)
            try container.encode(manifest, forKey: .manifest)
            try container.encode(genesisGroupInfo, forKey: .genesisGroupInfo)
            try container.encode(metadataSnapshot, forKey: .metadataSnapshot)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(conversationId)
            hasher.combine(conversationKind)
            hasher.combine(transitionId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(absence)
            hasher.combine(next)
            hasher.combine(manifest)
            hasher.combine(genesisGroupInfo)
            hasher.combine(metadataSnapshot)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if conversationKind != other.conversationKind {
                return false
            }
            if transitionId != other.transitionId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if absence != other.absence {
                return false
            }
            if next != other.next {
                return false
            }
            if manifest != other.manifest {
                return false
            }
            if genesisGroupInfo != other.genesisGroupInfo {
                return false
            }
            if metadataSnapshot != other.metadataSnapshot {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let conversationKindValue = try conversationKind.toCBORValue()
            map = map.adding(key: "conversationKind", value: conversationKindValue)
            let transitionIdValue = try transitionId.toCBORValue()
            map = map.adding(key: "transitionId", value: transitionIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let absenceValue = try absence.toCBORValue()
            map = map.adding(key: "absence", value: absenceValue)
            let nextValue = try next.toCBORValue()
            map = map.adding(key: "next", value: nextValue)
            let manifestValue = try manifest.toCBORValue()
            map = map.adding(key: "manifest", value: manifestValue)
            let genesisGroupInfoValue = try genesisGroupInfo.toCBORValue()
            map = map.adding(key: "genesisGroupInfo", value: genesisGroupInfoValue)
            let metadataSnapshotValue = try metadataSnapshot.toCBORValue()
            map = map.adding(key: "metadataSnapshot", value: metadataSnapshotValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case conversationId
            case conversationKind
            case transitionId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case absence
            case next
            case manifest
            case genesisGroupInfo
            case metadataSnapshot
            case idempotencyKey
            case signedAt
        }
    }

    public struct CommitTransitionBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#commitTransitionBody"
        public let signatureDomain: String
        public let transitionId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let next: ConversationCoordinates
        public let aad: CommitAad
        public let manifest: TransitionManifest
        public let commit: PublicCommit
        public let metadataSnapshot: MetadataSnapshot
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, transitionId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, next: ConversationCoordinates, aad: CommitAad, manifest: TransitionManifest, commit: PublicCommit, metadataSnapshot: MetadataSnapshot, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.transitionId = transitionId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.next = next
            self.aad = aad
            self.manifest = manifest
            self.commit = commit
            self.metadataSnapshot = metadataSnapshot
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                transitionId = try container.decode(OperationId.self, forKey: .transitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                next = try container.decode(ConversationCoordinates.self, forKey: .next)
            } catch {
                LogManager.logError("Decoding error for required property 'next': \(error)")
                throw error
            }
            do {
                aad = try container.decode(CommitAad.self, forKey: .aad)
            } catch {
                LogManager.logError("Decoding error for required property 'aad': \(error)")
                throw error
            }
            do {
                manifest = try container.decode(TransitionManifest.self, forKey: .manifest)
            } catch {
                LogManager.logError("Decoding error for required property 'manifest': \(error)")
                throw error
            }
            do {
                commit = try container.decode(PublicCommit.self, forKey: .commit)
            } catch {
                LogManager.logError("Decoding error for required property 'commit': \(error)")
                throw error
            }
            do {
                metadataSnapshot = try container.decode(MetadataSnapshot.self, forKey: .metadataSnapshot)
            } catch {
                LogManager.logError("Decoding error for required property 'metadataSnapshot': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(transitionId, forKey: .transitionId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(next, forKey: .next)
            try container.encode(aad, forKey: .aad)
            try container.encode(manifest, forKey: .manifest)
            try container.encode(commit, forKey: .commit)
            try container.encode(metadataSnapshot, forKey: .metadataSnapshot)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(transitionId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(next)
            hasher.combine(aad)
            hasher.combine(manifest)
            hasher.combine(commit)
            hasher.combine(metadataSnapshot)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if transitionId != other.transitionId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if next != other.next {
                return false
            }
            if aad != other.aad {
                return false
            }
            if manifest != other.manifest {
                return false
            }
            if commit != other.commit {
                return false
            }
            if metadataSnapshot != other.metadataSnapshot {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let transitionIdValue = try transitionId.toCBORValue()
            map = map.adding(key: "transitionId", value: transitionIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let nextValue = try next.toCBORValue()
            map = map.adding(key: "next", value: nextValue)
            let aadValue = try aad.toCBORValue()
            map = map.adding(key: "aad", value: aadValue)
            let manifestValue = try manifest.toCBORValue()
            map = map.adding(key: "manifest", value: manifestValue)
            let commitValue = try commit.toCBORValue()
            map = map.adding(key: "commit", value: commitValue)
            let metadataSnapshotValue = try metadataSnapshot.toCBORValue()
            map = map.adding(key: "metadataSnapshot", value: metadataSnapshotValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case transitionId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case next
            case aad
            case manifest
            case commit
            case metadataSnapshot
            case idempotencyKey
            case signedAt
        }
    }

    public struct PolicyTransitionBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#policyTransitionBody"
        public let signatureDomain: String
        public let transitionId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let next: ConversationCoordinates
        public let participantChanges: [ParticipantChange]
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, transitionId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, next: ConversationCoordinates, participantChanges: [ParticipantChange], idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.transitionId = transitionId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.next = next
            self.participantChanges = participantChanges
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                transitionId = try container.decode(OperationId.self, forKey: .transitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                next = try container.decode(ConversationCoordinates.self, forKey: .next)
            } catch {
                LogManager.logError("Decoding error for required property 'next': \(error)")
                throw error
            }
            do {
                participantChanges = try container.decode([ParticipantChange].self, forKey: .participantChanges)
            } catch {
                LogManager.logError("Decoding error for required property 'participantChanges': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(transitionId, forKey: .transitionId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(next, forKey: .next)
            try container.encode(participantChanges, forKey: .participantChanges)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(transitionId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(next)
            hasher.combine(participantChanges)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if transitionId != other.transitionId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if next != other.next {
                return false
            }
            if participantChanges != other.participantChanges {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let transitionIdValue = try transitionId.toCBORValue()
            map = map.adding(key: "transitionId", value: transitionIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let nextValue = try next.toCBORValue()
            map = map.adding(key: "next", value: nextValue)
            let participantChangesValue = try participantChanges.toCBORValue()
            map = map.adding(key: "participantChanges", value: participantChangesValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case transitionId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case next
            case participantChanges
            case idempotencyKey
            case signedAt
        }
    }

    public struct ParticipantAcceptanceBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#participantAcceptanceBody"
        public let signatureDomain: String
        public let transitionId: OperationId
        public let recoveryRequestId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let next: ConversationCoordinates
        public let invitationProvenance: InvitationProvenance
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, transitionId: OperationId, recoveryRequestId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, next: ConversationCoordinates, invitationProvenance: InvitationProvenance, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.transitionId = transitionId
            self.recoveryRequestId = recoveryRequestId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.next = next
            self.invitationProvenance = invitationProvenance
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                transitionId = try container.decode(OperationId.self, forKey: .transitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionId': \(error)")
                throw error
            }
            do {
                recoveryRequestId = try container.decode(OperationId.self, forKey: .recoveryRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryRequestId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                next = try container.decode(ConversationCoordinates.self, forKey: .next)
            } catch {
                LogManager.logError("Decoding error for required property 'next': \(error)")
                throw error
            }
            do {
                invitationProvenance = try container.decode(InvitationProvenance.self, forKey: .invitationProvenance)
            } catch {
                LogManager.logError("Decoding error for required property 'invitationProvenance': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(transitionId, forKey: .transitionId)
            try container.encode(recoveryRequestId, forKey: .recoveryRequestId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(next, forKey: .next)
            try container.encode(invitationProvenance, forKey: .invitationProvenance)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(transitionId)
            hasher.combine(recoveryRequestId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(next)
            hasher.combine(invitationProvenance)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if transitionId != other.transitionId {
                return false
            }
            if recoveryRequestId != other.recoveryRequestId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if next != other.next {
                return false
            }
            if invitationProvenance != other.invitationProvenance {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let transitionIdValue = try transitionId.toCBORValue()
            map = map.adding(key: "transitionId", value: transitionIdValue)
            let recoveryRequestIdValue = try recoveryRequestId.toCBORValue()
            map = map.adding(key: "recoveryRequestId", value: recoveryRequestIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let nextValue = try next.toCBORValue()
            map = map.adding(key: "next", value: nextValue)
            let invitationProvenanceValue = try invitationProvenance.toCBORValue()
            map = map.adding(key: "invitationProvenance", value: invitationProvenanceValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case transitionId
            case recoveryRequestId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case next
            case invitationProvenance
            case idempotencyKey
            case signedAt
        }
    }

    public struct ApplicationSendBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#applicationSendBody"
        public let signatureDomain: String
        public let messageId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let aad: ApplicationAad
        public let applicationMessage: PrivateApplicationMessage
        public let blobBindings: [ApplicationAttachmentBinding]
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, messageId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, aad: ApplicationAad, applicationMessage: PrivateApplicationMessage, blobBindings: [ApplicationAttachmentBinding], signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.messageId = messageId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.aad = aad
            self.applicationMessage = applicationMessage
            self.blobBindings = blobBindings
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                messageId = try container.decode(OperationId.self, forKey: .messageId)
            } catch {
                LogManager.logError("Decoding error for required property 'messageId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                aad = try container.decode(ApplicationAad.self, forKey: .aad)
            } catch {
                LogManager.logError("Decoding error for required property 'aad': \(error)")
                throw error
            }
            do {
                applicationMessage = try container.decode(PrivateApplicationMessage.self, forKey: .applicationMessage)
            } catch {
                LogManager.logError("Decoding error for required property 'applicationMessage': \(error)")
                throw error
            }
            do {
                blobBindings = try container.decode([ApplicationAttachmentBinding].self, forKey: .blobBindings)
            } catch {
                LogManager.logError("Decoding error for required property 'blobBindings': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(messageId, forKey: .messageId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(aad, forKey: .aad)
            try container.encode(applicationMessage, forKey: .applicationMessage)
            try container.encode(blobBindings, forKey: .blobBindings)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(messageId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(aad)
            hasher.combine(applicationMessage)
            hasher.combine(blobBindings)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if messageId != other.messageId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if aad != other.aad {
                return false
            }
            if applicationMessage != other.applicationMessage {
                return false
            }
            if blobBindings != other.blobBindings {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let messageIdValue = try messageId.toCBORValue()
            map = map.adding(key: "messageId", value: messageIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let aadValue = try aad.toCBORValue()
            map = map.adding(key: "aad", value: aadValue)
            let applicationMessageValue = try applicationMessage.toCBORValue()
            map = map.adding(key: "applicationMessage", value: applicationMessageValue)
            let blobBindingsValue = try blobBindings.toCBORValue()
            map = map.adding(key: "blobBindings", value: blobBindingsValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case messageId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case aad
            case applicationMessage
            case blobBindings
            case signedAt
        }
    }

    public struct MessageTarget: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#messageTarget"
        public let targetSeq: Int
        public let targetMessageId: Bytes

        public init(
            targetSeq: Int, targetMessageId: Bytes
        ) {
            self.targetSeq = targetSeq
            self.targetMessageId = targetMessageId
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                targetSeq = try container.decode(Int.self, forKey: .targetSeq)
            } catch {
                LogManager.logError("Decoding error for required property 'targetSeq': \(error)")
                throw error
            }
            do {
                targetMessageId = try container.decode(Bytes.self, forKey: .targetMessageId)
            } catch {
                LogManager.logError("Decoding error for required property 'targetMessageId': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(targetSeq, forKey: .targetSeq)
            try container.encode(targetMessageId, forKey: .targetMessageId)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(targetSeq)
            hasher.combine(targetMessageId)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if targetSeq != other.targetSeq {
                return false
            }
            if targetMessageId != other.targetMessageId {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let targetSeqValue = try targetSeq.toCBORValue()
            map = map.adding(key: "targetSeq", value: targetSeqValue)
            let targetMessageIdValue = try targetMessageId.toCBORValue()
            map = map.adding(key: "targetMessageId", value: targetMessageIdValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case targetSeq
            case targetMessageId
        }
    }

    public struct EncryptedImageEmbed: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#encryptedImageEmbed"
        public let blobId: Bytes
        public let purpose: String
        public let algorithm: String
        public let key: Bytes
        public let nonce: Bytes
        public let ciphertextSha256: Bytes
        public let ciphertextSize: Int
        public let plaintextSize: Int
        public let mimeType: EncryptedImageEmbedMimeType
        public let width: Int
        public let height: Int
        public let altText: String?
        public let blurhash: String?

        public init(
            blobId: Bytes, purpose: String, algorithm: String, key: Bytes, nonce: Bytes, ciphertextSha256: Bytes, ciphertextSize: Int, plaintextSize: Int, mimeType: EncryptedImageEmbedMimeType, width: Int, height: Int, altText: String?, blurhash: String?
        ) {
            self.blobId = blobId
            self.purpose = purpose
            self.algorithm = algorithm
            self.key = key
            self.nonce = nonce
            self.ciphertextSha256 = ciphertextSha256
            self.ciphertextSize = ciphertextSize
            self.plaintextSize = plaintextSize
            self.mimeType = mimeType
            self.width = width
            self.height = height
            self.altText = altText
            self.blurhash = blurhash
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                blobId = try container.decode(Bytes.self, forKey: .blobId)
            } catch {
                LogManager.logError("Decoding error for required property 'blobId': \(error)")
                throw error
            }
            do {
                purpose = try container.decode(String.self, forKey: .purpose)
            } catch {
                LogManager.logError("Decoding error for required property 'purpose': \(error)")
                throw error
            }
            do {
                algorithm = try container.decode(String.self, forKey: .algorithm)
            } catch {
                LogManager.logError("Decoding error for required property 'algorithm': \(error)")
                throw error
            }
            do {
                key = try container.decode(Bytes.self, forKey: .key)
            } catch {
                LogManager.logError("Decoding error for required property 'key': \(error)")
                throw error
            }
            do {
                nonce = try container.decode(Bytes.self, forKey: .nonce)
            } catch {
                LogManager.logError("Decoding error for required property 'nonce': \(error)")
                throw error
            }
            do {
                ciphertextSha256 = try container.decode(Bytes.self, forKey: .ciphertextSha256)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSha256': \(error)")
                throw error
            }
            do {
                ciphertextSize = try container.decode(Int.self, forKey: .ciphertextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSize': \(error)")
                throw error
            }
            do {
                plaintextSize = try container.decode(Int.self, forKey: .plaintextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'plaintextSize': \(error)")
                throw error
            }
            do {
                mimeType = try container.decode(EncryptedImageEmbedMimeType.self, forKey: .mimeType)
            } catch {
                LogManager.logError("Decoding error for required property 'mimeType': \(error)")
                throw error
            }
            do {
                width = try container.decode(Int.self, forKey: .width)
            } catch {
                LogManager.logError("Decoding error for required property 'width': \(error)")
                throw error
            }
            do {
                height = try container.decode(Int.self, forKey: .height)
            } catch {
                LogManager.logError("Decoding error for required property 'height': \(error)")
                throw error
            }
            do {
                altText = try container.decodeIfPresent(String.self, forKey: .altText)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'altText' — degrading to nil: \(error)")
                altText = nil
            }
            do {
                blurhash = try container.decodeIfPresent(String.self, forKey: .blurhash)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'blurhash' — degrading to nil: \(error)")
                blurhash = nil
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(blobId, forKey: .blobId)
            try container.encode(purpose, forKey: .purpose)
            try container.encode(algorithm, forKey: .algorithm)
            try container.encode(key, forKey: .key)
            try container.encode(nonce, forKey: .nonce)
            try container.encode(ciphertextSha256, forKey: .ciphertextSha256)
            try container.encode(ciphertextSize, forKey: .ciphertextSize)
            try container.encode(plaintextSize, forKey: .plaintextSize)
            try container.encode(mimeType, forKey: .mimeType)
            try container.encode(width, forKey: .width)
            try container.encode(height, forKey: .height)
            try container.encodeIfPresent(altText, forKey: .altText)
            try container.encodeIfPresent(blurhash, forKey: .blurhash)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(blobId)
            hasher.combine(purpose)
            hasher.combine(algorithm)
            hasher.combine(key)
            hasher.combine(nonce)
            hasher.combine(ciphertextSha256)
            hasher.combine(ciphertextSize)
            hasher.combine(plaintextSize)
            hasher.combine(mimeType)
            hasher.combine(width)
            hasher.combine(height)
            if let value = altText {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
            if let value = blurhash {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if blobId != other.blobId {
                return false
            }
            if purpose != other.purpose {
                return false
            }
            if algorithm != other.algorithm {
                return false
            }
            if key != other.key {
                return false
            }
            if nonce != other.nonce {
                return false
            }
            if ciphertextSha256 != other.ciphertextSha256 {
                return false
            }
            if ciphertextSize != other.ciphertextSize {
                return false
            }
            if plaintextSize != other.plaintextSize {
                return false
            }
            if mimeType != other.mimeType {
                return false
            }
            if width != other.width {
                return false
            }
            if height != other.height {
                return false
            }
            if altText != other.altText {
                return false
            }
            if blurhash != other.blurhash {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let blobIdValue = try blobId.toCBORValue()
            map = map.adding(key: "blobId", value: blobIdValue)
            let purposeValue = try purpose.toCBORValue()
            map = map.adding(key: "purpose", value: purposeValue)
            let algorithmValue = try algorithm.toCBORValue()
            map = map.adding(key: "algorithm", value: algorithmValue)
            let keyValue = try key.toCBORValue()
            map = map.adding(key: "key", value: keyValue)
            let nonceValue = try nonce.toCBORValue()
            map = map.adding(key: "nonce", value: nonceValue)
            let ciphertextSha256Value = try ciphertextSha256.toCBORValue()
            map = map.adding(key: "ciphertextSha256", value: ciphertextSha256Value)
            let ciphertextSizeValue = try ciphertextSize.toCBORValue()
            map = map.adding(key: "ciphertextSize", value: ciphertextSizeValue)
            let plaintextSizeValue = try plaintextSize.toCBORValue()
            map = map.adding(key: "plaintextSize", value: plaintextSizeValue)
            let mimeTypeValue = try mimeType.toCBORValue()
            map = map.adding(key: "mimeType", value: mimeTypeValue)
            let widthValue = try width.toCBORValue()
            map = map.adding(key: "width", value: widthValue)
            let heightValue = try height.toCBORValue()
            map = map.adding(key: "height", value: heightValue)
            if let value = altText {
                let altTextValue = try value.toCBORValue()
                map = map.adding(key: "altText", value: altTextValue)
            }
            if let value = blurhash {
                let blurhashValue = try value.toCBORValue()
                map = map.adding(key: "blurhash", value: blurhashValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case blobId
            case purpose
            case algorithm
            case key
            case nonce
            case ciphertextSha256
            case ciphertextSize
            case plaintextSize
            case mimeType
            case width
            case height
            case altText
            case blurhash
        }
    }

    public struct EncryptedAudioEmbed: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#encryptedAudioEmbed"
        public let blobId: Bytes
        public let purpose: String
        public let algorithm: String
        public let key: Bytes
        public let nonce: Bytes
        public let ciphertextSha256: Bytes
        public let ciphertextSize: Int
        public let plaintextSize: Int
        public let mimeType: EncryptedAudioEmbedMimeType
        public let durationMillis: Int
        public let waveform: Bytes
        public let transcript: String?

        public init(
            blobId: Bytes, purpose: String, algorithm: String, key: Bytes, nonce: Bytes, ciphertextSha256: Bytes, ciphertextSize: Int, plaintextSize: Int, mimeType: EncryptedAudioEmbedMimeType, durationMillis: Int, waveform: Bytes, transcript: String?
        ) {
            self.blobId = blobId
            self.purpose = purpose
            self.algorithm = algorithm
            self.key = key
            self.nonce = nonce
            self.ciphertextSha256 = ciphertextSha256
            self.ciphertextSize = ciphertextSize
            self.plaintextSize = plaintextSize
            self.mimeType = mimeType
            self.durationMillis = durationMillis
            self.waveform = waveform
            self.transcript = transcript
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                blobId = try container.decode(Bytes.self, forKey: .blobId)
            } catch {
                LogManager.logError("Decoding error for required property 'blobId': \(error)")
                throw error
            }
            do {
                purpose = try container.decode(String.self, forKey: .purpose)
            } catch {
                LogManager.logError("Decoding error for required property 'purpose': \(error)")
                throw error
            }
            do {
                algorithm = try container.decode(String.self, forKey: .algorithm)
            } catch {
                LogManager.logError("Decoding error for required property 'algorithm': \(error)")
                throw error
            }
            do {
                key = try container.decode(Bytes.self, forKey: .key)
            } catch {
                LogManager.logError("Decoding error for required property 'key': \(error)")
                throw error
            }
            do {
                nonce = try container.decode(Bytes.self, forKey: .nonce)
            } catch {
                LogManager.logError("Decoding error for required property 'nonce': \(error)")
                throw error
            }
            do {
                ciphertextSha256 = try container.decode(Bytes.self, forKey: .ciphertextSha256)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSha256': \(error)")
                throw error
            }
            do {
                ciphertextSize = try container.decode(Int.self, forKey: .ciphertextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSize': \(error)")
                throw error
            }
            do {
                plaintextSize = try container.decode(Int.self, forKey: .plaintextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'plaintextSize': \(error)")
                throw error
            }
            do {
                mimeType = try container.decode(EncryptedAudioEmbedMimeType.self, forKey: .mimeType)
            } catch {
                LogManager.logError("Decoding error for required property 'mimeType': \(error)")
                throw error
            }
            do {
                durationMillis = try container.decode(Int.self, forKey: .durationMillis)
            } catch {
                LogManager.logError("Decoding error for required property 'durationMillis': \(error)")
                throw error
            }
            do {
                waveform = try container.decode(Bytes.self, forKey: .waveform)
            } catch {
                LogManager.logError("Decoding error for required property 'waveform': \(error)")
                throw error
            }
            do {
                transcript = try container.decodeIfPresent(String.self, forKey: .transcript)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'transcript' — degrading to nil: \(error)")
                transcript = nil
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(blobId, forKey: .blobId)
            try container.encode(purpose, forKey: .purpose)
            try container.encode(algorithm, forKey: .algorithm)
            try container.encode(key, forKey: .key)
            try container.encode(nonce, forKey: .nonce)
            try container.encode(ciphertextSha256, forKey: .ciphertextSha256)
            try container.encode(ciphertextSize, forKey: .ciphertextSize)
            try container.encode(plaintextSize, forKey: .plaintextSize)
            try container.encode(mimeType, forKey: .mimeType)
            try container.encode(durationMillis, forKey: .durationMillis)
            try container.encode(waveform, forKey: .waveform)
            try container.encodeIfPresent(transcript, forKey: .transcript)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(blobId)
            hasher.combine(purpose)
            hasher.combine(algorithm)
            hasher.combine(key)
            hasher.combine(nonce)
            hasher.combine(ciphertextSha256)
            hasher.combine(ciphertextSize)
            hasher.combine(plaintextSize)
            hasher.combine(mimeType)
            hasher.combine(durationMillis)
            hasher.combine(waveform)
            if let value = transcript {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if blobId != other.blobId {
                return false
            }
            if purpose != other.purpose {
                return false
            }
            if algorithm != other.algorithm {
                return false
            }
            if key != other.key {
                return false
            }
            if nonce != other.nonce {
                return false
            }
            if ciphertextSha256 != other.ciphertextSha256 {
                return false
            }
            if ciphertextSize != other.ciphertextSize {
                return false
            }
            if plaintextSize != other.plaintextSize {
                return false
            }
            if mimeType != other.mimeType {
                return false
            }
            if durationMillis != other.durationMillis {
                return false
            }
            if waveform != other.waveform {
                return false
            }
            if transcript != other.transcript {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let blobIdValue = try blobId.toCBORValue()
            map = map.adding(key: "blobId", value: blobIdValue)
            let purposeValue = try purpose.toCBORValue()
            map = map.adding(key: "purpose", value: purposeValue)
            let algorithmValue = try algorithm.toCBORValue()
            map = map.adding(key: "algorithm", value: algorithmValue)
            let keyValue = try key.toCBORValue()
            map = map.adding(key: "key", value: keyValue)
            let nonceValue = try nonce.toCBORValue()
            map = map.adding(key: "nonce", value: nonceValue)
            let ciphertextSha256Value = try ciphertextSha256.toCBORValue()
            map = map.adding(key: "ciphertextSha256", value: ciphertextSha256Value)
            let ciphertextSizeValue = try ciphertextSize.toCBORValue()
            map = map.adding(key: "ciphertextSize", value: ciphertextSizeValue)
            let plaintextSizeValue = try plaintextSize.toCBORValue()
            map = map.adding(key: "plaintextSize", value: plaintextSizeValue)
            let mimeTypeValue = try mimeType.toCBORValue()
            map = map.adding(key: "mimeType", value: mimeTypeValue)
            let durationMillisValue = try durationMillis.toCBORValue()
            map = map.adding(key: "durationMillis", value: durationMillisValue)
            let waveformValue = try waveform.toCBORValue()
            map = map.adding(key: "waveform", value: waveformValue)
            if let value = transcript {
                let transcriptValue = try value.toCBORValue()
                map = map.adding(key: "transcript", value: transcriptValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case blobId
            case purpose
            case algorithm
            case key
            case nonce
            case ciphertextSha256
            case ciphertextSize
            case plaintextSize
            case mimeType
            case durationMillis
            case waveform
            case transcript
        }
    }

    public struct AtprotoRecordEmbed: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#atprotoRecordEmbed"
        public let uri: ATProtocolURI
        public let cid: CID

        public init(
            uri: ATProtocolURI, cid: CID
        ) {
            self.uri = uri
            self.cid = cid
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                uri = try container.decode(ATProtocolURI.self, forKey: .uri)
            } catch {
                LogManager.logError("Decoding error for required property 'uri': \(error)")
                throw error
            }
            do {
                cid = try container.decode(CID.self, forKey: .cid)
            } catch {
                LogManager.logError("Decoding error for required property 'cid': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(uri, forKey: .uri)
            try container.encode(cid, forKey: .cid)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(uri)
            hasher.combine(cid)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if uri != other.uri {
                return false
            }
            if cid != other.cid {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let uriValue = try uri.toCBORValue()
            map = map.adding(key: "uri", value: uriValue)
            let cidValue = try cid.toCBORValue()
            map = map.adding(key: "cid", value: cidValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case uri
            case cid
        }
    }

    public struct ExternalLinkEmbed: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#externalLinkEmbed"
        public let uri: URI
        public let title: String?
        public let description: String?

        public init(
            uri: URI, title: String?, description: String?
        ) {
            self.uri = uri
            self.title = title
            self.description = description
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                uri = try container.decode(URI.self, forKey: .uri)
            } catch {
                LogManager.logError("Decoding error for required property 'uri': \(error)")
                throw error
            }
            do {
                title = try container.decodeIfPresent(String.self, forKey: .title)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'title' — degrading to nil: \(error)")
                title = nil
            }
            do {
                description = try container.decodeIfPresent(String.self, forKey: .description)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'description' — degrading to nil: \(error)")
                description = nil
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(uri, forKey: .uri)
            try container.encodeIfPresent(title, forKey: .title)
            try container.encodeIfPresent(description, forKey: .description)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(uri)
            if let value = title {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
            if let value = description {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if uri != other.uri {
                return false
            }
            if title != other.title {
                return false
            }
            if description != other.description {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let uriValue = try uri.toCBORValue()
            map = map.adding(key: "uri", value: uriValue)
            if let value = title {
                let titleValue = try value.toCBORValue()
                map = map.adding(key: "title", value: titleValue)
            }
            if let value = description {
                let descriptionValue = try value.toCBORValue()
                map = map.adding(key: "description", value: descriptionValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case uri
            case title
            case description
        }
    }

    public struct EncryptedImageEmbedVariant: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#encryptedImageEmbedVariant"
        public let encryptedImage: EncryptedImageEmbed

        public init(
            encryptedImage: EncryptedImageEmbed
        ) {
            self.encryptedImage = encryptedImage
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                encryptedImage = try container.decode(EncryptedImageEmbed.self, forKey: .encryptedImage)
            } catch {
                LogManager.logError("Decoding error for required property 'encryptedImage': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(encryptedImage, forKey: .encryptedImage)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(encryptedImage)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if encryptedImage != other.encryptedImage {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let encryptedImageValue = try encryptedImage.toCBORValue()
            map = map.adding(key: "encryptedImage", value: encryptedImageValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case encryptedImage
        }
    }

    public struct EncryptedAudioEmbedVariant: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#encryptedAudioEmbedVariant"
        public let encryptedAudio: EncryptedAudioEmbed

        public init(
            encryptedAudio: EncryptedAudioEmbed
        ) {
            self.encryptedAudio = encryptedAudio
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                encryptedAudio = try container.decode(EncryptedAudioEmbed.self, forKey: .encryptedAudio)
            } catch {
                LogManager.logError("Decoding error for required property 'encryptedAudio': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(encryptedAudio, forKey: .encryptedAudio)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(encryptedAudio)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if encryptedAudio != other.encryptedAudio {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let encryptedAudioValue = try encryptedAudio.toCBORValue()
            map = map.adding(key: "encryptedAudio", value: encryptedAudioValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case encryptedAudio
        }
    }

    public struct AtprotoRecordEmbedVariant: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#atprotoRecordEmbedVariant"
        public let atprotoRecord: AtprotoRecordEmbed

        public init(
            atprotoRecord: AtprotoRecordEmbed
        ) {
            self.atprotoRecord = atprotoRecord
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                atprotoRecord = try container.decode(AtprotoRecordEmbed.self, forKey: .atprotoRecord)
            } catch {
                LogManager.logError("Decoding error for required property 'atprotoRecord': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(atprotoRecord, forKey: .atprotoRecord)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(atprotoRecord)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if atprotoRecord != other.atprotoRecord {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let atprotoRecordValue = try atprotoRecord.toCBORValue()
            map = map.adding(key: "atprotoRecord", value: atprotoRecordValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case atprotoRecord
        }
    }

    public struct ExternalLinkEmbedVariant: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#externalLinkEmbedVariant"
        public let externalLink: ExternalLinkEmbed

        public init(
            externalLink: ExternalLinkEmbed
        ) {
            self.externalLink = externalLink
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                externalLink = try container.decode(ExternalLinkEmbed.self, forKey: .externalLink)
            } catch {
                LogManager.logError("Decoding error for required property 'externalLink': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(externalLink, forKey: .externalLink)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(externalLink)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if externalLink != other.externalLink {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let externalLinkValue = try externalLink.toCBORValue()
            map = map.adding(key: "externalLink", value: externalLinkValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case externalLink
        }
    }

    public struct MessageFrameBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#messageFrameBody"
        public let text: String?
        public let replyTo: MessageTarget?
        public let embed: ApplicationEmbed?

        public init(
            text: String?, replyTo: MessageTarget?, embed: ApplicationEmbed?
        ) {
            self.text = text
            self.replyTo = replyTo
            self.embed = embed
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                text = try container.decodeIfPresent(String.self, forKey: .text)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'text' — degrading to nil: \(error)")
                text = nil
            }
            do {
                replyTo = try container.decodeIfPresent(MessageTarget.self, forKey: .replyTo)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'replyTo' — degrading to nil: \(error)")
                replyTo = nil
            }
            do {
                embed = try container.decodeIfPresent(ApplicationEmbed.self, forKey: .embed)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'embed' — degrading to nil: \(error)")
                embed = nil
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encodeIfPresent(text, forKey: .text)
            try container.encodeIfPresent(replyTo, forKey: .replyTo)
            try container.encodeIfPresent(embed, forKey: .embed)
        }

        public func hash(into hasher: inout Hasher) {
            if let value = text {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
            if let value = replyTo {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
            if let value = embed {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if text != other.text {
                return false
            }
            if replyTo != other.replyTo {
                return false
            }
            if embed != other.embed {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            if let value = text {
                let textValue = try value.toCBORValue()
                map = map.adding(key: "text", value: textValue)
            }
            if let value = replyTo {
                let replyToValue = try value.toCBORValue()
                map = map.adding(key: "replyTo", value: replyToValue)
            }
            if let value = embed {
                let embedValue = try value.toCBORValue()
                map = map.adding(key: "embed", value: embedValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case text
            case replyTo
            case embed
        }
    }

    public struct ReactionFrameBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#reactionFrameBody"
        public let target: MessageTarget
        public let emoji: String
        public let operation: ReactionFrameBodyOperation

        public init(
            target: MessageTarget, emoji: String, operation: ReactionFrameBodyOperation
        ) {
            self.target = target
            self.emoji = emoji
            self.operation = operation
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                target = try container.decode(MessageTarget.self, forKey: .target)
            } catch {
                LogManager.logError("Decoding error for required property 'target': \(error)")
                throw error
            }
            do {
                emoji = try container.decode(String.self, forKey: .emoji)
            } catch {
                LogManager.logError("Decoding error for required property 'emoji': \(error)")
                throw error
            }
            do {
                operation = try container.decode(ReactionFrameBodyOperation.self, forKey: .operation)
            } catch {
                LogManager.logError("Decoding error for required property 'operation': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(target, forKey: .target)
            try container.encode(emoji, forKey: .emoji)
            try container.encode(operation, forKey: .operation)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(target)
            hasher.combine(emoji)
            hasher.combine(operation)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if target != other.target {
                return false
            }
            if emoji != other.emoji {
                return false
            }
            if operation != other.operation {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let targetValue = try target.toCBORValue()
            map = map.adding(key: "target", value: targetValue)
            let emojiValue = try emoji.toCBORValue()
            map = map.adding(key: "emoji", value: emojiValue)
            let operationValue = try operation.toCBORValue()
            map = map.adding(key: "operation", value: operationValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case target
            case emoji
            case operation
        }
    }

    public struct EditFrameBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#editFrameBody"
        public let target: MessageTarget
        public let replacementText: String

        public init(
            target: MessageTarget, replacementText: String
        ) {
            self.target = target
            self.replacementText = replacementText
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                target = try container.decode(MessageTarget.self, forKey: .target)
            } catch {
                LogManager.logError("Decoding error for required property 'target': \(error)")
                throw error
            }
            do {
                replacementText = try container.decode(String.self, forKey: .replacementText)
            } catch {
                LogManager.logError("Decoding error for required property 'replacementText': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(target, forKey: .target)
            try container.encode(replacementText, forKey: .replacementText)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(target)
            hasher.combine(replacementText)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if target != other.target {
                return false
            }
            if replacementText != other.replacementText {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let targetValue = try target.toCBORValue()
            map = map.adding(key: "target", value: targetValue)
            let replacementTextValue = try replacementText.toCBORValue()
            map = map.adding(key: "replacementText", value: replacementTextValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case target
            case replacementText
        }
    }

    public struct TombstoneFrameBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#tombstoneFrameBody"
        public let target: MessageTarget

        public init(
            target: MessageTarget
        ) {
            self.target = target
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                target = try container.decode(MessageTarget.self, forKey: .target)
            } catch {
                LogManager.logError("Decoding error for required property 'target': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(target, forKey: .target)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(target)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if target != other.target {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let targetValue = try target.toCBORValue()
            map = map.adding(key: "target", value: targetValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case target
        }
    }

    public struct ReadStateFrameBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#readStateFrameBody"
        public let throughSeq: Int
        public let throughMessageId: Bytes

        public init(
            throughSeq: Int, throughMessageId: Bytes
        ) {
            self.throughSeq = throughSeq
            self.throughMessageId = throughMessageId
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                throughSeq = try container.decode(Int.self, forKey: .throughSeq)
            } catch {
                LogManager.logError("Decoding error for required property 'throughSeq': \(error)")
                throw error
            }
            do {
                throughMessageId = try container.decode(Bytes.self, forKey: .throughMessageId)
            } catch {
                LogManager.logError("Decoding error for required property 'throughMessageId': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(throughSeq, forKey: .throughSeq)
            try container.encode(throughMessageId, forKey: .throughMessageId)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(throughSeq)
            hasher.combine(throughMessageId)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if throughSeq != other.throughSeq {
                return false
            }
            if throughMessageId != other.throughMessageId {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let throughSeqValue = try throughSeq.toCBORValue()
            map = map.adding(key: "throughSeq", value: throughSeqValue)
            let throughMessageIdValue = try throughMessageId.toCBORValue()
            map = map.adding(key: "throughMessageId", value: throughMessageIdValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case throughSeq
            case throughMessageId
        }
    }

    public struct MessageFrameVariant: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#messageFrameVariant"
        public let message: MessageFrameBody

        public init(
            message: MessageFrameBody
        ) {
            self.message = message
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                message = try container.decode(MessageFrameBody.self, forKey: .message)
            } catch {
                LogManager.logError("Decoding error for required property 'message': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(message, forKey: .message)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(message)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if message != other.message {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let messageValue = try message.toCBORValue()
            map = map.adding(key: "message", value: messageValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case message
        }
    }

    public struct ReactionFrameVariant: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#reactionFrameVariant"
        public let reaction: ReactionFrameBody

        public init(
            reaction: ReactionFrameBody
        ) {
            self.reaction = reaction
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                reaction = try container.decode(ReactionFrameBody.self, forKey: .reaction)
            } catch {
                LogManager.logError("Decoding error for required property 'reaction': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(reaction, forKey: .reaction)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(reaction)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if reaction != other.reaction {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let reactionValue = try reaction.toCBORValue()
            map = map.adding(key: "reaction", value: reactionValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case reaction
        }
    }

    public struct EditFrameVariant: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#editFrameVariant"
        public let edit: EditFrameBody

        public init(
            edit: EditFrameBody
        ) {
            self.edit = edit
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                edit = try container.decode(EditFrameBody.self, forKey: .edit)
            } catch {
                LogManager.logError("Decoding error for required property 'edit': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(edit, forKey: .edit)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(edit)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if edit != other.edit {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let editValue = try edit.toCBORValue()
            map = map.adding(key: "edit", value: editValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case edit
        }
    }

    public struct TombstoneFrameVariant: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#tombstoneFrameVariant"
        public let tombstone: TombstoneFrameBody

        public init(
            tombstone: TombstoneFrameBody
        ) {
            self.tombstone = tombstone
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                tombstone = try container.decode(TombstoneFrameBody.self, forKey: .tombstone)
            } catch {
                LogManager.logError("Decoding error for required property 'tombstone': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(tombstone, forKey: .tombstone)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(tombstone)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if tombstone != other.tombstone {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let tombstoneValue = try tombstone.toCBORValue()
            map = map.adding(key: "tombstone", value: tombstoneValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case tombstone
        }
    }

    public struct ReadStateFrameVariant: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#readStateFrameVariant"
        public let readState: ReadStateFrameBody

        public init(
            readState: ReadStateFrameBody
        ) {
            self.readState = readState
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                readState = try container.decode(ReadStateFrameBody.self, forKey: .readState)
            } catch {
                LogManager.logError("Decoding error for required property 'readState': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(readState, forKey: .readState)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(readState)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if readState != other.readState {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let readStateValue = try readState.toCBORValue()
            map = map.adding(key: "readState", value: readStateValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case readState
        }
    }

    public struct ApplicationFrame: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#applicationFrame"
        public let `protocol`: String
        public let version: Int
        public let messageId: Bytes
        public let context: ApplicationFrameContext
        public let body: ApplicationFrameBody

        public init(
            protocol: String, version: Int, messageId: Bytes, context: ApplicationFrameContext, body: ApplicationFrameBody
        ) {
            self.protocol = `protocol`
            self.version = version
            self.messageId = messageId
            self.context = context
            self.body = body
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                self.protocol = try container.decode(String.self, forKey: .protocol)
            } catch {
                LogManager.logError("Decoding error for required property 'protocol': \(error)")
                throw error
            }
            do {
                version = try container.decode(Int.self, forKey: .version)
            } catch {
                LogManager.logError("Decoding error for required property 'version': \(error)")
                throw error
            }
            do {
                messageId = try container.decode(Bytes.self, forKey: .messageId)
            } catch {
                LogManager.logError("Decoding error for required property 'messageId': \(error)")
                throw error
            }
            do {
                context = try container.decode(ApplicationFrameContext.self, forKey: .context)
            } catch {
                LogManager.logError("Decoding error for required property 'context': \(error)")
                throw error
            }
            do {
                body = try container.decode(ApplicationFrameBody.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(`protocol`, forKey: .protocol)
            try container.encode(version, forKey: .version)
            try container.encode(messageId, forKey: .messageId)
            try container.encode(context, forKey: .context)
            try container.encode(body, forKey: .body)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(`protocol`)
            hasher.combine(version)
            hasher.combine(messageId)
            hasher.combine(context)
            hasher.combine(body)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if `protocol` != other.protocol {
                return false
            }
            if version != other.version {
                return false
            }
            if messageId != other.messageId {
                return false
            }
            if context != other.context {
                return false
            }
            if body != other.body {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let protocolValue = try `protocol`.toCBORValue()
            map = map.adding(key: "protocol", value: protocolValue)
            let versionValue = try version.toCBORValue()
            map = map.adding(key: "version", value: versionValue)
            let messageIdValue = try messageId.toCBORValue()
            map = map.adding(key: "messageId", value: messageIdValue)
            let contextValue = try context.toCBORValue()
            map = map.adding(key: "context", value: contextValue)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case `protocol`
            case version
            case messageId
            case context
            case body
        }
    }

    public struct BlobAad: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#blobAad"
        public let `protocol`: String
        public let context: ApplicationFrameContext
        public let messageId: Bytes
        public let attachmentIndex: Int
        public let blobId: Bytes
        public let purpose: String
        public let mediaType: String
        public let plaintextSize: Int

        public init(
            protocol: String, context: ApplicationFrameContext, messageId: Bytes, attachmentIndex: Int, blobId: Bytes, purpose: String, mediaType: String, plaintextSize: Int
        ) {
            self.protocol = `protocol`
            self.context = context
            self.messageId = messageId
            self.attachmentIndex = attachmentIndex
            self.blobId = blobId
            self.purpose = purpose
            self.mediaType = mediaType
            self.plaintextSize = plaintextSize
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                self.protocol = try container.decode(String.self, forKey: .protocol)
            } catch {
                LogManager.logError("Decoding error for required property 'protocol': \(error)")
                throw error
            }
            do {
                context = try container.decode(ApplicationFrameContext.self, forKey: .context)
            } catch {
                LogManager.logError("Decoding error for required property 'context': \(error)")
                throw error
            }
            do {
                messageId = try container.decode(Bytes.self, forKey: .messageId)
            } catch {
                LogManager.logError("Decoding error for required property 'messageId': \(error)")
                throw error
            }
            do {
                attachmentIndex = try container.decode(Int.self, forKey: .attachmentIndex)
            } catch {
                LogManager.logError("Decoding error for required property 'attachmentIndex': \(error)")
                throw error
            }
            do {
                blobId = try container.decode(Bytes.self, forKey: .blobId)
            } catch {
                LogManager.logError("Decoding error for required property 'blobId': \(error)")
                throw error
            }
            do {
                purpose = try container.decode(String.self, forKey: .purpose)
            } catch {
                LogManager.logError("Decoding error for required property 'purpose': \(error)")
                throw error
            }
            do {
                mediaType = try container.decode(String.self, forKey: .mediaType)
            } catch {
                LogManager.logError("Decoding error for required property 'mediaType': \(error)")
                throw error
            }
            do {
                plaintextSize = try container.decode(Int.self, forKey: .plaintextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'plaintextSize': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(`protocol`, forKey: .protocol)
            try container.encode(context, forKey: .context)
            try container.encode(messageId, forKey: .messageId)
            try container.encode(attachmentIndex, forKey: .attachmentIndex)
            try container.encode(blobId, forKey: .blobId)
            try container.encode(purpose, forKey: .purpose)
            try container.encode(mediaType, forKey: .mediaType)
            try container.encode(plaintextSize, forKey: .plaintextSize)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(`protocol`)
            hasher.combine(context)
            hasher.combine(messageId)
            hasher.combine(attachmentIndex)
            hasher.combine(blobId)
            hasher.combine(purpose)
            hasher.combine(mediaType)
            hasher.combine(plaintextSize)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if `protocol` != other.protocol {
                return false
            }
            if context != other.context {
                return false
            }
            if messageId != other.messageId {
                return false
            }
            if attachmentIndex != other.attachmentIndex {
                return false
            }
            if blobId != other.blobId {
                return false
            }
            if purpose != other.purpose {
                return false
            }
            if mediaType != other.mediaType {
                return false
            }
            if plaintextSize != other.plaintextSize {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let protocolValue = try `protocol`.toCBORValue()
            map = map.adding(key: "protocol", value: protocolValue)
            let contextValue = try context.toCBORValue()
            map = map.adding(key: "context", value: contextValue)
            let messageIdValue = try messageId.toCBORValue()
            map = map.adding(key: "messageId", value: messageIdValue)
            let attachmentIndexValue = try attachmentIndex.toCBORValue()
            map = map.adding(key: "attachmentIndex", value: attachmentIndexValue)
            let blobIdValue = try blobId.toCBORValue()
            map = map.adding(key: "blobId", value: blobIdValue)
            let purposeValue = try purpose.toCBORValue()
            map = map.adding(key: "purpose", value: purposeValue)
            let mediaTypeValue = try mediaType.toCBORValue()
            map = map.adding(key: "mediaType", value: mediaTypeValue)
            let plaintextSizeValue = try plaintextSize.toCBORValue()
            map = map.adding(key: "plaintextSize", value: plaintextSizeValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case `protocol`
            case context
            case messageId
            case attachmentIndex
            case blobId
            case purpose
            case mediaType
            case plaintextSize
        }
    }

    public struct MetadataAvatarEmbed: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#metadataAvatarEmbed"
        public let blobId: Bytes
        public let purpose: String
        public let algorithm: String
        public let key: Bytes
        public let nonce: Bytes
        public let ciphertextSha256: Bytes
        public let ciphertextSize: Int
        public let plaintextSize: Int
        public let mimeType: MetadataAvatarEmbedMimeType
        public let width: Int
        public let height: Int
        public let altText: String?
        public let blurhash: String?
        public let originTransitionId: Bytes
        public let originalMetadataVersion: Int

        public init(
            blobId: Bytes, purpose: String, algorithm: String, key: Bytes, nonce: Bytes, ciphertextSha256: Bytes, ciphertextSize: Int, plaintextSize: Int, mimeType: MetadataAvatarEmbedMimeType, width: Int, height: Int, altText: String?, blurhash: String?, originTransitionId: Bytes, originalMetadataVersion: Int
        ) {
            self.blobId = blobId
            self.purpose = purpose
            self.algorithm = algorithm
            self.key = key
            self.nonce = nonce
            self.ciphertextSha256 = ciphertextSha256
            self.ciphertextSize = ciphertextSize
            self.plaintextSize = plaintextSize
            self.mimeType = mimeType
            self.width = width
            self.height = height
            self.altText = altText
            self.blurhash = blurhash
            self.originTransitionId = originTransitionId
            self.originalMetadataVersion = originalMetadataVersion
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                blobId = try container.decode(Bytes.self, forKey: .blobId)
            } catch {
                LogManager.logError("Decoding error for required property 'blobId': \(error)")
                throw error
            }
            do {
                purpose = try container.decode(String.self, forKey: .purpose)
            } catch {
                LogManager.logError("Decoding error for required property 'purpose': \(error)")
                throw error
            }
            do {
                algorithm = try container.decode(String.self, forKey: .algorithm)
            } catch {
                LogManager.logError("Decoding error for required property 'algorithm': \(error)")
                throw error
            }
            do {
                key = try container.decode(Bytes.self, forKey: .key)
            } catch {
                LogManager.logError("Decoding error for required property 'key': \(error)")
                throw error
            }
            do {
                nonce = try container.decode(Bytes.self, forKey: .nonce)
            } catch {
                LogManager.logError("Decoding error for required property 'nonce': \(error)")
                throw error
            }
            do {
                ciphertextSha256 = try container.decode(Bytes.self, forKey: .ciphertextSha256)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSha256': \(error)")
                throw error
            }
            do {
                ciphertextSize = try container.decode(Int.self, forKey: .ciphertextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSize': \(error)")
                throw error
            }
            do {
                plaintextSize = try container.decode(Int.self, forKey: .plaintextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'plaintextSize': \(error)")
                throw error
            }
            do {
                mimeType = try container.decode(MetadataAvatarEmbedMimeType.self, forKey: .mimeType)
            } catch {
                LogManager.logError("Decoding error for required property 'mimeType': \(error)")
                throw error
            }
            do {
                width = try container.decode(Int.self, forKey: .width)
            } catch {
                LogManager.logError("Decoding error for required property 'width': \(error)")
                throw error
            }
            do {
                height = try container.decode(Int.self, forKey: .height)
            } catch {
                LogManager.logError("Decoding error for required property 'height': \(error)")
                throw error
            }
            do {
                altText = try container.decodeIfPresent(String.self, forKey: .altText)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'altText' — degrading to nil: \(error)")
                altText = nil
            }
            do {
                blurhash = try container.decodeIfPresent(String.self, forKey: .blurhash)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'blurhash' — degrading to nil: \(error)")
                blurhash = nil
            }
            do {
                originTransitionId = try container.decode(Bytes.self, forKey: .originTransitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'originTransitionId': \(error)")
                throw error
            }
            do {
                originalMetadataVersion = try container.decode(Int.self, forKey: .originalMetadataVersion)
            } catch {
                LogManager.logError("Decoding error for required property 'originalMetadataVersion': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(blobId, forKey: .blobId)
            try container.encode(purpose, forKey: .purpose)
            try container.encode(algorithm, forKey: .algorithm)
            try container.encode(key, forKey: .key)
            try container.encode(nonce, forKey: .nonce)
            try container.encode(ciphertextSha256, forKey: .ciphertextSha256)
            try container.encode(ciphertextSize, forKey: .ciphertextSize)
            try container.encode(plaintextSize, forKey: .plaintextSize)
            try container.encode(mimeType, forKey: .mimeType)
            try container.encode(width, forKey: .width)
            try container.encode(height, forKey: .height)
            try container.encodeIfPresent(altText, forKey: .altText)
            try container.encodeIfPresent(blurhash, forKey: .blurhash)
            try container.encode(originTransitionId, forKey: .originTransitionId)
            try container.encode(originalMetadataVersion, forKey: .originalMetadataVersion)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(blobId)
            hasher.combine(purpose)
            hasher.combine(algorithm)
            hasher.combine(key)
            hasher.combine(nonce)
            hasher.combine(ciphertextSha256)
            hasher.combine(ciphertextSize)
            hasher.combine(plaintextSize)
            hasher.combine(mimeType)
            hasher.combine(width)
            hasher.combine(height)
            if let value = altText {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
            if let value = blurhash {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
            hasher.combine(originTransitionId)
            hasher.combine(originalMetadataVersion)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if blobId != other.blobId {
                return false
            }
            if purpose != other.purpose {
                return false
            }
            if algorithm != other.algorithm {
                return false
            }
            if key != other.key {
                return false
            }
            if nonce != other.nonce {
                return false
            }
            if ciphertextSha256 != other.ciphertextSha256 {
                return false
            }
            if ciphertextSize != other.ciphertextSize {
                return false
            }
            if plaintextSize != other.plaintextSize {
                return false
            }
            if mimeType != other.mimeType {
                return false
            }
            if width != other.width {
                return false
            }
            if height != other.height {
                return false
            }
            if altText != other.altText {
                return false
            }
            if blurhash != other.blurhash {
                return false
            }
            if originTransitionId != other.originTransitionId {
                return false
            }
            if originalMetadataVersion != other.originalMetadataVersion {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let blobIdValue = try blobId.toCBORValue()
            map = map.adding(key: "blobId", value: blobIdValue)
            let purposeValue = try purpose.toCBORValue()
            map = map.adding(key: "purpose", value: purposeValue)
            let algorithmValue = try algorithm.toCBORValue()
            map = map.adding(key: "algorithm", value: algorithmValue)
            let keyValue = try key.toCBORValue()
            map = map.adding(key: "key", value: keyValue)
            let nonceValue = try nonce.toCBORValue()
            map = map.adding(key: "nonce", value: nonceValue)
            let ciphertextSha256Value = try ciphertextSha256.toCBORValue()
            map = map.adding(key: "ciphertextSha256", value: ciphertextSha256Value)
            let ciphertextSizeValue = try ciphertextSize.toCBORValue()
            map = map.adding(key: "ciphertextSize", value: ciphertextSizeValue)
            let plaintextSizeValue = try plaintextSize.toCBORValue()
            map = map.adding(key: "plaintextSize", value: plaintextSizeValue)
            let mimeTypeValue = try mimeType.toCBORValue()
            map = map.adding(key: "mimeType", value: mimeTypeValue)
            let widthValue = try width.toCBORValue()
            map = map.adding(key: "width", value: widthValue)
            let heightValue = try height.toCBORValue()
            map = map.adding(key: "height", value: heightValue)
            if let value = altText {
                let altTextValue = try value.toCBORValue()
                map = map.adding(key: "altText", value: altTextValue)
            }
            if let value = blurhash {
                let blurhashValue = try value.toCBORValue()
                map = map.adding(key: "blurhash", value: blurhashValue)
            }
            let originTransitionIdValue = try originTransitionId.toCBORValue()
            map = map.adding(key: "originTransitionId", value: originTransitionIdValue)
            let originalMetadataVersionValue = try originalMetadataVersion.toCBORValue()
            map = map.adding(key: "originalMetadataVersion", value: originalMetadataVersionValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case blobId
            case purpose
            case algorithm
            case key
            case nonce
            case ciphertextSha256
            case ciphertextSize
            case plaintextSize
            case mimeType
            case width
            case height
            case altText
            case blurhash
            case originTransitionId
            case originalMetadataVersion
        }
    }

    public struct MetadataAvatarBlobAad: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#metadataAvatarBlobAad"
        public let `protocol`: String
        public let conversationId: Bytes
        public let originTransitionId: Bytes
        public let originalMetadataVersion: Int
        public let blobId: Bytes
        public let purpose: String
        public let mediaType: String
        public let plaintextSize: Int

        public init(
            protocol: String, conversationId: Bytes, originTransitionId: Bytes, originalMetadataVersion: Int, blobId: Bytes, purpose: String, mediaType: String, plaintextSize: Int
        ) {
            self.protocol = `protocol`
            self.conversationId = conversationId
            self.originTransitionId = originTransitionId
            self.originalMetadataVersion = originalMetadataVersion
            self.blobId = blobId
            self.purpose = purpose
            self.mediaType = mediaType
            self.plaintextSize = plaintextSize
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                self.protocol = try container.decode(String.self, forKey: .protocol)
            } catch {
                LogManager.logError("Decoding error for required property 'protocol': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(Bytes.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                originTransitionId = try container.decode(Bytes.self, forKey: .originTransitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'originTransitionId': \(error)")
                throw error
            }
            do {
                originalMetadataVersion = try container.decode(Int.self, forKey: .originalMetadataVersion)
            } catch {
                LogManager.logError("Decoding error for required property 'originalMetadataVersion': \(error)")
                throw error
            }
            do {
                blobId = try container.decode(Bytes.self, forKey: .blobId)
            } catch {
                LogManager.logError("Decoding error for required property 'blobId': \(error)")
                throw error
            }
            do {
                purpose = try container.decode(String.self, forKey: .purpose)
            } catch {
                LogManager.logError("Decoding error for required property 'purpose': \(error)")
                throw error
            }
            do {
                mediaType = try container.decode(String.self, forKey: .mediaType)
            } catch {
                LogManager.logError("Decoding error for required property 'mediaType': \(error)")
                throw error
            }
            do {
                plaintextSize = try container.decode(Int.self, forKey: .plaintextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'plaintextSize': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(`protocol`, forKey: .protocol)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(originTransitionId, forKey: .originTransitionId)
            try container.encode(originalMetadataVersion, forKey: .originalMetadataVersion)
            try container.encode(blobId, forKey: .blobId)
            try container.encode(purpose, forKey: .purpose)
            try container.encode(mediaType, forKey: .mediaType)
            try container.encode(plaintextSize, forKey: .plaintextSize)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(`protocol`)
            hasher.combine(conversationId)
            hasher.combine(originTransitionId)
            hasher.combine(originalMetadataVersion)
            hasher.combine(blobId)
            hasher.combine(purpose)
            hasher.combine(mediaType)
            hasher.combine(plaintextSize)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if `protocol` != other.protocol {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if originTransitionId != other.originTransitionId {
                return false
            }
            if originalMetadataVersion != other.originalMetadataVersion {
                return false
            }
            if blobId != other.blobId {
                return false
            }
            if purpose != other.purpose {
                return false
            }
            if mediaType != other.mediaType {
                return false
            }
            if plaintextSize != other.plaintextSize {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let protocolValue = try `protocol`.toCBORValue()
            map = map.adding(key: "protocol", value: protocolValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let originTransitionIdValue = try originTransitionId.toCBORValue()
            map = map.adding(key: "originTransitionId", value: originTransitionIdValue)
            let originalMetadataVersionValue = try originalMetadataVersion.toCBORValue()
            map = map.adding(key: "originalMetadataVersion", value: originalMetadataVersionValue)
            let blobIdValue = try blobId.toCBORValue()
            map = map.adding(key: "blobId", value: blobIdValue)
            let purposeValue = try purpose.toCBORValue()
            map = map.adding(key: "purpose", value: purposeValue)
            let mediaTypeValue = try mediaType.toCBORValue()
            map = map.adding(key: "mediaType", value: mediaTypeValue)
            let plaintextSizeValue = try plaintextSize.toCBORValue()
            map = map.adding(key: "plaintextSize", value: plaintextSizeValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case `protocol`
            case conversationId
            case originTransitionId
            case originalMetadataVersion
            case blobId
            case purpose
            case mediaType
            case plaintextSize
        }
    }

    public struct MetadataExporterContext: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#metadataExporterContext"
        public let `protocol`: String
        public let version: Int
        public let conversationId: Bytes
        public let generation: Int
        public let epoch: Int
        public let groupContextHash: Bytes
        public let metadataVersion: Int

        public init(
            protocol: String, version: Int, conversationId: Bytes, generation: Int, epoch: Int, groupContextHash: Bytes, metadataVersion: Int
        ) {
            self.protocol = `protocol`
            self.version = version
            self.conversationId = conversationId
            self.generation = generation
            self.epoch = epoch
            self.groupContextHash = groupContextHash
            self.metadataVersion = metadataVersion
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                self.protocol = try container.decode(String.self, forKey: .protocol)
            } catch {
                LogManager.logError("Decoding error for required property 'protocol': \(error)")
                throw error
            }
            do {
                version = try container.decode(Int.self, forKey: .version)
            } catch {
                LogManager.logError("Decoding error for required property 'version': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(Bytes.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                generation = try container.decode(Int.self, forKey: .generation)
            } catch {
                LogManager.logError("Decoding error for required property 'generation': \(error)")
                throw error
            }
            do {
                epoch = try container.decode(Int.self, forKey: .epoch)
            } catch {
                LogManager.logError("Decoding error for required property 'epoch': \(error)")
                throw error
            }
            do {
                groupContextHash = try container.decode(Bytes.self, forKey: .groupContextHash)
            } catch {
                LogManager.logError("Decoding error for required property 'groupContextHash': \(error)")
                throw error
            }
            do {
                metadataVersion = try container.decode(Int.self, forKey: .metadataVersion)
            } catch {
                LogManager.logError("Decoding error for required property 'metadataVersion': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(`protocol`, forKey: .protocol)
            try container.encode(version, forKey: .version)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(generation, forKey: .generation)
            try container.encode(epoch, forKey: .epoch)
            try container.encode(groupContextHash, forKey: .groupContextHash)
            try container.encode(metadataVersion, forKey: .metadataVersion)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(`protocol`)
            hasher.combine(version)
            hasher.combine(conversationId)
            hasher.combine(generation)
            hasher.combine(epoch)
            hasher.combine(groupContextHash)
            hasher.combine(metadataVersion)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if `protocol` != other.protocol {
                return false
            }
            if version != other.version {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if generation != other.generation {
                return false
            }
            if epoch != other.epoch {
                return false
            }
            if groupContextHash != other.groupContextHash {
                return false
            }
            if metadataVersion != other.metadataVersion {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let protocolValue = try `protocol`.toCBORValue()
            map = map.adding(key: "protocol", value: protocolValue)
            let versionValue = try version.toCBORValue()
            map = map.adding(key: "version", value: versionValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let generationValue = try generation.toCBORValue()
            map = map.adding(key: "generation", value: generationValue)
            let epochValue = try epoch.toCBORValue()
            map = map.adding(key: "epoch", value: epochValue)
            let groupContextHashValue = try groupContextHash.toCBORValue()
            map = map.adding(key: "groupContextHash", value: groupContextHashValue)
            let metadataVersionValue = try metadataVersion.toCBORValue()
            map = map.adding(key: "metadataVersion", value: metadataVersionValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case `protocol`
            case version
            case conversationId
            case generation
            case epoch
            case groupContextHash
            case metadataVersion
        }
    }

    public struct MetadataAadAvatarBinding: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#metadataAadAvatarBinding"
        public let blobId: Bytes
        public let ciphertextSha256: Bytes
        public let ciphertextSize: Int
        public let purpose: String

        public init(
            blobId: Bytes, ciphertextSha256: Bytes, ciphertextSize: Int, purpose: String
        ) {
            self.blobId = blobId
            self.ciphertextSha256 = ciphertextSha256
            self.ciphertextSize = ciphertextSize
            self.purpose = purpose
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                blobId = try container.decode(Bytes.self, forKey: .blobId)
            } catch {
                LogManager.logError("Decoding error for required property 'blobId': \(error)")
                throw error
            }
            do {
                ciphertextSha256 = try container.decode(Bytes.self, forKey: .ciphertextSha256)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSha256': \(error)")
                throw error
            }
            do {
                ciphertextSize = try container.decode(Int.self, forKey: .ciphertextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSize': \(error)")
                throw error
            }
            do {
                purpose = try container.decode(String.self, forKey: .purpose)
            } catch {
                LogManager.logError("Decoding error for required property 'purpose': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(blobId, forKey: .blobId)
            try container.encode(ciphertextSha256, forKey: .ciphertextSha256)
            try container.encode(ciphertextSize, forKey: .ciphertextSize)
            try container.encode(purpose, forKey: .purpose)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(blobId)
            hasher.combine(ciphertextSha256)
            hasher.combine(ciphertextSize)
            hasher.combine(purpose)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if blobId != other.blobId {
                return false
            }
            if ciphertextSha256 != other.ciphertextSha256 {
                return false
            }
            if ciphertextSize != other.ciphertextSize {
                return false
            }
            if purpose != other.purpose {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let blobIdValue = try blobId.toCBORValue()
            map = map.adding(key: "blobId", value: blobIdValue)
            let ciphertextSha256Value = try ciphertextSha256.toCBORValue()
            map = map.adding(key: "ciphertextSha256", value: ciphertextSha256Value)
            let ciphertextSizeValue = try ciphertextSize.toCBORValue()
            map = map.adding(key: "ciphertextSize", value: ciphertextSizeValue)
            let purposeValue = try purpose.toCBORValue()
            map = map.adding(key: "purpose", value: purposeValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case blobId
            case ciphertextSha256
            case ciphertextSize
            case purpose
        }
    }

    public struct MetadataAad: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#metadataAad"
        public let `protocol`: String
        public let version: Int
        public let coordinate: MetadataCryptoContext
        public let metadataVersion: Int
        public let originTransitionId: Bytes
        public let ciphertextSize: Int
        public let avatarBinding: MetadataAadAvatarBinding?

        public init(
            protocol: String, version: Int, coordinate: MetadataCryptoContext, metadataVersion: Int, originTransitionId: Bytes, ciphertextSize: Int, avatarBinding: MetadataAadAvatarBinding?
        ) {
            self.protocol = `protocol`
            self.version = version
            self.coordinate = coordinate
            self.metadataVersion = metadataVersion
            self.originTransitionId = originTransitionId
            self.ciphertextSize = ciphertextSize
            self.avatarBinding = avatarBinding
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                self.protocol = try container.decode(String.self, forKey: .protocol)
            } catch {
                LogManager.logError("Decoding error for required property 'protocol': \(error)")
                throw error
            }
            do {
                version = try container.decode(Int.self, forKey: .version)
            } catch {
                LogManager.logError("Decoding error for required property 'version': \(error)")
                throw error
            }
            do {
                coordinate = try container.decode(MetadataCryptoContext.self, forKey: .coordinate)
            } catch {
                LogManager.logError("Decoding error for required property 'coordinate': \(error)")
                throw error
            }
            do {
                metadataVersion = try container.decode(Int.self, forKey: .metadataVersion)
            } catch {
                LogManager.logError("Decoding error for required property 'metadataVersion': \(error)")
                throw error
            }
            do {
                originTransitionId = try container.decode(Bytes.self, forKey: .originTransitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'originTransitionId': \(error)")
                throw error
            }
            do {
                ciphertextSize = try container.decode(Int.self, forKey: .ciphertextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSize': \(error)")
                throw error
            }
            do {
                avatarBinding = try container.decodeIfPresent(MetadataAadAvatarBinding.self, forKey: .avatarBinding)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'avatarBinding' — degrading to nil: \(error)")
                avatarBinding = nil
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(`protocol`, forKey: .protocol)
            try container.encode(version, forKey: .version)
            try container.encode(coordinate, forKey: .coordinate)
            try container.encode(metadataVersion, forKey: .metadataVersion)
            try container.encode(originTransitionId, forKey: .originTransitionId)
            try container.encode(ciphertextSize, forKey: .ciphertextSize)
            try container.encodeIfPresent(avatarBinding, forKey: .avatarBinding)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(`protocol`)
            hasher.combine(version)
            hasher.combine(coordinate)
            hasher.combine(metadataVersion)
            hasher.combine(originTransitionId)
            hasher.combine(ciphertextSize)
            if let value = avatarBinding {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if `protocol` != other.protocol {
                return false
            }
            if version != other.version {
                return false
            }
            if coordinate != other.coordinate {
                return false
            }
            if metadataVersion != other.metadataVersion {
                return false
            }
            if originTransitionId != other.originTransitionId {
                return false
            }
            if ciphertextSize != other.ciphertextSize {
                return false
            }
            if avatarBinding != other.avatarBinding {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let protocolValue = try `protocol`.toCBORValue()
            map = map.adding(key: "protocol", value: protocolValue)
            let versionValue = try version.toCBORValue()
            map = map.adding(key: "version", value: versionValue)
            let coordinateValue = try coordinate.toCBORValue()
            map = map.adding(key: "coordinate", value: coordinateValue)
            let metadataVersionValue = try metadataVersion.toCBORValue()
            map = map.adding(key: "metadataVersion", value: metadataVersionValue)
            let originTransitionIdValue = try originTransitionId.toCBORValue()
            map = map.adding(key: "originTransitionId", value: originTransitionIdValue)
            let ciphertextSizeValue = try ciphertextSize.toCBORValue()
            map = map.adding(key: "ciphertextSize", value: ciphertextSizeValue)
            if let value = avatarBinding {
                let avatarBindingValue = try value.toCBORValue()
                map = map.adding(key: "avatarBinding", value: avatarBindingValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case `protocol`
            case version
            case coordinate
            case metadataVersion
            case originTransitionId
            case ciphertextSize
            case avatarBinding
        }
    }

    public struct MetadataAuthorProof: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#metadataAuthorProof"
        public let authorDid: BareDid
        public let authorDeviceId: DeviceId
        public let authorKeyId: KeyId
        public let signaturePublicKey: Bytes
        public let authGenerationAtOrigin: Int
        public let originTransitionId: OperationId
        public let originSeq: Int
        public let roleAtOrigin: String
        public let deviceStatusAtOrigin: String

        public init(
            authorDid: BareDid, authorDeviceId: DeviceId, authorKeyId: KeyId, signaturePublicKey: Bytes, authGenerationAtOrigin: Int, originTransitionId: OperationId, originSeq: Int, roleAtOrigin: String, deviceStatusAtOrigin: String
        ) {
            self.authorDid = authorDid
            self.authorDeviceId = authorDeviceId
            self.authorKeyId = authorKeyId
            self.signaturePublicKey = signaturePublicKey
            self.authGenerationAtOrigin = authGenerationAtOrigin
            self.originTransitionId = originTransitionId
            self.originSeq = originSeq
            self.roleAtOrigin = roleAtOrigin
            self.deviceStatusAtOrigin = deviceStatusAtOrigin
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                authorDid = try container.decode(BareDid.self, forKey: .authorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'authorDid': \(error)")
                throw error
            }
            do {
                authorDeviceId = try container.decode(DeviceId.self, forKey: .authorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'authorDeviceId': \(error)")
                throw error
            }
            do {
                authorKeyId = try container.decode(KeyId.self, forKey: .authorKeyId)
            } catch {
                LogManager.logError("Decoding error for required property 'authorKeyId': \(error)")
                throw error
            }
            do {
                signaturePublicKey = try container.decode(Bytes.self, forKey: .signaturePublicKey)
            } catch {
                LogManager.logError("Decoding error for required property 'signaturePublicKey': \(error)")
                throw error
            }
            do {
                authGenerationAtOrigin = try container.decode(Int.self, forKey: .authGenerationAtOrigin)
            } catch {
                LogManager.logError("Decoding error for required property 'authGenerationAtOrigin': \(error)")
                throw error
            }
            do {
                originTransitionId = try container.decode(OperationId.self, forKey: .originTransitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'originTransitionId': \(error)")
                throw error
            }
            do {
                originSeq = try container.decode(Int.self, forKey: .originSeq)
            } catch {
                LogManager.logError("Decoding error for required property 'originSeq': \(error)")
                throw error
            }
            do {
                roleAtOrigin = try container.decode(String.self, forKey: .roleAtOrigin)
            } catch {
                LogManager.logError("Decoding error for required property 'roleAtOrigin': \(error)")
                throw error
            }
            do {
                deviceStatusAtOrigin = try container.decode(String.self, forKey: .deviceStatusAtOrigin)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceStatusAtOrigin': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(authorDid, forKey: .authorDid)
            try container.encode(authorDeviceId, forKey: .authorDeviceId)
            try container.encode(authorKeyId, forKey: .authorKeyId)
            try container.encode(signaturePublicKey, forKey: .signaturePublicKey)
            try container.encode(authGenerationAtOrigin, forKey: .authGenerationAtOrigin)
            try container.encode(originTransitionId, forKey: .originTransitionId)
            try container.encode(originSeq, forKey: .originSeq)
            try container.encode(roleAtOrigin, forKey: .roleAtOrigin)
            try container.encode(deviceStatusAtOrigin, forKey: .deviceStatusAtOrigin)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(authorDid)
            hasher.combine(authorDeviceId)
            hasher.combine(authorKeyId)
            hasher.combine(signaturePublicKey)
            hasher.combine(authGenerationAtOrigin)
            hasher.combine(originTransitionId)
            hasher.combine(originSeq)
            hasher.combine(roleAtOrigin)
            hasher.combine(deviceStatusAtOrigin)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if authorDid != other.authorDid {
                return false
            }
            if authorDeviceId != other.authorDeviceId {
                return false
            }
            if authorKeyId != other.authorKeyId {
                return false
            }
            if signaturePublicKey != other.signaturePublicKey {
                return false
            }
            if authGenerationAtOrigin != other.authGenerationAtOrigin {
                return false
            }
            if originTransitionId != other.originTransitionId {
                return false
            }
            if originSeq != other.originSeq {
                return false
            }
            if roleAtOrigin != other.roleAtOrigin {
                return false
            }
            if deviceStatusAtOrigin != other.deviceStatusAtOrigin {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let authorDidValue = try authorDid.toCBORValue()
            map = map.adding(key: "authorDid", value: authorDidValue)
            let authorDeviceIdValue = try authorDeviceId.toCBORValue()
            map = map.adding(key: "authorDeviceId", value: authorDeviceIdValue)
            let authorKeyIdValue = try authorKeyId.toCBORValue()
            map = map.adding(key: "authorKeyId", value: authorKeyIdValue)
            let signaturePublicKeyValue = try signaturePublicKey.toCBORValue()
            map = map.adding(key: "signaturePublicKey", value: signaturePublicKeyValue)
            let authGenerationAtOriginValue = try authGenerationAtOrigin.toCBORValue()
            map = map.adding(key: "authGenerationAtOrigin", value: authGenerationAtOriginValue)
            let originTransitionIdValue = try originTransitionId.toCBORValue()
            map = map.adding(key: "originTransitionId", value: originTransitionIdValue)
            let originSeqValue = try originSeq.toCBORValue()
            map = map.adding(key: "originSeq", value: originSeqValue)
            let roleAtOriginValue = try roleAtOrigin.toCBORValue()
            map = map.adding(key: "roleAtOrigin", value: roleAtOriginValue)
            let deviceStatusAtOriginValue = try deviceStatusAtOrigin.toCBORValue()
            map = map.adding(key: "deviceStatusAtOrigin", value: deviceStatusAtOriginValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case authorDid
            case authorDeviceId
            case authorKeyId
            case signaturePublicKey
            case authGenerationAtOrigin
            case originTransitionId
            case originSeq
            case roleAtOrigin
            case deviceStatusAtOrigin
        }
    }

    public struct MetadataContentProjection: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#metadataContentProjection"
        public let `protocol`: String
        public let version: Int
        public let originTransitionId: Bytes
        public let metadataVersion: Int
        public let authorDid: BareDid
        public let authorDeviceId: Bytes
        public let authorKeyId: KeyId
        public let title: String
        public let description: String?
        public let avatar: MetadataAvatarEmbed?

        public init(
            protocol: String, version: Int, originTransitionId: Bytes, metadataVersion: Int, authorDid: BareDid, authorDeviceId: Bytes, authorKeyId: KeyId, title: String, description: String?, avatar: MetadataAvatarEmbed?
        ) {
            self.protocol = `protocol`
            self.version = version
            self.originTransitionId = originTransitionId
            self.metadataVersion = metadataVersion
            self.authorDid = authorDid
            self.authorDeviceId = authorDeviceId
            self.authorKeyId = authorKeyId
            self.title = title
            self.description = description
            self.avatar = avatar
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                self.protocol = try container.decode(String.self, forKey: .protocol)
            } catch {
                LogManager.logError("Decoding error for required property 'protocol': \(error)")
                throw error
            }
            do {
                version = try container.decode(Int.self, forKey: .version)
            } catch {
                LogManager.logError("Decoding error for required property 'version': \(error)")
                throw error
            }
            do {
                originTransitionId = try container.decode(Bytes.self, forKey: .originTransitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'originTransitionId': \(error)")
                throw error
            }
            do {
                metadataVersion = try container.decode(Int.self, forKey: .metadataVersion)
            } catch {
                LogManager.logError("Decoding error for required property 'metadataVersion': \(error)")
                throw error
            }
            do {
                authorDid = try container.decode(BareDid.self, forKey: .authorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'authorDid': \(error)")
                throw error
            }
            do {
                authorDeviceId = try container.decode(Bytes.self, forKey: .authorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'authorDeviceId': \(error)")
                throw error
            }
            do {
                authorKeyId = try container.decode(KeyId.self, forKey: .authorKeyId)
            } catch {
                LogManager.logError("Decoding error for required property 'authorKeyId': \(error)")
                throw error
            }
            do {
                title = try container.decode(String.self, forKey: .title)
            } catch {
                LogManager.logError("Decoding error for required property 'title': \(error)")
                throw error
            }
            do {
                description = try container.decodeIfPresent(String.self, forKey: .description)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'description' — degrading to nil: \(error)")
                description = nil
            }
            do {
                avatar = try container.decodeIfPresent(MetadataAvatarEmbed.self, forKey: .avatar)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'avatar' — degrading to nil: \(error)")
                avatar = nil
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(`protocol`, forKey: .protocol)
            try container.encode(version, forKey: .version)
            try container.encode(originTransitionId, forKey: .originTransitionId)
            try container.encode(metadataVersion, forKey: .metadataVersion)
            try container.encode(authorDid, forKey: .authorDid)
            try container.encode(authorDeviceId, forKey: .authorDeviceId)
            try container.encode(authorKeyId, forKey: .authorKeyId)
            try container.encode(title, forKey: .title)
            try container.encodeIfPresent(description, forKey: .description)
            try container.encodeIfPresent(avatar, forKey: .avatar)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(`protocol`)
            hasher.combine(version)
            hasher.combine(originTransitionId)
            hasher.combine(metadataVersion)
            hasher.combine(authorDid)
            hasher.combine(authorDeviceId)
            hasher.combine(authorKeyId)
            hasher.combine(title)
            if let value = description {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
            if let value = avatar {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if `protocol` != other.protocol {
                return false
            }
            if version != other.version {
                return false
            }
            if originTransitionId != other.originTransitionId {
                return false
            }
            if metadataVersion != other.metadataVersion {
                return false
            }
            if authorDid != other.authorDid {
                return false
            }
            if authorDeviceId != other.authorDeviceId {
                return false
            }
            if authorKeyId != other.authorKeyId {
                return false
            }
            if title != other.title {
                return false
            }
            if description != other.description {
                return false
            }
            if avatar != other.avatar {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let protocolValue = try `protocol`.toCBORValue()
            map = map.adding(key: "protocol", value: protocolValue)
            let versionValue = try version.toCBORValue()
            map = map.adding(key: "version", value: versionValue)
            let originTransitionIdValue = try originTransitionId.toCBORValue()
            map = map.adding(key: "originTransitionId", value: originTransitionIdValue)
            let metadataVersionValue = try metadataVersion.toCBORValue()
            map = map.adding(key: "metadataVersion", value: metadataVersionValue)
            let authorDidValue = try authorDid.toCBORValue()
            map = map.adding(key: "authorDid", value: authorDidValue)
            let authorDeviceIdValue = try authorDeviceId.toCBORValue()
            map = map.adding(key: "authorDeviceId", value: authorDeviceIdValue)
            let authorKeyIdValue = try authorKeyId.toCBORValue()
            map = map.adding(key: "authorKeyId", value: authorKeyIdValue)
            let titleValue = try title.toCBORValue()
            map = map.adding(key: "title", value: titleValue)
            if let value = description {
                let descriptionValue = try value.toCBORValue()
                map = map.adding(key: "description", value: descriptionValue)
            }
            if let value = avatar {
                let avatarValue = try value.toCBORValue()
                map = map.adding(key: "avatar", value: avatarValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case `protocol`
            case version
            case originTransitionId
            case metadataVersion
            case authorDid
            case authorDeviceId
            case authorKeyId
            case title
            case description
            case avatar
        }
    }

    public struct MetadataPlaintext: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#metadataPlaintext"
        public let content: MetadataContentProjection
        public let contentSignature: Bytes

        public init(
            content: MetadataContentProjection, contentSignature: Bytes
        ) {
            self.content = content
            self.contentSignature = contentSignature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                content = try container.decode(MetadataContentProjection.self, forKey: .content)
            } catch {
                LogManager.logError("Decoding error for required property 'content': \(error)")
                throw error
            }
            do {
                contentSignature = try container.decode(Bytes.self, forKey: .contentSignature)
            } catch {
                LogManager.logError("Decoding error for required property 'contentSignature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(content, forKey: .content)
            try container.encode(contentSignature, forKey: .contentSignature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(content)
            hasher.combine(contentSignature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if content != other.content {
                return false
            }
            if contentSignature != other.contentSignature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let contentValue = try content.toCBORValue()
            map = map.adding(key: "content", value: contentValue)
            let contentSignatureValue = try contentSignature.toCBORValue()
            map = map.adding(key: "contentSignature", value: contentSignatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case content
            case contentSignature
        }
    }

    public struct MetadataSnapshot: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#metadataSnapshot"
        public let coordinate: MetadataCryptoContext
        public let originTransitionId: OperationId
        public let metadataVersion: Int
        public let nonce: Bytes
        public let ciphertext: Bytes
        public let ciphertextSha256: Bytes
        public let ciphertextSize: Int
        public let avatarBinding: MetadataAvatarBinding?
        public let authorProof: MetadataAuthorProof

        public init(
            coordinate: MetadataCryptoContext, originTransitionId: OperationId, metadataVersion: Int, nonce: Bytes, ciphertext: Bytes, ciphertextSha256: Bytes, ciphertextSize: Int, avatarBinding: MetadataAvatarBinding?, authorProof: MetadataAuthorProof
        ) {
            self.coordinate = coordinate
            self.originTransitionId = originTransitionId
            self.metadataVersion = metadataVersion
            self.nonce = nonce
            self.ciphertext = ciphertext
            self.ciphertextSha256 = ciphertextSha256
            self.ciphertextSize = ciphertextSize
            self.avatarBinding = avatarBinding
            self.authorProof = authorProof
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                coordinate = try container.decode(MetadataCryptoContext.self, forKey: .coordinate)
            } catch {
                LogManager.logError("Decoding error for required property 'coordinate': \(error)")
                throw error
            }
            do {
                originTransitionId = try container.decode(OperationId.self, forKey: .originTransitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'originTransitionId': \(error)")
                throw error
            }
            do {
                metadataVersion = try container.decode(Int.self, forKey: .metadataVersion)
            } catch {
                LogManager.logError("Decoding error for required property 'metadataVersion': \(error)")
                throw error
            }
            do {
                nonce = try container.decode(Bytes.self, forKey: .nonce)
            } catch {
                LogManager.logError("Decoding error for required property 'nonce': \(error)")
                throw error
            }
            do {
                ciphertext = try container.decode(Bytes.self, forKey: .ciphertext)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertext': \(error)")
                throw error
            }
            do {
                ciphertextSha256 = try container.decode(Bytes.self, forKey: .ciphertextSha256)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSha256': \(error)")
                throw error
            }
            do {
                ciphertextSize = try container.decode(Int.self, forKey: .ciphertextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSize': \(error)")
                throw error
            }
            do {
                avatarBinding = try container.decodeIfPresent(MetadataAvatarBinding.self, forKey: .avatarBinding)
            } catch {
                // Forward compatibility: a malformed or unknown-shaped optional field
                // must not fail the whole response.
                LogManager.logWarning("Decoding error for optional property 'avatarBinding' — degrading to nil: \(error)")
                avatarBinding = nil
            }
            do {
                authorProof = try container.decode(MetadataAuthorProof.self, forKey: .authorProof)
            } catch {
                LogManager.logError("Decoding error for required property 'authorProof': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(coordinate, forKey: .coordinate)
            try container.encode(originTransitionId, forKey: .originTransitionId)
            try container.encode(metadataVersion, forKey: .metadataVersion)
            try container.encode(nonce, forKey: .nonce)
            try container.encode(ciphertext, forKey: .ciphertext)
            try container.encode(ciphertextSha256, forKey: .ciphertextSha256)
            try container.encode(ciphertextSize, forKey: .ciphertextSize)
            try container.encodeIfPresent(avatarBinding, forKey: .avatarBinding)
            try container.encode(authorProof, forKey: .authorProof)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(coordinate)
            hasher.combine(originTransitionId)
            hasher.combine(metadataVersion)
            hasher.combine(nonce)
            hasher.combine(ciphertext)
            hasher.combine(ciphertextSha256)
            hasher.combine(ciphertextSize)
            if let value = avatarBinding {
                hasher.combine(value)
            } else {
                hasher.combine(nil as Int?)
            }
            hasher.combine(authorProof)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if coordinate != other.coordinate {
                return false
            }
            if originTransitionId != other.originTransitionId {
                return false
            }
            if metadataVersion != other.metadataVersion {
                return false
            }
            if nonce != other.nonce {
                return false
            }
            if ciphertext != other.ciphertext {
                return false
            }
            if ciphertextSha256 != other.ciphertextSha256 {
                return false
            }
            if ciphertextSize != other.ciphertextSize {
                return false
            }
            if avatarBinding != other.avatarBinding {
                return false
            }
            if authorProof != other.authorProof {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let coordinateValue = try coordinate.toCBORValue()
            map = map.adding(key: "coordinate", value: coordinateValue)
            let originTransitionIdValue = try originTransitionId.toCBORValue()
            map = map.adding(key: "originTransitionId", value: originTransitionIdValue)
            let metadataVersionValue = try metadataVersion.toCBORValue()
            map = map.adding(key: "metadataVersion", value: metadataVersionValue)
            let nonceValue = try nonce.toCBORValue()
            map = map.adding(key: "nonce", value: nonceValue)
            let ciphertextValue = try ciphertext.toCBORValue()
            map = map.adding(key: "ciphertext", value: ciphertextValue)
            let ciphertextSha256Value = try ciphertextSha256.toCBORValue()
            map = map.adding(key: "ciphertextSha256", value: ciphertextSha256Value)
            let ciphertextSizeValue = try ciphertextSize.toCBORValue()
            map = map.adding(key: "ciphertextSize", value: ciphertextSizeValue)
            if let value = avatarBinding {
                let avatarBindingValue = try value.toCBORValue()
                map = map.adding(key: "avatarBinding", value: avatarBindingValue)
            }
            let authorProofValue = try authorProof.toCBORValue()
            map = map.adding(key: "authorProof", value: authorProofValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case coordinate
            case originTransitionId
            case metadataVersion
            case nonce
            case ciphertext
            case ciphertextSha256
            case ciphertextSize
            case avatarBinding
            case authorProof
        }
    }

    public struct MetadataTransitionBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#metadataTransitionBody"
        public let signatureDomain: String
        public let transitionId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let next: ConversationCoordinates
        public let metadataSnapshot: MetadataSnapshot
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, transitionId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, next: ConversationCoordinates, metadataSnapshot: MetadataSnapshot, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.transitionId = transitionId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.next = next
            self.metadataSnapshot = metadataSnapshot
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                transitionId = try container.decode(OperationId.self, forKey: .transitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                next = try container.decode(ConversationCoordinates.self, forKey: .next)
            } catch {
                LogManager.logError("Decoding error for required property 'next': \(error)")
                throw error
            }
            do {
                metadataSnapshot = try container.decode(MetadataSnapshot.self, forKey: .metadataSnapshot)
            } catch {
                LogManager.logError("Decoding error for required property 'metadataSnapshot': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(transitionId, forKey: .transitionId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(next, forKey: .next)
            try container.encode(metadataSnapshot, forKey: .metadataSnapshot)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(transitionId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(next)
            hasher.combine(metadataSnapshot)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if transitionId != other.transitionId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if next != other.next {
                return false
            }
            if metadataSnapshot != other.metadataSnapshot {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let transitionIdValue = try transitionId.toCBORValue()
            map = map.adding(key: "transitionId", value: transitionIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let nextValue = try next.toCBORValue()
            map = map.adding(key: "next", value: nextValue)
            let metadataSnapshotValue = try metadataSnapshot.toCBORValue()
            map = map.adding(key: "metadataSnapshot", value: metadataSnapshotValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case transitionId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case next
            case metadataSnapshot
            case idempotencyKey
            case signedAt
        }
    }

    public struct TypingBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#typingBody"
        public let signatureDomain: String
        public let typingId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let coordinates: ConversationCoordinates
        public let isTyping: Bool
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, typingId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, coordinates: ConversationCoordinates, isTyping: Bool, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.typingId = typingId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.coordinates = coordinates
            self.isTyping = isTyping
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                typingId = try container.decode(OperationId.self, forKey: .typingId)
            } catch {
                LogManager.logError("Decoding error for required property 'typingId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                coordinates = try container.decode(ConversationCoordinates.self, forKey: .coordinates)
            } catch {
                LogManager.logError("Decoding error for required property 'coordinates': \(error)")
                throw error
            }
            do {
                isTyping = try container.decode(Bool.self, forKey: .isTyping)
            } catch {
                LogManager.logError("Decoding error for required property 'isTyping': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(typingId, forKey: .typingId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(coordinates, forKey: .coordinates)
            try container.encode(isTyping, forKey: .isTyping)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(typingId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(coordinates)
            hasher.combine(isTyping)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if typingId != other.typingId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if coordinates != other.coordinates {
                return false
            }
            if isTyping != other.isTyping {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let typingIdValue = try typingId.toCBORValue()
            map = map.adding(key: "typingId", value: typingIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let coordinatesValue = try coordinates.toCBORValue()
            map = map.adding(key: "coordinates", value: coordinatesValue)
            let isTypingValue = try isTyping.toCBORValue()
            map = map.adding(key: "isTyping", value: isTypingValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case typingId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case coordinates
            case isTyping
            case signedAt
        }
    }

    public struct TypingEvent: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#typingEvent"
        public let typingId: OperationId
        public let conversationId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let isTyping: Bool
        public let expiresAt: CanonicalDatetime

        public init(
            typingId: OperationId, conversationId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, isTyping: Bool, expiresAt: CanonicalDatetime
        ) {
            self.typingId = typingId
            self.conversationId = conversationId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.isTyping = isTyping
            self.expiresAt = expiresAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                typingId = try container.decode(OperationId.self, forKey: .typingId)
            } catch {
                LogManager.logError("Decoding error for required property 'typingId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                isTyping = try container.decode(Bool.self, forKey: .isTyping)
            } catch {
                LogManager.logError("Decoding error for required property 'isTyping': \(error)")
                throw error
            }
            do {
                expiresAt = try container.decode(CanonicalDatetime.self, forKey: .expiresAt)
            } catch {
                LogManager.logError("Decoding error for required property 'expiresAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(typingId, forKey: .typingId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(isTyping, forKey: .isTyping)
            try container.encode(expiresAt, forKey: .expiresAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(typingId)
            hasher.combine(conversationId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(isTyping)
            hasher.combine(expiresAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if typingId != other.typingId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if isTyping != other.isTyping {
                return false
            }
            if expiresAt != other.expiresAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let typingIdValue = try typingId.toCBORValue()
            map = map.adding(key: "typingId", value: typingIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let isTypingValue = try isTyping.toCBORValue()
            map = map.adding(key: "isTyping", value: isTypingValue)
            let expiresAtValue = try expiresAt.toCBORValue()
            map = map.adding(key: "expiresAt", value: expiresAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case typingId
            case conversationId
            case actorDid
            case actorDeviceId
            case isTyping
            case expiresAt
        }
    }

    public struct BlobUploadPreparationBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#blobUploadPreparationBody"
        public let signatureDomain: String
        public let blobId: OperationId
        public let conversationId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let ciphertextSha256: Bytes
        public let ciphertextSize: Int
        public let mediaType: String
        public let plaintextSize: Int
        public let purpose: DefsBlobPurpose
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, blobId: OperationId, conversationId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, ciphertextSha256: Bytes, ciphertextSize: Int, mediaType: String, plaintextSize: Int, purpose: DefsBlobPurpose, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.blobId = blobId
            self.conversationId = conversationId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.ciphertextSha256 = ciphertextSha256
            self.ciphertextSize = ciphertextSize
            self.mediaType = mediaType
            self.plaintextSize = plaintextSize
            self.purpose = purpose
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                blobId = try container.decode(OperationId.self, forKey: .blobId)
            } catch {
                LogManager.logError("Decoding error for required property 'blobId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                ciphertextSha256 = try container.decode(Bytes.self, forKey: .ciphertextSha256)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSha256': \(error)")
                throw error
            }
            do {
                ciphertextSize = try container.decode(Int.self, forKey: .ciphertextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'ciphertextSize': \(error)")
                throw error
            }
            do {
                mediaType = try container.decode(String.self, forKey: .mediaType)
            } catch {
                LogManager.logError("Decoding error for required property 'mediaType': \(error)")
                throw error
            }
            do {
                plaintextSize = try container.decode(Int.self, forKey: .plaintextSize)
            } catch {
                LogManager.logError("Decoding error for required property 'plaintextSize': \(error)")
                throw error
            }
            do {
                purpose = try container.decode(DefsBlobPurpose.self, forKey: .purpose)
            } catch {
                LogManager.logError("Decoding error for required property 'purpose': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(blobId, forKey: .blobId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(ciphertextSha256, forKey: .ciphertextSha256)
            try container.encode(ciphertextSize, forKey: .ciphertextSize)
            try container.encode(mediaType, forKey: .mediaType)
            try container.encode(plaintextSize, forKey: .plaintextSize)
            try container.encode(purpose, forKey: .purpose)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(blobId)
            hasher.combine(conversationId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(ciphertextSha256)
            hasher.combine(ciphertextSize)
            hasher.combine(mediaType)
            hasher.combine(plaintextSize)
            hasher.combine(purpose)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if blobId != other.blobId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if ciphertextSha256 != other.ciphertextSha256 {
                return false
            }
            if ciphertextSize != other.ciphertextSize {
                return false
            }
            if mediaType != other.mediaType {
                return false
            }
            if plaintextSize != other.plaintextSize {
                return false
            }
            if purpose != other.purpose {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let blobIdValue = try blobId.toCBORValue()
            map = map.adding(key: "blobId", value: blobIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let ciphertextSha256Value = try ciphertextSha256.toCBORValue()
            map = map.adding(key: "ciphertextSha256", value: ciphertextSha256Value)
            let ciphertextSizeValue = try ciphertextSize.toCBORValue()
            map = map.adding(key: "ciphertextSize", value: ciphertextSizeValue)
            let mediaTypeValue = try mediaType.toCBORValue()
            map = map.adding(key: "mediaType", value: mediaTypeValue)
            let plaintextSizeValue = try plaintextSize.toCBORValue()
            map = map.adding(key: "plaintextSize", value: plaintextSizeValue)
            let purposeValue = try purpose.toCBORValue()
            map = map.adding(key: "purpose", value: purposeValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case blobId
            case conversationId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case ciphertextSha256
            case ciphertextSize
            case mediaType
            case plaintextSize
            case purpose
            case idempotencyKey
            case signedAt
        }
    }

    public struct BlobDeletionBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#blobDeletionBody"
        public let signatureDomain: String
        public let blobId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, blobId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.blobId = blobId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                blobId = try container.decode(OperationId.self, forKey: .blobId)
            } catch {
                LogManager.logError("Decoding error for required property 'blobId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(blobId, forKey: .blobId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(blobId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if blobId != other.blobId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let blobIdValue = try blobId.toCBORValue()
            map = map.adding(key: "blobId", value: blobIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case blobId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case idempotencyKey
            case signedAt
        }
    }

    public struct ResetRequestBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#resetRequestBody"
        public let signatureDomain: String
        public let resetRequestId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let reason: DefsResetReason
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, resetRequestId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, reason: DefsResetReason, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.resetRequestId = resetRequestId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.reason = reason
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                resetRequestId = try container.decode(OperationId.self, forKey: .resetRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'resetRequestId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                reason = try container.decode(DefsResetReason.self, forKey: .reason)
            } catch {
                LogManager.logError("Decoding error for required property 'reason': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(resetRequestId, forKey: .resetRequestId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(reason, forKey: .reason)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(resetRequestId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(reason)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if resetRequestId != other.resetRequestId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if reason != other.reason {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let resetRequestIdValue = try resetRequestId.toCBORValue()
            map = map.adding(key: "resetRequestId", value: resetRequestIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let reasonValue = try reason.toCBORValue()
            map = map.adding(key: "reason", value: reasonValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case resetRequestId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case reason
            case idempotencyKey
            case signedAt
        }
    }

    public struct ResetActivationBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#resetActivationBody"
        public let signatureDomain: String
        public let resetRequestId: OperationId
        public let transitionId: OperationId
        public let conversationKind: DefsConversationKind
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let retired: ConversationCoordinates
        public let successor: ConversationCoordinates
        public let manifest: ResetActivationManifest
        public let genesisGroupInfo: GroupInfoArtifact
        public let metadataSnapshot: MetadataSnapshot
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, resetRequestId: OperationId, transitionId: OperationId, conversationKind: DefsConversationKind, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, retired: ConversationCoordinates, successor: ConversationCoordinates, manifest: ResetActivationManifest, genesisGroupInfo: GroupInfoArtifact, metadataSnapshot: MetadataSnapshot, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.resetRequestId = resetRequestId
            self.transitionId = transitionId
            self.conversationKind = conversationKind
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.retired = retired
            self.successor = successor
            self.manifest = manifest
            self.genesisGroupInfo = genesisGroupInfo
            self.metadataSnapshot = metadataSnapshot
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                resetRequestId = try container.decode(OperationId.self, forKey: .resetRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'resetRequestId': \(error)")
                throw error
            }
            do {
                transitionId = try container.decode(OperationId.self, forKey: .transitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionId': \(error)")
                throw error
            }
            do {
                conversationKind = try container.decode(DefsConversationKind.self, forKey: .conversationKind)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationKind': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                retired = try container.decode(ConversationCoordinates.self, forKey: .retired)
            } catch {
                LogManager.logError("Decoding error for required property 'retired': \(error)")
                throw error
            }
            do {
                successor = try container.decode(ConversationCoordinates.self, forKey: .successor)
            } catch {
                LogManager.logError("Decoding error for required property 'successor': \(error)")
                throw error
            }
            do {
                manifest = try container.decode(ResetActivationManifest.self, forKey: .manifest)
            } catch {
                LogManager.logError("Decoding error for required property 'manifest': \(error)")
                throw error
            }
            do {
                genesisGroupInfo = try container.decode(GroupInfoArtifact.self, forKey: .genesisGroupInfo)
            } catch {
                LogManager.logError("Decoding error for required property 'genesisGroupInfo': \(error)")
                throw error
            }
            do {
                metadataSnapshot = try container.decode(MetadataSnapshot.self, forKey: .metadataSnapshot)
            } catch {
                LogManager.logError("Decoding error for required property 'metadataSnapshot': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(resetRequestId, forKey: .resetRequestId)
            try container.encode(transitionId, forKey: .transitionId)
            try container.encode(conversationKind, forKey: .conversationKind)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(retired, forKey: .retired)
            try container.encode(successor, forKey: .successor)
            try container.encode(manifest, forKey: .manifest)
            try container.encode(genesisGroupInfo, forKey: .genesisGroupInfo)
            try container.encode(metadataSnapshot, forKey: .metadataSnapshot)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(resetRequestId)
            hasher.combine(transitionId)
            hasher.combine(conversationKind)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(retired)
            hasher.combine(successor)
            hasher.combine(manifest)
            hasher.combine(genesisGroupInfo)
            hasher.combine(metadataSnapshot)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if resetRequestId != other.resetRequestId {
                return false
            }
            if transitionId != other.transitionId {
                return false
            }
            if conversationKind != other.conversationKind {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if retired != other.retired {
                return false
            }
            if successor != other.successor {
                return false
            }
            if manifest != other.manifest {
                return false
            }
            if genesisGroupInfo != other.genesisGroupInfo {
                return false
            }
            if metadataSnapshot != other.metadataSnapshot {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let resetRequestIdValue = try resetRequestId.toCBORValue()
            map = map.adding(key: "resetRequestId", value: resetRequestIdValue)
            let transitionIdValue = try transitionId.toCBORValue()
            map = map.adding(key: "transitionId", value: transitionIdValue)
            let conversationKindValue = try conversationKind.toCBORValue()
            map = map.adding(key: "conversationKind", value: conversationKindValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let retiredValue = try retired.toCBORValue()
            map = map.adding(key: "retired", value: retiredValue)
            let successorValue = try successor.toCBORValue()
            map = map.adding(key: "successor", value: successorValue)
            let manifestValue = try manifest.toCBORValue()
            map = map.adding(key: "manifest", value: manifestValue)
            let genesisGroupInfoValue = try genesisGroupInfo.toCBORValue()
            map = map.adding(key: "genesisGroupInfo", value: genesisGroupInfoValue)
            let metadataSnapshotValue = try metadataSnapshot.toCBORValue()
            map = map.adding(key: "metadataSnapshot", value: metadataSnapshotValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case resetRequestId
            case transitionId
            case conversationKind
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case retired
            case successor
            case manifest
            case genesisGroupInfo
            case metadataSnapshot
            case idempotencyKey
            case signedAt
        }
    }

    public struct ResetRequestView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#resetRequestView"
        public let resetRequestId: OperationId
        public let conversationId: OperationId
        public let requesterDid: BareDid
        public let requesterDeviceId: DeviceId
        public let prior: ConversationCoordinates
        public let reason: DefsResetReason
        public let status: ResetRequestViewStatus
        public let requestedAt: CanonicalDatetime
        public let expiresAt: CanonicalDatetime

        public init(
            resetRequestId: OperationId, conversationId: OperationId, requesterDid: BareDid, requesterDeviceId: DeviceId, prior: ConversationCoordinates, reason: DefsResetReason, status: ResetRequestViewStatus, requestedAt: CanonicalDatetime, expiresAt: CanonicalDatetime
        ) {
            self.resetRequestId = resetRequestId
            self.conversationId = conversationId
            self.requesterDid = requesterDid
            self.requesterDeviceId = requesterDeviceId
            self.prior = prior
            self.reason = reason
            self.status = status
            self.requestedAt = requestedAt
            self.expiresAt = expiresAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                resetRequestId = try container.decode(OperationId.self, forKey: .resetRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'resetRequestId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                requesterDid = try container.decode(BareDid.self, forKey: .requesterDid)
            } catch {
                LogManager.logError("Decoding error for required property 'requesterDid': \(error)")
                throw error
            }
            do {
                requesterDeviceId = try container.decode(DeviceId.self, forKey: .requesterDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'requesterDeviceId': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                reason = try container.decode(DefsResetReason.self, forKey: .reason)
            } catch {
                LogManager.logError("Decoding error for required property 'reason': \(error)")
                throw error
            }
            do {
                status = try container.decode(ResetRequestViewStatus.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
            do {
                requestedAt = try container.decode(CanonicalDatetime.self, forKey: .requestedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'requestedAt': \(error)")
                throw error
            }
            do {
                expiresAt = try container.decode(CanonicalDatetime.self, forKey: .expiresAt)
            } catch {
                LogManager.logError("Decoding error for required property 'expiresAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(resetRequestId, forKey: .resetRequestId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(requesterDid, forKey: .requesterDid)
            try container.encode(requesterDeviceId, forKey: .requesterDeviceId)
            try container.encode(prior, forKey: .prior)
            try container.encode(reason, forKey: .reason)
            try container.encode(status, forKey: .status)
            try container.encode(requestedAt, forKey: .requestedAt)
            try container.encode(expiresAt, forKey: .expiresAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(resetRequestId)
            hasher.combine(conversationId)
            hasher.combine(requesterDid)
            hasher.combine(requesterDeviceId)
            hasher.combine(prior)
            hasher.combine(reason)
            hasher.combine(status)
            hasher.combine(requestedAt)
            hasher.combine(expiresAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if resetRequestId != other.resetRequestId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if requesterDid != other.requesterDid {
                return false
            }
            if requesterDeviceId != other.requesterDeviceId {
                return false
            }
            if prior != other.prior {
                return false
            }
            if reason != other.reason {
                return false
            }
            if status != other.status {
                return false
            }
            if requestedAt != other.requestedAt {
                return false
            }
            if expiresAt != other.expiresAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let resetRequestIdValue = try resetRequestId.toCBORValue()
            map = map.adding(key: "resetRequestId", value: resetRequestIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let requesterDidValue = try requesterDid.toCBORValue()
            map = map.adding(key: "requesterDid", value: requesterDidValue)
            let requesterDeviceIdValue = try requesterDeviceId.toCBORValue()
            map = map.adding(key: "requesterDeviceId", value: requesterDeviceIdValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let reasonValue = try reason.toCBORValue()
            map = map.adding(key: "reason", value: reasonValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            let requestedAtValue = try requestedAt.toCBORValue()
            map = map.adding(key: "requestedAt", value: requestedAtValue)
            let expiresAtValue = try expiresAt.toCBORValue()
            map = map.adding(key: "expiresAt", value: expiresAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case resetRequestId
            case conversationId
            case requesterDid
            case requesterDeviceId
            case prior
            case reason
            case status
            case requestedAt
            case expiresAt
        }
    }

    public struct LeafRecoveryRequestBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leafRecoveryRequestBody"
        public let signatureDomain: String
        public let recoveryRequestId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let recoveryKind: DefsLeafRecoveryKind
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, recoveryRequestId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, recoveryKind: DefsLeafRecoveryKind, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.recoveryRequestId = recoveryRequestId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.recoveryKind = recoveryKind
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                recoveryRequestId = try container.decode(OperationId.self, forKey: .recoveryRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryRequestId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                recoveryKind = try container.decode(DefsLeafRecoveryKind.self, forKey: .recoveryKind)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryKind': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(recoveryRequestId, forKey: .recoveryRequestId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(recoveryKind, forKey: .recoveryKind)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(recoveryRequestId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(recoveryKind)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if recoveryRequestId != other.recoveryRequestId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if recoveryKind != other.recoveryKind {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let recoveryRequestIdValue = try recoveryRequestId.toCBORValue()
            map = map.adding(key: "recoveryRequestId", value: recoveryRequestIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let recoveryKindValue = try recoveryKind.toCBORValue()
            map = map.adding(key: "recoveryKind", value: recoveryKindValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case recoveryRequestId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case recoveryKind
            case idempotencyKey
            case signedAt
        }
    }

    public struct LeafRecoveryCancellationBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leafRecoveryCancellationBody"
        public let signatureDomain: String
        public let recoveryRequestId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, recoveryRequestId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.recoveryRequestId = recoveryRequestId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                recoveryRequestId = try container.decode(OperationId.self, forKey: .recoveryRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryRequestId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(recoveryRequestId, forKey: .recoveryRequestId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(recoveryRequestId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if recoveryRequestId != other.recoveryRequestId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let recoveryRequestIdValue = try recoveryRequestId.toCBORValue()
            map = map.adding(key: "recoveryRequestId", value: recoveryRequestIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case recoveryRequestId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case idempotencyKey
            case signedAt
        }
    }

    public struct LeafRecoveryFulfillmentBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leafRecoveryFulfillmentBody"
        public let signatureDomain: String
        public let recoveryRequestId: OperationId
        public let transitionId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let next: ConversationCoordinates
        public let aad: CommitAad
        public let manifest: TransitionManifest
        public let commit: PublicCommit
        public let metadataSnapshot: MetadataSnapshot
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, recoveryRequestId: OperationId, transitionId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, next: ConversationCoordinates, aad: CommitAad, manifest: TransitionManifest, commit: PublicCommit, metadataSnapshot: MetadataSnapshot, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.recoveryRequestId = recoveryRequestId
            self.transitionId = transitionId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.next = next
            self.aad = aad
            self.manifest = manifest
            self.commit = commit
            self.metadataSnapshot = metadataSnapshot
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                recoveryRequestId = try container.decode(OperationId.self, forKey: .recoveryRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryRequestId': \(error)")
                throw error
            }
            do {
                transitionId = try container.decode(OperationId.self, forKey: .transitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                next = try container.decode(ConversationCoordinates.self, forKey: .next)
            } catch {
                LogManager.logError("Decoding error for required property 'next': \(error)")
                throw error
            }
            do {
                aad = try container.decode(CommitAad.self, forKey: .aad)
            } catch {
                LogManager.logError("Decoding error for required property 'aad': \(error)")
                throw error
            }
            do {
                manifest = try container.decode(TransitionManifest.self, forKey: .manifest)
            } catch {
                LogManager.logError("Decoding error for required property 'manifest': \(error)")
                throw error
            }
            do {
                commit = try container.decode(PublicCommit.self, forKey: .commit)
            } catch {
                LogManager.logError("Decoding error for required property 'commit': \(error)")
                throw error
            }
            do {
                metadataSnapshot = try container.decode(MetadataSnapshot.self, forKey: .metadataSnapshot)
            } catch {
                LogManager.logError("Decoding error for required property 'metadataSnapshot': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(recoveryRequestId, forKey: .recoveryRequestId)
            try container.encode(transitionId, forKey: .transitionId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(next, forKey: .next)
            try container.encode(aad, forKey: .aad)
            try container.encode(manifest, forKey: .manifest)
            try container.encode(commit, forKey: .commit)
            try container.encode(metadataSnapshot, forKey: .metadataSnapshot)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(recoveryRequestId)
            hasher.combine(transitionId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(next)
            hasher.combine(aad)
            hasher.combine(manifest)
            hasher.combine(commit)
            hasher.combine(metadataSnapshot)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if recoveryRequestId != other.recoveryRequestId {
                return false
            }
            if transitionId != other.transitionId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if next != other.next {
                return false
            }
            if aad != other.aad {
                return false
            }
            if manifest != other.manifest {
                return false
            }
            if commit != other.commit {
                return false
            }
            if metadataSnapshot != other.metadataSnapshot {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let recoveryRequestIdValue = try recoveryRequestId.toCBORValue()
            map = map.adding(key: "recoveryRequestId", value: recoveryRequestIdValue)
            let transitionIdValue = try transitionId.toCBORValue()
            map = map.adding(key: "transitionId", value: transitionIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let nextValue = try next.toCBORValue()
            map = map.adding(key: "next", value: nextValue)
            let aadValue = try aad.toCBORValue()
            map = map.adding(key: "aad", value: aadValue)
            let manifestValue = try manifest.toCBORValue()
            map = map.adding(key: "manifest", value: manifestValue)
            let commitValue = try commit.toCBORValue()
            map = map.adding(key: "commit", value: commitValue)
            let metadataSnapshotValue = try metadataSnapshot.toCBORValue()
            map = map.adding(key: "metadataSnapshot", value: metadataSnapshotValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case recoveryRequestId
            case transitionId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case next
            case aad
            case manifest
            case commit
            case metadataSnapshot
            case idempotencyKey
            case signedAt
        }
    }

    public struct LeafRecoveryView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leafRecoveryView"
        public let recoveryRequestId: OperationId
        public let conversationId: OperationId
        public let requesterDid: BareDid
        public let requesterDeviceId: DeviceId
        public let recoveryKind: DefsLeafRecoveryKind
        public let boundCoordinate: ConversationCoordinates
        public let reservation: LeafRecoveryReservation
        public let status: DefsLeafRecoveryStatus
        public let requestedAt: CanonicalDatetime
        public let expiresAt: CanonicalDatetime

        public init(
            recoveryRequestId: OperationId, conversationId: OperationId, requesterDid: BareDid, requesterDeviceId: DeviceId, recoveryKind: DefsLeafRecoveryKind, boundCoordinate: ConversationCoordinates, reservation: LeafRecoveryReservation, status: DefsLeafRecoveryStatus, requestedAt: CanonicalDatetime, expiresAt: CanonicalDatetime
        ) {
            self.recoveryRequestId = recoveryRequestId
            self.conversationId = conversationId
            self.requesterDid = requesterDid
            self.requesterDeviceId = requesterDeviceId
            self.recoveryKind = recoveryKind
            self.boundCoordinate = boundCoordinate
            self.reservation = reservation
            self.status = status
            self.requestedAt = requestedAt
            self.expiresAt = expiresAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                recoveryRequestId = try container.decode(OperationId.self, forKey: .recoveryRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryRequestId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                requesterDid = try container.decode(BareDid.self, forKey: .requesterDid)
            } catch {
                LogManager.logError("Decoding error for required property 'requesterDid': \(error)")
                throw error
            }
            do {
                requesterDeviceId = try container.decode(DeviceId.self, forKey: .requesterDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'requesterDeviceId': \(error)")
                throw error
            }
            do {
                recoveryKind = try container.decode(DefsLeafRecoveryKind.self, forKey: .recoveryKind)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryKind': \(error)")
                throw error
            }
            do {
                boundCoordinate = try container.decode(ConversationCoordinates.self, forKey: .boundCoordinate)
            } catch {
                LogManager.logError("Decoding error for required property 'boundCoordinate': \(error)")
                throw error
            }
            do {
                reservation = try container.decode(LeafRecoveryReservation.self, forKey: .reservation)
            } catch {
                LogManager.logError("Decoding error for required property 'reservation': \(error)")
                throw error
            }
            do {
                status = try container.decode(DefsLeafRecoveryStatus.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
            do {
                requestedAt = try container.decode(CanonicalDatetime.self, forKey: .requestedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'requestedAt': \(error)")
                throw error
            }
            do {
                expiresAt = try container.decode(CanonicalDatetime.self, forKey: .expiresAt)
            } catch {
                LogManager.logError("Decoding error for required property 'expiresAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(recoveryRequestId, forKey: .recoveryRequestId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(requesterDid, forKey: .requesterDid)
            try container.encode(requesterDeviceId, forKey: .requesterDeviceId)
            try container.encode(recoveryKind, forKey: .recoveryKind)
            try container.encode(boundCoordinate, forKey: .boundCoordinate)
            try container.encode(reservation, forKey: .reservation)
            try container.encode(status, forKey: .status)
            try container.encode(requestedAt, forKey: .requestedAt)
            try container.encode(expiresAt, forKey: .expiresAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(recoveryRequestId)
            hasher.combine(conversationId)
            hasher.combine(requesterDid)
            hasher.combine(requesterDeviceId)
            hasher.combine(recoveryKind)
            hasher.combine(boundCoordinate)
            hasher.combine(reservation)
            hasher.combine(status)
            hasher.combine(requestedAt)
            hasher.combine(expiresAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if recoveryRequestId != other.recoveryRequestId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if requesterDid != other.requesterDid {
                return false
            }
            if requesterDeviceId != other.requesterDeviceId {
                return false
            }
            if recoveryKind != other.recoveryKind {
                return false
            }
            if boundCoordinate != other.boundCoordinate {
                return false
            }
            if reservation != other.reservation {
                return false
            }
            if status != other.status {
                return false
            }
            if requestedAt != other.requestedAt {
                return false
            }
            if expiresAt != other.expiresAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let recoveryRequestIdValue = try recoveryRequestId.toCBORValue()
            map = map.adding(key: "recoveryRequestId", value: recoveryRequestIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let requesterDidValue = try requesterDid.toCBORValue()
            map = map.adding(key: "requesterDid", value: requesterDidValue)
            let requesterDeviceIdValue = try requesterDeviceId.toCBORValue()
            map = map.adding(key: "requesterDeviceId", value: requesterDeviceIdValue)
            let recoveryKindValue = try recoveryKind.toCBORValue()
            map = map.adding(key: "recoveryKind", value: recoveryKindValue)
            let boundCoordinateValue = try boundCoordinate.toCBORValue()
            map = map.adding(key: "boundCoordinate", value: boundCoordinateValue)
            let reservationValue = try reservation.toCBORValue()
            map = map.adding(key: "reservation", value: reservationValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            let requestedAtValue = try requestedAt.toCBORValue()
            map = map.adding(key: "requestedAt", value: requestedAtValue)
            let expiresAtValue = try expiresAt.toCBORValue()
            map = map.adding(key: "expiresAt", value: expiresAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case recoveryRequestId
            case conversationId
            case requesterDid
            case requesterDeviceId
            case recoveryKind
            case boundCoordinate
            case reservation
            case status
            case requestedAt
            case expiresAt
        }
    }

    public struct RecoveryWorkPendingView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#recoveryWorkPendingView"
        public let recoveryWorkId: OperationId
        public let conversationId: OperationId
        public let recipientDid: BareDid
        public let recipientDeviceId: DeviceId
        public let sourceKind: DefsRecoveryWorkSourceKind
        public let sourceId: OperationId
        public let sourceCoordinate: ConversationCoordinates
        public let status: String
        public let createdAt: CanonicalDatetime

        public init(
            recoveryWorkId: OperationId, conversationId: OperationId, recipientDid: BareDid, recipientDeviceId: DeviceId, sourceKind: DefsRecoveryWorkSourceKind, sourceId: OperationId, sourceCoordinate: ConversationCoordinates, status: String, createdAt: CanonicalDatetime
        ) {
            self.recoveryWorkId = recoveryWorkId
            self.conversationId = conversationId
            self.recipientDid = recipientDid
            self.recipientDeviceId = recipientDeviceId
            self.sourceKind = sourceKind
            self.sourceId = sourceId
            self.sourceCoordinate = sourceCoordinate
            self.status = status
            self.createdAt = createdAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                recoveryWorkId = try container.decode(OperationId.self, forKey: .recoveryWorkId)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryWorkId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                recipientDid = try container.decode(BareDid.self, forKey: .recipientDid)
            } catch {
                LogManager.logError("Decoding error for required property 'recipientDid': \(error)")
                throw error
            }
            do {
                recipientDeviceId = try container.decode(DeviceId.self, forKey: .recipientDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'recipientDeviceId': \(error)")
                throw error
            }
            do {
                sourceKind = try container.decode(DefsRecoveryWorkSourceKind.self, forKey: .sourceKind)
            } catch {
                LogManager.logError("Decoding error for required property 'sourceKind': \(error)")
                throw error
            }
            do {
                sourceId = try container.decode(OperationId.self, forKey: .sourceId)
            } catch {
                LogManager.logError("Decoding error for required property 'sourceId': \(error)")
                throw error
            }
            do {
                sourceCoordinate = try container.decode(ConversationCoordinates.self, forKey: .sourceCoordinate)
            } catch {
                LogManager.logError("Decoding error for required property 'sourceCoordinate': \(error)")
                throw error
            }
            do {
                status = try container.decode(String.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
            do {
                createdAt = try container.decode(CanonicalDatetime.self, forKey: .createdAt)
            } catch {
                LogManager.logError("Decoding error for required property 'createdAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(recoveryWorkId, forKey: .recoveryWorkId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(recipientDid, forKey: .recipientDid)
            try container.encode(recipientDeviceId, forKey: .recipientDeviceId)
            try container.encode(sourceKind, forKey: .sourceKind)
            try container.encode(sourceId, forKey: .sourceId)
            try container.encode(sourceCoordinate, forKey: .sourceCoordinate)
            try container.encode(status, forKey: .status)
            try container.encode(createdAt, forKey: .createdAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(recoveryWorkId)
            hasher.combine(conversationId)
            hasher.combine(recipientDid)
            hasher.combine(recipientDeviceId)
            hasher.combine(sourceKind)
            hasher.combine(sourceId)
            hasher.combine(sourceCoordinate)
            hasher.combine(status)
            hasher.combine(createdAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if recoveryWorkId != other.recoveryWorkId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if recipientDid != other.recipientDid {
                return false
            }
            if recipientDeviceId != other.recipientDeviceId {
                return false
            }
            if sourceKind != other.sourceKind {
                return false
            }
            if sourceId != other.sourceId {
                return false
            }
            if sourceCoordinate != other.sourceCoordinate {
                return false
            }
            if status != other.status {
                return false
            }
            if createdAt != other.createdAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let recoveryWorkIdValue = try recoveryWorkId.toCBORValue()
            map = map.adding(key: "recoveryWorkId", value: recoveryWorkIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let recipientDidValue = try recipientDid.toCBORValue()
            map = map.adding(key: "recipientDid", value: recipientDidValue)
            let recipientDeviceIdValue = try recipientDeviceId.toCBORValue()
            map = map.adding(key: "recipientDeviceId", value: recipientDeviceIdValue)
            let sourceKindValue = try sourceKind.toCBORValue()
            map = map.adding(key: "sourceKind", value: sourceKindValue)
            let sourceIdValue = try sourceId.toCBORValue()
            map = map.adding(key: "sourceId", value: sourceIdValue)
            let sourceCoordinateValue = try sourceCoordinate.toCBORValue()
            map = map.adding(key: "sourceCoordinate", value: sourceCoordinateValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            let createdAtValue = try createdAt.toCBORValue()
            map = map.adding(key: "createdAt", value: createdAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case recoveryWorkId
            case conversationId
            case recipientDid
            case recipientDeviceId
            case sourceKind
            case sourceId
            case sourceCoordinate
            case status
            case createdAt
        }
    }

    public struct RecoveryWorkCompletedByTransitionView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#recoveryWorkCompletedByTransitionView"
        public let recoveryWorkId: OperationId
        public let conversationId: OperationId
        public let recipientDid: BareDid
        public let recipientDeviceId: DeviceId
        public let sourceKind: DefsRecoveryWorkSourceKind
        public let sourceId: OperationId
        public let sourceCoordinate: ConversationCoordinates
        public let status: String
        public let createdAt: CanonicalDatetime
        public let terminalTransitionId: OperationId
        public let terminalAt: CanonicalDatetime

        public init(
            recoveryWorkId: OperationId, conversationId: OperationId, recipientDid: BareDid, recipientDeviceId: DeviceId, sourceKind: DefsRecoveryWorkSourceKind, sourceId: OperationId, sourceCoordinate: ConversationCoordinates, status: String, createdAt: CanonicalDatetime, terminalTransitionId: OperationId, terminalAt: CanonicalDatetime
        ) {
            self.recoveryWorkId = recoveryWorkId
            self.conversationId = conversationId
            self.recipientDid = recipientDid
            self.recipientDeviceId = recipientDeviceId
            self.sourceKind = sourceKind
            self.sourceId = sourceId
            self.sourceCoordinate = sourceCoordinate
            self.status = status
            self.createdAt = createdAt
            self.terminalTransitionId = terminalTransitionId
            self.terminalAt = terminalAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                recoveryWorkId = try container.decode(OperationId.self, forKey: .recoveryWorkId)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryWorkId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                recipientDid = try container.decode(BareDid.self, forKey: .recipientDid)
            } catch {
                LogManager.logError("Decoding error for required property 'recipientDid': \(error)")
                throw error
            }
            do {
                recipientDeviceId = try container.decode(DeviceId.self, forKey: .recipientDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'recipientDeviceId': \(error)")
                throw error
            }
            do {
                sourceKind = try container.decode(DefsRecoveryWorkSourceKind.self, forKey: .sourceKind)
            } catch {
                LogManager.logError("Decoding error for required property 'sourceKind': \(error)")
                throw error
            }
            do {
                sourceId = try container.decode(OperationId.self, forKey: .sourceId)
            } catch {
                LogManager.logError("Decoding error for required property 'sourceId': \(error)")
                throw error
            }
            do {
                sourceCoordinate = try container.decode(ConversationCoordinates.self, forKey: .sourceCoordinate)
            } catch {
                LogManager.logError("Decoding error for required property 'sourceCoordinate': \(error)")
                throw error
            }
            do {
                status = try container.decode(String.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
            do {
                createdAt = try container.decode(CanonicalDatetime.self, forKey: .createdAt)
            } catch {
                LogManager.logError("Decoding error for required property 'createdAt': \(error)")
                throw error
            }
            do {
                terminalTransitionId = try container.decode(OperationId.self, forKey: .terminalTransitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'terminalTransitionId': \(error)")
                throw error
            }
            do {
                terminalAt = try container.decode(CanonicalDatetime.self, forKey: .terminalAt)
            } catch {
                LogManager.logError("Decoding error for required property 'terminalAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(recoveryWorkId, forKey: .recoveryWorkId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(recipientDid, forKey: .recipientDid)
            try container.encode(recipientDeviceId, forKey: .recipientDeviceId)
            try container.encode(sourceKind, forKey: .sourceKind)
            try container.encode(sourceId, forKey: .sourceId)
            try container.encode(sourceCoordinate, forKey: .sourceCoordinate)
            try container.encode(status, forKey: .status)
            try container.encode(createdAt, forKey: .createdAt)
            try container.encode(terminalTransitionId, forKey: .terminalTransitionId)
            try container.encode(terminalAt, forKey: .terminalAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(recoveryWorkId)
            hasher.combine(conversationId)
            hasher.combine(recipientDid)
            hasher.combine(recipientDeviceId)
            hasher.combine(sourceKind)
            hasher.combine(sourceId)
            hasher.combine(sourceCoordinate)
            hasher.combine(status)
            hasher.combine(createdAt)
            hasher.combine(terminalTransitionId)
            hasher.combine(terminalAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if recoveryWorkId != other.recoveryWorkId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if recipientDid != other.recipientDid {
                return false
            }
            if recipientDeviceId != other.recipientDeviceId {
                return false
            }
            if sourceKind != other.sourceKind {
                return false
            }
            if sourceId != other.sourceId {
                return false
            }
            if sourceCoordinate != other.sourceCoordinate {
                return false
            }
            if status != other.status {
                return false
            }
            if createdAt != other.createdAt {
                return false
            }
            if terminalTransitionId != other.terminalTransitionId {
                return false
            }
            if terminalAt != other.terminalAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let recoveryWorkIdValue = try recoveryWorkId.toCBORValue()
            map = map.adding(key: "recoveryWorkId", value: recoveryWorkIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let recipientDidValue = try recipientDid.toCBORValue()
            map = map.adding(key: "recipientDid", value: recipientDidValue)
            let recipientDeviceIdValue = try recipientDeviceId.toCBORValue()
            map = map.adding(key: "recipientDeviceId", value: recipientDeviceIdValue)
            let sourceKindValue = try sourceKind.toCBORValue()
            map = map.adding(key: "sourceKind", value: sourceKindValue)
            let sourceIdValue = try sourceId.toCBORValue()
            map = map.adding(key: "sourceId", value: sourceIdValue)
            let sourceCoordinateValue = try sourceCoordinate.toCBORValue()
            map = map.adding(key: "sourceCoordinate", value: sourceCoordinateValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            let createdAtValue = try createdAt.toCBORValue()
            map = map.adding(key: "createdAt", value: createdAtValue)
            let terminalTransitionIdValue = try terminalTransitionId.toCBORValue()
            map = map.adding(key: "terminalTransitionId", value: terminalTransitionIdValue)
            let terminalAtValue = try terminalAt.toCBORValue()
            map = map.adding(key: "terminalAt", value: terminalAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case recoveryWorkId
            case conversationId
            case recipientDid
            case recipientDeviceId
            case sourceKind
            case sourceId
            case sourceCoordinate
            case status
            case createdAt
            case terminalTransitionId
            case terminalAt
        }
    }

    public struct RecoveryWorkSupersededByTransitionView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#recoveryWorkSupersededByTransitionView"
        public let recoveryWorkId: OperationId
        public let conversationId: OperationId
        public let recipientDid: BareDid
        public let recipientDeviceId: DeviceId
        public let sourceKind: DefsRecoveryWorkSourceKind
        public let sourceId: OperationId
        public let sourceCoordinate: ConversationCoordinates
        public let status: String
        public let createdAt: CanonicalDatetime
        public let terminalTransitionId: OperationId
        public let terminalAt: CanonicalDatetime

        public init(
            recoveryWorkId: OperationId, conversationId: OperationId, recipientDid: BareDid, recipientDeviceId: DeviceId, sourceKind: DefsRecoveryWorkSourceKind, sourceId: OperationId, sourceCoordinate: ConversationCoordinates, status: String, createdAt: CanonicalDatetime, terminalTransitionId: OperationId, terminalAt: CanonicalDatetime
        ) {
            self.recoveryWorkId = recoveryWorkId
            self.conversationId = conversationId
            self.recipientDid = recipientDid
            self.recipientDeviceId = recipientDeviceId
            self.sourceKind = sourceKind
            self.sourceId = sourceId
            self.sourceCoordinate = sourceCoordinate
            self.status = status
            self.createdAt = createdAt
            self.terminalTransitionId = terminalTransitionId
            self.terminalAt = terminalAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                recoveryWorkId = try container.decode(OperationId.self, forKey: .recoveryWorkId)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryWorkId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                recipientDid = try container.decode(BareDid.self, forKey: .recipientDid)
            } catch {
                LogManager.logError("Decoding error for required property 'recipientDid': \(error)")
                throw error
            }
            do {
                recipientDeviceId = try container.decode(DeviceId.self, forKey: .recipientDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'recipientDeviceId': \(error)")
                throw error
            }
            do {
                sourceKind = try container.decode(DefsRecoveryWorkSourceKind.self, forKey: .sourceKind)
            } catch {
                LogManager.logError("Decoding error for required property 'sourceKind': \(error)")
                throw error
            }
            do {
                sourceId = try container.decode(OperationId.self, forKey: .sourceId)
            } catch {
                LogManager.logError("Decoding error for required property 'sourceId': \(error)")
                throw error
            }
            do {
                sourceCoordinate = try container.decode(ConversationCoordinates.self, forKey: .sourceCoordinate)
            } catch {
                LogManager.logError("Decoding error for required property 'sourceCoordinate': \(error)")
                throw error
            }
            do {
                status = try container.decode(String.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
            do {
                createdAt = try container.decode(CanonicalDatetime.self, forKey: .createdAt)
            } catch {
                LogManager.logError("Decoding error for required property 'createdAt': \(error)")
                throw error
            }
            do {
                terminalTransitionId = try container.decode(OperationId.self, forKey: .terminalTransitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'terminalTransitionId': \(error)")
                throw error
            }
            do {
                terminalAt = try container.decode(CanonicalDatetime.self, forKey: .terminalAt)
            } catch {
                LogManager.logError("Decoding error for required property 'terminalAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(recoveryWorkId, forKey: .recoveryWorkId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(recipientDid, forKey: .recipientDid)
            try container.encode(recipientDeviceId, forKey: .recipientDeviceId)
            try container.encode(sourceKind, forKey: .sourceKind)
            try container.encode(sourceId, forKey: .sourceId)
            try container.encode(sourceCoordinate, forKey: .sourceCoordinate)
            try container.encode(status, forKey: .status)
            try container.encode(createdAt, forKey: .createdAt)
            try container.encode(terminalTransitionId, forKey: .terminalTransitionId)
            try container.encode(terminalAt, forKey: .terminalAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(recoveryWorkId)
            hasher.combine(conversationId)
            hasher.combine(recipientDid)
            hasher.combine(recipientDeviceId)
            hasher.combine(sourceKind)
            hasher.combine(sourceId)
            hasher.combine(sourceCoordinate)
            hasher.combine(status)
            hasher.combine(createdAt)
            hasher.combine(terminalTransitionId)
            hasher.combine(terminalAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if recoveryWorkId != other.recoveryWorkId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if recipientDid != other.recipientDid {
                return false
            }
            if recipientDeviceId != other.recipientDeviceId {
                return false
            }
            if sourceKind != other.sourceKind {
                return false
            }
            if sourceId != other.sourceId {
                return false
            }
            if sourceCoordinate != other.sourceCoordinate {
                return false
            }
            if status != other.status {
                return false
            }
            if createdAt != other.createdAt {
                return false
            }
            if terminalTransitionId != other.terminalTransitionId {
                return false
            }
            if terminalAt != other.terminalAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let recoveryWorkIdValue = try recoveryWorkId.toCBORValue()
            map = map.adding(key: "recoveryWorkId", value: recoveryWorkIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let recipientDidValue = try recipientDid.toCBORValue()
            map = map.adding(key: "recipientDid", value: recipientDidValue)
            let recipientDeviceIdValue = try recipientDeviceId.toCBORValue()
            map = map.adding(key: "recipientDeviceId", value: recipientDeviceIdValue)
            let sourceKindValue = try sourceKind.toCBORValue()
            map = map.adding(key: "sourceKind", value: sourceKindValue)
            let sourceIdValue = try sourceId.toCBORValue()
            map = map.adding(key: "sourceId", value: sourceIdValue)
            let sourceCoordinateValue = try sourceCoordinate.toCBORValue()
            map = map.adding(key: "sourceCoordinate", value: sourceCoordinateValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            let createdAtValue = try createdAt.toCBORValue()
            map = map.adding(key: "createdAt", value: createdAtValue)
            let terminalTransitionIdValue = try terminalTransitionId.toCBORValue()
            map = map.adding(key: "terminalTransitionId", value: terminalTransitionIdValue)
            let terminalAtValue = try terminalAt.toCBORValue()
            map = map.adding(key: "terminalAt", value: terminalAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case recoveryWorkId
            case conversationId
            case recipientDid
            case recipientDeviceId
            case sourceKind
            case sourceId
            case sourceCoordinate
            case status
            case createdAt
            case terminalTransitionId
            case terminalAt
        }
    }

    public struct RecoveryWorkSupersededByRevocationView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#recoveryWorkSupersededByRevocationView"
        public let recoveryWorkId: OperationId
        public let conversationId: OperationId
        public let recipientDid: BareDid
        public let recipientDeviceId: DeviceId
        public let sourceKind: DefsRecoveryWorkSourceKind
        public let sourceId: OperationId
        public let sourceCoordinate: ConversationCoordinates
        public let status: String
        public let createdAt: CanonicalDatetime
        public let terminalRevocationId: OperationId
        public let terminalAt: CanonicalDatetime

        public init(
            recoveryWorkId: OperationId, conversationId: OperationId, recipientDid: BareDid, recipientDeviceId: DeviceId, sourceKind: DefsRecoveryWorkSourceKind, sourceId: OperationId, sourceCoordinate: ConversationCoordinates, status: String, createdAt: CanonicalDatetime, terminalRevocationId: OperationId, terminalAt: CanonicalDatetime
        ) {
            self.recoveryWorkId = recoveryWorkId
            self.conversationId = conversationId
            self.recipientDid = recipientDid
            self.recipientDeviceId = recipientDeviceId
            self.sourceKind = sourceKind
            self.sourceId = sourceId
            self.sourceCoordinate = sourceCoordinate
            self.status = status
            self.createdAt = createdAt
            self.terminalRevocationId = terminalRevocationId
            self.terminalAt = terminalAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                recoveryWorkId = try container.decode(OperationId.self, forKey: .recoveryWorkId)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryWorkId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                recipientDid = try container.decode(BareDid.self, forKey: .recipientDid)
            } catch {
                LogManager.logError("Decoding error for required property 'recipientDid': \(error)")
                throw error
            }
            do {
                recipientDeviceId = try container.decode(DeviceId.self, forKey: .recipientDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'recipientDeviceId': \(error)")
                throw error
            }
            do {
                sourceKind = try container.decode(DefsRecoveryWorkSourceKind.self, forKey: .sourceKind)
            } catch {
                LogManager.logError("Decoding error for required property 'sourceKind': \(error)")
                throw error
            }
            do {
                sourceId = try container.decode(OperationId.self, forKey: .sourceId)
            } catch {
                LogManager.logError("Decoding error for required property 'sourceId': \(error)")
                throw error
            }
            do {
                sourceCoordinate = try container.decode(ConversationCoordinates.self, forKey: .sourceCoordinate)
            } catch {
                LogManager.logError("Decoding error for required property 'sourceCoordinate': \(error)")
                throw error
            }
            do {
                status = try container.decode(String.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
            do {
                createdAt = try container.decode(CanonicalDatetime.self, forKey: .createdAt)
            } catch {
                LogManager.logError("Decoding error for required property 'createdAt': \(error)")
                throw error
            }
            do {
                terminalRevocationId = try container.decode(OperationId.self, forKey: .terminalRevocationId)
            } catch {
                LogManager.logError("Decoding error for required property 'terminalRevocationId': \(error)")
                throw error
            }
            do {
                terminalAt = try container.decode(CanonicalDatetime.self, forKey: .terminalAt)
            } catch {
                LogManager.logError("Decoding error for required property 'terminalAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(recoveryWorkId, forKey: .recoveryWorkId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(recipientDid, forKey: .recipientDid)
            try container.encode(recipientDeviceId, forKey: .recipientDeviceId)
            try container.encode(sourceKind, forKey: .sourceKind)
            try container.encode(sourceId, forKey: .sourceId)
            try container.encode(sourceCoordinate, forKey: .sourceCoordinate)
            try container.encode(status, forKey: .status)
            try container.encode(createdAt, forKey: .createdAt)
            try container.encode(terminalRevocationId, forKey: .terminalRevocationId)
            try container.encode(terminalAt, forKey: .terminalAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(recoveryWorkId)
            hasher.combine(conversationId)
            hasher.combine(recipientDid)
            hasher.combine(recipientDeviceId)
            hasher.combine(sourceKind)
            hasher.combine(sourceId)
            hasher.combine(sourceCoordinate)
            hasher.combine(status)
            hasher.combine(createdAt)
            hasher.combine(terminalRevocationId)
            hasher.combine(terminalAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if recoveryWorkId != other.recoveryWorkId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if recipientDid != other.recipientDid {
                return false
            }
            if recipientDeviceId != other.recipientDeviceId {
                return false
            }
            if sourceKind != other.sourceKind {
                return false
            }
            if sourceId != other.sourceId {
                return false
            }
            if sourceCoordinate != other.sourceCoordinate {
                return false
            }
            if status != other.status {
                return false
            }
            if createdAt != other.createdAt {
                return false
            }
            if terminalRevocationId != other.terminalRevocationId {
                return false
            }
            if terminalAt != other.terminalAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let recoveryWorkIdValue = try recoveryWorkId.toCBORValue()
            map = map.adding(key: "recoveryWorkId", value: recoveryWorkIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let recipientDidValue = try recipientDid.toCBORValue()
            map = map.adding(key: "recipientDid", value: recipientDidValue)
            let recipientDeviceIdValue = try recipientDeviceId.toCBORValue()
            map = map.adding(key: "recipientDeviceId", value: recipientDeviceIdValue)
            let sourceKindValue = try sourceKind.toCBORValue()
            map = map.adding(key: "sourceKind", value: sourceKindValue)
            let sourceIdValue = try sourceId.toCBORValue()
            map = map.adding(key: "sourceId", value: sourceIdValue)
            let sourceCoordinateValue = try sourceCoordinate.toCBORValue()
            map = map.adding(key: "sourceCoordinate", value: sourceCoordinateValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            let createdAtValue = try createdAt.toCBORValue()
            map = map.adding(key: "createdAt", value: createdAtValue)
            let terminalRevocationIdValue = try terminalRevocationId.toCBORValue()
            map = map.adding(key: "terminalRevocationId", value: terminalRevocationIdValue)
            let terminalAtValue = try terminalAt.toCBORValue()
            map = map.adding(key: "terminalAt", value: terminalAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case recoveryWorkId
            case conversationId
            case recipientDid
            case recipientDeviceId
            case sourceKind
            case sourceId
            case sourceCoordinate
            case status
            case createdAt
            case terminalRevocationId
            case terminalAt
        }
    }

    public struct ConversationCloseBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#conversationCloseBody"
        public let signatureDomain: String
        public let transitionId: OperationId
        public let conversationKind: DefsConversationKind
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let retired: ConversationCoordinates
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, transitionId: OperationId, conversationKind: DefsConversationKind, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, retired: ConversationCoordinates, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.transitionId = transitionId
            self.conversationKind = conversationKind
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.retired = retired
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                transitionId = try container.decode(OperationId.self, forKey: .transitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionId': \(error)")
                throw error
            }
            do {
                conversationKind = try container.decode(DefsConversationKind.self, forKey: .conversationKind)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationKind': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                retired = try container.decode(ConversationCoordinates.self, forKey: .retired)
            } catch {
                LogManager.logError("Decoding error for required property 'retired': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(transitionId, forKey: .transitionId)
            try container.encode(conversationKind, forKey: .conversationKind)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(retired, forKey: .retired)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(transitionId)
            hasher.combine(conversationKind)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(retired)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if transitionId != other.transitionId {
                return false
            }
            if conversationKind != other.conversationKind {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if retired != other.retired {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let transitionIdValue = try transitionId.toCBORValue()
            map = map.adding(key: "transitionId", value: transitionIdValue)
            let conversationKindValue = try conversationKind.toCBORValue()
            map = map.adding(key: "conversationKind", value: conversationKindValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let retiredValue = try retired.toCBORValue()
            map = map.adding(key: "retired", value: retiredValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case transitionId
            case conversationKind
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case retired
            case idempotencyKey
            case signedAt
        }
    }

    public struct LeaveRequestBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leaveRequestBody"
        public let signatureDomain: String
        public let leaveRequestId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, leaveRequestId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.leaveRequestId = leaveRequestId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                leaveRequestId = try container.decode(OperationId.self, forKey: .leaveRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'leaveRequestId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(leaveRequestId, forKey: .leaveRequestId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(leaveRequestId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if leaveRequestId != other.leaveRequestId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let leaveRequestIdValue = try leaveRequestId.toCBORValue()
            map = map.adding(key: "leaveRequestId", value: leaveRequestIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case leaveRequestId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case idempotencyKey
            case signedAt
        }
    }

    public struct ZeroLeafLeaveBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#zeroLeafLeaveBody"
        public let signatureDomain: String
        public let transitionId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let next: ConversationCoordinates
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, transitionId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, next: ConversationCoordinates, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.transitionId = transitionId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.next = next
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                transitionId = try container.decode(OperationId.self, forKey: .transitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                next = try container.decode(ConversationCoordinates.self, forKey: .next)
            } catch {
                LogManager.logError("Decoding error for required property 'next': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(transitionId, forKey: .transitionId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(next, forKey: .next)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(transitionId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(next)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if transitionId != other.transitionId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if next != other.next {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let transitionIdValue = try transitionId.toCBORValue()
            map = map.adding(key: "transitionId", value: transitionIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let nextValue = try next.toCBORValue()
            map = map.adding(key: "next", value: nextValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case transitionId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case next
            case idempotencyKey
            case signedAt
        }
    }

    public struct LeaveCancellationBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leaveCancellationBody"
        public let signatureDomain: String
        public let conversationId: OperationId
        public let leaveRequestId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, conversationId: OperationId, leaveRequestId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.conversationId = conversationId
            self.leaveRequestId = leaveRequestId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                leaveRequestId = try container.decode(OperationId.self, forKey: .leaveRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'leaveRequestId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(leaveRequestId, forKey: .leaveRequestId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(conversationId)
            hasher.combine(leaveRequestId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if leaveRequestId != other.leaveRequestId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let leaveRequestIdValue = try leaveRequestId.toCBORValue()
            map = map.adding(key: "leaveRequestId", value: leaveRequestIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case conversationId
            case leaveRequestId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case idempotencyKey
            case signedAt
        }
    }

    public struct LeaveCommitFulfillmentBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leaveCommitFulfillmentBody"
        public let signatureDomain: String
        public let leaveRequestId: OperationId
        public let transitionId: OperationId
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let prior: ConversationCoordinates
        public let next: ConversationCoordinates
        public let aad: CommitAad
        public let manifest: TransitionManifest
        public let commit: PublicCommit
        public let metadataSnapshot: MetadataSnapshot
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, leaveRequestId: OperationId, transitionId: OperationId, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, prior: ConversationCoordinates, next: ConversationCoordinates, aad: CommitAad, manifest: TransitionManifest, commit: PublicCommit, metadataSnapshot: MetadataSnapshot, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.leaveRequestId = leaveRequestId
            self.transitionId = transitionId
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.prior = prior
            self.next = next
            self.aad = aad
            self.manifest = manifest
            self.commit = commit
            self.metadataSnapshot = metadataSnapshot
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                leaveRequestId = try container.decode(OperationId.self, forKey: .leaveRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'leaveRequestId': \(error)")
                throw error
            }
            do {
                transitionId = try container.decode(OperationId.self, forKey: .transitionId)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionId': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                next = try container.decode(ConversationCoordinates.self, forKey: .next)
            } catch {
                LogManager.logError("Decoding error for required property 'next': \(error)")
                throw error
            }
            do {
                aad = try container.decode(CommitAad.self, forKey: .aad)
            } catch {
                LogManager.logError("Decoding error for required property 'aad': \(error)")
                throw error
            }
            do {
                manifest = try container.decode(TransitionManifest.self, forKey: .manifest)
            } catch {
                LogManager.logError("Decoding error for required property 'manifest': \(error)")
                throw error
            }
            do {
                commit = try container.decode(PublicCommit.self, forKey: .commit)
            } catch {
                LogManager.logError("Decoding error for required property 'commit': \(error)")
                throw error
            }
            do {
                metadataSnapshot = try container.decode(MetadataSnapshot.self, forKey: .metadataSnapshot)
            } catch {
                LogManager.logError("Decoding error for required property 'metadataSnapshot': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(leaveRequestId, forKey: .leaveRequestId)
            try container.encode(transitionId, forKey: .transitionId)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(prior, forKey: .prior)
            try container.encode(next, forKey: .next)
            try container.encode(aad, forKey: .aad)
            try container.encode(manifest, forKey: .manifest)
            try container.encode(commit, forKey: .commit)
            try container.encode(metadataSnapshot, forKey: .metadataSnapshot)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(leaveRequestId)
            hasher.combine(transitionId)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(prior)
            hasher.combine(next)
            hasher.combine(aad)
            hasher.combine(manifest)
            hasher.combine(commit)
            hasher.combine(metadataSnapshot)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if leaveRequestId != other.leaveRequestId {
                return false
            }
            if transitionId != other.transitionId {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if prior != other.prior {
                return false
            }
            if next != other.next {
                return false
            }
            if aad != other.aad {
                return false
            }
            if manifest != other.manifest {
                return false
            }
            if commit != other.commit {
                return false
            }
            if metadataSnapshot != other.metadataSnapshot {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let leaveRequestIdValue = try leaveRequestId.toCBORValue()
            map = map.adding(key: "leaveRequestId", value: leaveRequestIdValue)
            let transitionIdValue = try transitionId.toCBORValue()
            map = map.adding(key: "transitionId", value: transitionIdValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let nextValue = try next.toCBORValue()
            map = map.adding(key: "next", value: nextValue)
            let aadValue = try aad.toCBORValue()
            map = map.adding(key: "aad", value: aadValue)
            let manifestValue = try manifest.toCBORValue()
            map = map.adding(key: "manifest", value: manifestValue)
            let commitValue = try commit.toCBORValue()
            map = map.adding(key: "commit", value: commitValue)
            let metadataSnapshotValue = try metadataSnapshot.toCBORValue()
            map = map.adding(key: "metadataSnapshot", value: metadataSnapshotValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case leaveRequestId
            case transitionId
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case prior
            case next
            case aad
            case manifest
            case commit
            case metadataSnapshot
            case idempotencyKey
            case signedAt
        }
    }

    public struct LeaveRequestView: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leaveRequestView"
        public let leaveRequestId: OperationId
        public let conversationId: OperationId
        public let requesterDid: BareDid
        public let requesterDeviceId: DeviceId
        public let prior: ConversationCoordinates
        public let status: DefsLeaveRequestStatus
        public let requestedAt: CanonicalDatetime
        public let expiresAt: CanonicalDatetime

        public init(
            leaveRequestId: OperationId, conversationId: OperationId, requesterDid: BareDid, requesterDeviceId: DeviceId, prior: ConversationCoordinates, status: DefsLeaveRequestStatus, requestedAt: CanonicalDatetime, expiresAt: CanonicalDatetime
        ) {
            self.leaveRequestId = leaveRequestId
            self.conversationId = conversationId
            self.requesterDid = requesterDid
            self.requesterDeviceId = requesterDeviceId
            self.prior = prior
            self.status = status
            self.requestedAt = requestedAt
            self.expiresAt = expiresAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                leaveRequestId = try container.decode(OperationId.self, forKey: .leaveRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'leaveRequestId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                requesterDid = try container.decode(BareDid.self, forKey: .requesterDid)
            } catch {
                LogManager.logError("Decoding error for required property 'requesterDid': \(error)")
                throw error
            }
            do {
                requesterDeviceId = try container.decode(DeviceId.self, forKey: .requesterDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'requesterDeviceId': \(error)")
                throw error
            }
            do {
                prior = try container.decode(ConversationCoordinates.self, forKey: .prior)
            } catch {
                LogManager.logError("Decoding error for required property 'prior': \(error)")
                throw error
            }
            do {
                status = try container.decode(DefsLeaveRequestStatus.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
            do {
                requestedAt = try container.decode(CanonicalDatetime.self, forKey: .requestedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'requestedAt': \(error)")
                throw error
            }
            do {
                expiresAt = try container.decode(CanonicalDatetime.self, forKey: .expiresAt)
            } catch {
                LogManager.logError("Decoding error for required property 'expiresAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(leaveRequestId, forKey: .leaveRequestId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(requesterDid, forKey: .requesterDid)
            try container.encode(requesterDeviceId, forKey: .requesterDeviceId)
            try container.encode(prior, forKey: .prior)
            try container.encode(status, forKey: .status)
            try container.encode(requestedAt, forKey: .requestedAt)
            try container.encode(expiresAt, forKey: .expiresAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(leaveRequestId)
            hasher.combine(conversationId)
            hasher.combine(requesterDid)
            hasher.combine(requesterDeviceId)
            hasher.combine(prior)
            hasher.combine(status)
            hasher.combine(requestedAt)
            hasher.combine(expiresAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if leaveRequestId != other.leaveRequestId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if requesterDid != other.requesterDid {
                return false
            }
            if requesterDeviceId != other.requesterDeviceId {
                return false
            }
            if prior != other.prior {
                return false
            }
            if status != other.status {
                return false
            }
            if requestedAt != other.requestedAt {
                return false
            }
            if expiresAt != other.expiresAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let leaveRequestIdValue = try leaveRequestId.toCBORValue()
            map = map.adding(key: "leaveRequestId", value: leaveRequestIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let requesterDidValue = try requesterDid.toCBORValue()
            map = map.adding(key: "requesterDid", value: requesterDidValue)
            let requesterDeviceIdValue = try requesterDeviceId.toCBORValue()
            map = map.adding(key: "requesterDeviceId", value: requesterDeviceIdValue)
            let priorValue = try prior.toCBORValue()
            map = map.adding(key: "prior", value: priorValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            let requestedAtValue = try requestedAt.toCBORValue()
            map = map.adding(key: "requestedAt", value: requestedAtValue)
            let expiresAtValue = try expiresAt.toCBORValue()
            map = map.adding(key: "expiresAt", value: expiresAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case leaveRequestId
            case conversationId
            case requesterDid
            case requesterDeviceId
            case prior
            case status
            case requestedAt
            case expiresAt
        }
    }

    public struct DurableLeaveRequestResult: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#durableLeaveRequestResult"
        public let leaveRequest: LeaveRequestView
        public let entry: LeaveRequestEntry

        public init(
            leaveRequest: LeaveRequestView, entry: LeaveRequestEntry
        ) {
            self.leaveRequest = leaveRequest
            self.entry = entry
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                leaveRequest = try container.decode(LeaveRequestView.self, forKey: .leaveRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'leaveRequest': \(error)")
                throw error
            }
            do {
                entry = try container.decode(LeaveRequestEntry.self, forKey: .entry)
            } catch {
                LogManager.logError("Decoding error for required property 'entry': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(leaveRequest, forKey: .leaveRequest)
            try container.encode(entry, forKey: .entry)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(leaveRequest)
            hasher.combine(entry)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if leaveRequest != other.leaveRequest {
                return false
            }
            if entry != other.entry {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let leaveRequestValue = try leaveRequest.toCBORValue()
            map = map.adding(key: "leaveRequest", value: leaveRequestValue)
            let entryValue = try entry.toCBORValue()
            map = map.adding(key: "entry", value: entryValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case leaveRequest
            case entry
        }
    }

    public struct ZeroLeafLeaveResult: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#zeroLeafLeaveResult"
        public let coordinates: ConversationCoordinates
        public let entry: ZeroLeafLeaveEntry

        public init(
            coordinates: ConversationCoordinates, entry: ZeroLeafLeaveEntry
        ) {
            self.coordinates = coordinates
            self.entry = entry
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                coordinates = try container.decode(ConversationCoordinates.self, forKey: .coordinates)
            } catch {
                LogManager.logError("Decoding error for required property 'coordinates': \(error)")
                throw error
            }
            do {
                entry = try container.decode(ZeroLeafLeaveEntry.self, forKey: .entry)
            } catch {
                LogManager.logError("Decoding error for required property 'entry': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(coordinates, forKey: .coordinates)
            try container.encode(entry, forKey: .entry)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(coordinates)
            hasher.combine(entry)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if coordinates != other.coordinates {
                return false
            }
            if entry != other.entry {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let coordinatesValue = try coordinates.toCBORValue()
            map = map.adding(key: "coordinates", value: coordinatesValue)
            let entryValue = try entry.toCBORValue()
            map = map.adding(key: "entry", value: entryValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case coordinates
            case entry
        }
    }

    public struct ConversationCreatedResult: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#conversationCreatedResult"
        public let coordinates: ConversationCoordinates
        public let entry: CreationEntry

        public init(
            coordinates: ConversationCoordinates, entry: CreationEntry
        ) {
            self.coordinates = coordinates
            self.entry = entry
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                coordinates = try container.decode(ConversationCoordinates.self, forKey: .coordinates)
            } catch {
                LogManager.logError("Decoding error for required property 'coordinates': \(error)")
                throw error
            }
            do {
                entry = try container.decode(CreationEntry.self, forKey: .entry)
            } catch {
                LogManager.logError("Decoding error for required property 'entry': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(coordinates, forKey: .coordinates)
            try container.encode(entry, forKey: .entry)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(coordinates)
            hasher.combine(entry)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if coordinates != other.coordinates {
                return false
            }
            if entry != other.entry {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let coordinatesValue = try coordinates.toCBORValue()
            map = map.adding(key: "coordinates", value: coordinatesValue)
            let entryValue = try entry.toCBORValue()
            map = map.adding(key: "entry", value: entryValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case coordinates
            case entry
        }
    }

    public struct ExistingDirectConversationResult: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#existingDirectConversationResult"
        public let conversationKind: String
        public let conversationId: OperationId
        public let coordinates: ConversationCoordinates

        public init(
            conversationKind: String, conversationId: OperationId, coordinates: ConversationCoordinates
        ) {
            self.conversationKind = conversationKind
            self.conversationId = conversationId
            self.coordinates = coordinates
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                conversationKind = try container.decode(String.self, forKey: .conversationKind)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationKind': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                coordinates = try container.decode(ConversationCoordinates.self, forKey: .coordinates)
            } catch {
                LogManager.logError("Decoding error for required property 'coordinates': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(conversationKind, forKey: .conversationKind)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(coordinates, forKey: .coordinates)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(conversationKind)
            hasher.combine(conversationId)
            hasher.combine(coordinates)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if conversationKind != other.conversationKind {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if coordinates != other.coordinates {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let conversationKindValue = try conversationKind.toCBORValue()
            map = map.adding(key: "conversationKind", value: conversationKindValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let coordinatesValue = try coordinates.toCBORValue()
            map = map.adding(key: "coordinates", value: coordinatesValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case conversationKind
            case conversationId
            case coordinates
        }
    }

    public struct WelcomeAcknowledgementBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#welcomeAcknowledgementBody"
        public let signatureDomain: String
        public let welcomeId: OperationId
        public let transitionSeq: Int
        public let coordinates: ConversationCoordinates
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, welcomeId: OperationId, transitionSeq: Int, coordinates: ConversationCoordinates, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.welcomeId = welcomeId
            self.transitionSeq = transitionSeq
            self.coordinates = coordinates
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                welcomeId = try container.decode(OperationId.self, forKey: .welcomeId)
            } catch {
                LogManager.logError("Decoding error for required property 'welcomeId': \(error)")
                throw error
            }
            do {
                transitionSeq = try container.decode(Int.self, forKey: .transitionSeq)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionSeq': \(error)")
                throw error
            }
            do {
                coordinates = try container.decode(ConversationCoordinates.self, forKey: .coordinates)
            } catch {
                LogManager.logError("Decoding error for required property 'coordinates': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(welcomeId, forKey: .welcomeId)
            try container.encode(transitionSeq, forKey: .transitionSeq)
            try container.encode(coordinates, forKey: .coordinates)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(welcomeId)
            hasher.combine(transitionSeq)
            hasher.combine(coordinates)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if welcomeId != other.welcomeId {
                return false
            }
            if transitionSeq != other.transitionSeq {
                return false
            }
            if coordinates != other.coordinates {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let welcomeIdValue = try welcomeId.toCBORValue()
            map = map.adding(key: "welcomeId", value: welcomeIdValue)
            let transitionSeqValue = try transitionSeq.toCBORValue()
            map = map.adding(key: "transitionSeq", value: transitionSeqValue)
            let coordinatesValue = try coordinates.toCBORValue()
            map = map.adding(key: "coordinates", value: coordinatesValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case welcomeId
            case transitionSeq
            case coordinates
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case idempotencyKey
            case signedAt
        }
    }

    public struct WelcomeRejectionBody: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#welcomeRejectionBody"
        public let signatureDomain: String
        public let welcomeId: OperationId
        public let transitionSeq: Int
        public let coordinates: ConversationCoordinates
        public let reason: DefsWelcomeRejectionReason
        public let actorDid: BareDid
        public let actorDeviceId: DeviceId
        public let keyId: KeyId
        public let authGeneration: Int
        public let idempotencyKey: OperationId
        public let signedAt: CanonicalDatetime

        public init(
            signatureDomain: String, welcomeId: OperationId, transitionSeq: Int, coordinates: ConversationCoordinates, reason: DefsWelcomeRejectionReason, actorDid: BareDid, actorDeviceId: DeviceId, keyId: KeyId, authGeneration: Int, idempotencyKey: OperationId, signedAt: CanonicalDatetime
        ) {
            self.signatureDomain = signatureDomain
            self.welcomeId = welcomeId
            self.transitionSeq = transitionSeq
            self.coordinates = coordinates
            self.reason = reason
            self.actorDid = actorDid
            self.actorDeviceId = actorDeviceId
            self.keyId = keyId
            self.authGeneration = authGeneration
            self.idempotencyKey = idempotencyKey
            self.signedAt = signedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                signatureDomain = try container.decode(String.self, forKey: .signatureDomain)
            } catch {
                LogManager.logError("Decoding error for required property 'signatureDomain': \(error)")
                throw error
            }
            do {
                welcomeId = try container.decode(OperationId.self, forKey: .welcomeId)
            } catch {
                LogManager.logError("Decoding error for required property 'welcomeId': \(error)")
                throw error
            }
            do {
                transitionSeq = try container.decode(Int.self, forKey: .transitionSeq)
            } catch {
                LogManager.logError("Decoding error for required property 'transitionSeq': \(error)")
                throw error
            }
            do {
                coordinates = try container.decode(ConversationCoordinates.self, forKey: .coordinates)
            } catch {
                LogManager.logError("Decoding error for required property 'coordinates': \(error)")
                throw error
            }
            do {
                reason = try container.decode(DefsWelcomeRejectionReason.self, forKey: .reason)
            } catch {
                LogManager.logError("Decoding error for required property 'reason': \(error)")
                throw error
            }
            do {
                actorDid = try container.decode(BareDid.self, forKey: .actorDid)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDid': \(error)")
                throw error
            }
            do {
                actorDeviceId = try container.decode(DeviceId.self, forKey: .actorDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'actorDeviceId': \(error)")
                throw error
            }
            do {
                keyId = try container.decode(KeyId.self, forKey: .keyId)
            } catch {
                LogManager.logError("Decoding error for required property 'keyId': \(error)")
                throw error
            }
            do {
                authGeneration = try container.decode(Int.self, forKey: .authGeneration)
            } catch {
                LogManager.logError("Decoding error for required property 'authGeneration': \(error)")
                throw error
            }
            do {
                idempotencyKey = try container.decode(OperationId.self, forKey: .idempotencyKey)
            } catch {
                LogManager.logError("Decoding error for required property 'idempotencyKey': \(error)")
                throw error
            }
            do {
                signedAt = try container.decode(CanonicalDatetime.self, forKey: .signedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'signedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(signatureDomain, forKey: .signatureDomain)
            try container.encode(welcomeId, forKey: .welcomeId)
            try container.encode(transitionSeq, forKey: .transitionSeq)
            try container.encode(coordinates, forKey: .coordinates)
            try container.encode(reason, forKey: .reason)
            try container.encode(actorDid, forKey: .actorDid)
            try container.encode(actorDeviceId, forKey: .actorDeviceId)
            try container.encode(keyId, forKey: .keyId)
            try container.encode(authGeneration, forKey: .authGeneration)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(signedAt, forKey: .signedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(signatureDomain)
            hasher.combine(welcomeId)
            hasher.combine(transitionSeq)
            hasher.combine(coordinates)
            hasher.combine(reason)
            hasher.combine(actorDid)
            hasher.combine(actorDeviceId)
            hasher.combine(keyId)
            hasher.combine(authGeneration)
            hasher.combine(idempotencyKey)
            hasher.combine(signedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if signatureDomain != other.signatureDomain {
                return false
            }
            if welcomeId != other.welcomeId {
                return false
            }
            if transitionSeq != other.transitionSeq {
                return false
            }
            if coordinates != other.coordinates {
                return false
            }
            if reason != other.reason {
                return false
            }
            if actorDid != other.actorDid {
                return false
            }
            if actorDeviceId != other.actorDeviceId {
                return false
            }
            if keyId != other.keyId {
                return false
            }
            if authGeneration != other.authGeneration {
                return false
            }
            if idempotencyKey != other.idempotencyKey {
                return false
            }
            if signedAt != other.signedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let signatureDomainValue = try signatureDomain.toCBORValue()
            map = map.adding(key: "signatureDomain", value: signatureDomainValue)
            let welcomeIdValue = try welcomeId.toCBORValue()
            map = map.adding(key: "welcomeId", value: welcomeIdValue)
            let transitionSeqValue = try transitionSeq.toCBORValue()
            map = map.adding(key: "transitionSeq", value: transitionSeqValue)
            let coordinatesValue = try coordinates.toCBORValue()
            map = map.adding(key: "coordinates", value: coordinatesValue)
            let reasonValue = try reason.toCBORValue()
            map = map.adding(key: "reason", value: reasonValue)
            let actorDidValue = try actorDid.toCBORValue()
            map = map.adding(key: "actorDid", value: actorDidValue)
            let actorDeviceIdValue = try actorDeviceId.toCBORValue()
            map = map.adding(key: "actorDeviceId", value: actorDeviceIdValue)
            let keyIdValue = try keyId.toCBORValue()
            map = map.adding(key: "keyId", value: keyIdValue)
            let authGenerationValue = try authGeneration.toCBORValue()
            map = map.adding(key: "authGeneration", value: authGenerationValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let signedAtValue = try signedAt.toCBORValue()
            map = map.adding(key: "signedAt", value: signedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case signatureDomain
            case welcomeId
            case transitionSeq
            case coordinates
            case reason
            case actorDid
            case actorDeviceId
            case keyId
            case authGeneration
            case idempotencyKey
            case signedAt
        }
    }

    public struct SignedDeviceEnrollment: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedDeviceEnrollment"
        public let body: SignedDeviceEnrollmentBodyUnion
        public let signature: Bytes

        public init(
            body: SignedDeviceEnrollmentBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedDeviceEnrollmentBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedKeyPackageReplenishment: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedKeyPackageReplenishment"
        public let body: SignedKeyPackageReplenishmentBodyUnion
        public let signature: Bytes

        public init(
            body: SignedKeyPackageReplenishmentBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedKeyPackageReplenishmentBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedDeviceAuthenticationRebind: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedDeviceAuthenticationRebind"
        public let body: SignedDeviceAuthenticationRebindBodyUnion
        public let signature: Bytes

        public init(
            body: SignedDeviceAuthenticationRebindBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedDeviceAuthenticationRebindBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedDeviceRevocation: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedDeviceRevocation"
        public let body: SignedDeviceRevocationBodyUnion
        public let signature: Bytes

        public init(
            body: SignedDeviceRevocationBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedDeviceRevocationBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedBlobUploadPreparation: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedBlobUploadPreparation"
        public let body: SignedBlobUploadPreparationBodyUnion
        public let signature: Bytes

        public init(
            body: SignedBlobUploadPreparationBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedBlobUploadPreparationBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedBlobDeletion: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedBlobDeletion"
        public let body: SignedBlobDeletionBodyUnion
        public let signature: Bytes

        public init(
            body: SignedBlobDeletionBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedBlobDeletionBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedCreation: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedCreation"
        public let body: SignedCreationBodyUnion
        public let signature: Bytes

        public init(
            body: SignedCreationBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedCreationBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedCommitTransition: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedCommitTransition"
        public let body: SignedCommitTransitionBodyUnion
        public let signature: Bytes

        public init(
            body: SignedCommitTransitionBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedCommitTransitionBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedPolicyTransition: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedPolicyTransition"
        public let body: SignedPolicyTransitionBodyUnion
        public let signature: Bytes

        public init(
            body: SignedPolicyTransitionBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedPolicyTransitionBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedParticipantAcceptance: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedParticipantAcceptance"
        public let body: SignedParticipantAcceptanceBodyUnion
        public let signature: Bytes

        public init(
            body: SignedParticipantAcceptanceBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedParticipantAcceptanceBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedApplicationSend: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedApplicationSend"
        public let body: SignedApplicationSendBodyUnion
        public let signature: Bytes

        public init(
            body: SignedApplicationSendBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedApplicationSendBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedTyping: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedTyping"
        public let body: SignedTypingBodyUnion
        public let signature: Bytes

        public init(
            body: SignedTypingBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedTypingBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedMetadataTransition: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedMetadataTransition"
        public let body: SignedMetadataTransitionBodyUnion
        public let signature: Bytes

        public init(
            body: SignedMetadataTransitionBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedMetadataTransitionBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedResetRequest: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedResetRequest"
        public let body: SignedResetRequestBodyUnion
        public let signature: Bytes

        public init(
            body: SignedResetRequestBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedResetRequestBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedResetActivation: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedResetActivation"
        public let body: SignedResetActivationBodyUnion
        public let signature: Bytes

        public init(
            body: SignedResetActivationBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedResetActivationBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedLeafRecoveryRequest: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedLeafRecoveryRequest"
        public let body: SignedLeafRecoveryRequestBodyUnion
        public let signature: Bytes

        public init(
            body: SignedLeafRecoveryRequestBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedLeafRecoveryRequestBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedLeafRecoveryCancellation: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedLeafRecoveryCancellation"
        public let body: SignedLeafRecoveryCancellationBodyUnion
        public let signature: Bytes

        public init(
            body: SignedLeafRecoveryCancellationBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedLeafRecoveryCancellationBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedLeafRecoveryFulfillment: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedLeafRecoveryFulfillment"
        public let body: SignedLeafRecoveryFulfillmentBodyUnion
        public let signature: Bytes

        public init(
            body: SignedLeafRecoveryFulfillmentBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedLeafRecoveryFulfillmentBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedConversationClose: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedConversationClose"
        public let body: SignedConversationCloseBodyUnion
        public let signature: Bytes

        public init(
            body: SignedConversationCloseBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedConversationCloseBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedLeaveRequest: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedLeaveRequest"
        public let body: SignedLeaveRequestBodyUnion
        public let signature: Bytes

        public init(
            body: SignedLeaveRequestBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedLeaveRequestBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedZeroLeafLeave: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedZeroLeafLeave"
        public let body: SignedZeroLeafLeaveBodyUnion
        public let signature: Bytes

        public init(
            body: SignedZeroLeafLeaveBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedZeroLeafLeaveBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedLeaveCancellation: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedLeaveCancellation"
        public let body: SignedLeaveCancellationBodyUnion
        public let signature: Bytes

        public init(
            body: SignedLeaveCancellationBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedLeaveCancellationBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedLeaveCommitFulfillment: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedLeaveCommitFulfillment"
        public let body: SignedLeaveCommitFulfillmentBodyUnion
        public let signature: Bytes

        public init(
            body: SignedLeaveCommitFulfillmentBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedLeaveCommitFulfillmentBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedWelcomeAcknowledgement: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedWelcomeAcknowledgement"
        public let body: SignedWelcomeAcknowledgementBodyUnion
        public let signature: Bytes

        public init(
            body: SignedWelcomeAcknowledgementBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedWelcomeAcknowledgementBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct SignedWelcomeRejection: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#signedWelcomeRejection"
        public let body: SignedWelcomeRejectionBodyUnion
        public let signature: Bytes

        public init(
            body: SignedWelcomeRejectionBodyUnion, signature: Bytes
        ) {
            self.body = body
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                body = try container.decode(SignedWelcomeRejectionBodyUnion.self, forKey: .body)
            } catch {
                LogManager.logError("Decoding error for required property 'body': \(error)")
                throw error
            }
            do {
                signature = try container.decode(Bytes.self, forKey: .signature)
            } catch {
                LogManager.logError("Decoding error for required property 'signature': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(body, forKey: .body)
            try container.encode(signature, forKey: .signature)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(body)
            hasher.combine(signature)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if body != other.body {
                return false
            }
            if signature != other.signature {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let bodyValue = try body.toCBORValue()
            map = map.adding(key: "body", value: bodyValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case body
            case signature
        }
    }

    public struct ConversationState: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#conversationState"
        public let conversationKind: DefsConversationKind
        public let coordinates: ConversationCoordinates
        public let cipherSuite: DefsCipherSuite
        public let participants: [ParticipantView]
        public let leaves: [DeviceLeafView]
        public let metadataSnapshot: MetadataSnapshot
        public let snapshotSeq: Int

        public init(
            conversationKind: DefsConversationKind, coordinates: ConversationCoordinates, cipherSuite: DefsCipherSuite, participants: [ParticipantView], leaves: [DeviceLeafView], metadataSnapshot: MetadataSnapshot, snapshotSeq: Int
        ) {
            self.conversationKind = conversationKind
            self.coordinates = coordinates
            self.cipherSuite = cipherSuite
            self.participants = participants
            self.leaves = leaves
            self.metadataSnapshot = metadataSnapshot
            self.snapshotSeq = snapshotSeq
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                conversationKind = try container.decode(DefsConversationKind.self, forKey: .conversationKind)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationKind': \(error)")
                throw error
            }
            do {
                coordinates = try container.decode(ConversationCoordinates.self, forKey: .coordinates)
            } catch {
                LogManager.logError("Decoding error for required property 'coordinates': \(error)")
                throw error
            }
            do {
                cipherSuite = try container.decode(DefsCipherSuite.self, forKey: .cipherSuite)
            } catch {
                LogManager.logError("Decoding error for required property 'cipherSuite': \(error)")
                throw error
            }
            do {
                participants = try container.decode([ParticipantView].self, forKey: .participants)
            } catch {
                LogManager.logError("Decoding error for required property 'participants': \(error)")
                throw error
            }
            do {
                leaves = try container.decode([DeviceLeafView].self, forKey: .leaves)
            } catch {
                LogManager.logError("Decoding error for required property 'leaves': \(error)")
                throw error
            }
            do {
                metadataSnapshot = try container.decode(MetadataSnapshot.self, forKey: .metadataSnapshot)
            } catch {
                LogManager.logError("Decoding error for required property 'metadataSnapshot': \(error)")
                throw error
            }
            do {
                snapshotSeq = try container.decode(Int.self, forKey: .snapshotSeq)
            } catch {
                LogManager.logError("Decoding error for required property 'snapshotSeq': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(conversationKind, forKey: .conversationKind)
            try container.encode(coordinates, forKey: .coordinates)
            try container.encode(cipherSuite, forKey: .cipherSuite)
            try container.encode(participants, forKey: .participants)
            try container.encode(leaves, forKey: .leaves)
            try container.encode(metadataSnapshot, forKey: .metadataSnapshot)
            try container.encode(snapshotSeq, forKey: .snapshotSeq)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(conversationKind)
            hasher.combine(coordinates)
            hasher.combine(cipherSuite)
            hasher.combine(participants)
            hasher.combine(leaves)
            hasher.combine(metadataSnapshot)
            hasher.combine(snapshotSeq)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if conversationKind != other.conversationKind {
                return false
            }
            if coordinates != other.coordinates {
                return false
            }
            if cipherSuite != other.cipherSuite {
                return false
            }
            if participants != other.participants {
                return false
            }
            if leaves != other.leaves {
                return false
            }
            if metadataSnapshot != other.metadataSnapshot {
                return false
            }
            if snapshotSeq != other.snapshotSeq {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let conversationKindValue = try conversationKind.toCBORValue()
            map = map.adding(key: "conversationKind", value: conversationKindValue)
            let coordinatesValue = try coordinates.toCBORValue()
            map = map.adding(key: "coordinates", value: coordinatesValue)
            let cipherSuiteValue = try cipherSuite.toCBORValue()
            map = map.adding(key: "cipherSuite", value: cipherSuiteValue)
            let participantsValue = try participants.toCBORValue()
            map = map.adding(key: "participants", value: participantsValue)
            let leavesValue = try leaves.toCBORValue()
            map = map.adding(key: "leaves", value: leavesValue)
            let metadataSnapshotValue = try metadataSnapshot.toCBORValue()
            map = map.adding(key: "metadataSnapshot", value: metadataSnapshotValue)
            let snapshotSeqValue = try snapshotSeq.toCBORValue()
            map = map.adding(key: "snapshotSeq", value: snapshotSeqValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case conversationKind
            case coordinates
            case cipherSuite
            case participants
            case leaves
            case metadataSnapshot
            case snapshotSeq
        }
    }

    public struct ConversationInventoryState: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#conversationInventoryState"
        public let state: ConversationState

        public init(
            state: ConversationState
        ) {
            self.state = state
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                state = try container.decode(ConversationState.self, forKey: .state)
            } catch {
                LogManager.logError("Decoding error for required property 'state': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(state, forKey: .state)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(state)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if state != other.state {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let stateValue = try state.toCBORValue()
            map = map.adding(key: "state", value: stateValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case state
        }
    }

    public struct ConversationRemovalTombstone: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#conversationRemovalTombstone"
        public let conversationId: OperationId
        public let membershipIntervalId: OperationId
        public let userDid: BareDid
        public let deviceId: DeviceId
        public let terminalSeq: Int
        public let removedAt: CanonicalDatetime

        public init(
            conversationId: OperationId, membershipIntervalId: OperationId, userDid: BareDid, deviceId: DeviceId, terminalSeq: Int, removedAt: CanonicalDatetime
        ) {
            self.conversationId = conversationId
            self.membershipIntervalId = membershipIntervalId
            self.userDid = userDid
            self.deviceId = deviceId
            self.terminalSeq = terminalSeq
            self.removedAt = removedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                membershipIntervalId = try container.decode(OperationId.self, forKey: .membershipIntervalId)
            } catch {
                LogManager.logError("Decoding error for required property 'membershipIntervalId': \(error)")
                throw error
            }
            do {
                userDid = try container.decode(BareDid.self, forKey: .userDid)
            } catch {
                LogManager.logError("Decoding error for required property 'userDid': \(error)")
                throw error
            }
            do {
                deviceId = try container.decode(DeviceId.self, forKey: .deviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceId': \(error)")
                throw error
            }
            do {
                terminalSeq = try container.decode(Int.self, forKey: .terminalSeq)
            } catch {
                LogManager.logError("Decoding error for required property 'terminalSeq': \(error)")
                throw error
            }
            do {
                removedAt = try container.decode(CanonicalDatetime.self, forKey: .removedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'removedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(membershipIntervalId, forKey: .membershipIntervalId)
            try container.encode(userDid, forKey: .userDid)
            try container.encode(deviceId, forKey: .deviceId)
            try container.encode(terminalSeq, forKey: .terminalSeq)
            try container.encode(removedAt, forKey: .removedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(conversationId)
            hasher.combine(membershipIntervalId)
            hasher.combine(userDid)
            hasher.combine(deviceId)
            hasher.combine(terminalSeq)
            hasher.combine(removedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if conversationId != other.conversationId {
                return false
            }
            if membershipIntervalId != other.membershipIntervalId {
                return false
            }
            if userDid != other.userDid {
                return false
            }
            if deviceId != other.deviceId {
                return false
            }
            if terminalSeq != other.terminalSeq {
                return false
            }
            if removedAt != other.removedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let membershipIntervalIdValue = try membershipIntervalId.toCBORValue()
            map = map.adding(key: "membershipIntervalId", value: membershipIntervalIdValue)
            let userDidValue = try userDid.toCBORValue()
            map = map.adding(key: "userDid", value: userDidValue)
            let deviceIdValue = try deviceId.toCBORValue()
            map = map.adding(key: "deviceId", value: deviceIdValue)
            let terminalSeqValue = try terminalSeq.toCBORValue()
            map = map.adding(key: "terminalSeq", value: terminalSeqValue)
            let removedAtValue = try removedAt.toCBORValue()
            map = map.adding(key: "removedAt", value: removedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case conversationId
            case membershipIntervalId
            case userDid
            case deviceId
            case terminalSeq
            case removedAt
        }
    }

    public struct ConversationCloseTombstone: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#conversationCloseTombstone"
        public let conversationId: OperationId
        public let conversationKind: DefsConversationKind
        public let retired: ConversationCoordinates
        public let closedByDid: BareDid
        public let closedByDeviceId: DeviceId
        public let terminalSeq: Int
        public let closedAt: CanonicalDatetime

        public init(
            conversationId: OperationId, conversationKind: DefsConversationKind, retired: ConversationCoordinates, closedByDid: BareDid, closedByDeviceId: DeviceId, terminalSeq: Int, closedAt: CanonicalDatetime
        ) {
            self.conversationId = conversationId
            self.conversationKind = conversationKind
            self.retired = retired
            self.closedByDid = closedByDid
            self.closedByDeviceId = closedByDeviceId
            self.terminalSeq = terminalSeq
            self.closedAt = closedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                conversationKind = try container.decode(DefsConversationKind.self, forKey: .conversationKind)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationKind': \(error)")
                throw error
            }
            do {
                retired = try container.decode(ConversationCoordinates.self, forKey: .retired)
            } catch {
                LogManager.logError("Decoding error for required property 'retired': \(error)")
                throw error
            }
            do {
                closedByDid = try container.decode(BareDid.self, forKey: .closedByDid)
            } catch {
                LogManager.logError("Decoding error for required property 'closedByDid': \(error)")
                throw error
            }
            do {
                closedByDeviceId = try container.decode(DeviceId.self, forKey: .closedByDeviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'closedByDeviceId': \(error)")
                throw error
            }
            do {
                terminalSeq = try container.decode(Int.self, forKey: .terminalSeq)
            } catch {
                LogManager.logError("Decoding error for required property 'terminalSeq': \(error)")
                throw error
            }
            do {
                closedAt = try container.decode(CanonicalDatetime.self, forKey: .closedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'closedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(conversationKind, forKey: .conversationKind)
            try container.encode(retired, forKey: .retired)
            try container.encode(closedByDid, forKey: .closedByDid)
            try container.encode(closedByDeviceId, forKey: .closedByDeviceId)
            try container.encode(terminalSeq, forKey: .terminalSeq)
            try container.encode(closedAt, forKey: .closedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(conversationId)
            hasher.combine(conversationKind)
            hasher.combine(retired)
            hasher.combine(closedByDid)
            hasher.combine(closedByDeviceId)
            hasher.combine(terminalSeq)
            hasher.combine(closedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if conversationId != other.conversationId {
                return false
            }
            if conversationKind != other.conversationKind {
                return false
            }
            if retired != other.retired {
                return false
            }
            if closedByDid != other.closedByDid {
                return false
            }
            if closedByDeviceId != other.closedByDeviceId {
                return false
            }
            if terminalSeq != other.terminalSeq {
                return false
            }
            if closedAt != other.closedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let conversationKindValue = try conversationKind.toCBORValue()
            map = map.adding(key: "conversationKind", value: conversationKindValue)
            let retiredValue = try retired.toCBORValue()
            map = map.adding(key: "retired", value: retiredValue)
            let closedByDidValue = try closedByDid.toCBORValue()
            map = map.adding(key: "closedByDid", value: closedByDidValue)
            let closedByDeviceIdValue = try closedByDeviceId.toCBORValue()
            map = map.adding(key: "closedByDeviceId", value: closedByDeviceIdValue)
            let terminalSeqValue = try terminalSeq.toCBORValue()
            map = map.adding(key: "terminalSeq", value: terminalSeqValue)
            let closedAtValue = try closedAt.toCBORValue()
            map = map.adding(key: "closedAt", value: closedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case conversationId
            case conversationKind
            case retired
            case closedByDid
            case closedByDeviceId
            case terminalSeq
            case closedAt
        }
    }

    public struct ConversationCloseResult: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#conversationCloseResult"
        public let tombstone: ConversationCloseTombstone
        public let entry: ConversationCloseEntry

        public init(
            tombstone: ConversationCloseTombstone, entry: ConversationCloseEntry
        ) {
            self.tombstone = tombstone
            self.entry = entry
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                tombstone = try container.decode(ConversationCloseTombstone.self, forKey: .tombstone)
            } catch {
                LogManager.logError("Decoding error for required property 'tombstone': \(error)")
                throw error
            }
            do {
                entry = try container.decode(ConversationCloseEntry.self, forKey: .entry)
            } catch {
                LogManager.logError("Decoding error for required property 'entry': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(tombstone, forKey: .tombstone)
            try container.encode(entry, forKey: .entry)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(tombstone)
            hasher.combine(entry)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if tombstone != other.tombstone {
                return false
            }
            if entry != other.entry {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let tombstoneValue = try tombstone.toCBORValue()
            map = map.adding(key: "tombstone", value: tombstoneValue)
            let entryValue = try entry.toCBORValue()
            map = map.adding(key: "entry", value: entryValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case tombstone
            case entry
        }
    }

    public struct ApplicationEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#applicationEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedApplicationSend
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedApplicationSend, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedApplicationSend.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case receivedAt
        }
    }

    public struct CommitEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#commitEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedCommitTransition
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedCommitTransition, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedCommitTransition.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case receivedAt
        }
    }

    public struct PolicyEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#policyEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedPolicyTransition
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedPolicyTransition, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedPolicyTransition.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case receivedAt
        }
    }

    public struct MetadataEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#metadataEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedMetadataTransition
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedMetadataTransition, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedMetadataTransition.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case receivedAt
        }
    }

    public struct CreationEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#creationEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedCreation
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedCreation, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedCreation.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case receivedAt
        }
    }

    public struct ParticipantAcceptanceEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#participantAcceptanceEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedParticipantAcceptance
        public let recovery: LeafRecoveryView
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedParticipantAcceptance, recovery: LeafRecoveryView, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.recovery = recovery
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedParticipantAcceptance.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                recovery = try container.decode(LeafRecoveryView.self, forKey: .recovery)
            } catch {
                LogManager.logError("Decoding error for required property 'recovery': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(recovery, forKey: .recovery)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(recovery)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if recovery != other.recovery {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let recoveryValue = try recovery.toCBORValue()
            map = map.adding(key: "recovery", value: recoveryValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case recovery
            case receivedAt
        }
    }

    public struct ConversationCloseEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#conversationCloseEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedConversationClose
        public let tombstone: ConversationCloseTombstone
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedConversationClose, tombstone: ConversationCloseTombstone, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.tombstone = tombstone
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedConversationClose.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                tombstone = try container.decode(ConversationCloseTombstone.self, forKey: .tombstone)
            } catch {
                LogManager.logError("Decoding error for required property 'tombstone': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(tombstone, forKey: .tombstone)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(tombstone)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if tombstone != other.tombstone {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let tombstoneValue = try tombstone.toCBORValue()
            map = map.adding(key: "tombstone", value: tombstoneValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case tombstone
            case receivedAt
        }
    }

    public struct ResetRequestEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#resetRequestEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedResetRequest
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedResetRequest, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedResetRequest.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case receivedAt
        }
    }

    public struct ResetActivationEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#resetActivationEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedResetActivation
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedResetActivation, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedResetActivation.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case receivedAt
        }
    }

    public struct LeafRecoveryFulfillmentEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leafRecoveryFulfillmentEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedLeafRecoveryFulfillment
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedLeafRecoveryFulfillment, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedLeafRecoveryFulfillment.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case receivedAt
        }
    }

    public struct LeaveRequestEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leaveRequestEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedLeaveRequest
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedLeaveRequest, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedLeaveRequest.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case receivedAt
        }
    }

    public struct ZeroLeafLeaveEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#zeroLeafLeaveEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedZeroLeafLeave
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedZeroLeafLeave, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedZeroLeafLeave.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case receivedAt
        }
    }

    public struct LeaveCancellationEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leaveCancellationEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedLeaveCancellation
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedLeaveCancellation, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedLeaveCancellation.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case receivedAt
        }
    }

    public struct LeaveCommitFulfillmentEntry: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leaveCommitFulfillmentEntry"
        public let entryId: OperationId
        public let conversationId: OperationId
        public let seq: Int
        public let signedRequest: SignedLeaveCommitFulfillment
        public let receivedAt: CanonicalDatetime

        public init(
            entryId: OperationId, conversationId: OperationId, seq: Int, signedRequest: SignedLeaveCommitFulfillment, receivedAt: CanonicalDatetime
        ) {
            self.entryId = entryId
            self.conversationId = conversationId
            self.seq = seq
            self.signedRequest = signedRequest
            self.receivedAt = receivedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                entryId = try container.decode(OperationId.self, forKey: .entryId)
            } catch {
                LogManager.logError("Decoding error for required property 'entryId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
            do {
                signedRequest = try container.decode(SignedLeaveCommitFulfillment.self, forKey: .signedRequest)
            } catch {
                LogManager.logError("Decoding error for required property 'signedRequest': \(error)")
                throw error
            }
            do {
                receivedAt = try container.decode(CanonicalDatetime.self, forKey: .receivedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'receivedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(entryId, forKey: .entryId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
            try container.encode(signedRequest, forKey: .signedRequest)
            try container.encode(receivedAt, forKey: .receivedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(entryId)
            hasher.combine(conversationId)
            hasher.combine(seq)
            hasher.combine(signedRequest)
            hasher.combine(receivedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if entryId != other.entryId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            if signedRequest != other.signedRequest {
                return false
            }
            if receivedAt != other.receivedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let entryIdValue = try entryId.toCBORValue()
            map = map.adding(key: "entryId", value: entryIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            let receivedAtValue = try receivedAt.toCBORValue()
            map = map.adding(key: "receivedAt", value: receivedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case entryId
            case conversationId
            case seq
            case signedRequest
            case receivedAt
        }
    }

    public struct ConversationChangedEvent: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#conversationChangedEvent"
        public let conversationId: OperationId

        public init(
            conversationId: OperationId
        ) {
            self.conversationId = conversationId
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(conversationId, forKey: .conversationId)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(conversationId)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if conversationId != other.conversationId {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case conversationId
        }
    }

    public struct ConversationClosedEvent: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#conversationClosedEvent"
        public let conversationId: OperationId
        public let conversationKind: DefsConversationKind
        public let terminalSeq: Int

        public init(
            conversationId: OperationId, conversationKind: DefsConversationKind, terminalSeq: Int
        ) {
            self.conversationId = conversationId
            self.conversationKind = conversationKind
            self.terminalSeq = terminalSeq
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                conversationKind = try container.decode(DefsConversationKind.self, forKey: .conversationKind)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationKind': \(error)")
                throw error
            }
            do {
                terminalSeq = try container.decode(Int.self, forKey: .terminalSeq)
            } catch {
                LogManager.logError("Decoding error for required property 'terminalSeq': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(conversationKind, forKey: .conversationKind)
            try container.encode(terminalSeq, forKey: .terminalSeq)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(conversationId)
            hasher.combine(conversationKind)
            hasher.combine(terminalSeq)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if conversationId != other.conversationId {
                return false
            }
            if conversationKind != other.conversationKind {
                return false
            }
            if terminalSeq != other.terminalSeq {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let conversationKindValue = try conversationKind.toCBORValue()
            map = map.adding(key: "conversationKind", value: conversationKindValue)
            let terminalSeqValue = try terminalSeq.toCBORValue()
            map = map.adding(key: "terminalSeq", value: terminalSeqValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case conversationId
            case conversationKind
            case terminalSeq
        }
    }

    public struct MessageAvailableEvent: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#messageAvailableEvent"
        public let conversationId: OperationId
        public let seq: Int

        public init(
            conversationId: OperationId, seq: Int
        ) {
            self.conversationId = conversationId
            self.seq = seq
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                seq = try container.decode(Int.self, forKey: .seq)
            } catch {
                LogManager.logError("Decoding error for required property 'seq': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(seq, forKey: .seq)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(conversationId)
            hasher.combine(seq)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if conversationId != other.conversationId {
                return false
            }
            if seq != other.seq {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let seqValue = try seq.toCBORValue()
            map = map.adding(key: "seq", value: seqValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case conversationId
            case seq
        }
    }

    public struct WelcomeAvailableEvent: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#welcomeAvailableEvent"
        public let welcomeId: OperationId
        public let conversationId: OperationId

        public init(
            welcomeId: OperationId, conversationId: OperationId
        ) {
            self.welcomeId = welcomeId
            self.conversationId = conversationId
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                welcomeId = try container.decode(OperationId.self, forKey: .welcomeId)
            } catch {
                LogManager.logError("Decoding error for required property 'welcomeId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(welcomeId, forKey: .welcomeId)
            try container.encode(conversationId, forKey: .conversationId)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(welcomeId)
            hasher.combine(conversationId)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if welcomeId != other.welcomeId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let welcomeIdValue = try welcomeId.toCBORValue()
            map = map.adding(key: "welcomeId", value: welcomeIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case welcomeId
            case conversationId
        }
    }

    public struct WelcomeDispositionEvent: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#welcomeDispositionEvent"
        public let welcomeId: OperationId
        public let status: DefsWelcomeStatus

        public init(
            welcomeId: OperationId, status: DefsWelcomeStatus
        ) {
            self.welcomeId = welcomeId
            self.status = status
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                welcomeId = try container.decode(OperationId.self, forKey: .welcomeId)
            } catch {
                LogManager.logError("Decoding error for required property 'welcomeId': \(error)")
                throw error
            }
            do {
                status = try container.decode(DefsWelcomeStatus.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(welcomeId, forKey: .welcomeId)
            try container.encode(status, forKey: .status)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(welcomeId)
            hasher.combine(status)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if welcomeId != other.welcomeId {
                return false
            }
            if status != other.status {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let welcomeIdValue = try welcomeId.toCBORValue()
            map = map.adding(key: "welcomeId", value: welcomeIdValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case welcomeId
            case status
        }
    }

    public struct ResetRequestedEvent: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#resetRequestedEvent"
        public let resetRequestId: OperationId
        public let conversationId: OperationId

        public init(
            resetRequestId: OperationId, conversationId: OperationId
        ) {
            self.resetRequestId = resetRequestId
            self.conversationId = conversationId
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                resetRequestId = try container.decode(OperationId.self, forKey: .resetRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'resetRequestId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(resetRequestId, forKey: .resetRequestId)
            try container.encode(conversationId, forKey: .conversationId)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(resetRequestId)
            hasher.combine(conversationId)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if resetRequestId != other.resetRequestId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let resetRequestIdValue = try resetRequestId.toCBORValue()
            map = map.adding(key: "resetRequestId", value: resetRequestIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case resetRequestId
            case conversationId
        }
    }

    public struct LeafRecoveryEvent: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leafRecoveryEvent"
        public let recoveryRequestId: OperationId
        public let conversationId: OperationId
        public let status: DefsLeafRecoveryStatus

        public init(
            recoveryRequestId: OperationId, conversationId: OperationId, status: DefsLeafRecoveryStatus
        ) {
            self.recoveryRequestId = recoveryRequestId
            self.conversationId = conversationId
            self.status = status
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                recoveryRequestId = try container.decode(OperationId.self, forKey: .recoveryRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'recoveryRequestId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                status = try container.decode(DefsLeafRecoveryStatus.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(recoveryRequestId, forKey: .recoveryRequestId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(status, forKey: .status)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(recoveryRequestId)
            hasher.combine(conversationId)
            hasher.combine(status)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if recoveryRequestId != other.recoveryRequestId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if status != other.status {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let recoveryRequestIdValue = try recoveryRequestId.toCBORValue()
            map = map.adding(key: "recoveryRequestId", value: recoveryRequestIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case recoveryRequestId
            case conversationId
            case status
        }
    }

    public struct LeaveRequestEvent: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#leaveRequestEvent"
        public let leaveRequestId: OperationId
        public let conversationId: OperationId
        public let status: DefsLeaveRequestStatus

        public init(
            leaveRequestId: OperationId, conversationId: OperationId, status: DefsLeaveRequestStatus
        ) {
            self.leaveRequestId = leaveRequestId
            self.conversationId = conversationId
            self.status = status
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                leaveRequestId = try container.decode(OperationId.self, forKey: .leaveRequestId)
            } catch {
                LogManager.logError("Decoding error for required property 'leaveRequestId': \(error)")
                throw error
            }
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                status = try container.decode(DefsLeaveRequestStatus.self, forKey: .status)
            } catch {
                LogManager.logError("Decoding error for required property 'status': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(leaveRequestId, forKey: .leaveRequestId)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(status, forKey: .status)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(leaveRequestId)
            hasher.combine(conversationId)
            hasher.combine(status)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if leaveRequestId != other.leaveRequestId {
                return false
            }
            if conversationId != other.conversationId {
                return false
            }
            if status != other.status {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let leaveRequestIdValue = try leaveRequestId.toCBORValue()
            map = map.adding(key: "leaveRequestId", value: leaveRequestIdValue)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let statusValue = try status.toCBORValue()
            map = map.adding(key: "status", value: statusValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case leaveRequestId
            case conversationId
            case status
        }
    }

    public struct AccessEndedEvent: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#accessEndedEvent"
        public let conversationId: OperationId
        public let membershipIntervalId: OperationId
        public let userDid: BareDid
        public let deviceId: DeviceId
        public let terminalSeq: Int

        public init(
            conversationId: OperationId, membershipIntervalId: OperationId, userDid: BareDid, deviceId: DeviceId, terminalSeq: Int
        ) {
            self.conversationId = conversationId
            self.membershipIntervalId = membershipIntervalId
            self.userDid = userDid
            self.deviceId = deviceId
            self.terminalSeq = terminalSeq
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                conversationId = try container.decode(OperationId.self, forKey: .conversationId)
            } catch {
                LogManager.logError("Decoding error for required property 'conversationId': \(error)")
                throw error
            }
            do {
                membershipIntervalId = try container.decode(OperationId.self, forKey: .membershipIntervalId)
            } catch {
                LogManager.logError("Decoding error for required property 'membershipIntervalId': \(error)")
                throw error
            }
            do {
                userDid = try container.decode(BareDid.self, forKey: .userDid)
            } catch {
                LogManager.logError("Decoding error for required property 'userDid': \(error)")
                throw error
            }
            do {
                deviceId = try container.decode(DeviceId.self, forKey: .deviceId)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceId': \(error)")
                throw error
            }
            do {
                terminalSeq = try container.decode(Int.self, forKey: .terminalSeq)
            } catch {
                LogManager.logError("Decoding error for required property 'terminalSeq': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(conversationId, forKey: .conversationId)
            try container.encode(membershipIntervalId, forKey: .membershipIntervalId)
            try container.encode(userDid, forKey: .userDid)
            try container.encode(deviceId, forKey: .deviceId)
            try container.encode(terminalSeq, forKey: .terminalSeq)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(conversationId)
            hasher.combine(membershipIntervalId)
            hasher.combine(userDid)
            hasher.combine(deviceId)
            hasher.combine(terminalSeq)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if conversationId != other.conversationId {
                return false
            }
            if membershipIntervalId != other.membershipIntervalId {
                return false
            }
            if userDid != other.userDid {
                return false
            }
            if deviceId != other.deviceId {
                return false
            }
            if terminalSeq != other.terminalSeq {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let conversationIdValue = try conversationId.toCBORValue()
            map = map.adding(key: "conversationId", value: conversationIdValue)
            let membershipIntervalIdValue = try membershipIntervalId.toCBORValue()
            map = map.adding(key: "membershipIntervalId", value: membershipIntervalIdValue)
            let userDidValue = try userDid.toCBORValue()
            map = map.adding(key: "userDid", value: userDidValue)
            let deviceIdValue = try deviceId.toCBORValue()
            map = map.adding(key: "deviceId", value: deviceIdValue)
            let terminalSeqValue = try terminalSeq.toCBORValue()
            map = map.adding(key: "terminalSeq", value: terminalSeqValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case conversationId
            case membershipIntervalId
            case userDid
            case deviceId
            case terminalSeq
        }
    }

    public struct WatermarkEvent: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#watermarkEvent"
        public let issuedAt: CanonicalDatetime

        public init(
            issuedAt: CanonicalDatetime
        ) {
            self.issuedAt = issuedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                issuedAt = try container.decode(CanonicalDatetime.self, forKey: .issuedAt)
            } catch {
                LogManager.logError("Decoding error for required property 'issuedAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(issuedAt, forKey: .issuedAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(issuedAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if issuedAt != other.issuedAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let issuedAtValue = try issuedAt.toCBORValue()
            map = map.adding(key: "issuedAt", value: issuedAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case issuedAt
        }
    }

    public struct EventEnvelope: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.chat.defs#eventEnvelope"
        public let previousCursor: String
        public let cursor: String
        public let payload: ProtocolEventPayload
        public let createdAt: CanonicalDatetime

        public init(
            previousCursor: String, cursor: String, payload: ProtocolEventPayload, createdAt: CanonicalDatetime
        ) {
            self.previousCursor = previousCursor
            self.cursor = cursor
            self.payload = payload
            self.createdAt = createdAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                previousCursor = try container.decode(String.self, forKey: .previousCursor)
            } catch {
                LogManager.logError("Decoding error for required property 'previousCursor': \(error)")
                throw error
            }
            do {
                cursor = try container.decode(String.self, forKey: .cursor)
            } catch {
                LogManager.logError("Decoding error for required property 'cursor': \(error)")
                throw error
            }
            do {
                payload = try container.decode(ProtocolEventPayload.self, forKey: .payload)
            } catch {
                LogManager.logError("Decoding error for required property 'payload': \(error)")
                throw error
            }
            do {
                createdAt = try container.decode(CanonicalDatetime.self, forKey: .createdAt)
            } catch {
                LogManager.logError("Decoding error for required property 'createdAt': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(previousCursor, forKey: .previousCursor)
            try container.encode(cursor, forKey: .cursor)
            try container.encode(payload, forKey: .payload)
            try container.encode(createdAt, forKey: .createdAt)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(previousCursor)
            hasher.combine(cursor)
            hasher.combine(payload)
            hasher.combine(createdAt)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if previousCursor != other.previousCursor {
                return false
            }
            if cursor != other.cursor {
                return false
            }
            if payload != other.payload {
                return false
            }
            if createdAt != other.createdAt {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let previousCursorValue = try previousCursor.toCBORValue()
            map = map.adding(key: "previousCursor", value: previousCursorValue)
            let cursorValue = try cursor.toCBORValue()
            map = map.adding(key: "cursor", value: cursorValue)
            let payloadValue = try payload.toCBORValue()
            map = map.adding(key: "payload", value: payloadValue)
            let createdAtValue = try createdAt.toCBORValue()
            map = map.adding(key: "createdAt", value: createdAtValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case previousCursor
            case cursor
            case payload
            case createdAt
        }
    }

    public enum DefsProtocolVersion: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_1 = "1"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsProtocolVersion else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsCipherSuite: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_MLS_u5f_256_u5f_XWING_u5f_CHACHA20POLY1305_u5f_SHA256_u5f_Ed25519 = "MLS_256_XWING_CHACHA20POLY1305_SHA256_Ed25519"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsCipherSuite else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public typealias CanonicalDatetime = ATProtocolDate

    public typealias DeviceId = String

    public typealias OperationId = String

    public typealias BareDid = DID

    public typealias KeyId = String

    public enum DefsLifecycle: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_active = "active"
        case value_superseded = "superseded"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsLifecycle else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsConversationKind: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_direct = "direct"
        case value_group = "group"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsConversationKind else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsMemberRole: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_admin = "admin"
        case value_member = "member"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsMemberRole else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsParticipantStatus: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_pending = "pending"
        case value_active = "active"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsParticipantStatus else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsIncomingConsentPolicy: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_all = "all"
        case value_none = "none"
        case value_following = "following"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsIncomingConsentPolicy else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsDeviceStatus: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_active = "active"
        case value_revoked = "revoked"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsDeviceStatus else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsPackageStatus: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_available = "available"
        case value_reserved = "reserved"
        case value_consumed = "consumed"
        case value_expired = "expired"
        case value_revoked = "revoked"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsPackageStatus else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsReservationStatus: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_active = "active"
        case value_consumed = "consumed"
        case value_expired = "expired"
        case value_released = "released"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsReservationStatus else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsWelcomeStatus: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_pending = "pending"
        case value_acknowledged = "acknowledged"
        case value_rejected = "rejected"
        case value_expired = "expired"
        case value_superseded = "superseded"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsWelcomeStatus else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsWelcomeRejectionReason: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_noMatchingKeyPackage = "noMatchingKeyPackage"
        case value_invalidWelcome = "invalidWelcome"
        case value_unsupportedCipherSuite = "unsupportedCipherSuite"
        case value_coordinateMismatch = "coordinateMismatch"
        case value_localStateConflict = "localStateConflict"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsWelcomeRejectionReason else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsLeafRecoveryStatus: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_open = "open"
        case value_fulfilled = "fulfilled"
        case value_cancelled = "cancelled"
        case value_expired = "expired"
        case value_superseded = "superseded"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsLeafRecoveryStatus else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsRecoveryWorkSourceKind: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_welcomeExpired = "welcomeExpired"
        case value_welcomeRejected = "welcomeRejected"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsRecoveryWorkSourceKind else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsRecoveryWorkStatus: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_pending = "pending"
        case value_completed = "completed"
        case value_superseded = "superseded"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsRecoveryWorkStatus else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsLeaveRequestStatus: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_pending = "pending"
        case value_fulfilled = "fulfilled"
        case value_cancelled = "cancelled"
        case value_expired = "expired"
        case value_stale = "stale"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsLeaveRequestStatus else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsLeafRecoveryKind: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_add = "add"
        case value_replace = "replace"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsLeafRecoveryKind else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsLeafOrigin: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_genesis = "genesis"
        case value_keyPackage = "keyPackage"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsLeafOrigin else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsResetReason: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_localStateLost = "localStateLost"
        case value_poisonedState = "poisonedState"
        case value_epochDivergence = "epochDivergence"
        case value_manualRecovery = "manualRecovery"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsResetReason else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum DefsBlobPurpose: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_attachment = "attachment"
        case value_metadata = "metadata"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? DefsBlobPurpose else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public typealias ArtifactHash = Bytes

    public enum ParticipantChange: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsAddParticipant(BlueCatbirdChatDefs.AddParticipant)
        case blueCatbirdChatDefsRemoveParticipant(BlueCatbirdChatDefs.RemoveParticipant)
        case blueCatbirdChatDefsChangeParticipantRole(BlueCatbirdChatDefs.ChangeParticipantRole)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.AddParticipant) {
            self = .blueCatbirdChatDefsAddParticipant(value)
        }

        public init(_ value: BlueCatbirdChatDefs.RemoveParticipant) {
            self = .blueCatbirdChatDefsRemoveParticipant(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ChangeParticipantRole) {
            self = .blueCatbirdChatDefsChangeParticipantRole(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#addParticipant":
                let value = try BlueCatbirdChatDefs.AddParticipant(from: decoder)
                self = .blueCatbirdChatDefsAddParticipant(value)
            case "blue.catbird.chat.defs#removeParticipant":
                let value = try BlueCatbirdChatDefs.RemoveParticipant(from: decoder)
                self = .blueCatbirdChatDefsRemoveParticipant(value)
            case "blue.catbird.chat.defs#changeParticipantRole":
                let value = try BlueCatbirdChatDefs.ChangeParticipantRole(from: decoder)
                self = .blueCatbirdChatDefsChangeParticipantRole(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsAddParticipant(value):
                try container.encode("blue.catbird.chat.defs#addParticipant", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsRemoveParticipant(value):
                try container.encode("blue.catbird.chat.defs#removeParticipant", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsChangeParticipantRole(value):
                try container.encode("blue.catbird.chat.defs#changeParticipantRole", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsAddParticipant(value):
                hasher.combine("blue.catbird.chat.defs#addParticipant")
                hasher.combine(value)
            case let .blueCatbirdChatDefsRemoveParticipant(value):
                hasher.combine("blue.catbird.chat.defs#removeParticipant")
                hasher.combine(value)
            case let .blueCatbirdChatDefsChangeParticipantRole(value):
                hasher.combine("blue.catbird.chat.defs#changeParticipantRole")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: ParticipantChange, rhs: ParticipantChange) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsAddParticipant(lhsValue),
                      .blueCatbirdChatDefsAddParticipant(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsRemoveParticipant(lhsValue),
                      .blueCatbirdChatDefsRemoveParticipant(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsChangeParticipantRole(lhsValue),
                      .blueCatbirdChatDefsChangeParticipantRole(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? ParticipantChange else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsAddParticipant(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#addParticipant")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsRemoveParticipant(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#removeParticipant")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsChangeParticipantRole(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#changeParticipantRole")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum LeafChange: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsAddLeafByRecovery(BlueCatbirdChatDefs.AddLeafByRecovery)
        case blueCatbirdChatDefsRemoveLeaf(BlueCatbirdChatDefs.RemoveLeaf)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.AddLeafByRecovery) {
            self = .blueCatbirdChatDefsAddLeafByRecovery(value)
        }

        public init(_ value: BlueCatbirdChatDefs.RemoveLeaf) {
            self = .blueCatbirdChatDefsRemoveLeaf(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#addLeafByRecovery":
                let value = try BlueCatbirdChatDefs.AddLeafByRecovery(from: decoder)
                self = .blueCatbirdChatDefsAddLeafByRecovery(value)
            case "blue.catbird.chat.defs#removeLeaf":
                let value = try BlueCatbirdChatDefs.RemoveLeaf(from: decoder)
                self = .blueCatbirdChatDefsRemoveLeaf(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsAddLeafByRecovery(value):
                try container.encode("blue.catbird.chat.defs#addLeafByRecovery", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsRemoveLeaf(value):
                try container.encode("blue.catbird.chat.defs#removeLeaf", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsAddLeafByRecovery(value):
                hasher.combine("blue.catbird.chat.defs#addLeafByRecovery")
                hasher.combine(value)
            case let .blueCatbirdChatDefsRemoveLeaf(value):
                hasher.combine("blue.catbird.chat.defs#removeLeaf")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: LeafChange, rhs: LeafChange) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsAddLeafByRecovery(lhsValue),
                      .blueCatbirdChatDefsAddLeafByRecovery(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsRemoveLeaf(lhsValue),
                      .blueCatbirdChatDefsRemoveLeaf(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? LeafChange else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsAddLeafByRecovery(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#addLeafByRecovery")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsRemoveLeaf(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#removeLeaf")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public typealias IdentifierBytes = Bytes

    public enum EncryptedImageEmbedMimeType: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_image_u2f_heic = "image/heic"
        case value_image_u2f_jpeg = "image/jpeg"
        case value_image_u2f_png = "image/png"
        case value_image_u2f_webp = "image/webp"
        case value_image_u2f_gif = "image/gif"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? EncryptedImageEmbedMimeType else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum EncryptedAudioEmbedMimeType: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_audio_u2f_aac = "audio/aac"
        case value_audio_u2f_mp4 = "audio/mp4"
        case value_audio_u2f_ogg = "audio/ogg"
        case value_audio_u2f_opus = "audio/opus"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? EncryptedAudioEmbedMimeType else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum ApplicationEmbed: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsEncryptedImageEmbedVariant(BlueCatbirdChatDefs.EncryptedImageEmbedVariant)
        case blueCatbirdChatDefsEncryptedAudioEmbedVariant(BlueCatbirdChatDefs.EncryptedAudioEmbedVariant)
        case blueCatbirdChatDefsAtprotoRecordEmbedVariant(BlueCatbirdChatDefs.AtprotoRecordEmbedVariant)
        case blueCatbirdChatDefsExternalLinkEmbedVariant(BlueCatbirdChatDefs.ExternalLinkEmbedVariant)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.EncryptedImageEmbedVariant) {
            self = .blueCatbirdChatDefsEncryptedImageEmbedVariant(value)
        }

        public init(_ value: BlueCatbirdChatDefs.EncryptedAudioEmbedVariant) {
            self = .blueCatbirdChatDefsEncryptedAudioEmbedVariant(value)
        }

        public init(_ value: BlueCatbirdChatDefs.AtprotoRecordEmbedVariant) {
            self = .blueCatbirdChatDefsAtprotoRecordEmbedVariant(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ExternalLinkEmbedVariant) {
            self = .blueCatbirdChatDefsExternalLinkEmbedVariant(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#encryptedImageEmbedVariant":
                let value = try BlueCatbirdChatDefs.EncryptedImageEmbedVariant(from: decoder)
                self = .blueCatbirdChatDefsEncryptedImageEmbedVariant(value)
            case "blue.catbird.chat.defs#encryptedAudioEmbedVariant":
                let value = try BlueCatbirdChatDefs.EncryptedAudioEmbedVariant(from: decoder)
                self = .blueCatbirdChatDefsEncryptedAudioEmbedVariant(value)
            case "blue.catbird.chat.defs#atprotoRecordEmbedVariant":
                let value = try BlueCatbirdChatDefs.AtprotoRecordEmbedVariant(from: decoder)
                self = .blueCatbirdChatDefsAtprotoRecordEmbedVariant(value)
            case "blue.catbird.chat.defs#externalLinkEmbedVariant":
                let value = try BlueCatbirdChatDefs.ExternalLinkEmbedVariant(from: decoder)
                self = .blueCatbirdChatDefsExternalLinkEmbedVariant(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsEncryptedImageEmbedVariant(value):
                try container.encode("blue.catbird.chat.defs#encryptedImageEmbedVariant", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsEncryptedAudioEmbedVariant(value):
                try container.encode("blue.catbird.chat.defs#encryptedAudioEmbedVariant", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsAtprotoRecordEmbedVariant(value):
                try container.encode("blue.catbird.chat.defs#atprotoRecordEmbedVariant", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsExternalLinkEmbedVariant(value):
                try container.encode("blue.catbird.chat.defs#externalLinkEmbedVariant", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsEncryptedImageEmbedVariant(value):
                hasher.combine("blue.catbird.chat.defs#encryptedImageEmbedVariant")
                hasher.combine(value)
            case let .blueCatbirdChatDefsEncryptedAudioEmbedVariant(value):
                hasher.combine("blue.catbird.chat.defs#encryptedAudioEmbedVariant")
                hasher.combine(value)
            case let .blueCatbirdChatDefsAtprotoRecordEmbedVariant(value):
                hasher.combine("blue.catbird.chat.defs#atprotoRecordEmbedVariant")
                hasher.combine(value)
            case let .blueCatbirdChatDefsExternalLinkEmbedVariant(value):
                hasher.combine("blue.catbird.chat.defs#externalLinkEmbedVariant")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: ApplicationEmbed, rhs: ApplicationEmbed) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsEncryptedImageEmbedVariant(lhsValue),
                      .blueCatbirdChatDefsEncryptedImageEmbedVariant(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsEncryptedAudioEmbedVariant(lhsValue),
                      .blueCatbirdChatDefsEncryptedAudioEmbedVariant(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsAtprotoRecordEmbedVariant(lhsValue),
                      .blueCatbirdChatDefsAtprotoRecordEmbedVariant(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsExternalLinkEmbedVariant(lhsValue),
                      .blueCatbirdChatDefsExternalLinkEmbedVariant(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? ApplicationEmbed else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsEncryptedImageEmbedVariant(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#encryptedImageEmbedVariant")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsEncryptedAudioEmbedVariant(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#encryptedAudioEmbedVariant")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsAtprotoRecordEmbedVariant(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#atprotoRecordEmbedVariant")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsExternalLinkEmbedVariant(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#externalLinkEmbedVariant")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum ReactionFrameBodyOperation: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_add = "add"
        case value_remove = "remove"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? ReactionFrameBodyOperation else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum ApplicationFrameBody: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsMessageFrameVariant(BlueCatbirdChatDefs.MessageFrameVariant)
        case blueCatbirdChatDefsReactionFrameVariant(BlueCatbirdChatDefs.ReactionFrameVariant)
        case blueCatbirdChatDefsEditFrameVariant(BlueCatbirdChatDefs.EditFrameVariant)
        case blueCatbirdChatDefsTombstoneFrameVariant(BlueCatbirdChatDefs.TombstoneFrameVariant)
        case blueCatbirdChatDefsReadStateFrameVariant(BlueCatbirdChatDefs.ReadStateFrameVariant)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.MessageFrameVariant) {
            self = .blueCatbirdChatDefsMessageFrameVariant(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ReactionFrameVariant) {
            self = .blueCatbirdChatDefsReactionFrameVariant(value)
        }

        public init(_ value: BlueCatbirdChatDefs.EditFrameVariant) {
            self = .blueCatbirdChatDefsEditFrameVariant(value)
        }

        public init(_ value: BlueCatbirdChatDefs.TombstoneFrameVariant) {
            self = .blueCatbirdChatDefsTombstoneFrameVariant(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ReadStateFrameVariant) {
            self = .blueCatbirdChatDefsReadStateFrameVariant(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#messageFrameVariant":
                let value = try BlueCatbirdChatDefs.MessageFrameVariant(from: decoder)
                self = .blueCatbirdChatDefsMessageFrameVariant(value)
            case "blue.catbird.chat.defs#reactionFrameVariant":
                let value = try BlueCatbirdChatDefs.ReactionFrameVariant(from: decoder)
                self = .blueCatbirdChatDefsReactionFrameVariant(value)
            case "blue.catbird.chat.defs#editFrameVariant":
                let value = try BlueCatbirdChatDefs.EditFrameVariant(from: decoder)
                self = .blueCatbirdChatDefsEditFrameVariant(value)
            case "blue.catbird.chat.defs#tombstoneFrameVariant":
                let value = try BlueCatbirdChatDefs.TombstoneFrameVariant(from: decoder)
                self = .blueCatbirdChatDefsTombstoneFrameVariant(value)
            case "blue.catbird.chat.defs#readStateFrameVariant":
                let value = try BlueCatbirdChatDefs.ReadStateFrameVariant(from: decoder)
                self = .blueCatbirdChatDefsReadStateFrameVariant(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsMessageFrameVariant(value):
                try container.encode("blue.catbird.chat.defs#messageFrameVariant", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsReactionFrameVariant(value):
                try container.encode("blue.catbird.chat.defs#reactionFrameVariant", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsEditFrameVariant(value):
                try container.encode("blue.catbird.chat.defs#editFrameVariant", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsTombstoneFrameVariant(value):
                try container.encode("blue.catbird.chat.defs#tombstoneFrameVariant", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsReadStateFrameVariant(value):
                try container.encode("blue.catbird.chat.defs#readStateFrameVariant", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsMessageFrameVariant(value):
                hasher.combine("blue.catbird.chat.defs#messageFrameVariant")
                hasher.combine(value)
            case let .blueCatbirdChatDefsReactionFrameVariant(value):
                hasher.combine("blue.catbird.chat.defs#reactionFrameVariant")
                hasher.combine(value)
            case let .blueCatbirdChatDefsEditFrameVariant(value):
                hasher.combine("blue.catbird.chat.defs#editFrameVariant")
                hasher.combine(value)
            case let .blueCatbirdChatDefsTombstoneFrameVariant(value):
                hasher.combine("blue.catbird.chat.defs#tombstoneFrameVariant")
                hasher.combine(value)
            case let .blueCatbirdChatDefsReadStateFrameVariant(value):
                hasher.combine("blue.catbird.chat.defs#readStateFrameVariant")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: ApplicationFrameBody, rhs: ApplicationFrameBody) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsMessageFrameVariant(lhsValue),
                      .blueCatbirdChatDefsMessageFrameVariant(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsReactionFrameVariant(lhsValue),
                      .blueCatbirdChatDefsReactionFrameVariant(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsEditFrameVariant(lhsValue),
                      .blueCatbirdChatDefsEditFrameVariant(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsTombstoneFrameVariant(lhsValue),
                      .blueCatbirdChatDefsTombstoneFrameVariant(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsReadStateFrameVariant(lhsValue),
                      .blueCatbirdChatDefsReadStateFrameVariant(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? ApplicationFrameBody else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsMessageFrameVariant(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#messageFrameVariant")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsReactionFrameVariant(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#reactionFrameVariant")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsEditFrameVariant(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#editFrameVariant")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsTombstoneFrameVariant(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#tombstoneFrameVariant")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsReadStateFrameVariant(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#readStateFrameVariant")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum MetadataAvatarEmbedMimeType: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_image_u2f_heic = "image/heic"
        case value_image_u2f_jpeg = "image/jpeg"
        case value_image_u2f_png = "image/png"
        case value_image_u2f_webp = "image/webp"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? MetadataAvatarEmbedMimeType else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum ResetRequestViewStatus: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_pending = "pending"
        case value_stale = "stale"
        case value_consumed = "consumed"
        case value_expired = "expired"
        case value_revoked = "revoked"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? ResetRequestViewStatus else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }

    public enum RecoveryWorkView: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsRecoveryWorkPendingView(BlueCatbirdChatDefs.RecoveryWorkPendingView)
        case blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(BlueCatbirdChatDefs.RecoveryWorkCompletedByTransitionView)
        case blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(BlueCatbirdChatDefs.RecoveryWorkSupersededByTransitionView)
        case blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(BlueCatbirdChatDefs.RecoveryWorkSupersededByRevocationView)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.RecoveryWorkPendingView) {
            self = .blueCatbirdChatDefsRecoveryWorkPendingView(value)
        }

        public init(_ value: BlueCatbirdChatDefs.RecoveryWorkCompletedByTransitionView) {
            self = .blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(value)
        }

        public init(_ value: BlueCatbirdChatDefs.RecoveryWorkSupersededByTransitionView) {
            self = .blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(value)
        }

        public init(_ value: BlueCatbirdChatDefs.RecoveryWorkSupersededByRevocationView) {
            self = .blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#recoveryWorkPendingView":
                let value = try BlueCatbirdChatDefs.RecoveryWorkPendingView(from: decoder)
                self = .blueCatbirdChatDefsRecoveryWorkPendingView(value)
            case "blue.catbird.chat.defs#recoveryWorkCompletedByTransitionView":
                let value = try BlueCatbirdChatDefs.RecoveryWorkCompletedByTransitionView(from: decoder)
                self = .blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(value)
            case "blue.catbird.chat.defs#recoveryWorkSupersededByTransitionView":
                let value = try BlueCatbirdChatDefs.RecoveryWorkSupersededByTransitionView(from: decoder)
                self = .blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(value)
            case "blue.catbird.chat.defs#recoveryWorkSupersededByRevocationView":
                let value = try BlueCatbirdChatDefs.RecoveryWorkSupersededByRevocationView(from: decoder)
                self = .blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsRecoveryWorkPendingView(value):
                try container.encode("blue.catbird.chat.defs#recoveryWorkPendingView", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(value):
                try container.encode("blue.catbird.chat.defs#recoveryWorkCompletedByTransitionView", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(value):
                try container.encode("blue.catbird.chat.defs#recoveryWorkSupersededByTransitionView", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(value):
                try container.encode("blue.catbird.chat.defs#recoveryWorkSupersededByRevocationView", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsRecoveryWorkPendingView(value):
                hasher.combine("blue.catbird.chat.defs#recoveryWorkPendingView")
                hasher.combine(value)
            case let .blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(value):
                hasher.combine("blue.catbird.chat.defs#recoveryWorkCompletedByTransitionView")
                hasher.combine(value)
            case let .blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(value):
                hasher.combine("blue.catbird.chat.defs#recoveryWorkSupersededByTransitionView")
                hasher.combine(value)
            case let .blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(value):
                hasher.combine("blue.catbird.chat.defs#recoveryWorkSupersededByRevocationView")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: RecoveryWorkView, rhs: RecoveryWorkView) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsRecoveryWorkPendingView(lhsValue),
                      .blueCatbirdChatDefsRecoveryWorkPendingView(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(lhsValue),
                      .blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(lhsValue),
                      .blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(lhsValue),
                      .blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? RecoveryWorkView else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsRecoveryWorkPendingView(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#recoveryWorkPendingView")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#recoveryWorkCompletedByTransitionView")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#recoveryWorkSupersededByTransitionView")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#recoveryWorkSupersededByRevocationView")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum LeafRecoveryInboxItem: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsLeafRecoveryView(BlueCatbirdChatDefs.LeafRecoveryView)
        case blueCatbirdChatDefsRecoveryWorkPendingView(BlueCatbirdChatDefs.RecoveryWorkPendingView)
        case blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(BlueCatbirdChatDefs.RecoveryWorkCompletedByTransitionView)
        case blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(BlueCatbirdChatDefs.RecoveryWorkSupersededByTransitionView)
        case blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(BlueCatbirdChatDefs.RecoveryWorkSupersededByRevocationView)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.LeafRecoveryView) {
            self = .blueCatbirdChatDefsLeafRecoveryView(value)
        }

        public init(_ value: BlueCatbirdChatDefs.RecoveryWorkPendingView) {
            self = .blueCatbirdChatDefsRecoveryWorkPendingView(value)
        }

        public init(_ value: BlueCatbirdChatDefs.RecoveryWorkCompletedByTransitionView) {
            self = .blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(value)
        }

        public init(_ value: BlueCatbirdChatDefs.RecoveryWorkSupersededByTransitionView) {
            self = .blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(value)
        }

        public init(_ value: BlueCatbirdChatDefs.RecoveryWorkSupersededByRevocationView) {
            self = .blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#leafRecoveryView":
                let value = try BlueCatbirdChatDefs.LeafRecoveryView(from: decoder)
                self = .blueCatbirdChatDefsLeafRecoveryView(value)
            case "blue.catbird.chat.defs#recoveryWorkPendingView":
                let value = try BlueCatbirdChatDefs.RecoveryWorkPendingView(from: decoder)
                self = .blueCatbirdChatDefsRecoveryWorkPendingView(value)
            case "blue.catbird.chat.defs#recoveryWorkCompletedByTransitionView":
                let value = try BlueCatbirdChatDefs.RecoveryWorkCompletedByTransitionView(from: decoder)
                self = .blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(value)
            case "blue.catbird.chat.defs#recoveryWorkSupersededByTransitionView":
                let value = try BlueCatbirdChatDefs.RecoveryWorkSupersededByTransitionView(from: decoder)
                self = .blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(value)
            case "blue.catbird.chat.defs#recoveryWorkSupersededByRevocationView":
                let value = try BlueCatbirdChatDefs.RecoveryWorkSupersededByRevocationView(from: decoder)
                self = .blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsLeafRecoveryView(value):
                try container.encode("blue.catbird.chat.defs#leafRecoveryView", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsRecoveryWorkPendingView(value):
                try container.encode("blue.catbird.chat.defs#recoveryWorkPendingView", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(value):
                try container.encode("blue.catbird.chat.defs#recoveryWorkCompletedByTransitionView", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(value):
                try container.encode("blue.catbird.chat.defs#recoveryWorkSupersededByTransitionView", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(value):
                try container.encode("blue.catbird.chat.defs#recoveryWorkSupersededByRevocationView", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsLeafRecoveryView(value):
                hasher.combine("blue.catbird.chat.defs#leafRecoveryView")
                hasher.combine(value)
            case let .blueCatbirdChatDefsRecoveryWorkPendingView(value):
                hasher.combine("blue.catbird.chat.defs#recoveryWorkPendingView")
                hasher.combine(value)
            case let .blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(value):
                hasher.combine("blue.catbird.chat.defs#recoveryWorkCompletedByTransitionView")
                hasher.combine(value)
            case let .blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(value):
                hasher.combine("blue.catbird.chat.defs#recoveryWorkSupersededByTransitionView")
                hasher.combine(value)
            case let .blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(value):
                hasher.combine("blue.catbird.chat.defs#recoveryWorkSupersededByRevocationView")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: LeafRecoveryInboxItem, rhs: LeafRecoveryInboxItem) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsLeafRecoveryView(lhsValue),
                      .blueCatbirdChatDefsLeafRecoveryView(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsRecoveryWorkPendingView(lhsValue),
                      .blueCatbirdChatDefsRecoveryWorkPendingView(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(lhsValue),
                      .blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(lhsValue),
                      .blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(lhsValue),
                      .blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? LeafRecoveryInboxItem else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsLeafRecoveryView(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#leafRecoveryView")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsRecoveryWorkPendingView(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#recoveryWorkPendingView")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsRecoveryWorkCompletedByTransitionView(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#recoveryWorkCompletedByTransitionView")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsRecoveryWorkSupersededByTransitionView(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#recoveryWorkSupersededByTransitionView")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsRecoveryWorkSupersededByRevocationView(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#recoveryWorkSupersededByRevocationView")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum LeaveOperationResult: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsDurableLeaveRequestResult(BlueCatbirdChatDefs.DurableLeaveRequestResult)
        case blueCatbirdChatDefsZeroLeafLeaveResult(BlueCatbirdChatDefs.ZeroLeafLeaveResult)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.DurableLeaveRequestResult) {
            self = .blueCatbirdChatDefsDurableLeaveRequestResult(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ZeroLeafLeaveResult) {
            self = .blueCatbirdChatDefsZeroLeafLeaveResult(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#durableLeaveRequestResult":
                let value = try BlueCatbirdChatDefs.DurableLeaveRequestResult(from: decoder)
                self = .blueCatbirdChatDefsDurableLeaveRequestResult(value)
            case "blue.catbird.chat.defs#zeroLeafLeaveResult":
                let value = try BlueCatbirdChatDefs.ZeroLeafLeaveResult(from: decoder)
                self = .blueCatbirdChatDefsZeroLeafLeaveResult(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsDurableLeaveRequestResult(value):
                try container.encode("blue.catbird.chat.defs#durableLeaveRequestResult", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsZeroLeafLeaveResult(value):
                try container.encode("blue.catbird.chat.defs#zeroLeafLeaveResult", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsDurableLeaveRequestResult(value):
                hasher.combine("blue.catbird.chat.defs#durableLeaveRequestResult")
                hasher.combine(value)
            case let .blueCatbirdChatDefsZeroLeafLeaveResult(value):
                hasher.combine("blue.catbird.chat.defs#zeroLeafLeaveResult")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: LeaveOperationResult, rhs: LeaveOperationResult) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsDurableLeaveRequestResult(lhsValue),
                      .blueCatbirdChatDefsDurableLeaveRequestResult(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsZeroLeafLeaveResult(lhsValue),
                      .blueCatbirdChatDefsZeroLeafLeaveResult(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? LeaveOperationResult else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsDurableLeaveRequestResult(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#durableLeaveRequestResult")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsZeroLeafLeaveResult(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#zeroLeafLeaveResult")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum ConversationCreationResult: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsConversationCreatedResult(BlueCatbirdChatDefs.ConversationCreatedResult)
        case blueCatbirdChatDefsExistingDirectConversationResult(BlueCatbirdChatDefs.ExistingDirectConversationResult)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.ConversationCreatedResult) {
            self = .blueCatbirdChatDefsConversationCreatedResult(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ExistingDirectConversationResult) {
            self = .blueCatbirdChatDefsExistingDirectConversationResult(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#conversationCreatedResult":
                let value = try BlueCatbirdChatDefs.ConversationCreatedResult(from: decoder)
                self = .blueCatbirdChatDefsConversationCreatedResult(value)
            case "blue.catbird.chat.defs#existingDirectConversationResult":
                let value = try BlueCatbirdChatDefs.ExistingDirectConversationResult(from: decoder)
                self = .blueCatbirdChatDefsExistingDirectConversationResult(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsConversationCreatedResult(value):
                try container.encode("blue.catbird.chat.defs#conversationCreatedResult", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsExistingDirectConversationResult(value):
                try container.encode("blue.catbird.chat.defs#existingDirectConversationResult", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsConversationCreatedResult(value):
                hasher.combine("blue.catbird.chat.defs#conversationCreatedResult")
                hasher.combine(value)
            case let .blueCatbirdChatDefsExistingDirectConversationResult(value):
                hasher.combine("blue.catbird.chat.defs#existingDirectConversationResult")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: ConversationCreationResult, rhs: ConversationCreationResult) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsConversationCreatedResult(lhsValue),
                      .blueCatbirdChatDefsConversationCreatedResult(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsExistingDirectConversationResult(lhsValue),
                      .blueCatbirdChatDefsExistingDirectConversationResult(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? ConversationCreationResult else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsConversationCreatedResult(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#conversationCreatedResult")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsExistingDirectConversationResult(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#existingDirectConversationResult")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedDeviceEnrollmentBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsDeviceEnrollmentBody(BlueCatbirdChatDefs.DeviceEnrollmentBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.DeviceEnrollmentBody) {
            self = .blueCatbirdChatDefsDeviceEnrollmentBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#deviceEnrollmentBody":
                let value = try BlueCatbirdChatDefs.DeviceEnrollmentBody(from: decoder)
                self = .blueCatbirdChatDefsDeviceEnrollmentBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsDeviceEnrollmentBody(value):
                try container.encode("blue.catbird.chat.defs#deviceEnrollmentBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsDeviceEnrollmentBody(value):
                hasher.combine("blue.catbird.chat.defs#deviceEnrollmentBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedDeviceEnrollmentBodyUnion, rhs: SignedDeviceEnrollmentBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsDeviceEnrollmentBody(lhsValue),
                      .blueCatbirdChatDefsDeviceEnrollmentBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedDeviceEnrollmentBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsDeviceEnrollmentBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#deviceEnrollmentBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedKeyPackageReplenishmentBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsKeyPackageReplenishmentBody(BlueCatbirdChatDefs.KeyPackageReplenishmentBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.KeyPackageReplenishmentBody) {
            self = .blueCatbirdChatDefsKeyPackageReplenishmentBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#keyPackageReplenishmentBody":
                let value = try BlueCatbirdChatDefs.KeyPackageReplenishmentBody(from: decoder)
                self = .blueCatbirdChatDefsKeyPackageReplenishmentBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsKeyPackageReplenishmentBody(value):
                try container.encode("blue.catbird.chat.defs#keyPackageReplenishmentBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsKeyPackageReplenishmentBody(value):
                hasher.combine("blue.catbird.chat.defs#keyPackageReplenishmentBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedKeyPackageReplenishmentBodyUnion, rhs: SignedKeyPackageReplenishmentBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsKeyPackageReplenishmentBody(lhsValue),
                      .blueCatbirdChatDefsKeyPackageReplenishmentBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedKeyPackageReplenishmentBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsKeyPackageReplenishmentBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#keyPackageReplenishmentBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedDeviceAuthenticationRebindBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsDeviceAuthenticationRebindBody(BlueCatbirdChatDefs.DeviceAuthenticationRebindBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.DeviceAuthenticationRebindBody) {
            self = .blueCatbirdChatDefsDeviceAuthenticationRebindBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#deviceAuthenticationRebindBody":
                let value = try BlueCatbirdChatDefs.DeviceAuthenticationRebindBody(from: decoder)
                self = .blueCatbirdChatDefsDeviceAuthenticationRebindBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsDeviceAuthenticationRebindBody(value):
                try container.encode("blue.catbird.chat.defs#deviceAuthenticationRebindBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsDeviceAuthenticationRebindBody(value):
                hasher.combine("blue.catbird.chat.defs#deviceAuthenticationRebindBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedDeviceAuthenticationRebindBodyUnion, rhs: SignedDeviceAuthenticationRebindBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsDeviceAuthenticationRebindBody(lhsValue),
                      .blueCatbirdChatDefsDeviceAuthenticationRebindBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedDeviceAuthenticationRebindBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsDeviceAuthenticationRebindBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#deviceAuthenticationRebindBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedDeviceRevocationBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsDeviceRevocationBody(BlueCatbirdChatDefs.DeviceRevocationBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.DeviceRevocationBody) {
            self = .blueCatbirdChatDefsDeviceRevocationBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#deviceRevocationBody":
                let value = try BlueCatbirdChatDefs.DeviceRevocationBody(from: decoder)
                self = .blueCatbirdChatDefsDeviceRevocationBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsDeviceRevocationBody(value):
                try container.encode("blue.catbird.chat.defs#deviceRevocationBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsDeviceRevocationBody(value):
                hasher.combine("blue.catbird.chat.defs#deviceRevocationBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedDeviceRevocationBodyUnion, rhs: SignedDeviceRevocationBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsDeviceRevocationBody(lhsValue),
                      .blueCatbirdChatDefsDeviceRevocationBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedDeviceRevocationBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsDeviceRevocationBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#deviceRevocationBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedBlobUploadPreparationBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsBlobUploadPreparationBody(BlueCatbirdChatDefs.BlobUploadPreparationBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.BlobUploadPreparationBody) {
            self = .blueCatbirdChatDefsBlobUploadPreparationBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#blobUploadPreparationBody":
                let value = try BlueCatbirdChatDefs.BlobUploadPreparationBody(from: decoder)
                self = .blueCatbirdChatDefsBlobUploadPreparationBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsBlobUploadPreparationBody(value):
                try container.encode("blue.catbird.chat.defs#blobUploadPreparationBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsBlobUploadPreparationBody(value):
                hasher.combine("blue.catbird.chat.defs#blobUploadPreparationBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedBlobUploadPreparationBodyUnion, rhs: SignedBlobUploadPreparationBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsBlobUploadPreparationBody(lhsValue),
                      .blueCatbirdChatDefsBlobUploadPreparationBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedBlobUploadPreparationBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsBlobUploadPreparationBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#blobUploadPreparationBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedBlobDeletionBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsBlobDeletionBody(BlueCatbirdChatDefs.BlobDeletionBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.BlobDeletionBody) {
            self = .blueCatbirdChatDefsBlobDeletionBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#blobDeletionBody":
                let value = try BlueCatbirdChatDefs.BlobDeletionBody(from: decoder)
                self = .blueCatbirdChatDefsBlobDeletionBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsBlobDeletionBody(value):
                try container.encode("blue.catbird.chat.defs#blobDeletionBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsBlobDeletionBody(value):
                hasher.combine("blue.catbird.chat.defs#blobDeletionBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedBlobDeletionBodyUnion, rhs: SignedBlobDeletionBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsBlobDeletionBody(lhsValue),
                      .blueCatbirdChatDefsBlobDeletionBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedBlobDeletionBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsBlobDeletionBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#blobDeletionBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedCreationBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsCreationBody(BlueCatbirdChatDefs.CreationBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.CreationBody) {
            self = .blueCatbirdChatDefsCreationBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#creationBody":
                let value = try BlueCatbirdChatDefs.CreationBody(from: decoder)
                self = .blueCatbirdChatDefsCreationBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsCreationBody(value):
                try container.encode("blue.catbird.chat.defs#creationBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsCreationBody(value):
                hasher.combine("blue.catbird.chat.defs#creationBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedCreationBodyUnion, rhs: SignedCreationBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsCreationBody(lhsValue),
                      .blueCatbirdChatDefsCreationBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedCreationBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsCreationBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#creationBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedCommitTransitionBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsCommitTransitionBody(BlueCatbirdChatDefs.CommitTransitionBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.CommitTransitionBody) {
            self = .blueCatbirdChatDefsCommitTransitionBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#commitTransitionBody":
                let value = try BlueCatbirdChatDefs.CommitTransitionBody(from: decoder)
                self = .blueCatbirdChatDefsCommitTransitionBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsCommitTransitionBody(value):
                try container.encode("blue.catbird.chat.defs#commitTransitionBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsCommitTransitionBody(value):
                hasher.combine("blue.catbird.chat.defs#commitTransitionBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedCommitTransitionBodyUnion, rhs: SignedCommitTransitionBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsCommitTransitionBody(lhsValue),
                      .blueCatbirdChatDefsCommitTransitionBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedCommitTransitionBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsCommitTransitionBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#commitTransitionBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedPolicyTransitionBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsPolicyTransitionBody(BlueCatbirdChatDefs.PolicyTransitionBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.PolicyTransitionBody) {
            self = .blueCatbirdChatDefsPolicyTransitionBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#policyTransitionBody":
                let value = try BlueCatbirdChatDefs.PolicyTransitionBody(from: decoder)
                self = .blueCatbirdChatDefsPolicyTransitionBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsPolicyTransitionBody(value):
                try container.encode("blue.catbird.chat.defs#policyTransitionBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsPolicyTransitionBody(value):
                hasher.combine("blue.catbird.chat.defs#policyTransitionBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedPolicyTransitionBodyUnion, rhs: SignedPolicyTransitionBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsPolicyTransitionBody(lhsValue),
                      .blueCatbirdChatDefsPolicyTransitionBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedPolicyTransitionBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsPolicyTransitionBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#policyTransitionBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedParticipantAcceptanceBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsParticipantAcceptanceBody(BlueCatbirdChatDefs.ParticipantAcceptanceBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.ParticipantAcceptanceBody) {
            self = .blueCatbirdChatDefsParticipantAcceptanceBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#participantAcceptanceBody":
                let value = try BlueCatbirdChatDefs.ParticipantAcceptanceBody(from: decoder)
                self = .blueCatbirdChatDefsParticipantAcceptanceBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsParticipantAcceptanceBody(value):
                try container.encode("blue.catbird.chat.defs#participantAcceptanceBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsParticipantAcceptanceBody(value):
                hasher.combine("blue.catbird.chat.defs#participantAcceptanceBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedParticipantAcceptanceBodyUnion, rhs: SignedParticipantAcceptanceBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsParticipantAcceptanceBody(lhsValue),
                      .blueCatbirdChatDefsParticipantAcceptanceBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedParticipantAcceptanceBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsParticipantAcceptanceBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#participantAcceptanceBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedApplicationSendBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsApplicationSendBody(BlueCatbirdChatDefs.ApplicationSendBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.ApplicationSendBody) {
            self = .blueCatbirdChatDefsApplicationSendBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#applicationSendBody":
                let value = try BlueCatbirdChatDefs.ApplicationSendBody(from: decoder)
                self = .blueCatbirdChatDefsApplicationSendBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsApplicationSendBody(value):
                try container.encode("blue.catbird.chat.defs#applicationSendBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsApplicationSendBody(value):
                hasher.combine("blue.catbird.chat.defs#applicationSendBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedApplicationSendBodyUnion, rhs: SignedApplicationSendBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsApplicationSendBody(lhsValue),
                      .blueCatbirdChatDefsApplicationSendBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedApplicationSendBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsApplicationSendBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#applicationSendBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedTypingBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsTypingBody(BlueCatbirdChatDefs.TypingBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.TypingBody) {
            self = .blueCatbirdChatDefsTypingBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#typingBody":
                let value = try BlueCatbirdChatDefs.TypingBody(from: decoder)
                self = .blueCatbirdChatDefsTypingBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsTypingBody(value):
                try container.encode("blue.catbird.chat.defs#typingBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsTypingBody(value):
                hasher.combine("blue.catbird.chat.defs#typingBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedTypingBodyUnion, rhs: SignedTypingBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsTypingBody(lhsValue),
                      .blueCatbirdChatDefsTypingBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedTypingBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsTypingBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#typingBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedMetadataTransitionBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsMetadataTransitionBody(BlueCatbirdChatDefs.MetadataTransitionBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.MetadataTransitionBody) {
            self = .blueCatbirdChatDefsMetadataTransitionBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#metadataTransitionBody":
                let value = try BlueCatbirdChatDefs.MetadataTransitionBody(from: decoder)
                self = .blueCatbirdChatDefsMetadataTransitionBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsMetadataTransitionBody(value):
                try container.encode("blue.catbird.chat.defs#metadataTransitionBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsMetadataTransitionBody(value):
                hasher.combine("blue.catbird.chat.defs#metadataTransitionBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedMetadataTransitionBodyUnion, rhs: SignedMetadataTransitionBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsMetadataTransitionBody(lhsValue),
                      .blueCatbirdChatDefsMetadataTransitionBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedMetadataTransitionBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsMetadataTransitionBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#metadataTransitionBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedResetRequestBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsResetRequestBody(BlueCatbirdChatDefs.ResetRequestBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.ResetRequestBody) {
            self = .blueCatbirdChatDefsResetRequestBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#resetRequestBody":
                let value = try BlueCatbirdChatDefs.ResetRequestBody(from: decoder)
                self = .blueCatbirdChatDefsResetRequestBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsResetRequestBody(value):
                try container.encode("blue.catbird.chat.defs#resetRequestBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsResetRequestBody(value):
                hasher.combine("blue.catbird.chat.defs#resetRequestBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedResetRequestBodyUnion, rhs: SignedResetRequestBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsResetRequestBody(lhsValue),
                      .blueCatbirdChatDefsResetRequestBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedResetRequestBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsResetRequestBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#resetRequestBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedResetActivationBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsResetActivationBody(BlueCatbirdChatDefs.ResetActivationBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.ResetActivationBody) {
            self = .blueCatbirdChatDefsResetActivationBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#resetActivationBody":
                let value = try BlueCatbirdChatDefs.ResetActivationBody(from: decoder)
                self = .blueCatbirdChatDefsResetActivationBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsResetActivationBody(value):
                try container.encode("blue.catbird.chat.defs#resetActivationBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsResetActivationBody(value):
                hasher.combine("blue.catbird.chat.defs#resetActivationBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedResetActivationBodyUnion, rhs: SignedResetActivationBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsResetActivationBody(lhsValue),
                      .blueCatbirdChatDefsResetActivationBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedResetActivationBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsResetActivationBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#resetActivationBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedLeafRecoveryRequestBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsLeafRecoveryRequestBody(BlueCatbirdChatDefs.LeafRecoveryRequestBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.LeafRecoveryRequestBody) {
            self = .blueCatbirdChatDefsLeafRecoveryRequestBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#leafRecoveryRequestBody":
                let value = try BlueCatbirdChatDefs.LeafRecoveryRequestBody(from: decoder)
                self = .blueCatbirdChatDefsLeafRecoveryRequestBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsLeafRecoveryRequestBody(value):
                try container.encode("blue.catbird.chat.defs#leafRecoveryRequestBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsLeafRecoveryRequestBody(value):
                hasher.combine("blue.catbird.chat.defs#leafRecoveryRequestBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedLeafRecoveryRequestBodyUnion, rhs: SignedLeafRecoveryRequestBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsLeafRecoveryRequestBody(lhsValue),
                      .blueCatbirdChatDefsLeafRecoveryRequestBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedLeafRecoveryRequestBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsLeafRecoveryRequestBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#leafRecoveryRequestBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedLeafRecoveryCancellationBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsLeafRecoveryCancellationBody(BlueCatbirdChatDefs.LeafRecoveryCancellationBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.LeafRecoveryCancellationBody) {
            self = .blueCatbirdChatDefsLeafRecoveryCancellationBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#leafRecoveryCancellationBody":
                let value = try BlueCatbirdChatDefs.LeafRecoveryCancellationBody(from: decoder)
                self = .blueCatbirdChatDefsLeafRecoveryCancellationBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsLeafRecoveryCancellationBody(value):
                try container.encode("blue.catbird.chat.defs#leafRecoveryCancellationBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsLeafRecoveryCancellationBody(value):
                hasher.combine("blue.catbird.chat.defs#leafRecoveryCancellationBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedLeafRecoveryCancellationBodyUnion, rhs: SignedLeafRecoveryCancellationBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsLeafRecoveryCancellationBody(lhsValue),
                      .blueCatbirdChatDefsLeafRecoveryCancellationBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedLeafRecoveryCancellationBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsLeafRecoveryCancellationBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#leafRecoveryCancellationBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedLeafRecoveryFulfillmentBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsLeafRecoveryFulfillmentBody(BlueCatbirdChatDefs.LeafRecoveryFulfillmentBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.LeafRecoveryFulfillmentBody) {
            self = .blueCatbirdChatDefsLeafRecoveryFulfillmentBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#leafRecoveryFulfillmentBody":
                let value = try BlueCatbirdChatDefs.LeafRecoveryFulfillmentBody(from: decoder)
                self = .blueCatbirdChatDefsLeafRecoveryFulfillmentBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsLeafRecoveryFulfillmentBody(value):
                try container.encode("blue.catbird.chat.defs#leafRecoveryFulfillmentBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsLeafRecoveryFulfillmentBody(value):
                hasher.combine("blue.catbird.chat.defs#leafRecoveryFulfillmentBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedLeafRecoveryFulfillmentBodyUnion, rhs: SignedLeafRecoveryFulfillmentBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsLeafRecoveryFulfillmentBody(lhsValue),
                      .blueCatbirdChatDefsLeafRecoveryFulfillmentBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedLeafRecoveryFulfillmentBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsLeafRecoveryFulfillmentBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#leafRecoveryFulfillmentBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedConversationCloseBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsConversationCloseBody(BlueCatbirdChatDefs.ConversationCloseBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.ConversationCloseBody) {
            self = .blueCatbirdChatDefsConversationCloseBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#conversationCloseBody":
                let value = try BlueCatbirdChatDefs.ConversationCloseBody(from: decoder)
                self = .blueCatbirdChatDefsConversationCloseBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsConversationCloseBody(value):
                try container.encode("blue.catbird.chat.defs#conversationCloseBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsConversationCloseBody(value):
                hasher.combine("blue.catbird.chat.defs#conversationCloseBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedConversationCloseBodyUnion, rhs: SignedConversationCloseBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsConversationCloseBody(lhsValue),
                      .blueCatbirdChatDefsConversationCloseBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedConversationCloseBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsConversationCloseBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#conversationCloseBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedLeaveRequestBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsLeaveRequestBody(BlueCatbirdChatDefs.LeaveRequestBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.LeaveRequestBody) {
            self = .blueCatbirdChatDefsLeaveRequestBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#leaveRequestBody":
                let value = try BlueCatbirdChatDefs.LeaveRequestBody(from: decoder)
                self = .blueCatbirdChatDefsLeaveRequestBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsLeaveRequestBody(value):
                try container.encode("blue.catbird.chat.defs#leaveRequestBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsLeaveRequestBody(value):
                hasher.combine("blue.catbird.chat.defs#leaveRequestBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedLeaveRequestBodyUnion, rhs: SignedLeaveRequestBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsLeaveRequestBody(lhsValue),
                      .blueCatbirdChatDefsLeaveRequestBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedLeaveRequestBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsLeaveRequestBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#leaveRequestBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedZeroLeafLeaveBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsZeroLeafLeaveBody(BlueCatbirdChatDefs.ZeroLeafLeaveBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.ZeroLeafLeaveBody) {
            self = .blueCatbirdChatDefsZeroLeafLeaveBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#zeroLeafLeaveBody":
                let value = try BlueCatbirdChatDefs.ZeroLeafLeaveBody(from: decoder)
                self = .blueCatbirdChatDefsZeroLeafLeaveBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsZeroLeafLeaveBody(value):
                try container.encode("blue.catbird.chat.defs#zeroLeafLeaveBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsZeroLeafLeaveBody(value):
                hasher.combine("blue.catbird.chat.defs#zeroLeafLeaveBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedZeroLeafLeaveBodyUnion, rhs: SignedZeroLeafLeaveBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsZeroLeafLeaveBody(lhsValue),
                      .blueCatbirdChatDefsZeroLeafLeaveBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedZeroLeafLeaveBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsZeroLeafLeaveBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#zeroLeafLeaveBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedLeaveOperation: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsSignedLeaveRequest(BlueCatbirdChatDefs.SignedLeaveRequest)
        case blueCatbirdChatDefsSignedZeroLeafLeave(BlueCatbirdChatDefs.SignedZeroLeafLeave)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.SignedLeaveRequest) {
            self = .blueCatbirdChatDefsSignedLeaveRequest(value)
        }

        public init(_ value: BlueCatbirdChatDefs.SignedZeroLeafLeave) {
            self = .blueCatbirdChatDefsSignedZeroLeafLeave(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#signedLeaveRequest":
                let value = try BlueCatbirdChatDefs.SignedLeaveRequest(from: decoder)
                self = .blueCatbirdChatDefsSignedLeaveRequest(value)
            case "blue.catbird.chat.defs#signedZeroLeafLeave":
                let value = try BlueCatbirdChatDefs.SignedZeroLeafLeave(from: decoder)
                self = .blueCatbirdChatDefsSignedZeroLeafLeave(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsSignedLeaveRequest(value):
                try container.encode("blue.catbird.chat.defs#signedLeaveRequest", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsSignedZeroLeafLeave(value):
                try container.encode("blue.catbird.chat.defs#signedZeroLeafLeave", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsSignedLeaveRequest(value):
                hasher.combine("blue.catbird.chat.defs#signedLeaveRequest")
                hasher.combine(value)
            case let .blueCatbirdChatDefsSignedZeroLeafLeave(value):
                hasher.combine("blue.catbird.chat.defs#signedZeroLeafLeave")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedLeaveOperation, rhs: SignedLeaveOperation) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsSignedLeaveRequest(lhsValue),
                      .blueCatbirdChatDefsSignedLeaveRequest(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsSignedZeroLeafLeave(lhsValue),
                      .blueCatbirdChatDefsSignedZeroLeafLeave(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedLeaveOperation else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsSignedLeaveRequest(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#signedLeaveRequest")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsSignedZeroLeafLeave(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#signedZeroLeafLeave")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedLeaveCancellationBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsLeaveCancellationBody(BlueCatbirdChatDefs.LeaveCancellationBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.LeaveCancellationBody) {
            self = .blueCatbirdChatDefsLeaveCancellationBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#leaveCancellationBody":
                let value = try BlueCatbirdChatDefs.LeaveCancellationBody(from: decoder)
                self = .blueCatbirdChatDefsLeaveCancellationBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsLeaveCancellationBody(value):
                try container.encode("blue.catbird.chat.defs#leaveCancellationBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsLeaveCancellationBody(value):
                hasher.combine("blue.catbird.chat.defs#leaveCancellationBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedLeaveCancellationBodyUnion, rhs: SignedLeaveCancellationBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsLeaveCancellationBody(lhsValue),
                      .blueCatbirdChatDefsLeaveCancellationBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedLeaveCancellationBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsLeaveCancellationBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#leaveCancellationBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedLeaveCommitFulfillmentBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsLeaveCommitFulfillmentBody(BlueCatbirdChatDefs.LeaveCommitFulfillmentBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.LeaveCommitFulfillmentBody) {
            self = .blueCatbirdChatDefsLeaveCommitFulfillmentBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#leaveCommitFulfillmentBody":
                let value = try BlueCatbirdChatDefs.LeaveCommitFulfillmentBody(from: decoder)
                self = .blueCatbirdChatDefsLeaveCommitFulfillmentBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsLeaveCommitFulfillmentBody(value):
                try container.encode("blue.catbird.chat.defs#leaveCommitFulfillmentBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsLeaveCommitFulfillmentBody(value):
                hasher.combine("blue.catbird.chat.defs#leaveCommitFulfillmentBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedLeaveCommitFulfillmentBodyUnion, rhs: SignedLeaveCommitFulfillmentBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsLeaveCommitFulfillmentBody(lhsValue),
                      .blueCatbirdChatDefsLeaveCommitFulfillmentBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedLeaveCommitFulfillmentBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsLeaveCommitFulfillmentBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#leaveCommitFulfillmentBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedWelcomeAcknowledgementBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsWelcomeAcknowledgementBody(BlueCatbirdChatDefs.WelcomeAcknowledgementBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.WelcomeAcknowledgementBody) {
            self = .blueCatbirdChatDefsWelcomeAcknowledgementBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#welcomeAcknowledgementBody":
                let value = try BlueCatbirdChatDefs.WelcomeAcknowledgementBody(from: decoder)
                self = .blueCatbirdChatDefsWelcomeAcknowledgementBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsWelcomeAcknowledgementBody(value):
                try container.encode("blue.catbird.chat.defs#welcomeAcknowledgementBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsWelcomeAcknowledgementBody(value):
                hasher.combine("blue.catbird.chat.defs#welcomeAcknowledgementBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedWelcomeAcknowledgementBodyUnion, rhs: SignedWelcomeAcknowledgementBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsWelcomeAcknowledgementBody(lhsValue),
                      .blueCatbirdChatDefsWelcomeAcknowledgementBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedWelcomeAcknowledgementBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsWelcomeAcknowledgementBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#welcomeAcknowledgementBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedWelcomeRejectionBodyUnion: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsWelcomeRejectionBody(BlueCatbirdChatDefs.WelcomeRejectionBody)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.WelcomeRejectionBody) {
            self = .blueCatbirdChatDefsWelcomeRejectionBody(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#welcomeRejectionBody":
                let value = try BlueCatbirdChatDefs.WelcomeRejectionBody(from: decoder)
                self = .blueCatbirdChatDefsWelcomeRejectionBody(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsWelcomeRejectionBody(value):
                try container.encode("blue.catbird.chat.defs#welcomeRejectionBody", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsWelcomeRejectionBody(value):
                hasher.combine("blue.catbird.chat.defs#welcomeRejectionBody")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedWelcomeRejectionBodyUnion, rhs: SignedWelcomeRejectionBodyUnion) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsWelcomeRejectionBody(lhsValue),
                      .blueCatbirdChatDefsWelcomeRejectionBody(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedWelcomeRejectionBodyUnion else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsWelcomeRejectionBody(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#welcomeRejectionBody")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SignedTransition: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsSignedCommitTransition(BlueCatbirdChatDefs.SignedCommitTransition)
        case blueCatbirdChatDefsSignedPolicyTransition(BlueCatbirdChatDefs.SignedPolicyTransition)
        case blueCatbirdChatDefsSignedLeafRecoveryFulfillment(BlueCatbirdChatDefs.SignedLeafRecoveryFulfillment)
        case blueCatbirdChatDefsSignedMetadataTransition(BlueCatbirdChatDefs.SignedMetadataTransition)
        case blueCatbirdChatDefsSignedLeaveCommitFulfillment(BlueCatbirdChatDefs.SignedLeaveCommitFulfillment)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.SignedCommitTransition) {
            self = .blueCatbirdChatDefsSignedCommitTransition(value)
        }

        public init(_ value: BlueCatbirdChatDefs.SignedPolicyTransition) {
            self = .blueCatbirdChatDefsSignedPolicyTransition(value)
        }

        public init(_ value: BlueCatbirdChatDefs.SignedLeafRecoveryFulfillment) {
            self = .blueCatbirdChatDefsSignedLeafRecoveryFulfillment(value)
        }

        public init(_ value: BlueCatbirdChatDefs.SignedMetadataTransition) {
            self = .blueCatbirdChatDefsSignedMetadataTransition(value)
        }

        public init(_ value: BlueCatbirdChatDefs.SignedLeaveCommitFulfillment) {
            self = .blueCatbirdChatDefsSignedLeaveCommitFulfillment(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#signedCommitTransition":
                let value = try BlueCatbirdChatDefs.SignedCommitTransition(from: decoder)
                self = .blueCatbirdChatDefsSignedCommitTransition(value)
            case "blue.catbird.chat.defs#signedPolicyTransition":
                let value = try BlueCatbirdChatDefs.SignedPolicyTransition(from: decoder)
                self = .blueCatbirdChatDefsSignedPolicyTransition(value)
            case "blue.catbird.chat.defs#signedLeafRecoveryFulfillment":
                let value = try BlueCatbirdChatDefs.SignedLeafRecoveryFulfillment(from: decoder)
                self = .blueCatbirdChatDefsSignedLeafRecoveryFulfillment(value)
            case "blue.catbird.chat.defs#signedMetadataTransition":
                let value = try BlueCatbirdChatDefs.SignedMetadataTransition(from: decoder)
                self = .blueCatbirdChatDefsSignedMetadataTransition(value)
            case "blue.catbird.chat.defs#signedLeaveCommitFulfillment":
                let value = try BlueCatbirdChatDefs.SignedLeaveCommitFulfillment(from: decoder)
                self = .blueCatbirdChatDefsSignedLeaveCommitFulfillment(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsSignedCommitTransition(value):
                try container.encode("blue.catbird.chat.defs#signedCommitTransition", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsSignedPolicyTransition(value):
                try container.encode("blue.catbird.chat.defs#signedPolicyTransition", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsSignedLeafRecoveryFulfillment(value):
                try container.encode("blue.catbird.chat.defs#signedLeafRecoveryFulfillment", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsSignedMetadataTransition(value):
                try container.encode("blue.catbird.chat.defs#signedMetadataTransition", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsSignedLeaveCommitFulfillment(value):
                try container.encode("blue.catbird.chat.defs#signedLeaveCommitFulfillment", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsSignedCommitTransition(value):
                hasher.combine("blue.catbird.chat.defs#signedCommitTransition")
                hasher.combine(value)
            case let .blueCatbirdChatDefsSignedPolicyTransition(value):
                hasher.combine("blue.catbird.chat.defs#signedPolicyTransition")
                hasher.combine(value)
            case let .blueCatbirdChatDefsSignedLeafRecoveryFulfillment(value):
                hasher.combine("blue.catbird.chat.defs#signedLeafRecoveryFulfillment")
                hasher.combine(value)
            case let .blueCatbirdChatDefsSignedMetadataTransition(value):
                hasher.combine("blue.catbird.chat.defs#signedMetadataTransition")
                hasher.combine(value)
            case let .blueCatbirdChatDefsSignedLeaveCommitFulfillment(value):
                hasher.combine("blue.catbird.chat.defs#signedLeaveCommitFulfillment")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SignedTransition, rhs: SignedTransition) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsSignedCommitTransition(lhsValue),
                      .blueCatbirdChatDefsSignedCommitTransition(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsSignedPolicyTransition(lhsValue),
                      .blueCatbirdChatDefsSignedPolicyTransition(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsSignedLeafRecoveryFulfillment(lhsValue),
                      .blueCatbirdChatDefsSignedLeafRecoveryFulfillment(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsSignedMetadataTransition(lhsValue),
                      .blueCatbirdChatDefsSignedMetadataTransition(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsSignedLeaveCommitFulfillment(lhsValue),
                      .blueCatbirdChatDefsSignedLeaveCommitFulfillment(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SignedTransition else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsSignedCommitTransition(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#signedCommitTransition")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsSignedPolicyTransition(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#signedPolicyTransition")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsSignedLeafRecoveryFulfillment(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#signedLeafRecoveryFulfillment")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsSignedMetadataTransition(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#signedMetadataTransition")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsSignedLeaveCommitFulfillment(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#signedLeaveCommitFulfillment")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum ConversationInventoryItem: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsConversationInventoryState(BlueCatbirdChatDefs.ConversationInventoryState)
        case blueCatbirdChatDefsConversationRemovalTombstone(BlueCatbirdChatDefs.ConversationRemovalTombstone)
        case blueCatbirdChatDefsConversationCloseTombstone(BlueCatbirdChatDefs.ConversationCloseTombstone)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.ConversationInventoryState) {
            self = .blueCatbirdChatDefsConversationInventoryState(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ConversationRemovalTombstone) {
            self = .blueCatbirdChatDefsConversationRemovalTombstone(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ConversationCloseTombstone) {
            self = .blueCatbirdChatDefsConversationCloseTombstone(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#conversationInventoryState":
                let value = try BlueCatbirdChatDefs.ConversationInventoryState(from: decoder)
                self = .blueCatbirdChatDefsConversationInventoryState(value)
            case "blue.catbird.chat.defs#conversationRemovalTombstone":
                let value = try BlueCatbirdChatDefs.ConversationRemovalTombstone(from: decoder)
                self = .blueCatbirdChatDefsConversationRemovalTombstone(value)
            case "blue.catbird.chat.defs#conversationCloseTombstone":
                let value = try BlueCatbirdChatDefs.ConversationCloseTombstone(from: decoder)
                self = .blueCatbirdChatDefsConversationCloseTombstone(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsConversationInventoryState(value):
                try container.encode("blue.catbird.chat.defs#conversationInventoryState", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsConversationRemovalTombstone(value):
                try container.encode("blue.catbird.chat.defs#conversationRemovalTombstone", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsConversationCloseTombstone(value):
                try container.encode("blue.catbird.chat.defs#conversationCloseTombstone", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsConversationInventoryState(value):
                hasher.combine("blue.catbird.chat.defs#conversationInventoryState")
                hasher.combine(value)
            case let .blueCatbirdChatDefsConversationRemovalTombstone(value):
                hasher.combine("blue.catbird.chat.defs#conversationRemovalTombstone")
                hasher.combine(value)
            case let .blueCatbirdChatDefsConversationCloseTombstone(value):
                hasher.combine("blue.catbird.chat.defs#conversationCloseTombstone")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: ConversationInventoryItem, rhs: ConversationInventoryItem) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsConversationInventoryState(lhsValue),
                      .blueCatbirdChatDefsConversationInventoryState(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsConversationRemovalTombstone(lhsValue),
                      .blueCatbirdChatDefsConversationRemovalTombstone(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsConversationCloseTombstone(lhsValue),
                      .blueCatbirdChatDefsConversationCloseTombstone(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? ConversationInventoryItem else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsConversationInventoryState(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#conversationInventoryState")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsConversationRemovalTombstone(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#conversationRemovalTombstone")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsConversationCloseTombstone(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#conversationCloseTombstone")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum ConversationEntry: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsApplicationEntry(BlueCatbirdChatDefs.ApplicationEntry)
        case blueCatbirdChatDefsCommitEntry(BlueCatbirdChatDefs.CommitEntry)
        case blueCatbirdChatDefsPolicyEntry(BlueCatbirdChatDefs.PolicyEntry)
        case blueCatbirdChatDefsMetadataEntry(BlueCatbirdChatDefs.MetadataEntry)
        case blueCatbirdChatDefsCreationEntry(BlueCatbirdChatDefs.CreationEntry)
        case blueCatbirdChatDefsParticipantAcceptanceEntry(BlueCatbirdChatDefs.ParticipantAcceptanceEntry)
        case blueCatbirdChatDefsConversationCloseEntry(BlueCatbirdChatDefs.ConversationCloseEntry)
        case blueCatbirdChatDefsResetRequestEntry(BlueCatbirdChatDefs.ResetRequestEntry)
        case blueCatbirdChatDefsResetActivationEntry(BlueCatbirdChatDefs.ResetActivationEntry)
        case blueCatbirdChatDefsLeafRecoveryFulfillmentEntry(BlueCatbirdChatDefs.LeafRecoveryFulfillmentEntry)
        case blueCatbirdChatDefsLeaveRequestEntry(BlueCatbirdChatDefs.LeaveRequestEntry)
        case blueCatbirdChatDefsZeroLeafLeaveEntry(BlueCatbirdChatDefs.ZeroLeafLeaveEntry)
        case blueCatbirdChatDefsLeaveCancellationEntry(BlueCatbirdChatDefs.LeaveCancellationEntry)
        case blueCatbirdChatDefsLeaveCommitFulfillmentEntry(BlueCatbirdChatDefs.LeaveCommitFulfillmentEntry)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.ApplicationEntry) {
            self = .blueCatbirdChatDefsApplicationEntry(value)
        }

        public init(_ value: BlueCatbirdChatDefs.CommitEntry) {
            self = .blueCatbirdChatDefsCommitEntry(value)
        }

        public init(_ value: BlueCatbirdChatDefs.PolicyEntry) {
            self = .blueCatbirdChatDefsPolicyEntry(value)
        }

        public init(_ value: BlueCatbirdChatDefs.MetadataEntry) {
            self = .blueCatbirdChatDefsMetadataEntry(value)
        }

        public init(_ value: BlueCatbirdChatDefs.CreationEntry) {
            self = .blueCatbirdChatDefsCreationEntry(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ParticipantAcceptanceEntry) {
            self = .blueCatbirdChatDefsParticipantAcceptanceEntry(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ConversationCloseEntry) {
            self = .blueCatbirdChatDefsConversationCloseEntry(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ResetRequestEntry) {
            self = .blueCatbirdChatDefsResetRequestEntry(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ResetActivationEntry) {
            self = .blueCatbirdChatDefsResetActivationEntry(value)
        }

        public init(_ value: BlueCatbirdChatDefs.LeafRecoveryFulfillmentEntry) {
            self = .blueCatbirdChatDefsLeafRecoveryFulfillmentEntry(value)
        }

        public init(_ value: BlueCatbirdChatDefs.LeaveRequestEntry) {
            self = .blueCatbirdChatDefsLeaveRequestEntry(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ZeroLeafLeaveEntry) {
            self = .blueCatbirdChatDefsZeroLeafLeaveEntry(value)
        }

        public init(_ value: BlueCatbirdChatDefs.LeaveCancellationEntry) {
            self = .blueCatbirdChatDefsLeaveCancellationEntry(value)
        }

        public init(_ value: BlueCatbirdChatDefs.LeaveCommitFulfillmentEntry) {
            self = .blueCatbirdChatDefsLeaveCommitFulfillmentEntry(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#applicationEntry":
                let value = try BlueCatbirdChatDefs.ApplicationEntry(from: decoder)
                self = .blueCatbirdChatDefsApplicationEntry(value)
            case "blue.catbird.chat.defs#commitEntry":
                let value = try BlueCatbirdChatDefs.CommitEntry(from: decoder)
                self = .blueCatbirdChatDefsCommitEntry(value)
            case "blue.catbird.chat.defs#policyEntry":
                let value = try BlueCatbirdChatDefs.PolicyEntry(from: decoder)
                self = .blueCatbirdChatDefsPolicyEntry(value)
            case "blue.catbird.chat.defs#metadataEntry":
                let value = try BlueCatbirdChatDefs.MetadataEntry(from: decoder)
                self = .blueCatbirdChatDefsMetadataEntry(value)
            case "blue.catbird.chat.defs#creationEntry":
                let value = try BlueCatbirdChatDefs.CreationEntry(from: decoder)
                self = .blueCatbirdChatDefsCreationEntry(value)
            case "blue.catbird.chat.defs#participantAcceptanceEntry":
                let value = try BlueCatbirdChatDefs.ParticipantAcceptanceEntry(from: decoder)
                self = .blueCatbirdChatDefsParticipantAcceptanceEntry(value)
            case "blue.catbird.chat.defs#conversationCloseEntry":
                let value = try BlueCatbirdChatDefs.ConversationCloseEntry(from: decoder)
                self = .blueCatbirdChatDefsConversationCloseEntry(value)
            case "blue.catbird.chat.defs#resetRequestEntry":
                let value = try BlueCatbirdChatDefs.ResetRequestEntry(from: decoder)
                self = .blueCatbirdChatDefsResetRequestEntry(value)
            case "blue.catbird.chat.defs#resetActivationEntry":
                let value = try BlueCatbirdChatDefs.ResetActivationEntry(from: decoder)
                self = .blueCatbirdChatDefsResetActivationEntry(value)
            case "blue.catbird.chat.defs#leafRecoveryFulfillmentEntry":
                let value = try BlueCatbirdChatDefs.LeafRecoveryFulfillmentEntry(from: decoder)
                self = .blueCatbirdChatDefsLeafRecoveryFulfillmentEntry(value)
            case "blue.catbird.chat.defs#leaveRequestEntry":
                let value = try BlueCatbirdChatDefs.LeaveRequestEntry(from: decoder)
                self = .blueCatbirdChatDefsLeaveRequestEntry(value)
            case "blue.catbird.chat.defs#zeroLeafLeaveEntry":
                let value = try BlueCatbirdChatDefs.ZeroLeafLeaveEntry(from: decoder)
                self = .blueCatbirdChatDefsZeroLeafLeaveEntry(value)
            case "blue.catbird.chat.defs#leaveCancellationEntry":
                let value = try BlueCatbirdChatDefs.LeaveCancellationEntry(from: decoder)
                self = .blueCatbirdChatDefsLeaveCancellationEntry(value)
            case "blue.catbird.chat.defs#leaveCommitFulfillmentEntry":
                let value = try BlueCatbirdChatDefs.LeaveCommitFulfillmentEntry(from: decoder)
                self = .blueCatbirdChatDefsLeaveCommitFulfillmentEntry(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsApplicationEntry(value):
                try container.encode("blue.catbird.chat.defs#applicationEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsCommitEntry(value):
                try container.encode("blue.catbird.chat.defs#commitEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsPolicyEntry(value):
                try container.encode("blue.catbird.chat.defs#policyEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsMetadataEntry(value):
                try container.encode("blue.catbird.chat.defs#metadataEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsCreationEntry(value):
                try container.encode("blue.catbird.chat.defs#creationEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsParticipantAcceptanceEntry(value):
                try container.encode("blue.catbird.chat.defs#participantAcceptanceEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsConversationCloseEntry(value):
                try container.encode("blue.catbird.chat.defs#conversationCloseEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsResetRequestEntry(value):
                try container.encode("blue.catbird.chat.defs#resetRequestEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsResetActivationEntry(value):
                try container.encode("blue.catbird.chat.defs#resetActivationEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsLeafRecoveryFulfillmentEntry(value):
                try container.encode("blue.catbird.chat.defs#leafRecoveryFulfillmentEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsLeaveRequestEntry(value):
                try container.encode("blue.catbird.chat.defs#leaveRequestEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsZeroLeafLeaveEntry(value):
                try container.encode("blue.catbird.chat.defs#zeroLeafLeaveEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsLeaveCancellationEntry(value):
                try container.encode("blue.catbird.chat.defs#leaveCancellationEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsLeaveCommitFulfillmentEntry(value):
                try container.encode("blue.catbird.chat.defs#leaveCommitFulfillmentEntry", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsApplicationEntry(value):
                hasher.combine("blue.catbird.chat.defs#applicationEntry")
                hasher.combine(value)
            case let .blueCatbirdChatDefsCommitEntry(value):
                hasher.combine("blue.catbird.chat.defs#commitEntry")
                hasher.combine(value)
            case let .blueCatbirdChatDefsPolicyEntry(value):
                hasher.combine("blue.catbird.chat.defs#policyEntry")
                hasher.combine(value)
            case let .blueCatbirdChatDefsMetadataEntry(value):
                hasher.combine("blue.catbird.chat.defs#metadataEntry")
                hasher.combine(value)
            case let .blueCatbirdChatDefsCreationEntry(value):
                hasher.combine("blue.catbird.chat.defs#creationEntry")
                hasher.combine(value)
            case let .blueCatbirdChatDefsParticipantAcceptanceEntry(value):
                hasher.combine("blue.catbird.chat.defs#participantAcceptanceEntry")
                hasher.combine(value)
            case let .blueCatbirdChatDefsConversationCloseEntry(value):
                hasher.combine("blue.catbird.chat.defs#conversationCloseEntry")
                hasher.combine(value)
            case let .blueCatbirdChatDefsResetRequestEntry(value):
                hasher.combine("blue.catbird.chat.defs#resetRequestEntry")
                hasher.combine(value)
            case let .blueCatbirdChatDefsResetActivationEntry(value):
                hasher.combine("blue.catbird.chat.defs#resetActivationEntry")
                hasher.combine(value)
            case let .blueCatbirdChatDefsLeafRecoveryFulfillmentEntry(value):
                hasher.combine("blue.catbird.chat.defs#leafRecoveryFulfillmentEntry")
                hasher.combine(value)
            case let .blueCatbirdChatDefsLeaveRequestEntry(value):
                hasher.combine("blue.catbird.chat.defs#leaveRequestEntry")
                hasher.combine(value)
            case let .blueCatbirdChatDefsZeroLeafLeaveEntry(value):
                hasher.combine("blue.catbird.chat.defs#zeroLeafLeaveEntry")
                hasher.combine(value)
            case let .blueCatbirdChatDefsLeaveCancellationEntry(value):
                hasher.combine("blue.catbird.chat.defs#leaveCancellationEntry")
                hasher.combine(value)
            case let .blueCatbirdChatDefsLeaveCommitFulfillmentEntry(value):
                hasher.combine("blue.catbird.chat.defs#leaveCommitFulfillmentEntry")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: ConversationEntry, rhs: ConversationEntry) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsApplicationEntry(lhsValue),
                      .blueCatbirdChatDefsApplicationEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsCommitEntry(lhsValue),
                      .blueCatbirdChatDefsCommitEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsPolicyEntry(lhsValue),
                      .blueCatbirdChatDefsPolicyEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsMetadataEntry(lhsValue),
                      .blueCatbirdChatDefsMetadataEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsCreationEntry(lhsValue),
                      .blueCatbirdChatDefsCreationEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsParticipantAcceptanceEntry(lhsValue),
                      .blueCatbirdChatDefsParticipantAcceptanceEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsConversationCloseEntry(lhsValue),
                      .blueCatbirdChatDefsConversationCloseEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsResetRequestEntry(lhsValue),
                      .blueCatbirdChatDefsResetRequestEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsResetActivationEntry(lhsValue),
                      .blueCatbirdChatDefsResetActivationEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsLeafRecoveryFulfillmentEntry(lhsValue),
                      .blueCatbirdChatDefsLeafRecoveryFulfillmentEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsLeaveRequestEntry(lhsValue),
                      .blueCatbirdChatDefsLeaveRequestEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsZeroLeafLeaveEntry(lhsValue),
                      .blueCatbirdChatDefsZeroLeafLeaveEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsLeaveCancellationEntry(lhsValue),
                      .blueCatbirdChatDefsLeaveCancellationEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsLeaveCommitFulfillmentEntry(lhsValue),
                      .blueCatbirdChatDefsLeaveCommitFulfillmentEntry(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? ConversationEntry else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsApplicationEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#applicationEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsCommitEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#commitEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsPolicyEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#policyEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsMetadataEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#metadataEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsCreationEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#creationEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsParticipantAcceptanceEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#participantAcceptanceEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsConversationCloseEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#conversationCloseEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsResetRequestEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#resetRequestEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsResetActivationEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#resetActivationEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsLeafRecoveryFulfillmentEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#leafRecoveryFulfillmentEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsLeaveRequestEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#leaveRequestEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsZeroLeafLeaveEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#zeroLeafLeaveEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsLeaveCancellationEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#leaveCancellationEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsLeaveCommitFulfillmentEntry(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#leaveCommitFulfillmentEntry")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum ProtocolEventPayload: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsConversationChangedEvent(BlueCatbirdChatDefs.ConversationChangedEvent)
        case blueCatbirdChatDefsConversationClosedEvent(BlueCatbirdChatDefs.ConversationClosedEvent)
        case blueCatbirdChatDefsMessageAvailableEvent(BlueCatbirdChatDefs.MessageAvailableEvent)
        case blueCatbirdChatDefsWelcomeAvailableEvent(BlueCatbirdChatDefs.WelcomeAvailableEvent)
        case blueCatbirdChatDefsWelcomeDispositionEvent(BlueCatbirdChatDefs.WelcomeDispositionEvent)
        case blueCatbirdChatDefsResetRequestedEvent(BlueCatbirdChatDefs.ResetRequestedEvent)
        case blueCatbirdChatDefsLeafRecoveryEvent(BlueCatbirdChatDefs.LeafRecoveryEvent)
        case blueCatbirdChatDefsLeaveRequestEvent(BlueCatbirdChatDefs.LeaveRequestEvent)
        case blueCatbirdChatDefsAccessEndedEvent(BlueCatbirdChatDefs.AccessEndedEvent)
        case blueCatbirdChatDefsWatermarkEvent(BlueCatbirdChatDefs.WatermarkEvent)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.ConversationChangedEvent) {
            self = .blueCatbirdChatDefsConversationChangedEvent(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ConversationClosedEvent) {
            self = .blueCatbirdChatDefsConversationClosedEvent(value)
        }

        public init(_ value: BlueCatbirdChatDefs.MessageAvailableEvent) {
            self = .blueCatbirdChatDefsMessageAvailableEvent(value)
        }

        public init(_ value: BlueCatbirdChatDefs.WelcomeAvailableEvent) {
            self = .blueCatbirdChatDefsWelcomeAvailableEvent(value)
        }

        public init(_ value: BlueCatbirdChatDefs.WelcomeDispositionEvent) {
            self = .blueCatbirdChatDefsWelcomeDispositionEvent(value)
        }

        public init(_ value: BlueCatbirdChatDefs.ResetRequestedEvent) {
            self = .blueCatbirdChatDefsResetRequestedEvent(value)
        }

        public init(_ value: BlueCatbirdChatDefs.LeafRecoveryEvent) {
            self = .blueCatbirdChatDefsLeafRecoveryEvent(value)
        }

        public init(_ value: BlueCatbirdChatDefs.LeaveRequestEvent) {
            self = .blueCatbirdChatDefsLeaveRequestEvent(value)
        }

        public init(_ value: BlueCatbirdChatDefs.AccessEndedEvent) {
            self = .blueCatbirdChatDefsAccessEndedEvent(value)
        }

        public init(_ value: BlueCatbirdChatDefs.WatermarkEvent) {
            self = .blueCatbirdChatDefsWatermarkEvent(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#conversationChangedEvent":
                let value = try BlueCatbirdChatDefs.ConversationChangedEvent(from: decoder)
                self = .blueCatbirdChatDefsConversationChangedEvent(value)
            case "blue.catbird.chat.defs#conversationClosedEvent":
                let value = try BlueCatbirdChatDefs.ConversationClosedEvent(from: decoder)
                self = .blueCatbirdChatDefsConversationClosedEvent(value)
            case "blue.catbird.chat.defs#messageAvailableEvent":
                let value = try BlueCatbirdChatDefs.MessageAvailableEvent(from: decoder)
                self = .blueCatbirdChatDefsMessageAvailableEvent(value)
            case "blue.catbird.chat.defs#welcomeAvailableEvent":
                let value = try BlueCatbirdChatDefs.WelcomeAvailableEvent(from: decoder)
                self = .blueCatbirdChatDefsWelcomeAvailableEvent(value)
            case "blue.catbird.chat.defs#welcomeDispositionEvent":
                let value = try BlueCatbirdChatDefs.WelcomeDispositionEvent(from: decoder)
                self = .blueCatbirdChatDefsWelcomeDispositionEvent(value)
            case "blue.catbird.chat.defs#resetRequestedEvent":
                let value = try BlueCatbirdChatDefs.ResetRequestedEvent(from: decoder)
                self = .blueCatbirdChatDefsResetRequestedEvent(value)
            case "blue.catbird.chat.defs#leafRecoveryEvent":
                let value = try BlueCatbirdChatDefs.LeafRecoveryEvent(from: decoder)
                self = .blueCatbirdChatDefsLeafRecoveryEvent(value)
            case "blue.catbird.chat.defs#leaveRequestEvent":
                let value = try BlueCatbirdChatDefs.LeaveRequestEvent(from: decoder)
                self = .blueCatbirdChatDefsLeaveRequestEvent(value)
            case "blue.catbird.chat.defs#accessEndedEvent":
                let value = try BlueCatbirdChatDefs.AccessEndedEvent(from: decoder)
                self = .blueCatbirdChatDefsAccessEndedEvent(value)
            case "blue.catbird.chat.defs#watermarkEvent":
                let value = try BlueCatbirdChatDefs.WatermarkEvent(from: decoder)
                self = .blueCatbirdChatDefsWatermarkEvent(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsConversationChangedEvent(value):
                try container.encode("blue.catbird.chat.defs#conversationChangedEvent", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsConversationClosedEvent(value):
                try container.encode("blue.catbird.chat.defs#conversationClosedEvent", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsMessageAvailableEvent(value):
                try container.encode("blue.catbird.chat.defs#messageAvailableEvent", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsWelcomeAvailableEvent(value):
                try container.encode("blue.catbird.chat.defs#welcomeAvailableEvent", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsWelcomeDispositionEvent(value):
                try container.encode("blue.catbird.chat.defs#welcomeDispositionEvent", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsResetRequestedEvent(value):
                try container.encode("blue.catbird.chat.defs#resetRequestedEvent", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsLeafRecoveryEvent(value):
                try container.encode("blue.catbird.chat.defs#leafRecoveryEvent", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsLeaveRequestEvent(value):
                try container.encode("blue.catbird.chat.defs#leaveRequestEvent", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsAccessEndedEvent(value):
                try container.encode("blue.catbird.chat.defs#accessEndedEvent", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsWatermarkEvent(value):
                try container.encode("blue.catbird.chat.defs#watermarkEvent", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsConversationChangedEvent(value):
                hasher.combine("blue.catbird.chat.defs#conversationChangedEvent")
                hasher.combine(value)
            case let .blueCatbirdChatDefsConversationClosedEvent(value):
                hasher.combine("blue.catbird.chat.defs#conversationClosedEvent")
                hasher.combine(value)
            case let .blueCatbirdChatDefsMessageAvailableEvent(value):
                hasher.combine("blue.catbird.chat.defs#messageAvailableEvent")
                hasher.combine(value)
            case let .blueCatbirdChatDefsWelcomeAvailableEvent(value):
                hasher.combine("blue.catbird.chat.defs#welcomeAvailableEvent")
                hasher.combine(value)
            case let .blueCatbirdChatDefsWelcomeDispositionEvent(value):
                hasher.combine("blue.catbird.chat.defs#welcomeDispositionEvent")
                hasher.combine(value)
            case let .blueCatbirdChatDefsResetRequestedEvent(value):
                hasher.combine("blue.catbird.chat.defs#resetRequestedEvent")
                hasher.combine(value)
            case let .blueCatbirdChatDefsLeafRecoveryEvent(value):
                hasher.combine("blue.catbird.chat.defs#leafRecoveryEvent")
                hasher.combine(value)
            case let .blueCatbirdChatDefsLeaveRequestEvent(value):
                hasher.combine("blue.catbird.chat.defs#leaveRequestEvent")
                hasher.combine(value)
            case let .blueCatbirdChatDefsAccessEndedEvent(value):
                hasher.combine("blue.catbird.chat.defs#accessEndedEvent")
                hasher.combine(value)
            case let .blueCatbirdChatDefsWatermarkEvent(value):
                hasher.combine("blue.catbird.chat.defs#watermarkEvent")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: ProtocolEventPayload, rhs: ProtocolEventPayload) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsConversationChangedEvent(lhsValue),
                      .blueCatbirdChatDefsConversationChangedEvent(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsConversationClosedEvent(lhsValue),
                      .blueCatbirdChatDefsConversationClosedEvent(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsMessageAvailableEvent(lhsValue),
                      .blueCatbirdChatDefsMessageAvailableEvent(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsWelcomeAvailableEvent(lhsValue),
                      .blueCatbirdChatDefsWelcomeAvailableEvent(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsWelcomeDispositionEvent(lhsValue),
                      .blueCatbirdChatDefsWelcomeDispositionEvent(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsResetRequestedEvent(lhsValue),
                      .blueCatbirdChatDefsResetRequestedEvent(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsLeafRecoveryEvent(lhsValue),
                      .blueCatbirdChatDefsLeafRecoveryEvent(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsLeaveRequestEvent(lhsValue),
                      .blueCatbirdChatDefsLeaveRequestEvent(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsAccessEndedEvent(lhsValue),
                      .blueCatbirdChatDefsAccessEndedEvent(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsWatermarkEvent(lhsValue),
                      .blueCatbirdChatDefsWatermarkEvent(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? ProtocolEventPayload else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsConversationChangedEvent(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#conversationChangedEvent")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsConversationClosedEvent(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#conversationClosedEvent")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsMessageAvailableEvent(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#messageAvailableEvent")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsWelcomeAvailableEvent(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#welcomeAvailableEvent")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsWelcomeDispositionEvent(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#welcomeDispositionEvent")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsResetRequestedEvent(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#resetRequestedEvent")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsLeafRecoveryEvent(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#leafRecoveryEvent")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsLeaveRequestEvent(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#leaveRequestEvent")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsAccessEndedEvent(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#accessEndedEvent")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsWatermarkEvent(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#watermarkEvent")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }

    public enum SubscriptionMessage: Codable, ATProtocolCodable, ATProtocolValue, Sendable, Equatable {
        case blueCatbirdChatDefsEventEnvelope(BlueCatbirdChatDefs.EventEnvelope)
        case blueCatbirdChatDefsTypingEvent(BlueCatbirdChatDefs.TypingEvent)
        case unexpected(ATProtocolValueContainer)
        public init(_ value: BlueCatbirdChatDefs.EventEnvelope) {
            self = .blueCatbirdChatDefsEventEnvelope(value)
        }

        public init(_ value: BlueCatbirdChatDefs.TypingEvent) {
            self = .blueCatbirdChatDefsTypingEvent(value)
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            let typeValue = try container.decode(String.self, forKey: .type)

            switch typeValue {
            case "blue.catbird.chat.defs#eventEnvelope":
                let value = try BlueCatbirdChatDefs.EventEnvelope(from: decoder)
                self = .blueCatbirdChatDefsEventEnvelope(value)
            case "blue.catbird.chat.defs#typingEvent":
                let value = try BlueCatbirdChatDefs.TypingEvent(from: decoder)
                self = .blueCatbirdChatDefsTypingEvent(value)
            default:
                let unknownValue = try ATProtocolValueContainer(from: decoder)
                self = .unexpected(unknownValue)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            switch self {
            case let .blueCatbirdChatDefsEventEnvelope(value):
                try container.encode("blue.catbird.chat.defs#eventEnvelope", forKey: .type)
                try value.encode(to: encoder)
            case let .blueCatbirdChatDefsTypingEvent(value):
                try container.encode("blue.catbird.chat.defs#typingEvent", forKey: .type)
                try value.encode(to: encoder)
            case let .unexpected(container):
                try container.encode(to: encoder)
            }
        }

        public func hash(into hasher: inout Hasher) {
            switch self {
            case let .blueCatbirdChatDefsEventEnvelope(value):
                hasher.combine("blue.catbird.chat.defs#eventEnvelope")
                hasher.combine(value)
            case let .blueCatbirdChatDefsTypingEvent(value):
                hasher.combine("blue.catbird.chat.defs#typingEvent")
                hasher.combine(value)
            case let .unexpected(container):
                hasher.combine("unexpected")
                hasher.combine(container)
            }
        }

        private enum CodingKeys: String, CodingKey {
            case type = "$type"
        }

        public static func == (lhs: SubscriptionMessage, rhs: SubscriptionMessage) -> Bool {
            switch (lhs, rhs) {
            case let (.blueCatbirdChatDefsEventEnvelope(lhsValue),
                      .blueCatbirdChatDefsEventEnvelope(rhsValue)):
                return lhsValue == rhsValue
            case let (.blueCatbirdChatDefsTypingEvent(lhsValue),
                      .blueCatbirdChatDefsTypingEvent(rhsValue)):
                return lhsValue == rhsValue
            case let (.unexpected(lhsValue), .unexpected(rhsValue)):
                return lhsValue.isEqual(to: rhsValue)
            default:
                return false
            }
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? SubscriptionMessage else { return false }
            return self == other
        }

        /// DAGCBOR encoding with field ordering
        public func toCBORValue() throws -> Any {
            // Create an ordered map to maintain field order
            var map = OrderedCBORMap()

            switch self {
            case let .blueCatbirdChatDefsEventEnvelope(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#eventEnvelope")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .blueCatbirdChatDefsTypingEvent(value):
                map = map.adding(key: "$type", value: "blue.catbird.chat.defs#typingEvent")

                let valueDict = try value.toCBORValue()

                // If the value is already an OrderedCBORMap, merge its entries
                if let orderedMap = valueDict as? OrderedCBORMap {
                    for (key, value) in orderedMap.entries where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                } else if let dict = valueDict as? [String: Any] {
                    // Otherwise add each key-value pair from the dictionary
                    for (key, value) in dict where key != "$type" {
                        map = map.adding(key: key, value: value)
                    }
                }
                return map
            case let .unexpected(container):
                return try container.toCBORValue()
            }
        }
    }
}

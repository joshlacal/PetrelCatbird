// Generated by Petrel. DO NOT EDIT.
import Foundation
import Petrel

// lexicon: 1, id: blue.catbird.mlsChat.beginTransitionAttestation

public enum BlueCatbirdMlsChatBeginTransitionAttestation {
    public static let typeIdentifier = "blue.catbird.mlsChat.beginTransitionAttestation"

    public struct TransitionDeviceDelta: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.mlsChat.beginTransitionAttestation#transitionDeviceDelta"
        public let adds: [TransitionDeviceAdd]
        public let removes: [TransitionDeviceRemove]

        public init(
            adds: [TransitionDeviceAdd], removes: [TransitionDeviceRemove]
        ) {
            self.adds = adds
            self.removes = removes
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                adds = try container.decode([TransitionDeviceAdd].self, forKey: .adds)
            } catch {
                LogManager.logError("Decoding error for required property 'adds': \(error)")
                throw error
            }
            do {
                removes = try container.decode([TransitionDeviceRemove].self, forKey: .removes)
            } catch {
                LogManager.logError("Decoding error for required property 'removes': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(adds, forKey: .adds)
            try container.encode(removes, forKey: .removes)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(adds)
            hasher.combine(removes)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if adds != other.adds {
                return false
            }
            if removes != other.removes {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let addsValue = try adds.toCBORValue()
            map = map.adding(key: "adds", value: addsValue)
            let removesValue = try removes.toCBORValue()
            map = map.adding(key: "removes", value: removesValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case adds
            case removes
        }
    }

    public struct TransitionDeviceAdd: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.mlsChat.beginTransitionAttestation#transitionDeviceAdd"
        public let deviceCredentialDid: String
        public let keyPackageHash: Bytes

        public init(
            deviceCredentialDid: String, keyPackageHash: Bytes
        ) {
            self.deviceCredentialDid = deviceCredentialDid
            self.keyPackageHash = keyPackageHash
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                deviceCredentialDid = try container.decode(String.self, forKey: .deviceCredentialDid)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceCredentialDid': \(error)")
                throw error
            }
            do {
                keyPackageHash = try container.decode(Bytes.self, forKey: .keyPackageHash)
            } catch {
                LogManager.logError("Decoding error for required property 'keyPackageHash': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(deviceCredentialDid, forKey: .deviceCredentialDid)
            try container.encode(keyPackageHash, forKey: .keyPackageHash)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(deviceCredentialDid)
            hasher.combine(keyPackageHash)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if deviceCredentialDid != other.deviceCredentialDid {
                return false
            }
            if keyPackageHash != other.keyPackageHash {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let deviceCredentialDidValue = try deviceCredentialDid.toCBORValue()
            map = map.adding(key: "deviceCredentialDid", value: deviceCredentialDidValue)
            let keyPackageHashValue = try keyPackageHash.toCBORValue()
            map = map.adding(key: "keyPackageHash", value: keyPackageHashValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case deviceCredentialDid
            case keyPackageHash
        }
    }

    public struct TransitionDeviceRemove: ATProtocolCodable, ATProtocolValue {
        public static let typeIdentifier = "blue.catbird.mlsChat.beginTransitionAttestation#transitionDeviceRemove"
        public let deviceCredentialDid: String
        public let priorLeafIndex: Int

        public init(
            deviceCredentialDid: String, priorLeafIndex: Int
        ) {
            self.deviceCredentialDid = deviceCredentialDid
            self.priorLeafIndex = priorLeafIndex
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            do {
                deviceCredentialDid = try container.decode(String.self, forKey: .deviceCredentialDid)
            } catch {
                LogManager.logError("Decoding error for required property 'deviceCredentialDid': \(error)")
                throw error
            }
            do {
                priorLeafIndex = try container.decode(Int.self, forKey: .priorLeafIndex)
            } catch {
                LogManager.logError("Decoding error for required property 'priorLeafIndex': \(error)")
                throw error
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(Self.typeIdentifier, forKey: .typeIdentifier)
            try container.encode(deviceCredentialDid, forKey: .deviceCredentialDid)
            try container.encode(priorLeafIndex, forKey: .priorLeafIndex)
        }

        public func hash(into hasher: inout Hasher) {
            hasher.combine(deviceCredentialDid)
            hasher.combine(priorLeafIndex)
        }

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let other = other as? Self else { return false }
            if deviceCredentialDid != other.deviceCredentialDid {
                return false
            }
            if priorLeafIndex != other.priorLeafIndex {
                return false
            }
            return true
        }

        public static func == (lhs: Self, rhs: Self) -> Bool {
            return lhs.isEqual(to: rhs)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            map = map.adding(key: "$type", value: Self.typeIdentifier)
            let deviceCredentialDidValue = try deviceCredentialDid.toCBORValue()
            map = map.adding(key: "deviceCredentialDid", value: deviceCredentialDidValue)
            let priorLeafIndexValue = try priorLeafIndex.toCBORValue()
            map = map.adding(key: "priorLeafIndex", value: priorLeafIndexValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case typeIdentifier = "$type"
            case deviceCredentialDid
            case priorLeafIndex
        }
    }

    public struct Input: ATProtocolCodable {
        public let convoId: String
        public let action: InputAction
        public let commit: Bytes
        public let sourceGroupInfo: Bytes
        public let targetGroupInfo: Bytes
        public let targetConfirmationTag: Bytes
        public let idempotencyKey: String
        public let deviceDelta: TransitionDeviceDelta
        public let welcome: Bytes?
        public let epochAuthenticator: Bytes?

        /// Standard public initializer
        public init(convoId: String, action: InputAction, commit: Bytes, sourceGroupInfo: Bytes, targetGroupInfo: Bytes, targetConfirmationTag: Bytes, idempotencyKey: String, deviceDelta: TransitionDeviceDelta, welcome: Bytes? = nil, epochAuthenticator: Bytes? = nil) {
            self.convoId = convoId
            self.action = action
            self.commit = commit
            self.sourceGroupInfo = sourceGroupInfo
            self.targetGroupInfo = targetGroupInfo
            self.targetConfirmationTag = targetConfirmationTag
            self.idempotencyKey = idempotencyKey
            self.deviceDelta = deviceDelta
            self.welcome = welcome
            self.epochAuthenticator = epochAuthenticator
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            convoId = try container.decode(String.self, forKey: .convoId)
            action = try container.decode(InputAction.self, forKey: .action)
            commit = try container.decode(Bytes.self, forKey: .commit)
            sourceGroupInfo = try container.decode(Bytes.self, forKey: .sourceGroupInfo)
            targetGroupInfo = try container.decode(Bytes.self, forKey: .targetGroupInfo)
            targetConfirmationTag = try container.decode(Bytes.self, forKey: .targetConfirmationTag)
            idempotencyKey = try container.decode(String.self, forKey: .idempotencyKey)
            deviceDelta = try container.decode(TransitionDeviceDelta.self, forKey: .deviceDelta)
            welcome = try container.decodeIfPresent(Bytes.self, forKey: .welcome)
            epochAuthenticator = try container.decodeIfPresent(Bytes.self, forKey: .epochAuthenticator)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(convoId, forKey: .convoId)
            try container.encode(action, forKey: .action)
            try container.encode(commit, forKey: .commit)
            try container.encode(sourceGroupInfo, forKey: .sourceGroupInfo)
            try container.encode(targetGroupInfo, forKey: .targetGroupInfo)
            try container.encode(targetConfirmationTag, forKey: .targetConfirmationTag)
            try container.encode(idempotencyKey, forKey: .idempotencyKey)
            try container.encode(deviceDelta, forKey: .deviceDelta)
            try container.encodeIfPresent(welcome, forKey: .welcome)
            try container.encodeIfPresent(epochAuthenticator, forKey: .epochAuthenticator)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            let convoIdValue = try convoId.toCBORValue()
            map = map.adding(key: "convoId", value: convoIdValue)
            let actionValue = try action.toCBORValue()
            map = map.adding(key: "action", value: actionValue)
            let commitValue = try commit.toCBORValue()
            map = map.adding(key: "commit", value: commitValue)
            let sourceGroupInfoValue = try sourceGroupInfo.toCBORValue()
            map = map.adding(key: "sourceGroupInfo", value: sourceGroupInfoValue)
            let targetGroupInfoValue = try targetGroupInfo.toCBORValue()
            map = map.adding(key: "targetGroupInfo", value: targetGroupInfoValue)
            let targetConfirmationTagValue = try targetConfirmationTag.toCBORValue()
            map = map.adding(key: "targetConfirmationTag", value: targetConfirmationTagValue)
            let idempotencyKeyValue = try idempotencyKey.toCBORValue()
            map = map.adding(key: "idempotencyKey", value: idempotencyKeyValue)
            let deviceDeltaValue = try deviceDelta.toCBORValue()
            map = map.adding(key: "deviceDelta", value: deviceDeltaValue)
            if let value = welcome {
                let welcomeValue = try value.toCBORValue()
                map = map.adding(key: "welcome", value: welcomeValue)
            }
            if let value = epochAuthenticator {
                let epochAuthenticatorValue = try value.toCBORValue()
                map = map.adding(key: "epochAuthenticator", value: epochAuthenticatorValue)
            }
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case convoId
            case action
            case commit
            case sourceGroupInfo
            case targetGroupInfo
            case targetConfirmationTag
            case idempotencyKey
            case deviceDelta
            case welcome
            case epochAuthenticator
        }
    }

    public struct Output: ATProtocolCodable {
        public let challengeId: String

        public let challenge: Bytes

        public let expiresAt: ATProtocolDate

        public let version: Int

        /// Standard public initializer
        public init(
            challengeId: String,

            challenge: Bytes,

            expiresAt: ATProtocolDate,

            version: Int

        ) {
            self.challengeId = challengeId

            self.challenge = challenge

            self.expiresAt = expiresAt

            self.version = version
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)

            challengeId = try container.decode(String.self, forKey: .challengeId)

            challenge = try container.decode(Bytes.self, forKey: .challenge)

            expiresAt = try container.decode(ATProtocolDate.self, forKey: .expiresAt)

            version = try container.decode(Int.self, forKey: .version)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            try container.encode(challengeId, forKey: .challengeId)

            try container.encode(challenge, forKey: .challenge)

            try container.encode(expiresAt, forKey: .expiresAt)

            try container.encode(version, forKey: .version)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()

            let challengeIdValue = try challengeId.toCBORValue()
            map = map.adding(key: "challengeId", value: challengeIdValue)

            let challengeValue = try challenge.toCBORValue()
            map = map.adding(key: "challenge", value: challengeValue)

            let expiresAtValue = try expiresAt.toCBORValue()
            map = map.adding(key: "expiresAt", value: expiresAtValue)

            let versionValue = try version.toCBORValue()
            map = map.adding(key: "version", value: versionValue)

            return map
        }

        private enum CodingKeys: String, CodingKey {
            case challengeId
            case challenge
            case expiresAt
            case version
        }
    }

    public enum InputAction: String, Codable, ATProtocolCodable, ATProtocolValue, Sendable {
        case value_addMembers = "addMembers"
        case value_removeMembers = "removeMembers"
        case value_swapMembers = "swapMembers"
        case value_commit = "commit"
        case value_updateMetadata = "updateMetadata"

        public func isEqual(to other: any ATProtocolValue) -> Bool {
            guard let otherValue = other as? InputAction else { return false }
            return self == otherValue
        }

        public func toCBORValue() throws -> Any {
            return rawValue
        }
    }
}

public extension ATProtoClient.Blue.Catbird.MlsChat {
    // MARK: - beginTransitionAttestation

    // Prepare a single-use, device-signed attestation challenge for a private MLS transition. Verify the proposed private MLS transition against one resolved conversation and authorized-device snapshot, then return the exact canonical challenge bytes to sign.
    //
    // - Parameter input: The input parameters for the request

    ///
    /// - Returns: A tuple containing the HTTP response code and the decoded response data
    /// - Throws: NetworkError if the request fails or the response cannot be processed
    func beginTransitionAttestation(
        input: BlueCatbirdMlsChatBeginTransitionAttestation.Input

    ) async throws -> (responseCode: Int, data: BlueCatbirdMlsChatBeginTransitionAttestation.Output?) {
        let endpoint = "blue.catbird.mlsChat.beginTransitionAttestation"

        var headers: [String: String] = [:]

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        let requestData: Data? = try JSONEncoder().encode(input)

        let queryItems: [URLQueryItem]? = nil

        let urlRequest = try await networkService.createURLRequest(
            endpoint: endpoint,
            method: "POST",
            headers: headers,
            body: requestData,
            queryItems: queryItems
        )

        // Determine service DID for this endpoint
        let serviceDID = await networkService.getServiceDID(for: "blue.catbird.mlsChat.beginTransitionAttestation")
        let proxyHeaders = serviceDID.map { ["atproto-proxy": $0] }
        let (responseData, response) = try await networkService.performRequest(urlRequest, skipTokenRefresh: false, additionalHeaders: proxyHeaders)
        let responseCode = response.statusCode

        // Only validate Content-Type and decode on success. Error responses
        // (4xx/5xx) may have missing or different Content-Type headers and
        // are handled by the caller via the status code.
        if (200 ... 299).contains(responseCode) {
            guard let contentType = response.allHeaderFields["Content-Type"] as? String else {
                throw NetworkError.invalidContentType(expected: "application/json", actual: "nil")
            }

            if !contentType.lowercased().contains("application/json") {
                throw NetworkError.invalidContentType(expected: "application/json", actual: contentType)
            }

            do {
                let decoder = JSONDecoder()
                let decodedData = try decoder.decode(BlueCatbirdMlsChatBeginTransitionAttestation.Output.self, from: responseData)

                return (responseCode, decodedData)
            } catch {
                // Log the decoding error for debugging but still return the response code
                LogManager.logError("Failed to decode successful response for blue.catbird.mlsChat.beginTransitionAttestation: \(error)")
                return (responseCode, nil)
            }
        } else {
            // Don't try to decode unknown or malformed error responses as success types
            return (responseCode, nil)
        }
    }
}

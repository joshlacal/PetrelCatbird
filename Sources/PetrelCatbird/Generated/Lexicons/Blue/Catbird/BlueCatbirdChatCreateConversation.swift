// Generated by Petrel. DO NOT EDIT.
import Foundation
import Petrel

// lexicon: 1, id: blue.catbird.chat.createConversation

public enum BlueCatbirdChatCreateConversation {
    public static let typeIdentifier = "blue.catbird.chat.createConversation"
    public struct Input: ATProtocolCodable {
        public let signedRequest: BlueCatbirdChatDefs.SignedCreation

        /// Standard public initializer
        public init(signedRequest: BlueCatbirdChatDefs.SignedCreation) {
            self.signedRequest = signedRequest
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            signedRequest = try container.decode(BlueCatbirdChatDefs.SignedCreation.self, forKey: .signedRequest)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(signedRequest, forKey: .signedRequest)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            let signedRequestValue = try signedRequest.toCBORValue()
            map = map.adding(key: "signedRequest", value: signedRequestValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case signedRequest
        }
    }

    public struct Output: ATProtocolCodable {
        public let result: BlueCatbirdChatDefs.ConversationCreationResult

        /// Standard public initializer
        public init(
            result: BlueCatbirdChatDefs.ConversationCreationResult

        ) {
            self.result = result
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)

            result = try container.decode(BlueCatbirdChatDefs.ConversationCreationResult.self, forKey: .result)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            try container.encode(result, forKey: .result)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()

            let resultValue = try result.toCBORValue()
            map = map.adding(key: "result", value: resultValue)

            return map
        }

        private enum CodingKeys: String, CodingKey {
            case result
        }
    }

    public enum Error: String, Swift.Error, ATProtoErrorType, CustomStringConvertible {
        case blockedRelationship = "BlockedRelationship"
        case conversationAlreadyExists = "ConversationAlreadyExists"
        case cutoverRequired = "CutoverRequired"
        case deviceNotRegistered = "DeviceNotRegistered"
        case deviceRevoked = "DeviceRevoked"
        case groupInvitesDisabled = "GroupInvitesDisabled"
        case idempotencyConflict = "IdempotencyConflict"
        case invalidDPoP = "InvalidDPoP"
        case invalidGenesisGroupInfo = "InvalidGenesisGroupInfo"
        case invalidMetadataSnapshot = "InvalidMetadataSnapshot"
        case invalidMlsArtifact = "InvalidMlsArtifact"
        case invalidRequest = "InvalidRequest"
        case invalidSignature = "InvalidSignature"
        case invitationLimitReached = "InvitationLimitReached"
        case messagesDisabled = "MessagesDisabled"
        case metadataNonceReuse = "MetadataNonceReuse"
        case notAuthorized = "NotAuthorized"
        case notFollowedByRecipient = "NotFollowedByRecipient"
        case relationshipPolicyUnavailable = "RelationshipPolicyUnavailable"
        case unsupportedMlsProfile = "UnsupportedMlsProfile"
        public var description: String {
            return rawValue
        }

        public var errorName: String {
            return rawValue
        }
    }
}

public extension ATProtoClient.Blue.Catbird.Chat {
    // MARK: - createConversation

    // True-absence-CAS creates immutable direct or group state from a client-staged creator-only epoch-zero group. Every noncreator is a pending zero-leaf consent request with no server-readable invitation body. Direct is exactly creator plus one pending admin-role invitee, is unique per unordered DID pair while nonterminal, and a concurrent loser receives the existing identity as a typed success outcome. Group invitees are pending members. Consent, invitation limits, and every-pair block policy are checked under lock. There is no preparation, general reservation, bootstrap Commit, or Welcome.
    //
    // - Parameter input: The input parameters for the request

    ///
    /// - Returns: A tuple containing the HTTP response code and the decoded response data
    /// - Throws: NetworkError if the request fails or the response cannot be processed
    func createConversation(
        input: BlueCatbirdChatCreateConversation.Input

    ) async throws -> (responseCode: Int, data: BlueCatbirdChatCreateConversation.Output?) {
        let endpoint = "blue.catbird.chat.createConversation"

        var headers: [String: String] = [:]

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        let requestData: Data? = try JSONEncoder().encode(input)

        let queryItems: [URLQueryItem]? = nil

        let urlRequest = try await networkService.createURLRequest(
            endpoint: endpoint,
            method: "POST",
            headers: headers,
            body: requestData,
            queryItems: queryItems
        )

        // Determine service DID for this endpoint
        let serviceDID = await networkService.getServiceDID(for: "blue.catbird.chat.createConversation")
        let proxyHeaders = serviceDID.map { ["atproto-proxy": $0] }
        let (responseData, response) = try await networkService.performRequestReturningHTTPErrorResponses(urlRequest, skipTokenRefresh: false, additionalHeaders: proxyHeaders)
        let responseCode = response.statusCode

        // Only validate Content-Type and decode on success. Error responses
        // (4xx/5xx) may have missing or different Content-Type headers and
        // are handled by the caller via the status code.
        if (200 ... 299).contains(responseCode) {
            guard let contentType = response.allHeaderFields["Content-Type"] as? String else {
                throw NetworkError.invalidContentType(expected: "application/json", actual: "nil")
            }

            if !contentType.lowercased().contains("application/json") {
                throw NetworkError.invalidContentType(expected: "application/json", actual: contentType)
            }

            do {
                let decoder = JSONDecoder()
                let decodedData = try decoder.decode(BlueCatbirdChatCreateConversation.Output.self, from: responseData)

                return (responseCode, decodedData)
            } catch {
                // Log the decoding error for debugging but still return the response code
                LogManager.logError("Failed to decode successful response for blue.catbird.chat.createConversation: \(error)")
                return (responseCode, nil)
            }
        } else {
            // Try to parse a declared structured error response
            if let atprotoError = ATProtoErrorParser.parse(
                data: responseData,
                statusCode: responseCode,
                errorType: BlueCatbirdChatCreateConversation.Error.self
            ) {
                throw atprotoError
            }

            // Don't try to decode unknown or malformed error responses as success types
            return (responseCode, nil)
        }
    }
}

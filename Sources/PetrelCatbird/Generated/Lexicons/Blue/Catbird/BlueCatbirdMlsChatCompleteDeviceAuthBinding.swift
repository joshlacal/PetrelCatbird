// Generated by Petrel. DO NOT EDIT.
import Foundation
import Petrel

// lexicon: 1, id: blue.catbird.mlsChat.completeDeviceAuthBinding

public enum BlueCatbirdMlsChatCompleteDeviceAuthBinding {
    public static let typeIdentifier = "blue.catbird.mlsChat.completeDeviceAuthBinding"
    public struct Input: ATProtocolCodable {
        public let challengeId: String
        public let signature: Bytes

        /// Standard public initializer
        public init(challengeId: String, signature: Bytes) {
            self.challengeId = challengeId
            self.signature = signature
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            challengeId = try container.decode(String.self, forKey: .challengeId)
            signature = try container.decode(Bytes.self, forKey: .signature)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(challengeId, forKey: .challengeId)
            try container.encode(signature, forKey: .signature)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            let challengeIdValue = try challengeId.toCBORValue()
            map = map.adding(key: "challengeId", value: challengeIdValue)
            let signatureValue = try signature.toCBORValue()
            map = map.adding(key: "signature", value: signatureValue)
            return map
        }

        private enum CodingKeys: String, CodingKey {
            case challengeId
            case signature
        }
    }

    public struct Output: ATProtocolCodable {
        public let deviceId: String

        public let boundAt: ATProtocolDate

        public let bindingVersion: Int

        /// Standard public initializer
        public init(
            deviceId: String,

            boundAt: ATProtocolDate,

            bindingVersion: Int

        ) {
            self.deviceId = deviceId

            self.boundAt = boundAt

            self.bindingVersion = bindingVersion
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)

            deviceId = try container.decode(String.self, forKey: .deviceId)

            boundAt = try container.decode(ATProtocolDate.self, forKey: .boundAt)

            bindingVersion = try container.decode(Int.self, forKey: .bindingVersion)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            try container.encode(deviceId, forKey: .deviceId)

            try container.encode(boundAt, forKey: .boundAt)

            try container.encode(bindingVersion, forKey: .bindingVersion)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()

            let deviceIdValue = try deviceId.toCBORValue()
            map = map.adding(key: "deviceId", value: deviceIdValue)

            let boundAtValue = try boundAt.toCBORValue()
            map = map.adding(key: "boundAt", value: boundAtValue)

            let bindingVersionValue = try bindingVersion.toCBORValue()
            map = map.adding(key: "bindingVersion", value: bindingVersionValue)

            return map
        }

        private enum CodingKeys: String, CodingKey {
            case deviceId
            case boundAt
            case bindingVersion
        }
    }

    public enum Error: String, Swift.Error, ATProtoErrorType, CustomStringConvertible {
        case dpopRequired = "DpopRequired"
        case challengeNotFound = "ChallengeNotFound"
        case challengeExpired = "ChallengeExpired"
        case challengeAlreadyUsed = "ChallengeAlreadyUsed"
        case bindingMismatch = "BindingMismatch"
        case invalidSignature = "InvalidSignature"
        case deviceNotFound = "DeviceNotFound"
        case unauthorized = "Unauthorized"
        case bindingUnavailable = "BindingUnavailable"
        public var description: String {
            return rawValue
        }

        public var errorName: String {
            return rawValue
        }
    }
}

public extension ATProtoClient.Blue.Catbird.MlsChat {
    // MARK: - completeDeviceAuthBinding

    // Complete proof-of-possession binding for an MLS device registration. Atomically consumes a binding challenge after verifying its signature with the registered MLS device identity key and requiring the same authenticated DPoP key used to begin it.
    //
    // - Parameter input: The input parameters for the request

    ///
    /// - Returns: A tuple containing the HTTP response code and the decoded response data
    /// - Throws: NetworkError if the request fails or the response cannot be processed
    func completeDeviceAuthBinding(
        input: BlueCatbirdMlsChatCompleteDeviceAuthBinding.Input

    ) async throws -> (responseCode: Int, data: BlueCatbirdMlsChatCompleteDeviceAuthBinding.Output?) {
        let endpoint = "blue.catbird.mlsChat.completeDeviceAuthBinding"

        var headers: [String: String] = [:]

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        let requestData: Data? = try JSONEncoder().encode(input)

        let queryItems: [URLQueryItem]? = nil

        let urlRequest = try await networkService.createURLRequest(
            endpoint: endpoint,
            method: "POST",
            headers: headers,
            body: requestData,
            queryItems: queryItems
        )

        // Determine service DID for this endpoint
        let serviceDID = await networkService.getServiceDID(for: "blue.catbird.mlsChat.completeDeviceAuthBinding")
        let proxyHeaders = serviceDID.map { ["atproto-proxy": $0] }
        let (responseData, response) = try await networkService.performRequestReturningHTTPErrorResponses(urlRequest, skipTokenRefresh: false, additionalHeaders: proxyHeaders)
        let responseCode = response.statusCode

        // Only validate Content-Type and decode on success. Error responses
        // (4xx/5xx) may have missing or different Content-Type headers and
        // are handled by the caller via the status code.
        if (200 ... 299).contains(responseCode) {
            guard let contentType = response.allHeaderFields["Content-Type"] as? String else {
                throw NetworkError.invalidContentType(expected: "application/json", actual: "nil")
            }

            if !contentType.lowercased().contains("application/json") {
                throw NetworkError.invalidContentType(expected: "application/json", actual: contentType)
            }

            do {
                let decoder = JSONDecoder()
                let decodedData = try decoder.decode(BlueCatbirdMlsChatCompleteDeviceAuthBinding.Output.self, from: responseData)

                return (responseCode, decodedData)
            } catch {
                // Log the decoding error for debugging but still return the response code
                LogManager.logError("Failed to decode successful response for blue.catbird.mlsChat.completeDeviceAuthBinding: \(error)")
                return (responseCode, nil)
            }
        } else {
            // Try to parse a declared structured error response
            if let atprotoError = ATProtoErrorParser.parse(
                data: responseData,
                statusCode: responseCode,
                errorType: BlueCatbirdMlsChatCompleteDeviceAuthBinding.Error.self
            ) {
                throw atprotoError
            }

            // Don't try to decode unknown or malformed error responses as success types
            return (responseCode, nil)
        }
    }
}

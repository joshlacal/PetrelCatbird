// Generated by Petrel. DO NOT EDIT.
import Foundation
import Petrel

// lexicon: 1, id: blue.catbird.mlsChat.finalizeGroupChange

public enum BlueCatbirdMlsChatFinalizeGroupChange {
    public static let typeIdentifier = "blue.catbird.mlsChat.finalizeGroupChange"
    public struct Input: ATProtocolCodable {
        public let convoId: String
        public let challengeId: String
        public let targetGroupInfo: Bytes
        public let receipt: BlueCatbirdMlsChatCommitGroupChange.SequencerReceipt

        /// Standard public initializer
        public init(convoId: String, challengeId: String, targetGroupInfo: Bytes, receipt: BlueCatbirdMlsChatCommitGroupChange.SequencerReceipt) {
            self.convoId = convoId
            self.challengeId = challengeId
            self.targetGroupInfo = targetGroupInfo
            self.receipt = receipt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)
            convoId = try container.decode(String.self, forKey: .convoId)
            challengeId = try container.decode(String.self, forKey: .challengeId)
            targetGroupInfo = try container.decode(Bytes.self, forKey: .targetGroupInfo)
            receipt = try Self.decodeStrictReference(BlueCatbirdMlsChatCommitGroupChange.SequencerReceipt.self, from: container, forKey: .receipt, allowedKeys: ["commitHash", "convoId", "epoch", "issuedAt", "sequencerDid", "sequencerTerm", "signature"], expectedTypeIdentifier: "blue.catbird.mlsChat.commitGroupChange#sequencerReceipt")
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            try container.encode(convoId, forKey: .convoId)
            try container.encode(challengeId, forKey: .challengeId)
            try container.encode(targetGroupInfo, forKey: .targetGroupInfo)
            try container.encode(receipt, forKey: .receipt)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()
            let convoIdValue = try convoId.toCBORValue()
            map = map.adding(key: "convoId", value: convoIdValue)
            let challengeIdValue = try challengeId.toCBORValue()
            map = map.adding(key: "challengeId", value: challengeIdValue)
            let targetGroupInfoValue = try targetGroupInfo.toCBORValue()
            map = map.adding(key: "targetGroupInfo", value: targetGroupInfoValue)
            let receiptValue = try receipt.toCBORValue()
            map = map.adding(key: "receipt", value: receiptValue)
            return map
        }

        private struct StrictReferenceCodingKey: CodingKey {
            let stringValue: String
            let intValue: Int? = nil

            init?(stringValue: String) {
                self.stringValue = stringValue
            }

            init?(intValue _: Int) {
                return nil
            }
        }

        private static func decodeStrictReference<T: Codable>(
            _: T.Type,
            from container: KeyedDecodingContainer<CodingKeys>,
            forKey key: CodingKeys,
            allowedKeys: Set<String>,
            expectedTypeIdentifier: String
        ) throws -> T {
            let nestedDecoder = try container.superDecoder(forKey: key)
            let nestedContainer = try nestedDecoder.container(keyedBy: StrictReferenceCodingKey.self)
            let incomingKeys = Set(nestedContainer.allKeys.map(\.stringValue))
            let discriminatorKey = StrictReferenceCodingKey(stringValue: "$type")!
            let unknownKeys = incomingKeys.subtracting(allowedKeys.union(["$type"]))
            guard unknownKeys.isEmpty else {
                throw DecodingError.dataCorrupted(
                    DecodingError.Context(
                        codingPath: nestedDecoder.codingPath,
                        debugDescription: "Strict reference contains unknown keys: \(unknownKeys.sorted())"
                    )
                )
            }
            if nestedContainer.contains(discriminatorKey) {
                let actualTypeIdentifier = try nestedContainer.decode(
                    String.self,
                    forKey: discriminatorKey
                )
                guard actualTypeIdentifier == expectedTypeIdentifier else {
                    throw DecodingError.dataCorruptedError(
                        forKey: discriminatorKey,
                        in: nestedContainer,
                        debugDescription: "Strict reference discriminator mismatch"
                    )
                }
            }
            return try T(from: nestedDecoder)
        }

        private enum CodingKeys: String, CodingKey {
            case convoId
            case challengeId
            case targetGroupInfo
            case receipt
        }
    }

    public struct Output: ATProtocolCodable {
        public let success: Bool

        public let epoch: Int

        public let groupInfoHash: Bytes

        public let finalizedAt: ATProtocolDate

        /// Standard public initializer
        public init(
            success: Bool,

            epoch: Int,

            groupInfoHash: Bytes,

            finalizedAt: ATProtocolDate

        ) {
            self.success = success

            self.epoch = epoch

            self.groupInfoHash = groupInfoHash

            self.finalizedAt = finalizedAt
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.container(keyedBy: CodingKeys.self)

            success = try container.decode(Bool.self, forKey: .success)

            epoch = try container.decode(Int.self, forKey: .epoch)

            groupInfoHash = try container.decode(Bytes.self, forKey: .groupInfoHash)

            finalizedAt = try container.decode(ATProtocolDate.self, forKey: .finalizedAt)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)

            try container.encode(success, forKey: .success)

            try container.encode(epoch, forKey: .epoch)

            try container.encode(groupInfoHash, forKey: .groupInfoHash)

            try container.encode(finalizedAt, forKey: .finalizedAt)
        }

        public func toCBORValue() throws -> Any {
            var map = OrderedCBORMap()

            let successValue = try success.toCBORValue()
            map = map.adding(key: "success", value: successValue)

            let epochValue = try epoch.toCBORValue()
            map = map.adding(key: "epoch", value: epochValue)

            let groupInfoHashValue = try groupInfoHash.toCBORValue()
            map = map.adding(key: "groupInfoHash", value: groupInfoHashValue)

            let finalizedAtValue = try finalizedAt.toCBORValue()
            map = map.adding(key: "finalizedAt", value: finalizedAtValue)

            return map
        }

        private enum CodingKeys: String, CodingKey {
            case success
            case epoch
            case groupInfoHash
            case finalizedAt
        }
    }
}

public extension ATProtoClient.Blue.Catbird.MlsChat {
    // MARK: - finalizeGroupChange

    // Finalize an accepted private MLS transition after the client durably authenticates its sequencer receipt. Install the exact verified target GroupInfo for an accepted TransitionAttestationV1 challenge.
    //
    // - Parameter input: The input parameters for the request

    ///
    /// - Returns: A tuple containing the HTTP response code and the decoded response data
    /// - Throws: NetworkError if the request fails or the response cannot be processed
    func finalizeGroupChange(
        input: BlueCatbirdMlsChatFinalizeGroupChange.Input

    ) async throws -> (responseCode: Int, data: BlueCatbirdMlsChatFinalizeGroupChange.Output?) {
        let endpoint = "blue.catbird.mlsChat.finalizeGroupChange"

        var headers: [String: String] = [:]

        headers["Content-Type"] = "application/json"

        headers["Accept"] = "application/json"

        let requestData: Data? = try JSONEncoder().encode(input)

        let queryItems: [URLQueryItem]? = nil

        let urlRequest = try await networkService.createURLRequest(
            endpoint: endpoint,
            method: "POST",
            headers: headers,
            body: requestData,
            queryItems: queryItems
        )

        // Determine service DID for this endpoint
        let serviceDID = await networkService.getServiceDID(for: "blue.catbird.mlsChat.finalizeGroupChange")
        let proxyHeaders = serviceDID.map { ["atproto-proxy": $0] }
        let (responseData, response) = try await networkService.performRequest(urlRequest, skipTokenRefresh: false, additionalHeaders: proxyHeaders)
        let responseCode = response.statusCode

        // Only validate Content-Type and decode on success. Error responses
        // (4xx/5xx) may have missing or different Content-Type headers and
        // are handled by the caller via the status code.
        if (200 ... 299).contains(responseCode) {
            guard let contentType = response.allHeaderFields["Content-Type"] as? String else {
                throw NetworkError.invalidContentType(expected: "application/json", actual: "nil")
            }

            if !contentType.lowercased().contains("application/json") {
                throw NetworkError.invalidContentType(expected: "application/json", actual: contentType)
            }

            do {
                let decoder = JSONDecoder()
                let decodedData = try decoder.decode(BlueCatbirdMlsChatFinalizeGroupChange.Output.self, from: responseData)

                return (responseCode, decodedData)
            } catch {
                // Log the decoding error for debugging but still return the response code
                LogManager.logError("Failed to decode successful response for blue.catbird.mlsChat.finalizeGroupChange: \(error)")
                return (responseCode, nil)
            }
        } else {
            // Don't try to decode unknown or malformed error responses as success types
            return (responseCode, nil)
        }
    }
}
